# SCimage 图片生成集成 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 SCimage 的图片生成能力原生集成到 OpenDeepSea，覆盖独立工作台、会话内生成和智能体工具调用三个入口。

**Architecture:** 新增 `packages/backend/src/image-generation/` 能力域，统一管理 provider 配置、image job、运行时、输出文件沉淀和事件广播。生成图片写入现有项目文件目录并登记到 `files` 表，前端用 React 原生页面和会话弹窗消费同一套 API。智能体工具通过后端服务函数创建任务，不通过 HTTP 回环。

**Tech Stack:** Node.js 20、TypeScript、Express、better-sqlite3、WebSocket、React 18、Vite、TanStack Query、Radix UI、lucide-react、node:test。

---

## Scope Check

本计划覆盖五个阶段：

1. 后端核心能力。
2. 独立图片工作台。
3. 会话内生成。
4. 智能体工具。
5. 高级能力。

这些阶段共享同一套后端数据模型和任务服务，不能拆成彼此独立的实现计划。执行时应按阶段推进，每个任务形成可测试、可提交的小闭环。

当前工作区已有其他未提交改动。执行本计划前应优先创建隔离 worktree，或至少确认本计划相关文件没有与现有改动冲突。执行者不得覆盖当前工作区中非本计划产生的改动。

## Existing Context

- 已确认设计文档：[docs/superpowers/specs/2026-06-07-SCimage图片生成集成设计.md](/Users/chendimao/WWW/openDeepSea/docs/superpowers/specs/2026-06-07-SCimage图片生成集成设计.md)
- SCimage 分析版本：`0bb31f6 发布 1.1.3 并优化滚动交互`
- OpenDeepSea 后端入口：`packages/backend/src/routes.ts`、`packages/backend/src/server.ts`
- OpenDeepSea 文件沉淀：`packages/backend/src/uploads.ts`、`packages/backend/src/repos/files.ts`
- OpenDeepSea 资源库：`packages/backend/src/repos/resource-assets.ts`
- OpenDeepSea WebSocket：`packages/backend/src/ws-hub.ts`、`packages/frontend/src/lib/ws.ts`
- OpenDeepSea 会话派发：`packages/backend/src/session-message-dispatch.ts`

## File Structure

### Backend Files

- Create: `packages/backend/src/image-generation/types.ts`
  - 定义 provider、job、output、source image、runtime request/response、API input/output 类型。
- Create: `packages/backend/src/image-generation/validation.ts`
  - 归一化 Base URL、API Key、模型、workflow、size、quality、count 和错误消息。
- Create: `packages/backend/src/image-generation/provider-profiles.ts`
  - provider profile 仓储、激活、更新、删除、API 安全输出。
- Create: `packages/backend/src/image-generation/provider-models.ts`
  - `/models` 拉取、分类、失败非阻断响应。
- Create: `packages/backend/src/image-generation/jobs.ts`
  - image job、output、source image 仓储和状态流转。
- Create: `packages/backend/src/image-generation/openai-compatible.ts`
  - OpenAI-compatible 文生图、图生图请求和响应解析。
- Create: `packages/backend/src/image-generation/outputs.ts`
  - 将 runtime 输出写入项目文件目录，并通过 `fileRepo.create` 登记。
- Create: `packages/backend/src/image-generation/source-images.ts`
  - 校验图生图源图项目归属、图片类型，并解析 generated output lineage。
- Create: `packages/backend/src/image-generation/queue.ts`
  - 本地任务队列、取消、重试、AbortController 和 snapshot。
- Create: `packages/backend/src/image-generation/service.ts`
  - 统一创建任务、运行任务、重试任务、取消任务、输出广播。
- Create: `packages/backend/src/image-generation/routes.ts`
  - Express router，挂载 provider profile、models、image jobs API。
- Create: `packages/backend/src/image-generation/tool.ts`
  - 智能体工具调用入口，隐藏 API Key，返回文件和资源引用。
- Create tests:
  - `packages/backend/src/image-generation/provider-profiles.test.ts`
  - `packages/backend/src/image-generation/provider-models.test.ts`
  - `packages/backend/src/image-generation/openai-compatible.test.ts`
  - `packages/backend/src/image-generation/jobs.test.ts`
  - `packages/backend/src/image-generation/outputs.test.ts`
  - `packages/backend/src/image-generation/queue.test.ts`
  - `packages/backend/src/image-generation/routes.test.ts`
  - `packages/backend/src/image-generation/tool.test.ts`
- Modify: `packages/backend/src/db.ts`
  - 新增表结构、索引和启动恢复辅助 SQL。
- Modify: `packages/backend/src/types.ts`
  - 导出 image generation 类型和 WebSocket event 类型。
- Modify: `packages/backend/src/routes.ts`
  - 挂载 image generation router。
- Modify: `packages/backend/src/ws-hub.ts`
  - 增加 project-level subscription，供独立工作台订阅项目图片任务。
- Modify: `packages/backend/src/server.ts`
  - 处理 `project:subscribe` / `project:unsubscribe` WebSocket client event。
- Modify: `packages/backend/src/session-message-dispatch.ts`
  - 会话消息 metadata 支持图片生成任务附件或引用。
- Modify: `packages/backend/src/session-runtime.ts`
  - 注册智能体工具能力时接入 `generate_image`。

### Frontend Files

- Modify: `packages/frontend/src/lib/types.ts`
  - 添加图片生成 provider、job、output、工具结果、WebSocket event 类型。
- Modify: `packages/frontend/src/lib/api.ts`
  - 添加 provider profile、models、image job API client。
- Modify: `packages/frontend/src/lib/ws.ts`
  - 添加 project subscription 和 image job events。
- Modify: `packages/frontend/src/main.tsx`
  - 添加 `/projects/:projectId/images` 路由。
- Create: `packages/frontend/src/pages/ImageWorkbenchPage.tsx`
  - 项目级图片工作台 route surface。
- Create directory: `packages/frontend/src/image-generation/`
  - `ImageGenerationShell.tsx`
  - `ProviderProfilePanel.tsx`
  - `ImageJobForm.tsx`
  - `SourceImagePicker.tsx`
  - `ImageJobQueue.tsx`
  - `ImageGalleryPanel.tsx`
  - `ImageJobStatusCard.tsx`
  - `ImageGenerationDialog.tsx`
  - `imageGenerationModel.ts`
  - `useImageGenerationEvents.ts`
- Modify: `packages/frontend/src/components/AppShell.tsx`
  - 项目导航增加图片工作台入口。
- Modify: `packages/frontend/src/session-ui/SessionComposer.tsx`
  - 添加会话内图片生成按钮。
- Modify: `packages/frontend/src/session-ui/SessionTranscript.tsx`
  - 展示图片任务状态卡片或图片附件。
- Create frontend tests:
  - `packages/frontend/src/image-generation/imageGenerationModel.test.ts`
  - `packages/frontend/src/image-generation/ImageJobForm.test.tsx`
  - `packages/frontend/src/image-generation/ImageJobQueue.test.tsx`
  - `packages/frontend/src/image-generation/ImageGenerationDialog.test.tsx`
  - `packages/frontend/src/lib/imageGenerationApi.test.ts`
  - `packages/frontend/src/lib/imageGenerationWs.test.ts`

## Shared Contracts

所有阶段应复用以下核心契约。实现时可以按仓库实际风格拆分，但字段名保持一致。

```ts
export type ImageGenerationWorkflow = 'generate' | 'image-to-image';
export type ImageGenerationStatus = 'queued' | 'running' | 'canceling' | 'completed' | 'failed' | 'canceled';
export type ImageProviderCompatProfileId = 'openai' | 'openai-sdk' | 'images-edits' | 'chat-completions';

export interface ImageProviderProfile {
  id: string;
  project_id: string;
  name: string;
  base_url: string;
  model: string;
  compat_profile_id: ImageProviderCompatProfileId;
  supports_count_parameter: 0 | 1;
  active: 0 | 1;
  has_api_key: 0 | 1;
  created_at: number;
  updated_at: number;
}

export interface ImageGenerationJob {
  id: string;
  project_id: string;
  room_id: string | null;
  session_id: string | null;
  source_message_id: string | null;
  source_agent_id: string | null;
  source_task_id: string | null;
  provider_profile_id: string;
  workflow: ImageGenerationWorkflow;
  prompt: string;
  count: number;
  quality: string;
  size: string;
  status: ImageGenerationStatus;
  message: string | null;
  error: string | null;
  created_at: number;
  started_at: number | null;
  completed_at: number | null;
  updated_at: number;
}

export interface ImageGenerationOutput {
  id: string;
  job_id: string;
  file_id: string;
  slot: number;
  name: string;
  url: string;
  mime_type: string;
  size: number;
  width: number | null;
  height: number | null;
  created_at: number;
}

export interface ImageGenerationSourceImage {
  id: string;
  job_id: string;
  file_id: string;
  slot: number;
  url: string;
  origin_job_id: string | null;
  origin_output_id: string | null;
  created_at: number;
}
```

`room_id` 用于旧群聊/房间上下文，`session_id` 用于当前 `SessionWorkspace`。会话入口创建图片任务时必须写入 `session_id`；如果调用来自旧 room 页面，则写入 `room_id`。

WebSocket event contract：

```ts
export type ImageGenerationWsEvent =
  | { type: 'image_job:created'; projectId: string; sessionId?: string | null; roomId?: string | null; job: ImageGenerationJob }
  | { type: 'image_job:updated'; projectId: string; sessionId?: string | null; roomId?: string | null; job: ImageGenerationJob }
  | { type: 'image_job:output_added'; projectId: string; sessionId?: string | null; roomId?: string | null; jobId: string; output: ImageGenerationOutput }
  | { type: 'image_job:completed'; projectId: string; sessionId?: string | null; roomId?: string | null; job: ImageGenerationJob; outputs: ImageGenerationOutput[] }
  | { type: 'image_job:failed'; projectId: string; sessionId?: string | null; roomId?: string | null; job: ImageGenerationJob }
  | { type: 'image_job:canceled'; projectId: string; sessionId?: string | null; roomId?: string | null; job: ImageGenerationJob };
```

## Phase 1: 后端核心能力

### Task 1: 数据库 schema 与基础类型

**Files:**
- Modify: `packages/backend/src/db.ts`
- Modify: `packages/backend/src/types.ts`
- Create: `packages/backend/src/image-generation/types.ts`
- Test: `packages/backend/src/image-generation/jobs.test.ts`

- [x] **Step 1: Write failing schema tests**

Add tests that import `db`, create a project, and verify all image generation tables exist and queued/running jobs can be recovered.

```ts
test('image generation schema creates provider job output and source tables', () => {
  const tables = db.prepare(`
    SELECT name FROM sqlite_master
    WHERE type = 'table' AND name IN (
      'image_provider_profiles',
      'image_generation_jobs',
      'image_generation_outputs',
      'image_generation_source_images'
    )
    ORDER BY name
  `).all() as Array<{ name: string }>;

  assert.deepEqual(tables.map((row) => row.name), [
    'image_generation_jobs',
    'image_generation_outputs',
    'image_generation_source_images',
    'image_provider_profiles',
  ]);
});
```

- [x] **Step 2: Run failing test**

Run:

```bash
cd packages/backend && node --import tsx --test src/image-generation/jobs.test.ts
```

Expected: fails because `src/image-generation/jobs.test.ts` or the image generation tables do not exist.

- [x] **Step 3: Add schema**

Add SQL to `packages/backend/src/db.ts` near existing `files` and `resource_assets` tables.

```sql
CREATE TABLE IF NOT EXISTS image_provider_profiles (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  name TEXT NOT NULL,
  base_url TEXT NOT NULL,
  api_key TEXT NOT NULL,
  model TEXT NOT NULL,
  compat_profile_id TEXT NOT NULL,
  supports_count_parameter INTEGER NOT NULL DEFAULT 1 CHECK (supports_count_parameter IN (0, 1)),
  active INTEGER NOT NULL DEFAULT 0 CHECK (active IN (0, 1)),
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  deleted_at INTEGER,
  FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
);
CREATE INDEX IF NOT EXISTS idx_image_provider_profiles_project ON image_provider_profiles(project_id, deleted_at, active, updated_at DESC);
CREATE UNIQUE INDEX IF NOT EXISTS idx_image_provider_profiles_project_name
  ON image_provider_profiles(project_id, lower(name))
  WHERE deleted_at IS NULL;
CREATE UNIQUE INDEX IF NOT EXISTS idx_image_provider_profiles_one_active
  ON image_provider_profiles(project_id)
  WHERE active = 1 AND deleted_at IS NULL;

CREATE TABLE IF NOT EXISTS image_generation_jobs (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  room_id TEXT,
  session_id TEXT,
  source_message_id TEXT,
  source_agent_id TEXT,
  source_task_id TEXT,
  provider_profile_id TEXT NOT NULL,
  workflow TEXT NOT NULL CHECK (workflow IN ('generate', 'image-to-image')),
  prompt TEXT NOT NULL,
  count INTEGER NOT NULL,
  quality TEXT NOT NULL,
  size TEXT NOT NULL,
  status TEXT NOT NULL CHECK (status IN ('queued', 'running', 'canceling', 'completed', 'failed', 'canceled')),
  message TEXT,
  error TEXT,
  created_at INTEGER NOT NULL,
  started_at INTEGER,
  completed_at INTEGER,
  updated_at INTEGER NOT NULL,
  FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE,
  FOREIGN KEY (room_id) REFERENCES rooms(id) ON DELETE SET NULL,
  FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE SET NULL,
  FOREIGN KEY (source_message_id) REFERENCES messages(id) ON DELETE SET NULL,
  FOREIGN KEY (source_task_id) REFERENCES tasks(id) ON DELETE SET NULL,
  FOREIGN KEY (provider_profile_id) REFERENCES image_provider_profiles(id) ON DELETE RESTRICT
);
CREATE INDEX IF NOT EXISTS idx_image_generation_jobs_project ON image_generation_jobs(project_id, status, updated_at DESC);
CREATE INDEX IF NOT EXISTS idx_image_generation_jobs_session ON image_generation_jobs(session_id, updated_at DESC);
CREATE INDEX IF NOT EXISTS idx_image_generation_jobs_room ON image_generation_jobs(room_id, updated_at DESC);

CREATE TABLE IF NOT EXISTS image_generation_outputs (
  id TEXT PRIMARY KEY,
  job_id TEXT NOT NULL,
  file_id TEXT NOT NULL,
  slot INTEGER NOT NULL,
  name TEXT NOT NULL,
  url TEXT NOT NULL,
  mime_type TEXT NOT NULL,
  size INTEGER NOT NULL,
  width INTEGER,
  height INTEGER,
  created_at INTEGER NOT NULL,
  FOREIGN KEY (job_id) REFERENCES image_generation_jobs(id) ON DELETE CASCADE,
  FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE CASCADE,
  UNIQUE (job_id, slot)
);
CREATE INDEX IF NOT EXISTS idx_image_generation_outputs_job ON image_generation_outputs(job_id, slot);

CREATE TABLE IF NOT EXISTS image_generation_source_images (
  id TEXT PRIMARY KEY,
  job_id TEXT NOT NULL,
  file_id TEXT NOT NULL,
  slot INTEGER NOT NULL,
  url TEXT NOT NULL,
  origin_job_id TEXT,
  origin_output_id TEXT,
  created_at INTEGER NOT NULL,
  FOREIGN KEY (job_id) REFERENCES image_generation_jobs(id) ON DELETE CASCADE,
  FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE RESTRICT,
  FOREIGN KEY (origin_job_id) REFERENCES image_generation_jobs(id) ON DELETE SET NULL,
  FOREIGN KEY (origin_output_id) REFERENCES image_generation_outputs(id) ON DELETE SET NULL,
  UNIQUE (job_id, slot)
);
CREATE INDEX IF NOT EXISTS idx_image_generation_source_images_job ON image_generation_source_images(job_id, slot);
```

- [x] **Step 4: Create shared backend types**

Create `packages/backend/src/image-generation/types.ts` with the contracts in the Shared Contracts section and export safe API variants:

```ts
export type ImageProviderProfileInput = {
  name: string;
  base_url: string;
  api_key?: string | null;
  model: string;
  compat_profile_id?: ImageProviderCompatProfileId;
  supports_count_parameter?: boolean;
};

export type SafeImageProviderProfile = Omit<ImageProviderProfile, 'api_key'> & {
  has_api_key: 0 | 1;
};
```

- [x] **Step 5: Export WebSocket event type**

Modify `packages/backend/src/types.ts` so `WsServerEvent` includes `ImageGenerationWsEvent`. Keep imports type-only and add it as one union member before the existing members.

```ts
import type { ImageGenerationWsEvent } from './image-generation/types.js';

export type WsServerEvent =
  | ImageGenerationWsEvent
  | { type: 'message:new'; roomId: string; message: Message }
  | { type: 'task_event:new'; roomId: string; event: TaskEvent }
  | { type: 'session_message:new'; sessionId: string; message: SessionMessage };
```

The snippet shows the insertion pattern only. Preserve every current `WsServerEvent` member already present in `packages/backend/src/types.ts`; do not replace the union with only these three examples.

- [x] **Step 6: Run schema test**

Run:

```bash
cd packages/backend && node --import tsx --test src/image-generation/jobs.test.ts
```

Expected: pass.

- [x] **Step 7: Commit**

```bash
git add packages/backend/src/db.ts packages/backend/src/types.ts packages/backend/src/image-generation/types.ts packages/backend/src/image-generation/jobs.test.ts
git commit -m "feat(backend): 添加图片生成数据结构"
```

### Task 2: Provider Profile 仓储与校验

**Files:**
- Create: `packages/backend/src/image-generation/validation.ts`
- Create: `packages/backend/src/image-generation/provider-profiles.ts`
- Test: `packages/backend/src/image-generation/provider-profiles.test.ts`

- [x] **Step 1: Write failing tests**

Cover create, update, activation, API key preservation, duplicate name rejection, and safe output hiding raw API key.

```ts
test('provider profiles create active safe profile and hide api key', () => {
  const project = projectRepo.create({ name: 'images', path: mkdtempSync(join(tmpdir(), 'images-')) });
  const created = imageProviderProfileRepo.create(project.id, {
    name: 'OpenAI Images',
    base_url: 'https://api.openai.com',
    api_key: 'secret-key',
    model: 'gpt-image-2',
    compat_profile_id: 'openai',
    supports_count_parameter: true,
  });

  assert.equal(created.base_url, 'https://api.openai.com/v1');
  assert.equal(created.active, 1);
  assert.equal(created.has_api_key, 1);
  assert.equal('api_key' in created, false);
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/provider-profiles.test.ts
```

Expected: fails because repo files do not exist.

- [x] **Step 3: Implement validation helpers**

Create `validation.ts`:

```ts
export function normalizeImageBaseUrl(value: string): string {
  const trimmed = value.trim().replace(/\/+$/, '');
  if (!trimmed) throw new Error('base_url is required');
  try {
    const url = new URL(trimmed);
    if (!['http:', 'https:'].includes(url.protocol)) throw new Error('invalid protocol');
    const pathname = url.pathname.replace(/\/+$/, '');
    url.pathname = pathname.endsWith('/v1') ? pathname : `${pathname}/v1`;
    url.search = '';
    url.hash = '';
    return url.toString().replace(/\/+$/, '');
  } catch {
    throw new Error('base_url must be a valid http(s) URL');
  }
}

export function normalizeImageProfileName(value: string): string {
  const name = value.trim();
  if (!name) throw new Error('name is required');
  if (name.length > 80) throw new Error('name is too long');
  return name;
}

export function normalizeImageModel(value: string): string {
  const model = value.trim();
  if (!model) throw new Error('model is required');
  return model;
}

export function normalizeOptionalApiKey(value: string | null | undefined): string | null {
  if (value === undefined || value === null) return null;
  return value.trim();
}
```

- [x] **Step 4: Implement repository**

Create `provider-profiles.ts` with this exported interface and repository object. The implementation must use `db.prepare` / transactions for each method; no method should return raw `api_key` through the safe profile API.

```ts
export interface ImageProviderProfileRepository {
  list(projectId: string): SafeImageProviderProfile[];
  getForProject(projectId: string, profileId: string): ImageProviderProfileWithSecret | undefined;
  getActive(projectId: string): ImageProviderProfileWithSecret | undefined;
  create(projectId: string, input: ImageProviderProfileInput): SafeImageProviderProfile;
  update(projectId: string, profileId: string, input: ImageProviderProfileInput): SafeImageProviderProfile;
  activate(projectId: string, profileId: string): SafeImageProviderProfile;
  softDelete(projectId: string, profileId: string): SafeImageProviderProfile | undefined;
}

export const imageProviderProfileRepo: ImageProviderProfileRepository = {
  list,
  getForProject,
  getActive,
  create,
  update,
  activate,
  softDelete,
};
```

Use a transaction for `create` and `activate`:

```ts
const activateProfile = db.transaction((projectId: string, profileId: string) => {
  db.prepare('UPDATE image_provider_profiles SET active = 0, updated_at = ? WHERE project_id = ? AND deleted_at IS NULL')
    .run(now(), projectId);
  db.prepare('UPDATE image_provider_profiles SET active = 1, updated_at = ? WHERE id = ? AND project_id = ? AND deleted_at IS NULL')
    .run(now(), profileId, projectId);
});
```

When updating with empty or omitted `api_key`, preserve the existing key:

```ts
const normalizedApiKey = normalizeOptionalApiKey(input.api_key);
const nextApiKey = normalizedApiKey === null || normalizedApiKey === ''
  ? current.api_key
  : normalizedApiKey;
```

- [x] **Step 5: Run provider profile tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/provider-profiles.test.ts
```

Expected: pass.

- [x] **Step 6: Commit**

```bash
git add packages/backend/src/image-generation/validation.ts packages/backend/src/image-generation/provider-profiles.ts packages/backend/src/image-generation/provider-profiles.test.ts
git commit -m "feat(backend): 添加图片提供方配置"
```

### Task 3: Provider Model Discovery

**Files:**
- Create: `packages/backend/src/image-generation/provider-models.ts`
- Test: `packages/backend/src/image-generation/provider-models.test.ts`

- [x] **Step 1: Write failing tests**

Use injected `fetchImpl` to avoid network.

```ts
test('provider model discovery normalizes image and other models', async () => {
  const result = await listImageProviderModels({
    baseUrl: 'https://example.com/v1',
    apiKey: 'secret',
    fetchImpl: async () => new Response(JSON.stringify({
      data: [{ id: 'gpt-image-2' }, { id: 'gpt-5.5' }],
    })),
  });

  assert.deepEqual(result.models, [
    { id: 'gpt-image-2', category: 'image' },
    { id: 'gpt-5.5', category: 'other' },
  ]);
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/provider-models.test.ts
```

Expected: fails because function is missing.

- [x] **Step 3: Implement model discovery**

Create:

```ts
export type ImageProviderModel = { id: string; category: 'image' | 'other' };

export async function listImageProviderModels(input: {
  baseUrl: string;
  apiKey: string;
  fetchImpl?: typeof fetch;
}): Promise<{ normalized_base_url: string; models: ImageProviderModel[]; warning: string | null }> {
  const normalizedBaseUrl = normalizeImageBaseUrl(input.baseUrl);
  const fetcher = input.fetchImpl ?? fetch;
  try {
    const response = await fetcher(`${normalizedBaseUrl}/models`, {
      headers: { Authorization: `Bearer ${input.apiKey}` },
    });
    if (!response.ok) {
      return { normalized_base_url: normalizedBaseUrl, models: [], warning: `模型列表拉取失败：HTTP ${response.status}` };
    }
    const payload = await response.json() as { data?: Array<{ id?: unknown }> };
    const models = (payload.data ?? [])
      .map((item) => String(item.id ?? '').trim())
      .filter(Boolean)
      .map((id) => ({ id, category: inferImageModelCategory(id) }));
    return { normalized_base_url: normalizedBaseUrl, models, warning: null };
  } catch (error) {
    return { normalized_base_url: normalizedBaseUrl, models: [], warning: `模型列表拉取失败：${(error as Error).message}` };
  }
}
```

- [x] **Step 4: Run tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/provider-models.test.ts
```

Expected: pass.

- [x] **Step 5: Commit**

```bash
git add packages/backend/src/image-generation/provider-models.ts packages/backend/src/image-generation/provider-models.test.ts
git commit -m "feat(backend): 添加图片模型发现"
```

### Task 4: OpenAI-compatible Runtime

**Files:**
- Create: `packages/backend/src/image-generation/openai-compatible.ts`
- Test: `packages/backend/src/image-generation/openai-compatible.test.ts`

- [x] **Step 1: Write failing runtime tests**

Cover base64 response, URL response, non-JSON errors, HTTP code normalization, and image edit multipart shape.

```ts
test('runtime saves base64 image generation result into buffers', async () => {
  const pngBytes = Buffer.from('fake-png');
  const response = await requestOpenAICompatibleImageGeneration({
    baseUrl: 'https://example.com/v1',
    apiKey: 'secret',
    model: 'gpt-image-2',
    prompt: 'apple',
    count: 1,
    quality: 'auto',
    size: '1024x1024',
    fetchImpl: async () => new Response(JSON.stringify({
      data: [{ b64_json: pngBytes.toString('base64') }],
    }), { status: 200 }),
  });

  assert.equal(response.images.length, 1);
  assert.deepEqual(response.images[0]?.data, pngBytes);
  assert.equal(response.images[0]?.mimeType, 'image/png');
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/openai-compatible.test.ts
```

Expected: fails because runtime file does not exist.

- [x] **Step 3: Implement text-to-image transport**

Create:

```ts
export async function requestOpenAICompatibleImageGeneration(input: ImageGenerationRuntimeRequest): Promise<ImageGenerationRuntimeResponse> {
  const response = await input.fetchImpl(`${input.baseUrl}/images/generations`, {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${input.apiKey}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      model: input.model,
      prompt: input.prompt,
      n: input.count,
      quality: input.quality === 'auto' ? undefined : input.quality,
      size: input.size === 'auto' ? undefined : input.size,
    }),
    signal: input.signal,
  });
  return parseImageRuntimeResponse(response, input.fetchImpl, input.signal);
}
```

- [x] **Step 4: Implement response parser**

Support `b64_json`, `url`, and `data:image/...;base64`.

```ts
async function parseImageRuntimeResponse(response: Response, fetchImpl: typeof fetch, signal?: AbortSignal): Promise<ImageGenerationRuntimeResponse> {
  if (!response.ok) throw new Error(normalizeImageGenerationError(await response.text(), response.status));
  const payload = await response.json() as { data?: Array<{ b64_json?: string; url?: string }> };
  const images = [];
  for (const item of payload.data ?? []) {
    if (item.b64_json) {
      images.push({ data: Buffer.from(item.b64_json, 'base64'), mimeType: 'image/png' });
      continue;
    }
    if (item.url?.startsWith('data:')) {
      images.push(decodeDataUrlImage(item.url));
      continue;
    }
    if (item.url) {
      const asset = await fetchImpl(item.url, { signal });
      if (!asset.ok) throw new Error(`图片资源下载失败：HTTP ${asset.status}`);
      images.push({ data: Buffer.from(await asset.arrayBuffer()), mimeType: asset.headers.get('content-type') ?? 'image/png' });
    }
  }
  return { images };
}
```

- [x] **Step 5: Implement image-to-image transport**

Use `FormData` for `/images/edits` with project-owned source file buffers.

```ts
export async function requestOpenAICompatibleImageEdit(input: ImageGenerationEditRuntimeRequest): Promise<ImageGenerationRuntimeResponse> {
  const body = new FormData();
  body.set('model', input.model);
  body.set('prompt', input.prompt);
  body.set('n', String(input.count));
  if (input.quality !== 'auto') body.set('quality', input.quality);
  if (input.size !== 'auto') body.set('size', input.size);
  for (const source of input.sourceImages) {
    body.append('image[]', new Blob([source.data], { type: source.mimeType }), source.name);
  }
  const response = await input.fetchImpl(`${input.baseUrl}/images/edits`, {
    method: 'POST',
    headers: { Authorization: `Bearer ${input.apiKey}` },
    body,
    signal: input.signal,
  });
  return parseImageRuntimeResponse(response, input.fetchImpl, input.signal);
}
```

- [x] **Step 6: Run runtime tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/openai-compatible.test.ts
```

Expected: pass.

- [x] **Step 7: Commit**

```bash
git add packages/backend/src/image-generation/openai-compatible.ts packages/backend/src/image-generation/openai-compatible.test.ts
git commit -m "feat(backend): 添加图片生成运行时"
```

### Task 5: Job Repository 与状态流转

**Files:**
- Create: `packages/backend/src/image-generation/jobs.ts`
- Test: `packages/backend/src/image-generation/jobs.test.ts`

- [x] **Step 1: Extend failing tests**

Add tests for create/list/get/status/output/source images/startup recovery.

```ts
test('image job repo creates queued job and appends output', () => {
  const project = projectRepo.create({ name: 'images', path: mkdtempSync(join(tmpdir(), 'images-')) });
  const profile = createImageProviderProfileForTest(project.id);
  const job = imageGenerationJobRepo.create({
    project_id: project.id,
    provider_profile_id: profile.id,
    workflow: 'generate',
    prompt: 'apple',
    count: 1,
    quality: 'auto',
    size: 'auto',
  });

  assert.equal(job.status, 'queued');
  imageGenerationJobRepo.markRunning(job.id);
  const running = imageGenerationJobRepo.get(job.id);
  assert.equal(running?.status, 'running');
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/jobs.test.ts
```

Expected: fails until repo is implemented.

- [x] **Step 3: Implement repository**

Create `jobs.ts` with this exported repository interface. Each status method must read the updated row after writing and throw `image generation job not found` when the target job does not exist.

```ts
export interface ImageGenerationJobRepository {
  create(input: ImageGenerationJobCreateInput): ImageGenerationJob;
  get(jobId: string): ImageGenerationJob | undefined;
  listByProject(projectId: string, filters?: { status?: ImageGenerationStatus; sessionId?: string; roomId?: string }): ImageGenerationJob[];
  markRunning(jobId: string): ImageGenerationJob;
  markCanceling(jobId: string): ImageGenerationJob;
  markCanceled(jobId: string, message: string): ImageGenerationJob;
  markFailed(jobId: string, error: string): ImageGenerationJob;
  markCompleted(jobId: string, message: string | null): ImageGenerationJob;
  appendOutput(input: ImageGenerationOutputCreateInput): ImageGenerationOutput;
  listOutputs(jobId: string): ImageGenerationOutput[];
  addSourceImage(input: ImageGenerationSourceImageCreateInput): ImageGenerationSourceImage;
  listSourceImages(jobId: string): ImageGenerationSourceImage[];
  recoverInterruptedJobs(): number;
}

export const imageGenerationJobRepo: ImageGenerationJobRepository = {
  create,
  get,
  listByProject,
  markRunning,
  markCanceling,
  markCanceled,
  markFailed,
  markCompleted,
  appendOutput,
  listOutputs,
  addSourceImage,
  listSourceImages,
  recoverInterruptedJobs,
};
```

`recoverInterruptedJobs()` must mark `queued`, `running`, and `canceling` as `canceled` with message `后端重启，图片生成任务已停止。`

- [x] **Step 4: Run tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/jobs.test.ts
```

Expected: pass.

- [x] **Step 5: Commit**

```bash
git add packages/backend/src/image-generation/jobs.ts packages/backend/src/image-generation/jobs.test.ts
git commit -m "feat(backend): 添加图片任务仓储"
```

### Task 6: 输出文件沉淀到项目文件库

**Files:**
- Create: `packages/backend/src/image-generation/outputs.ts`
- Test: `packages/backend/src/image-generation/outputs.test.ts`

- [x] **Step 1: Write failing tests**

Verify file is written under `projectFileUploadRoot`, creates `files` row, and output links to `file_id`.

```ts
test('persist generated output writes project file and creates output record', async () => {
  const result = await persistImageGenerationOutput({
    projectId: project.id,
    jobId: job.id,
    slot: 1,
    image: { data: Buffer.from('png'), mimeType: 'image/png' },
  });

  assert.equal(result.output.slot, 1);
  assert.equal(result.file.project_id, project.id);
  assert.equal(result.file.uploaded_by_id, 'image-generation');
  assert.equal(result.file.uploaded_by_name, '图片生成');
  await access(result.file.storage_path, constants.F_OK);
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/outputs.test.ts
```

Expected: fails because persistence function does not exist.

- [x] **Step 3: Implement output persistence**

Use existing upload helpers.

```ts
export async function persistImageGenerationOutput(input: PersistImageGenerationOutputInput): Promise<{
  file: ProjectFile;
  output: ImageGenerationOutput;
}> {
  const uploadDir = await ensureProjectFileUploadDir(input.projectId);
  const extension = extensionFromMimeType(input.image.mimeType);
  const storedName = safeUploadFileName(`image-${input.slot}.${extension}`);
  const storagePath = join(uploadDir, storedName);
  await writeFile(storagePath, input.image.data);
  const file = fileRepo.create({
    project_id: input.projectId,
    original_name: `generated-image-${input.slot}.${extension}`,
    stored_name: storedName,
    mime_type: input.image.mimeType,
    size: input.image.data.byteLength,
    url: buildProjectFileUrl(input.projectId, storedName),
    storage_path: storagePath,
    uploaded_by_id: 'image-generation',
    uploaded_by_name: '图片生成',
  });
  const output = imageGenerationJobRepo.appendOutput({
    job_id: input.jobId,
    file_id: file.id,
    slot: input.slot,
    name: file.original_name,
    url: file.url,
    mime_type: file.mime_type,
    size: file.size,
    width: null,
    height: null,
  });
  return { file, output };
}
```

- [x] **Step 4: Run tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/outputs.test.ts
```

Expected: pass.

- [x] **Step 5: Commit**

```bash
git add packages/backend/src/image-generation/outputs.ts packages/backend/src/image-generation/outputs.test.ts
git commit -m "feat(backend): 沉淀图片生成结果"
```

### Task 7: 队列与统一服务

**Files:**
- Create: `packages/backend/src/image-generation/queue.ts`
- Create: `packages/backend/src/image-generation/service.ts`
- Test: `packages/backend/src/image-generation/queue.test.ts`

- [x] **Step 1: Write failing queue tests**

Cover serial execution, cancel queued, cancel running with abort, partial success when provider does not support count.

```ts
test('image generation queue runs one job at a time', async () => {
  const firstStarted = new Promise<void>((resolve) => { resolveFirstStarted = resolve; });
  const releaseFirst = new Promise<void>((resolve) => { releaseFirstJob = resolve; });
  const service = createImageGenerationServiceForTest({
    runtime: async (request) => {
      if (request.jobId === first.id) {
        resolveFirstStarted();
        await releaseFirst;
      }
      return { images: [{ data: Buffer.from('png'), mimeType: 'image/png' }] };
    },
  });

  service.enqueue(first.id);
  service.enqueue(second.id);

  await firstStarted;
  assert.equal(imageGenerationJobRepo.get(second.id)?.status, 'queued');
  releaseFirstJob();
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/queue.test.ts
```

Expected: fails because queue/service are missing.

- [x] **Step 3: Implement queue**

```ts
export class ImageGenerationQueue {
  private runningJobId: string | null = null;
  private pending: string[] = [];
  private abortControllers = new Map<string, AbortController>();

  enqueue(jobId: string): void {
    if (this.runningJobId === jobId || this.pending.includes(jobId)) return;
    this.pending.push(jobId);
    void this.drain();
  }

  cancel(jobId: string): boolean {
    const pendingIndex = this.pending.indexOf(jobId);
    if (pendingIndex >= 0) {
      this.pending.splice(pendingIndex, 1);
      return true;
    }
    this.abortControllers.get(jobId)?.abort();
    return this.runningJobId === jobId;
  }

  snapshot(): { running: string[]; pending: string[]; running_count: number; pending_count: number } {
    return {
      running: this.runningJobId ? [this.runningJobId] : [],
      pending: [...this.pending],
      running_count: this.runningJobId ? 1 : 0,
      pending_count: this.pending.length,
    };
  }
}
```

- [x] **Step 4: Implement service**

`service.ts` should provide a default singleton and factory for tests. Define the interface first, then implement the object with real functions in the same file.

```ts
export interface ImageGenerationService {
  createJob(input: ImageGenerationCreateJobInput): Promise<{ job: ImageGenerationJob; outputs: ImageGenerationOutput[] }>;
  enqueue(jobId: string): void;
  cancelJob(jobId: string): Promise<{ job: ImageGenerationJob }>;
  retryJob(jobId: string): Promise<{ job: ImageGenerationJob }>;
  runJob(jobId: string, signal: AbortSignal): Promise<void>;
  waitForCompletion(jobId: string): Promise<{ job: ImageGenerationJob; outputs: ImageGenerationOutput[] }>;
}

export function createImageGenerationService(deps: ImageGenerationServiceDeps = {}): ImageGenerationService {
  const queue = new ImageGenerationQueue((jobId, signal) => runJob(jobId, signal, deps));
  return {
    createJob: (input) => createJob(input, queue, deps),
    enqueue: (jobId) => queue.enqueue(jobId),
    cancelJob: (jobId) => cancelJob(jobId, queue, deps),
    retryJob: (jobId) => retryJob(jobId, queue, deps),
    runJob: (jobId, signal) => runJob(jobId, signal, deps),
    waitForCompletion: (jobId) => waitForCompletion(jobId, deps),
  };
}

export const imageGenerationService = createImageGenerationService();
```

`runJob` must:

1. Load job and provider profile.
2. Mark running.
3. Execute runtime.
4. Persist each output.
5. Mark completed if not aborted.
6. Mark canceled on abort.
7. Mark failed with normalized error on failure.
8. Publish image job events through injectable callback.

`waitForCompletion(jobId)` should poll the repository until the job reaches `completed`, `failed`, or `canceled`, then return `{ job, outputs }`. Use a timeout in tests so a missing terminal transition fails quickly.

- [x] **Step 5: Run queue tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/queue.test.ts
```

Expected: pass.

- [x] **Step 6: Commit**

```bash
git add packages/backend/src/image-generation/queue.ts packages/backend/src/image-generation/service.ts packages/backend/src/image-generation/queue.test.ts
git commit -m "feat(backend): 添加图片生成队列"
```

执行记录：

- RED：`rtk node --import tsx --test packages/backend/src/image-generation/queue.test.ts` 先失败于 `service.js` 缺失，证明测试覆盖新入口。
- GREEN：新增 `queue.ts`、`service.ts`，覆盖串行执行、取消 queued、取消 running abort、不支持 count 的部分成功、生命周期事件。
- Review 修复：三轮实际代码审查；已修复 unmanaged running/canceling 取消、project-scoped 服务方法、服务层 count 限制、非 queued 任务执行门禁，最终审查结论为“无 Critical/Important 阻塞”。
- 验证：`rtk node --import tsx --test packages/backend/src/image-generation/queue.test.ts packages/backend/src/image-generation/jobs.test.ts packages/backend/src/image-generation/outputs.test.ts packages/backend/src/image-generation/provider-profiles.test.ts packages/backend/src/image-generation/provider-models.test.ts packages/backend/src/image-generation/openai-compatible.test.ts` 通过 46/46；`rtk npm run build -w @openclaw-room/backend` 通过；`rtk git diff --cached --check` 通过。

### Task 8: API Routes

**Files:**
- Create: `packages/backend/src/image-generation/routes.ts`
- Modify: `packages/backend/src/routes.ts`
- Test: `packages/backend/src/image-generation/routes.test.ts`

- [x] **Step 1: Write failing route tests**

Use existing in-memory Express test style from `files.routes.test.ts`. Cover project boundary, provider profile CRUD, model discovery, job create/list/get/cancel/retry.

```ts
test('image job route creates queued job for project', async () => {
  const project = createProject('image-route');
  createActiveImageProviderProfile(project.id);

  const response = await request(`/api/projects/${project.id}/image-jobs`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      workflow: 'generate',
      prompt: 'apple',
      count: 1,
      quality: 'auto',
      size: 'auto',
    }),
  });

  assert.equal(response.status, 202);
  const payload = await response.json() as { job: ImageGenerationJob };
  assert.equal(payload.job.project_id, project.id);
  assert.equal(payload.job.status, 'queued');
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/routes.test.ts
```

Expected: fails because router is missing.

- [x] **Step 3: Implement router**

Create `imageGenerationRouter` and add handlers for every endpoint in the route table below. The code block shows one complete handler shape; use the same validation/error pattern for the remaining routes.

```ts
export const imageGenerationRouter = Router({ mergeParams: true });

imageGenerationRouter.get('/projects/:projectId/image-provider-profiles', (req, res) => {
  if (!projectRepo.get(req.params.projectId)) {
    res.status(404).json({ error: 'project not found' });
    return;
  }
  res.json(imageProviderProfileRepo.list(req.params.projectId));
});
```

Route table:

```text
GET    /projects/:projectId/image-provider-profiles              -> imageProviderProfileRepo.list
POST   /projects/:projectId/image-provider-profiles              -> imageProviderProfileRepo.create
PATCH  /projects/:projectId/image-provider-profiles/:profileId   -> imageProviderProfileRepo.update
DELETE /projects/:projectId/image-provider-profiles/:profileId   -> imageProviderProfileRepo.softDelete
POST   /projects/:projectId/image-provider-profiles/:profileId/activate -> imageProviderProfileRepo.activate
POST   /projects/:projectId/image-provider-profiles/models       -> listImageProviderModels
GET    /projects/:projectId/image-jobs                           -> imageGenerationJobRepo.listByProject
POST   /projects/:projectId/image-jobs                           -> imageGenerationService.createJob
GET    /projects/:projectId/image-jobs/:jobId                    -> imageGenerationJobRepo.get + listOutputs
POST   /projects/:projectId/image-jobs/:jobId/cancel             -> imageGenerationService.cancelJob
POST   /projects/:projectId/image-jobs/:jobId/retry              -> imageGenerationService.retryJob
DELETE /projects/:projectId/image-jobs/:jobId                    -> imageGenerationJobRepo.markCanceled for terminal cleanup only
```

Validation rules:

- `projectId` must exist.
- `session_id`, if provided, must belong to project.
- `room_id`, if provided, must belong to project.
- `source_file_ids` must belong to project and be active image files.
- `prompt` must be non-empty and at most 8000 characters.
- `count` must be integer from 1 to 6.
- `workflow = image-to-image` requires at least one source file.

- [x] **Step 4: Mount router**

In `packages/backend/src/routes.ts`:

```ts
import { imageGenerationRouter } from './image-generation/routes.js';

router.use(imageGenerationRouter);
```

- [x] **Step 5: Run route tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/routes.test.ts
```

Expected: pass.

- [x] **Step 6: Commit**

```bash
git add packages/backend/src/image-generation/routes.ts packages/backend/src/image-generation/routes.test.ts packages/backend/src/routes.ts
git commit -m "feat(backend): 添加图片生成接口"
```

执行记录：

- RED：`rtk node --import tsx --test packages/backend/src/image-generation/routes.test.ts` 先失败于 `image-generation/routes.js` 缺失。
- GREEN：新增 `image-generation/routes.ts` 并挂载到主 router，覆盖 provider profile CRUD/activate、models、job create/list/detail/cancel/retry。
- 安全调整：`DELETE /projects/:projectId/image-jobs/:jobId` 暂返回 405，不再把 completed/failed 历史状态改成 canceled；models 路由只接受同项目已保存 `profile_id`，不接受任意 `base_url/api_key` body。
- Review 修复：实际代码审查指出 DELETE 状态破坏和任意触网风险；已修复并补跨项目 GET/cancel/retry/source_file 隐藏为 404 的测试，复审结论为“无 Critical/Important 阻塞”。
- 验证：`rtk node --import tsx --test packages/backend/src/image-generation/routes.test.ts packages/backend/src/image-generation/queue.test.ts packages/backend/src/image-generation/jobs.test.ts packages/backend/src/image-generation/outputs.test.ts packages/backend/src/image-generation/provider-profiles.test.ts packages/backend/src/image-generation/provider-models.test.ts packages/backend/src/image-generation/openai-compatible.test.ts` 通过 51/51；`rtk npm run build -w @openclaw-room/backend` 通过；`rtk git diff --cached --check` 通过；临时后端 HTTP smoke 使用 `curl` 创建 project/provider、通过 `profile_id` 调用 models、拒绝任意 base URL body、创建 job 并查询 list/detail 成功。

### Task 9: WebSocket 项目订阅与图片任务事件

**Files:**
- Modify: `packages/backend/src/ws-hub.ts`
- Modify: `packages/backend/src/server.ts`
- Modify: `packages/backend/src/types.ts`
- Test: `packages/backend/src/image-generation/routes.test.ts`
- Modify: `packages/frontend/src/lib/ws.ts`
- Test: `packages/frontend/src/lib/imageGenerationWs.test.ts`

- [x] **Step 1: Write failing tests**

Backend: verify project subscription receives image job events. Frontend: verify `roomSocket.subscribeProject(projectId)` sends `project:subscribe`.

```ts
test('ws hub broadcasts image job events to project subscribers', () => {
  const socket = createFakeOpenSocket();
  wsHub.subscribeProject('project-1', socket);
  wsHub.broadcastProject('project-1', {
    type: 'image_job:updated',
    projectId: 'project-1',
    job: fakeImageJob,
  });
  assert.equal(socket.sent.length, 1);
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/routes.test.ts
cd packages/frontend && node --import tsx --test src/lib/imageGenerationWs.test.ts
```

Expected: fails because project subscription methods do not exist.

- [x] **Step 3: Add backend project subscription**

In `ws-hub.ts`:

```ts
private projectSubscriptions = new Map<string, Set<WebSocket>>();

subscribeProject(projectId: string, socket: WebSocket): void {
  this.add(this.projectSubscriptions, projectId, socket);
}

unsubscribeProject(projectId: string, socket: WebSocket): void {
  this.projectSubscriptions.get(projectId)?.delete(socket);
}

broadcastProject(projectId: string, event: WsServerEvent): void {
  this.broadcastTo(this.projectSubscriptions, projectId, event);
}
```

Update `removeSocket` and `broadcastAll` to include project subscriptions.

- [x] **Step 4: Add backend WebSocket client event handling**

In `server.ts`:

```ts
else if (event.type === 'project:subscribe') wsHub.subscribeProject(event.projectId, socket);
else if (event.type === 'project:unsubscribe') wsHub.unsubscribeProject(event.projectId, socket);
```

Update `WsClientEvent` type in backend and frontend:

```ts
| { type: 'project:subscribe'; projectId: string }
| { type: 'project:unsubscribe'; projectId: string }
```

- [x] **Step 5: Add frontend project subscription methods**

In `packages/frontend/src/lib/ws.ts`:

```ts
private subscribedProjects = new Set<string>();

subscribeProject(projectId: string): void {
  this.closeWhenOpen = false;
  this.subscribedProjects.add(projectId);
  if (this.ws?.readyState === WebSocket.OPEN) {
    this.ws.send(JSON.stringify({ type: 'project:subscribe', projectId }));
  } else {
    this.connectSoon();
  }
}

unsubscribeProject(projectId: string): void {
  this.subscribedProjects.delete(projectId);
  if (this.ws?.readyState === WebSocket.OPEN) {
    this.ws.send(JSON.stringify({ type: 'project:unsubscribe', projectId }));
  }
  this.closeIfIdle();
}
```

- [x] **Step 6: Wire service event publisher**

In `image-generation/service.ts`, publish:

```ts
function publishImageJobEvent(event: ImageGenerationWsEvent): void {
  wsHub.broadcastProject(event.projectId, event);
  if (event.sessionId) wsHub.broadcastSession(event.sessionId, event);
  if (event.roomId) wsHub.broadcast(event.roomId, event);
}
```

- [x] **Step 7: Run tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/routes.test.ts
cd packages/frontend && node --import tsx --test src/lib/imageGenerationWs.test.ts
```

Expected: pass.

- [x] **Step 8: Commit**

```bash
git add packages/backend/src/ws-hub.ts packages/backend/src/server.ts packages/backend/src/types.ts packages/backend/src/image-generation/service.ts packages/frontend/src/lib/ws.ts packages/frontend/src/lib/imageGenerationWs.test.ts
git commit -m "feat: 推送图片生成任务事件"
```

执行记录：

- RED：`rtk node --import tsx --test packages/backend/src/ws-hub.test.ts packages/backend/src/image-generation/queue.test.ts` 先失败于 `wsHub.subscribeProject is not a function`；`rtk node --import tsx --test packages/frontend/src/lib/imageGenerationWs.test.ts packages/frontend/src/lib/ws.test.ts` 先失败于 `sessionSocket.subscribeProject is not a function`。
- GREEN：新增 backend project-level WebSocket subscription，`server.ts` 支持 `project:subscribe` / `project:unsubscribe`；`image-generation/service.ts` 默认发布图片任务事件；frontend `sessionSocket` 支持项目订阅、断线恢复和 idle close。
- Review 修复：实际代码审查指出同一 socket 同时订阅 project/session/room 时会收到重复 `image_job:*` 事件，`broadcastAll` 也存在跨 scope 重复风险，且单个 `ws.send` 抛错可能阻断后续投递；已改为 `wsHub.broadcastScoped` 聚合目标 socket 后一次投递，`broadcastAll` 同样跨集合去重，`send` 异常按 socket 隔离，并清理空 subscription bucket。另补 frontend `destroy()` 清空 project subscriptions，避免 close event 后误重连。
- 复审：第二轮只读代码审查无 Critical/Important；Minor 记录为重复 `subscribeProject` 与既有 room/session 语义一致，后续在 Phase 2 hook 层按单 owner/refcount 处理。
- 验证：`rtk node --import tsx --test packages/backend/src/ws-hub.test.ts packages/backend/src/image-generation/routes.test.ts packages/backend/src/image-generation/queue.test.ts packages/backend/src/image-generation/jobs.test.ts packages/backend/src/image-generation/outputs.test.ts packages/backend/src/image-generation/provider-profiles.test.ts packages/backend/src/image-generation/provider-models.test.ts packages/backend/src/image-generation/openai-compatible.test.ts` 通过 60/60；`rtk node --import tsx --test packages/frontend/src/lib/imageGenerationWs.test.ts packages/frontend/src/lib/ws.test.ts` 通过 9/9；`rtk npm run build -w @openclaw-room/backend` 通过；`rtk npm run build -w @openclaw-room/frontend` 通过，Vite 仅保留既有 chunk size warning。
- 后端功能测试：临时后端 WebSocket smoke 使用 `ws://127.0.0.1:17333/ws?localToken=ws-smoke-token` 发送 `project:subscribe`，再通过 HTTP 创建 project/provider/image job，收到 `image_job:created`、`image_job:updated`、`image_job:failed`，并确认事件 JSON 不包含 API Key。

### Phase 1 总审查修复记录

- [x] **启动恢复接入**

总审查发现 `imageGenerationJobRepo.recoverInterruptedJobs()` 只有 repo 层实现，没有在生产启动路径调用，重启后 queued/running/canceling 图片任务可能永久卡死。已在 `server.ts` 启动监听后调用恢复函数，并捕获错误避免阻断服务启动。

- [x] **来源资源项目边界**

总审查发现创建 image job 时 `source_message_id` / `source_task_id` 只依赖 FK 存在性，没有校验同项目。已在 routes 创建入口补充 source message 所属 room 的 project 校验，以及 source task 的 project 校验；跨项目来源隐藏为 404。

- [x] **回归测试**

新增真实 server startup 测试：父进程在临时 DB 创建 queued/running/canceling job，再启动 `server.ts` 子进程并确认任务恢复为 canceled。扩展 routes 测试，覆盖跨项目 `source_message_id` / `source_task_id` 创建请求返回 404。

- [x] **复审与最终验证**

复审结果：针对修复项的只读复审无 Critical/Important；Minor 指出 startup 测试固定端口、未读取 stdout、未等待子进程退出。已改为动态端口、读取 stdout/stderr、等待进程退出，并断言输出流不包含测试 API key。

验证：`rtk node --import tsx --test packages/backend/src/image-generation/*.test.ts packages/backend/src/ws-hub.test.ts` 通过 61/61；`rtk node --import tsx --test packages/frontend/src/lib/imageGenerationWs.test.ts packages/frontend/src/lib/ws.test.ts` 通过 9/9；`rtk npm run build` 通过，Vite 仅保留既有 chunk size warning。最终提交前还会重跑完整 smoke。

## Phase 2: 独立图片工作台

### Task 10: 前端 API 与类型

**Files:**
- Modify: `packages/frontend/src/lib/types.ts`
- Modify: `packages/frontend/src/lib/api.ts`
- Test: `packages/frontend/src/lib/imageGenerationApi.test.ts`

- [x] **Step 1: Write failing frontend API tests**

```ts
test('image generation api posts image job payload', async () => {
  const calls: Array<{ url: string; init?: RequestInit }> = [];
  mockFetch(async (url, init) => {
    calls.push({ url: String(url), init });
    return jsonResponse({ job: fakeImageJob, outputs: [] }, 202);
  });

  await api.createImageJob('project-1', {
    workflow: 'generate',
    prompt: 'apple',
    count: 1,
    quality: 'auto',
    size: 'auto',
  });

  assert.equal(calls[0]?.url, '/api/projects/project-1/image-jobs');
  assert.equal(calls[0]?.init?.method, 'POST');
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/frontend && node --import tsx --test src/lib/imageGenerationApi.test.ts
```

Expected: fails because API functions do not exist.

- [x] **Step 3: Add frontend types**

Add `ImageProviderProfile`, `ImageGenerationJob`, `ImageGenerationOutput`, `ImageGenerationSourceImage`, `ImageJobCreateInput`, and `ImageProviderProfileInput` matching Shared Contracts.

- [x] **Step 4: Add API client methods**

In `api.ts`:

```ts
listImageProviderProfiles: (projectId: string) =>
  request<ImageProviderProfile[]>(`/projects/${projectId}/image-provider-profiles`),
createImageProviderProfile: (projectId: string, input: ImageProviderProfileInput) =>
  request<ImageProviderProfile>(`/projects/${projectId}/image-provider-profiles`, {
    method: 'POST',
    body: JSON.stringify(input),
  }),
listImageJobs: (projectId: string, filters: { sessionId?: string; roomId?: string; status?: ImageGenerationStatus } = {}) => {
  const params = new URLSearchParams();
  if (filters.sessionId) params.set('sessionId', filters.sessionId);
  if (filters.roomId) params.set('roomId', filters.roomId);
  if (filters.status) params.set('status', filters.status);
  const query = params.toString();
  return request<Array<{ job: ImageGenerationJob; outputs: ImageGenerationOutput[] }>>(
    `/projects/${projectId}/image-jobs${query ? `?${query}` : ''}`,
  );
},
createImageJob: (projectId: string, input: ImageJobCreateInput) =>
  request<{ job: ImageGenerationJob; outputs: ImageGenerationOutput[] }>(`/projects/${projectId}/image-jobs`, {
    method: 'POST',
    body: JSON.stringify(input),
  }),
cancelImageJob: (projectId: string, jobId: string) =>
  request<{ job: ImageGenerationJob }>(`/projects/${projectId}/image-jobs/${jobId}/cancel`, { method: 'POST' }),
retryImageJob: (projectId: string, jobId: string) =>
  request<{ job: ImageGenerationJob }>(`/projects/${projectId}/image-jobs/${jobId}/retry`, { method: 'POST' }),
```

- [x] **Step 5: Run tests**

```bash
cd packages/frontend && node --import tsx --test src/lib/imageGenerationApi.test.ts
```

Expected: pass.

- [x] **Step 6: Commit**

```bash
git add packages/frontend/src/lib/types.ts packages/frontend/src/lib/api.ts packages/frontend/src/lib/imageGenerationApi.test.ts
git commit -m "feat(frontend): 添加图片生成客户端"
```

执行记录：

- RED：`rtk node --import tsx --test packages/frontend/src/lib/imageGenerationApi.test.ts` 先失败于 `api.createImageJob/listImageJobs/listImageProviderProfiles is not a function`。
- GREEN：新增前端 image generation 类型和 API client，覆盖 provider profile CRUD/activate、models、job list/detail/create/cancel/retry；list jobs 按后端真实契约返回 `{ jobs }`，detail 返回 `{ job, outputs, source_images }`。
- Review：只读代码审查无 Critical/Important；按 Minor 建议补充 provider create/update body 断言和 detail `source_images` 断言。图片 WebSocket 事件类型仍在 `lib/ws.ts` 内私有重复定义，留到后续事件 hook 任务收敛。
- 最终验证：`rtk node --import tsx --test packages/frontend/src/lib/imageGenerationApi.test.ts` 通过 3/3；`rtk npm run build -w @openclaw-room/frontend` 通过，Vite 仅保留既有 chunk size warning；`rtk git diff --cached --check` 通过。

### Task 11: 工作台页面与导航

**Files:**
- Create: `packages/frontend/src/pages/ImageWorkbenchPage.tsx`
- Create: `packages/frontend/src/image-generation/ImageGenerationShell.tsx`
- Modify: `packages/frontend/src/main.tsx`
- Modify: `packages/frontend/src/components/AppShell.tsx`
- Test: `packages/frontend/src/image-generation/ImageGenerationShell.test.tsx`
- Test: `packages/frontend/src/components/AppShell.test.tsx`

- [x] **Step 1: Write failing render test**

```tsx
test('image workbench shell renders three working regions', () => {
  render(<ImageGenerationShell projectId="project-1" />);
  assert.ok(screen.getByRole('region', { name: '图片生成配置' }));
  assert.ok(screen.getByRole('region', { name: '图片生成任务' }));
  assert.ok(screen.getByRole('region', { name: '图片生成图库' }));
});
```

- [x] **Step 2: Run failing test**

```bash
cd packages/frontend && node --import tsx --test src/image-generation/ImageGenerationShell.test.tsx
```

Expected: fails because component is missing.

- [x] **Step 3: Implement page**

`ImageWorkbenchPage.tsx`:

```tsx
export function ImageWorkbenchPage(): JSX.Element {
  const { projectId } = useParams();
  if (!projectId) return <Navigate to="/" replace />;
  return <ImageGenerationShell projectId={projectId} />;
}
```

`ImageGenerationShell.tsx`:

```tsx
export function ImageGenerationShell({ projectId }: { projectId: string }): JSX.Element {
  return (
    <div className="grid h-full min-h-0 grid-cols-[320px_minmax(280px,420px)_1fr] gap-3 p-3">
      <section aria-label="图片生成配置" className="min-h-0 border-r border-border/70 pr-3">
        <ProviderProfilePanel projectId={projectId} />
        <ImageJobForm projectId={projectId} />
      </section>
      <section aria-label="图片生成任务" className="min-h-0">
        <ImageJobQueue projectId={projectId} />
      </section>
      <section aria-label="图片生成图库" className="min-h-0">
        <ImageGalleryPanel projectId={projectId} />
      </section>
    </div>
  );
}
```

- [x] **Step 4: Add route**

In `main.tsx`:

```tsx
<Route path="/projects/:projectId/images" element={<ImageWorkbenchPage />} />
```

In `AppShell.tsx`, add project-level navigation item with lucide `Image` icon and label `图片`.

- [x] **Step 5: Run test**

```bash
cd packages/frontend && node --import tsx --test src/image-generation/ImageGenerationShell.test.tsx
```

Expected: pass.

- [x] **Step 6: Commit**

```bash
git add packages/frontend/src/pages/ImageWorkbenchPage.tsx packages/frontend/src/image-generation/ImageGenerationShell.tsx packages/frontend/src/main.tsx packages/frontend/src/components/AppShell.tsx packages/frontend/src/image-generation/ImageGenerationShell.test.tsx packages/frontend/src/components/AppShell.test.tsx docs/superpowers/plans/2026-06-07-SCimage图片生成集成实施计划.md
git commit -m "feat(frontend): 添加图片工作台页面"
```

执行记录：

- RED：`rtk node --import tsx --test packages/frontend/src/image-generation/ImageGenerationShell.test.tsx` 先失败于 `ImageGenerationShell` 模块缺失。
- GREEN：新增 `/projects/:projectId/images` route、`ImageWorkbenchPage`、`ImageGenerationShell` 三区域工作台骨架；`AppShell` 增加项目图片导航入口，当前项目来自 URL、最近会话路由或项目列表 fallback。
- Review 修复：只读代码审查指出工作台主区在 `AppShell` `overflow-hidden` 容器内缺少 `overflow-y-auto`、右侧状态胶囊在 `sm` 断点可能挤压顶部导航，并建议使用语义化 `h1/h2` + `aria-labelledby`；已修复。复核未发现 Critical/Important 阻塞；Minor 记录为当前占位 section 使用 `overflow-hidden`，后续接入真实表单/列表时按局部滚动需求调整。
- 验证：`rtk node --import tsx --test packages/frontend/src/image-generation/ImageGenerationShell.test.tsx packages/frontend/src/components/AppShell.test.tsx` 通过 8/8；`rtk npm run build -w @openclaw-room/frontend` 通过，Vite 仅保留既有 chunk size warning，AppShell SSR 测试保留既有 React Router `useLayoutEffect` warning。
- 后端/API smoke：临时后端 `:17335`，用 `POST /api/projects` 传入 `name/path/description` 创建浏览器测试项目，返回 `201` 项目 `Di_RXm6ZHVk4`。
- 浏览器测试：临时前端 `http://127.0.0.1:5174`，访问 `/projects/Di_RXm6ZHVk4/images`；Playwright 桌面 1440x900 与移动 390x844 均确认 `图片工作台` 标题、`生成设置 / 任务队列 / 项目图库` 三个可访问 region、图片导航 href 与 `is-active`、无水平溢出。移动端工作台主区 `overflow-y=auto`，`clientHeight=609`、`scrollHeight=748`，确认可纵向滚动；截图保存为 `/tmp/scimage-workbench-desktop.png` 与 `/tmp/scimage-workbench-mobile.png` 并完成目视复核。

### Task 12: Provider 配置面板

**Files:**
- Create: `packages/frontend/src/image-generation/ProviderProfilePanel.tsx`
- Create: `packages/frontend/src/image-generation/imageGenerationModel.ts`
- Test: `packages/frontend/src/image-generation/imageGenerationModel.test.ts`
- Test: `packages/frontend/src/image-generation/ProviderProfilePanel.test.tsx`
- Modify: `packages/frontend/src/image-generation/ImageGenerationShell.tsx`
- Modify: `packages/frontend/src/image-generation/ImageGenerationShell.test.tsx`

- [x] **Step 1: Write failing model tests**

```ts
test('provider form keeps blank api key as preserve existing secret', () => {
  const payload = buildProviderProfilePayload({
    name: 'OpenAI',
    baseUrl: 'https://api.openai.com',
    apiKey: '',
    model: 'gpt-image-2',
    compatProfileId: 'openai',
    supportsCountParameter: true,
  });

  assert.equal('api_key' in payload, false);
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/frontend && node --import tsx --test src/image-generation/imageGenerationModel.test.ts
```

Expected: fails.

- [x] **Step 3: Implement model helper**

```ts
export function buildProviderProfilePayload(form: ProviderProfileFormState): ImageProviderProfileInput {
  return {
    name: form.name.trim(),
    base_url: form.baseUrl.trim(),
    ...(form.apiKey.trim() ? { api_key: form.apiKey.trim() } : {}),
    model: form.model.trim(),
    compat_profile_id: form.compatProfileId,
    supports_count_parameter: form.supportsCountParameter,
  };
}
```

- [x] **Step 4: Implement panel**

Panel behavior:

- List profiles with active indicator.
- Save new profile.
- Update selected profile.
- Activate profile.
- Delete profile.
- Fetch models and show warning as non-blocking text.
- Never render raw saved API Key.

Use existing UI primitives from `components/ui`.

- [x] **Step 5: Run tests**

```bash
cd packages/frontend && node --import tsx --test src/image-generation/imageGenerationModel.test.ts
npm run build -w @openclaw-room/frontend
```

Expected: tests pass and frontend build exits 0.

- [x] **Step 6: Commit**

```bash
git add packages/frontend/src/image-generation/ProviderProfilePanel.tsx packages/frontend/src/image-generation/ProviderProfilePanel.test.tsx packages/frontend/src/image-generation/imageGenerationModel.ts packages/frontend/src/image-generation/imageGenerationModel.test.ts packages/frontend/src/image-generation/ImageGenerationShell.tsx packages/frontend/src/image-generation/ImageGenerationShell.test.tsx docs/superpowers/plans/2026-06-07-SCimage图片生成集成实施计划.md
git commit -m "feat(frontend): 添加图片提供方配置面板"
```

执行记录：

- RED：先新增 `imageGenerationModel.test.ts`，`rtk node --import tsx --test packages/frontend/src/image-generation/imageGenerationModel.test.ts` 失败于 `ERR_MODULE_NOT_FOUND`；再新增 `ProviderProfilePanel.test.tsx`，联合测试失败于 `ProviderProfilePanel` 模块缺失。
- GREEN：新增 `imageGenerationModel.ts`，覆盖空 API Key 不写入 payload、字段 trim/API 映射、已保存 profile 不回填原始密钥；新增 `ProviderProfilePanel.tsx`，支持 list/create/update/activate/delete、模型列表非阻塞 warning、已保存密钥提示和 API Key password input；接入 `ImageGenerationShell` 左侧“生成设置”。
- Debug 修复：接入 Provider 面板后，`ImageGenerationShell.test.tsx` 失败于缺少 React Query `QueryClientProvider`，已补测试 harness。浏览器目视发现移动端 Provider 表单先被 `overflow-hidden` 裁剪，随后又因 visible overflow 与后续区域重叠；已将工作台移动布局改为 `flex-col` 文档流，桌面端保留三列 grid 与 section 内部滚动。
- Review：只读代码审查未发现 Critical/Important 阻塞；重点复核 API Key 不回填/不渲染、React Query selection 同步、删除后选中项回落、移动端布局和测试覆盖。
- 验证：`rtk node --import tsx --test packages/frontend/src/image-generation/imageGenerationModel.test.ts packages/frontend/src/image-generation/ProviderProfilePanel.test.tsx packages/frontend/src/image-generation/ImageGenerationShell.test.tsx` 通过 6/6；`rtk npm run build -w @openclaw-room/frontend` 通过，Vite 仅保留既有 chunk size warning。
- 后端/API smoke：临时后端 `:17335`，用 `POST /api/projects` 创建测试项目 `V-ZZSQeegb8F`；浏览器 UI 创建 `Browser Primary` / `Browser Backup` 两个 Provider 后，直接调用 `GET /api/projects/V-ZZSQeegb8F/image-provider-profiles` 确认 active 状态正确且响应不含 `api_key` 字段。
- 浏览器测试：临时前端 `http://127.0.0.1:5174`，访问 `/projects/V-ZZSQeegb8F/images`；Playwright 桌面 1440x900 完成 Provider 新建、保存、API Key 保存后清空、选择非 active Provider 并设为当前、删除备用 Provider；移动 390x844 确认 Provider 表单可滚动可见、配置区 `clientHeight=scrollHeight=801`、无水平溢出。截图保存为 `/tmp/scimage-provider-panel-desktop.png` 与 `/tmp/scimage-provider-panel-mobile.png` 并完成目视复核。

### Task 13: 生成表单与源图选择

**Files:**
- Create: `packages/frontend/src/image-generation/ImageJobForm.tsx`
- Create: `packages/frontend/src/image-generation/SourceImagePicker.tsx`
- Test: `packages/frontend/src/image-generation/ImageJobForm.test.tsx`
- Modify: `packages/frontend/src/image-generation/ImageGenerationShell.tsx`

- [x] **Step 1: Write failing form tests**

```tsx
test('image job form disables submit for image-to-image without source files', async () => {
  render(<ImageJobForm projectId="project-1" />);
  await userEvent.click(screen.getByRole('button', { name: '图生图' }));
  assert.equal(screen.getByRole('button', { name: '生成图片' }).hasAttribute('disabled'), true);
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/frontend && node --import tsx --test src/image-generation/ImageJobForm.test.tsx
```

Expected: fails because components are missing.

- [x] **Step 3: Implement form**

Form state:

```ts
type ImageJobFormState = {
  workflow: 'generate' | 'image-to-image';
  prompt: string;
  sourceFileIds: string[];
  count: number;
  quality: string;
  size: string;
};
```

Submit payload:

```ts
await api.createImageJob(projectId, {
  workflow: state.workflow,
  prompt: state.prompt.trim(),
  source_file_ids: state.sourceFileIds,
  count: state.count,
  quality: state.quality,
  size: state.size,
});
```

- [x] **Step 4: Implement source picker**

Use `api.listProjectResources(projectId, { resourceType: 'uploaded_file' })` and filter images by `mime_type?.startsWith('image/')`. Selected source files are rendered as thumbnails with remove buttons.

- [x] **Step 5: Run tests**

```bash
cd packages/frontend && node --import tsx --test src/image-generation/ImageJobForm.test.tsx
```

Expected: pass.

- [x] **Step 6: Commit**

```bash
git add packages/frontend/src/image-generation/ImageJobForm.tsx packages/frontend/src/image-generation/SourceImagePicker.tsx packages/frontend/src/image-generation/ImageJobForm.test.tsx packages/frontend/src/image-generation/ImageGenerationShell.tsx docs/superpowers/plans/2026-06-07-SCimage图片生成集成实施计划.md
git commit -m "feat(frontend): 添加图片生成表单"
```

执行记录：

- RED：新增 `ImageJobForm.test.tsx`，`rtk node --import tsx --test packages/frontend/src/image-generation/ImageJobForm.test.tsx` 失败于 `ImageJobForm` 模块缺失。
- GREEN：新增 `ImageJobForm.tsx` 和 `SourceImagePicker.tsx`；表单支持文生图/图生图模式、prompt、count、quality、size、图生图源图校验和 `api.createImageJob` 提交；源图选择使用 `api.listProjectFiles(projectId, { sourceType: 'uploaded_file' })`，过滤 `mime_type` 为 `image/*` 的真实 `ProjectFile.id`，避免 resource-assets `file:` 包装 id 传给后端。
- Debug 修复：组件 SSR 测试因 `SourceImagePicker` 使用 React Query 失败于缺少 `QueryClientProvider`，已补测试 harness；浏览器 smoke 初版断言源图文件名必须在正文文本中，实际选中源图通过缩略图和 `移除源图 ...` 可访问标签呈现，已改用可访问标签断言。Review 后修复所有图片被选中时的空态文案，从“项目里还没有可用图片”改为“所有可用图片已选择”。
- Review：只读代码审查未发现 Critical/Important 阻塞；重点复核图生图无源图禁用、payload trim 和 source ids、源图过滤、API 提交、移动端无重叠。
- 验证：`rtk node --import tsx --test packages/frontend/src/image-generation/ImageJobForm.test.tsx packages/frontend/src/image-generation/ImageGenerationShell.test.tsx packages/frontend/src/image-generation/ProviderProfilePanel.test.tsx packages/frontend/src/image-generation/imageGenerationModel.test.ts` 通过 9/9；`rtk npm run build -w @openclaw-room/frontend` 通过，Vite 仅保留既有 chunk size warning。
- 后端/API smoke：临时后端 `:17335` 创建项目 `tBtAd6GJKmN6`，创建 active Provider `Qr4QX9yV-Xax`，上传源图文件 `nLHzWr3IOGAry0kY`；浏览器提交文生图和图生图后，`GET /api/projects/tBtAd6GJKmN6/image-jobs` 确认分别创建 `generate` 与 `image-to-image` 任务，图生图 detail 的 `source_images[0].file_id` 为 `nLHzWr3IOGAry0kY`。任务状态为 `failed` 是预期 smoke 结果，因为测试 Provider 指向本地后端 `/v1/images/*` 并返回 404；创建、绑定和校验路径已完成。
- 浏览器测试：临时前端 `http://127.0.0.1:5174`，访问 `/projects/tBtAd6GJKmN6/images`；Playwright 桌面 1440x900 与移动 390x844 均确认文生图提交、图生图无源图时禁用、源图选择、选中后“所有可用图片已选择”、无水平溢出、工作台纵向滚动。截图保存为 `/tmp/scimage-job-form-desktop.png` 与 `/tmp/scimage-job-form-mobile.png` 并完成目视复核。

### Task 14: 任务队列、图库和实时刷新

**Files:**
- Create: `packages/frontend/src/image-generation/ImageJobQueue.tsx`
- Create: `packages/frontend/src/image-generation/ImageGalleryPanel.tsx`
- Create: `packages/frontend/src/image-generation/useImageGenerationEvents.ts`
- Test: `packages/frontend/src/image-generation/ImageJobQueue.test.tsx`
- Modify: `packages/frontend/src/image-generation/ImageGenerationShell.tsx`

- [x] **Step 1: Write failing queue tests**

```tsx
test('image job queue renders retry for failed job and cancel for running job', () => {
  const noop = () => undefined;
  render(<ImageJobQueueView
    projectId="project-1"
    jobs={[failedJob, runningJob]}
    onRetry={noop}
    onCancel={noop}
  />);
  assert.ok(screen.getByRole('button', { name: '重试失败任务' }));
  assert.ok(screen.getByRole('button', { name: '取消运行任务' }));
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/frontend && node --import tsx --test src/image-generation/ImageJobQueue.test.tsx
```

Expected: fails.

- [x] **Step 3: Implement project event hook**

```ts
export function useImageGenerationEvents(projectId: string, onEvent: (event: ImageGenerationWsEvent) => void): void {
  useEffect(() => {
    roomSocket.subscribeProject(projectId);
    const off = roomSocket.on((event) => {
      if (event.type.startsWith('image_job:') && event.projectId === projectId) {
        onEvent(event as ImageGenerationWsEvent);
      }
    });
    return () => {
      off();
      roomSocket.unsubscribeProject(projectId);
    };
  }, [projectId, onEvent]);
}
```

- [x] **Step 4: Implement queue and gallery**

Queue:

- Poll initial jobs through `api.listImageJobs`.
- Update local query cache on WebSocket events.
- Buttons call cancel/retry API.

Gallery:

- Show outputs from jobs plus project image resources.
- Each card links to resource detail using existing file/resource route conventions.
- Use fixed aspect ratio containers to avoid layout shift.

- [x] **Step 5: Run tests and frontend build**

```bash
cd packages/frontend && node --import tsx --test src/image-generation/ImageJobQueue.test.tsx
npm run build -w @openclaw-room/frontend
```

Expected: tests pass and build exits 0.

- [x] **Step 6: Commit**

```bash
git add packages/frontend/src/image-generation/ImageJobQueue.tsx packages/frontend/src/image-generation/ImageGalleryPanel.tsx packages/frontend/src/image-generation/useImageGenerationEvents.ts packages/frontend/src/image-generation/ImageJobQueue.test.tsx packages/frontend/src/image-generation/ImageGenerationShell.tsx docs/superpowers/plans/2026-06-07-SCimage图片生成集成实施计划.md
git commit -m "feat(frontend): 展示图片任务与图库"
```

执行记录：

- RED：新增 `ImageJobQueue.test.tsx`，`rtk node --import tsx --test packages/frontend/src/image-generation/ImageJobQueue.test.tsx` 失败于 `ImageJobQueue` 模块缺失。
- GREEN：新增 `useImageGenerationEvents.ts`，通过 `roomSocket.subscribeProject/unsubscribeProject` 订阅项目级 `image_job:*` 事件；新增 `ImageJobQueue.tsx`，初始拉取 `api.listImageJobs`，事件到达时更新 `['image-jobs', projectId]` cache，失败任务可 retry、queued/running 任务可 cancel；新增 `ImageGalleryPanel.tsx`，展示已完成任务 outputs 与项目 image resources，并使用固定 aspect ratio 避免布局跳动；替换工作台任务队列和图库占位。
- Review 修复：只读代码审查发现 `ImageJobQueue` 的 query key 每次 render 都创建新数组，导致 `updateJobCache` identity 改变并使 `useImageGenerationEvents` 反复退订/订阅；已用 `useMemo` 固定 query key。复核后无 Critical/Important 阻塞；后续可在资源详情深链完善时把图库卡片从项目资源页链接升级为直接详情链接。
- 验证：`rtk node --import tsx --test packages/frontend/src/image-generation/ImageJobQueue.test.tsx packages/frontend/src/image-generation/ImageJobForm.test.tsx packages/frontend/src/image-generation/ImageGenerationShell.test.tsx packages/frontend/src/image-generation/ProviderProfilePanel.test.tsx packages/frontend/src/image-generation/imageGenerationModel.test.ts packages/frontend/src/lib/imageGenerationWs.test.ts` 通过 13/13；`rtk npm run build -w @openclaw-room/frontend` 通过，Vite 仅保留既有 chunk size warning。
- 后端/API/WS smoke：沿用临时后端 `:17335` 和项目 `tBtAd6GJKmN6`；浏览器打开 `/projects/tBtAd6GJKmN6/images` 后，直接调用 `POST /api/projects/tBtAd6GJKmN6/image-jobs` 创建 prompt `ws queue smoke final 1780869297149` 的任务，已打开页面通过项目 WebSocket 在队列中显示该任务；点击队列内“重试失败任务”实际命中 `/retry`。
- 浏览器测试：临时前端 `http://127.0.0.1:5174`；Playwright 桌面 1440x900 与移动 390x844 均确认队列渲染失败/排队任务、图库显示上传图片 `scimage-form-source.png`、无水平溢出、工作台纵向滚动。截图保存为 `/tmp/scimage-queue-gallery-desktop.png` 与 `/tmp/scimage-queue-gallery-mobile.png` 并完成目视复核。

## Phase 3: 会话内图片生成

### Task 15: 会话图片任务后端绑定

**Files:**
- Modify: `packages/backend/src/image-generation/routes.ts`
- Modify: `packages/backend/src/image-generation/service.ts`
- Modify: `packages/backend/src/session-message-dispatch.ts`
- Test: `packages/backend/src/image-generation/routes.test.ts`
- Test: `packages/backend/src/image-generation/queue.test.ts`
- Test: `packages/backend/src/session-message-dispatch.test.ts`

- [x] **Step 1: Write failing session binding tests**

```ts
test('session image job requires session to belong to project', async () => {
  const project = createProject('session-images');
  const otherProject = createProject('other-session-images');
  const session = sessionRepo.create({ project_id: otherProject.id, title: 'Other', mode: 'build' });

  const response = await request(`/api/projects/${project.id}/image-jobs`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      session_id: session.id,
      workflow: 'generate',
      prompt: 'apple',
      count: 1,
      quality: 'auto',
      size: 'auto',
    }),
  });

  assert.equal(response.status, 400);
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/routes.test.ts src/session-message-dispatch.test.ts
```

Expected: fails until session binding is implemented.

- [x] **Step 3: Validate session ownership**

In route/service input validation:

```ts
if (input.session_id) {
  const session = sessionRepo.get(input.session_id);
  if (!session) throw new Error('session not found');
  if (session.project_id !== projectId) throw new Error('session does not belong to project');
}
```

- [x] **Step 4: Add session message metadata shape**

When a user creates a session image job from UI, write a user/session message metadata reference:

```ts
metadata: {
  image_generation_job_id: job.id,
  attachments: outputs.map((output) => ({
    id: output.file_id,
    fileId: output.file_id,
    name: output.name,
    mimeType: output.mime_type,
    size: output.size,
    url: output.url,
    isImage: true,
  })),
}
```

If outputs are not available at creation time, create a status message with `image_generation_job_id` and update UI through WebSocket.

- [x] **Step 5: Run tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/routes.test.ts src/session-message-dispatch.test.ts
```

Expected: pass.

- [x] **Step 6: Commit**

```bash
git add packages/backend/src/image-generation/service.ts packages/backend/src/session-message-dispatch.ts packages/backend/src/image-generation/routes.test.ts packages/backend/src/image-generation/queue.test.ts packages/backend/src/session-message-dispatch.test.ts docs/superpowers/plans/2026-06-07-SCimage图片生成集成实施计划.md
git commit -m "feat(backend): 绑定会话图片生成任务"
```

执行记录：

- RED：新增 `routes.test.ts` 用例，验证 `POST /api/projects/:projectId/image-jobs` 带 `session_id` 时会写入 session status message；首次运行 `rtk node --import tsx --test packages/backend/src/image-generation/routes.test.ts packages/backend/src/session-message-dispatch.test.ts` 失败于会话消息不存在。新增 `session-message-dispatch.test.ts` helper 用例，首次失败于 `recordSessionImageGenerationJobMessage is not a function`。
- GREEN：在 `session-message-dispatch.ts` 增加 `assertSessionCanReceiveImageGenerationJob` 和 `recordSessionImageGenerationJobMessage`，写入 `role=system`、`sender_id=image-generation`、`message_type=system` 的状态消息，metadata 包含 `image_generation_job_id`、`image_generation_status`，有 outputs 时映射为 `attachments`；在 `service.ts` 创建 job 前校验 session/project，创建 job 后写入 session status message 并广播 `session_message:new`。
- Review 修复：只读代码审查发现 service 直调边界缺少测试，已在 `queue.test.ts` 增加跨项目 session 拒绝用例；新增状态消息会让同一 socket 收到一次 `session_message:new`，已调整 WS 去重测试为分别断言 `image_job:*` 去重和 status message 单次投递；另补 helper 防误用校验，禁止把 job 记录到同项目内错误 session。
- 定向验证：`rtk node --import tsx --test packages/backend/src/image-generation/routes.test.ts packages/backend/src/image-generation/queue.test.ts packages/backend/src/session-message-dispatch.test.ts` 通过 27/27。
- API smoke：通过临时 Express HTTP 服务调用 `POST /api/projects/:projectId/sessions`、`POST /api/projects/:projectId/image-jobs`、`GET /api/sessions/:sessionId`，创建图片任务返回 202，会话详情返回 200，消息 metadata 中 `image_generation_job_id` 与创建的 job id 一致，`image_generation_status=queued`。
- 全仓库验证：`rtk git diff --check` 通过；`rtk npm run build` 通过后端 `tsc` 与前端 `tsc/vite build`，Vite 仅保留既有 chunk size warning。

### Task 16: 会话生成弹窗与状态卡片

**Files:**
- Create: `packages/frontend/src/image-generation/ImageGenerationDialog.tsx`
- Create: `packages/frontend/src/image-generation/ImageJobStatusCard.tsx`
- Modify: `packages/frontend/src/session-ui/SessionComposer.tsx`
- Modify: `packages/frontend/src/session-ui/SessionTranscript.tsx`
- Modify: `packages/frontend/src/session-ui/SessionFileComposer.tsx`
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
- Modify: `packages/frontend/src/session-ui/ActiveSessionSurface.tsx`
- Modify: `packages/frontend/src/image-generation/useImageGenerationEvents.ts`
- Modify: `packages/frontend/src/lib/messageMetadata.ts`
- Modify: `packages/frontend/src/lib/types.ts`
- Modify: `packages/frontend/src/session-ui/session-os.css`
- Test: `packages/frontend/src/image-generation/ImageGenerationDialog.test.tsx`

- [x] **Step 1: Write failing dialog tests**

```tsx
test('session image generation dialog submits session id', async () => {
  const createImageJob = mockApiCreateImageJob();
  const noop = () => undefined;
  render(<ImageGenerationDialog projectId="project-1" sessionId="session-1" open onOpenChange={noop} />);

  await userEvent.type(screen.getByLabelText('提示词'), 'apple');
  await userEvent.click(screen.getByRole('button', { name: '生成图片' }));

  assert.equal(createImageJob.calls[0].input.session_id, 'session-1');
});
```

- [x] **Step 2: Run failing test**

```bash
cd packages/frontend && node --import tsx --test src/image-generation/ImageGenerationDialog.test.tsx
```

Expected: fails.

- [x] **Step 3: Implement dialog**

Use compact form:

- Prompt textarea.
- Workflow segmented control.
- Source image picker for image-to-image.
- Count/size/quality controls.
- Submit button.

Submit:

```ts
await api.createImageJob(projectId, {
  session_id: sessionId,
  workflow,
  prompt,
  source_file_ids: sourceFileIds,
  count,
  quality,
  size,
});
```

- [x] **Step 4: Add composer button**

In `SessionComposer.tsx`, add icon button using lucide `ImagePlus` with accessible label `生成图片`. Open `ImageGenerationDialog`.

- [x] **Step 5: Render status card**

`ImageJobStatusCard` displays:

- prompt excerpt.
- status label.
- generated thumbnails.
- cancel/retry buttons when allowed.
- link to `/projects/:projectId/images`.

`SessionTranscript.tsx` should render the card when message metadata contains `image_generation_job_id`.

- [x] **Step 6: Run tests and build**

```bash
cd packages/frontend && node --import tsx --test src/image-generation/ImageGenerationDialog.test.tsx
npm run build -w @openclaw-room/frontend
```

Expected: pass.

- [x] **Step 7: Commit**

```bash
git add packages/frontend/src/image-generation/ImageGenerationDialog.tsx packages/frontend/src/image-generation/ImageJobStatusCard.tsx packages/frontend/src/image-generation/ImageGenerationDialog.test.tsx packages/frontend/src/image-generation/useImageGenerationEvents.ts packages/frontend/src/session-ui/SessionComposer.tsx packages/frontend/src/session-ui/SessionTranscript.tsx packages/frontend/src/session-ui/SessionFileComposer.tsx packages/frontend/src/session-ui/SessionShellView.tsx packages/frontend/src/session-ui/ActiveSessionSurface.tsx packages/frontend/src/session-ui/session-os.css packages/frontend/src/lib/messageMetadata.ts packages/frontend/src/lib/types.ts docs/superpowers/plans/2026-06-07-SCimage图片生成集成实施计划.md
git commit -m "feat(frontend): 添加会话图片生成入口"
```

执行记录：

- RED：新增 `ImageGenerationDialog.test.tsx`，覆盖 session payload 带 `session_id`、弹窗 view 控件、composer 图片按钮、metadata parser、状态卡缩略图与动作；首次运行 `rtk node --import tsx --test packages/frontend/src/image-generation/ImageGenerationDialog.test.tsx` 失败于 `ImageGenerationDialog` 模块缺失。
- GREEN：新增 `ImageGenerationDialog.tsx`，复用 `ImageJobForm` 的 state/payload 模型与 `SourceImagePicker`，提交时调用 `api.createImageJob(projectId, { session_id, ...payload })`；新增 `ImageJobStatusCard.tsx`，按 `image_generation_job_id` 拉取 job detail，显示 prompt、状态、输出缩略图、错误、取消/重试与图片工作台链接。
- 接入：实际 Session 工作区使用 `SessionShellView -> SessionFileComposer`，因此除计划中的 `SessionComposer`/`SessionTranscript` 外，也在 `SessionFileComposer.tsx` 增加 `ImagePlus` 图标按钮，在 `SessionShellView.tsx` 的 transcript 消息后渲染状态卡，并通过 `ActiveSessionSurface.tsx` 保持旧 surface 可用。
- Review 修复：发现状态卡失败无输出时仍显示“等待生成结果”，改为按状态显示“未生成图片/任务已取消/未返回图片”；发现状态卡异步展开可能被 composer 遮挡，给 loading 和最终卡片设置稳定最小高度；发现多个状态卡与队列会重复项目 WS 订阅，给 `useImageGenerationEvents` 增加引用计数；发现 retry 返回新 job id 后卡片仍绑定旧 job，已让卡片在 retry 成功后跟随新 job id。
- 定向验证：`rtk node --import tsx --test packages/frontend/src/image-generation/ImageGenerationDialog.test.tsx packages/frontend/src/image-generation/ImageJobForm.test.tsx packages/frontend/src/image-generation/ImageJobQueue.test.tsx packages/frontend/src/lib/imageGenerationWs.test.ts packages/frontend/src/session-ui/SessionShell.test.tsx` 通过 45/45。
- 全仓库验证：`rtk npm run build` 通过后端 `tsc` 与前端 `tsc/vite build`，Vite 仅保留既有 chunk size warning。
- 浏览器测试：临时后端 `http://127.0.0.1:17337`、前端 `http://127.0.0.1:5175`；Playwright 桌面 1440x900 实际打开 `/projects/UI37i-dGCIzi/sessions/qDt90mPB6j5jVwVs`，点击 composer 内 `生成图片` 图标，填写 prompt `browser session image smoke review 1780871883282` 并提交，状态卡在 transcript 中出现；桌面与移动 390x844 均无水平溢出。截图保存为 `/tmp/scimage-task16-session-desktop.png` 与 `/tmp/scimage-task16-session-mobile.png`，已目视复核。

## Phase 4: 智能体工具

### Task 17: 后端 generate_image 工具服务

**Files:**
- Create: `packages/backend/src/image-generation/tool.ts`
- Modify: `packages/backend/src/session-runtime.ts`
- Test: `packages/backend/src/image-generation/tool.test.ts`
- Test: `packages/backend/src/session-runtime.test.ts`

- [x] **Step 1: Write failing tool tests**

```ts
test('generate image tool creates project-scoped job and hides provider secret', async () => {
  const result = await runGenerateImageTool({
    project_id: project.id,
    session_id: session.id,
    prompt: 'apple',
    workflow: 'generate',
    source_file_ids: [],
    count: 1,
    size: 'auto',
    quality: 'auto',
    provider_profile_id: null,
  }, { service: fakeCompletedImageGenerationService });

  assert.equal(result.job_id, 'job-1');
  assert.equal(result.outputs[0]?.resource_id, `file:${result.outputs[0]?.file_id}`);
  assert.equal(JSON.stringify(result).includes('secret'), false);
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/tool.test.ts src/session-runtime.test.ts
```

Expected: fails.

实际 RED：`rtk node --import tsx --test packages/backend/src/image-generation/tool.test.ts packages/backend/src/session-runtime.test.ts` 失败于 `packages/backend/src/image-generation/tool.js` 模块缺失，并且 `setSessionRuntimeGenerateImageToolDepsForTest` 未实现。Review 回归红测追加 `generate image session tool rejects unknown workflow instead of defaulting silently`，首次失败于未知 workflow 被静默归一为 `generate` 后落到 active profile 缺失错误。

- [x] **Step 3: Implement tool contract**

```ts
export interface GenerateImageToolInput {
  project_id: string;
  session_id?: string | null;
  room_id?: string | null;
  task_id?: string | null;
  prompt: string;
  workflow: ImageGenerationWorkflow;
  source_file_ids?: string[];
  count?: number;
  size?: string;
  quality?: string;
  provider_profile_id?: string | null;
}

export interface GenerateImageToolOutput {
  job_id: string;
  status: ImageGenerationStatus;
  outputs: Array<{ file_id: string; resource_id: string; url: string; slot: number }>;
  error: string | null;
}
```

Tool execution:

```ts
export async function runGenerateImageTool(input: GenerateImageToolInput, deps = defaultDeps): Promise<GenerateImageToolOutput> {
  const jobResult = await deps.service.createJob({
    project_id: input.project_id,
    session_id: input.session_id ?? null,
    room_id: input.room_id ?? null,
    source_task_id: input.task_id ?? null,
    workflow: input.workflow,
    prompt: input.prompt,
    source_file_ids: input.source_file_ids ?? [],
    count: input.count ?? 1,
    size: input.size ?? 'auto',
    quality: input.quality ?? 'auto',
    provider_profile_id: input.provider_profile_id ?? null,
    source_agent_id: 'agent-tool',
  });
  const completed = await deps.service.waitForCompletion(jobResult.job.id);
  return {
    job_id: completed.job.id,
    status: completed.job.status,
    outputs: completed.outputs.map((output) => ({
      file_id: output.file_id,
      resource_id: `file:${output.file_id}`,
      url: output.url,
      slot: output.slot,
    })),
    error: completed.job.error,
  };
}
```

- [x] **Step 4: Register with session runtime**

In `session-runtime.ts`, expose a tool only when planner runtime is allowed to use project tools:

```ts
{
  name: 'generate_image',
  description: 'Generate text-to-image or image-to-image outputs and save them as project resources.',
  input_schema: generateImageToolInputSchema,
  execute: (input) => runGenerateImageTool({ ...input, project_id: session.project_id, session_id: session.id }),
}
```

The schema must not expose API Key fields.

实际接入：新增 `SessionToolDefinition` 与 `SessionAdapter.invoke(...).tools` 可选契约；`runSessionAgent` 仅在 `workspace-write` / `bypass` 权限下传入绑定当前 `project_id/session_id` 的 `generate_image` 工具，默认 `read-only` 不暴露项目写入工具。session tool schema 不包含 `api_key` / `base_url` / `project_id` / `session_id`，执行时由 runtime 强制绑定当前 session，忽略工具输入中的跨项目伪造值。

- [x] **Step 5: Run tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/tool.test.ts src/session-runtime.test.ts
```

Expected: pass.

实际 GREEN / 验证：

- `rtk node --import tsx --test packages/backend/src/image-generation/tool.test.ts packages/backend/src/session-runtime.test.ts`：16/16 通过。
- `rtk npm run build`：后端 `tsc`、前端 `tsc -b` 与 Vite build 通过，保留既有 chunk size warning。
- 后端接口 smoke：临时 SQLite + 真实 repo/service/tool/session runtime，fake provider runtime 替代外部网络；直接调用 `runGenerateImageTool` 与通过 `runSessionAgent(... permissionMode: 'workspace-write')` 的 adapter `tools[0].execute` 均返回 `completed`、各 1 个输出，adapter 工具结果绑定当前 project/session，结果 JSON 不含 `smoke-secret-token`。
- 代码审查：发现并修复两个问题：默认 `read-only` 下不应暴露会创建项目资源的 `generate_image`；session tool 不应把未知 workflow 静默当成 `generate`。已分别补 `runSessionAgent omits project tools in read-only mode` 与 `generate image session tool rejects unknown workflow instead of defaulting silently` 回归测试。

- [x] **Step 6: Commit**

```bash
git add packages/backend/src/image-generation/tool.ts packages/backend/src/acp/types.ts packages/backend/src/session-runtime.ts packages/backend/src/image-generation/tool.test.ts packages/backend/src/session-runtime.test.ts docs/superpowers/plans/2026-06-07-SCimage图片生成集成实施计划.md
git commit -m "feat(backend): 添加智能体图片生成工具"
```

### Task 18: 工具结果会话展示与证据记录

**Files:**
- Modify: `packages/backend/src/session-message-dispatch.ts`
- Modify: `packages/backend/src/session-runtime.ts`
- Modify: `packages/frontend/src/session-ui/SessionTranscript.tsx`
- Test: `packages/backend/src/session-message-dispatch.test.ts`
- Test: `packages/frontend/src/session-ui/SessionShell.test.tsx`

- [x] **Step 1: Write failing tests**

Backend: tool result should create evidence or metadata with output file ids. Frontend: transcript should render generated image attachments from tool result metadata.

```ts
test('session runtime records generate_image outputs as session evidence', async () => {
  const event = sessionEvidenceRepo.listBySession(session.id).find((item) => item.event_type === 'tool_result');
  assert.ok(event);
  assert.equal(event?.title, '图片生成结果');
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/session-message-dispatch.test.ts
cd packages/frontend && node --import tsx --test src/session-ui/SessionShell.test.tsx
```

Expected: fails.

实际 RED：

- `rtk node --import tsx --test packages/backend/src/session-message-dispatch.test.ts packages/backend/src/session-runtime.test.ts` 失败于 `recordSessionImageGenerationToolResultEvidence is not a function`，以及 session runtime 执行 `generate_image` 后找不到 `图片生成结果` evidence。
- `rtk node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx` 失败于缺少 `.deepsea-generated-artifacts` 渲染。
- 计划中的 `artifact` event type 未存在于当前 `SessionEvidenceType` 与 SQLite CHECK；为避免扩大 DB 迁移面，本任务采用既有 `tool_result` 表达 generate_image 工具输出，并在 payload 中记录 `tool_name: 'generate_image'`。

- [x] **Step 3: Record evidence**

When `generate_image` completes, create a session evidence event:

```ts
sessionEvidenceRepo.create({
  session_id: session.id,
  event_type: 'tool_result',
  severity: 'info',
  title: '图片生成结果',
  payload: {
    tool_name: 'generate_image',
    job_id: result.job_id,
    outputs: result.outputs,
  },
});
```

- [x] **Step 4: Render tool artifacts**

Frontend transcript should display thumbnails for evidence/tool outputs using existing attachment styles. Each image links to resource detail or opens preview.

实际实现：新增 `recordSessionImageGenerationToolResultEvidence`，在 `createGenerateImageSessionTool` 的 `onResult` 回调中由 `runSessionAgent` 绑定当前 run 后写入 evidence 并广播；前端在 `SessionShellView` 的 run transcript 下渲染 `generate_image` 的 `tool_result` 输出缩略图，同时兼容旧 `SessionTranscript`。

- [x] **Step 5: Run tests**

```bash
cd packages/backend && node --import tsx --test src/session-message-dispatch.test.ts
cd packages/frontend && node --import tsx --test src/session-ui/SessionShell.test.tsx
```

Expected: pass.

实际 GREEN / 验证：

- `rtk node --import tsx --test packages/backend/src/session-message-dispatch.test.ts packages/backend/src/session-runtime.test.ts`：22/22 通过。
- `rtk node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx packages/frontend/src/image-generation/ImageGenerationDialog.test.tsx`：39/39 通过。
- `rtk npm run build`：后端 `tsc`、前端 `tsc -b` 与 Vite build 通过，保留既有 chunk size warning。
- 后端接口 smoke：临时 SQLite + fake provider runtime，通过 `runSessionAgent(... permissionMode: 'workspace-write')` 执行 `generate_image`，返回 `completed`，落库 `tool_result` evidence，payload outputs 与工具输出一致且不含 `evidence-secret-token`。
- 浏览器测试：临时后端 `http://127.0.0.1:17338`、前端 `http://127.0.0.1:5176`；Playwright 打开 `/projects/CebkOYMUlKf-/sessions/CqVnJ_vjYPcTwmNJ`，DOM 检查 `.deepsea-generated-artifacts` 为 1、图片 complete 且 natural size 64x64、桌面与 390x844 移动宽度均无水平溢出。截图：`/tmp/scimage-task18-artifact-desktop.png`、`/tmp/scimage-task18-artifact-mobile.png`。临时服务已停止，端口已释放。
- 代码审查：避免 `session-runtime` 与 `session-message-dispatch` 静态循环依赖，改为工具回调执行时动态导入 evidence 记录函数；修复 `buildSessionRuntimeTools` 覆盖注入 `onResult` 的问题，改为先调用注入 hook 再记录 evidence。

- [x] **Step 6: Commit**

```bash
git add packages/backend/src/image-generation/tool.ts packages/backend/src/session-message-dispatch.ts packages/backend/src/session-runtime.ts packages/frontend/src/session-ui/SessionShellView.tsx packages/frontend/src/session-ui/SessionTranscript.tsx packages/frontend/src/session-ui/session-os.css packages/backend/src/session-message-dispatch.test.ts packages/backend/src/session-runtime.test.ts packages/frontend/src/session-ui/SessionShell.test.tsx docs/superpowers/plans/2026-06-07-SCimage图片生成集成实施计划.md
git commit -m "feat(frontend): 展示智能体图片生成结果"
```

## Phase 5: 高级能力

### Task 19: 图生图源图与 lineage

**Files:**
- Modify: `packages/backend/src/image-generation/source-images.ts`
- Modify: `packages/backend/src/image-generation/jobs.ts`
- Modify: `packages/frontend/src/image-generation/ImageGalleryPanel.tsx`
- Create: `packages/frontend/src/image-generation/ImageLineagePanel.tsx`
- Test: `packages/backend/src/image-generation/jobs.test.ts`
- Test: `packages/frontend/src/image-generation/ImageJobQueue.test.tsx`

- [x] **Step 1: Write failing lineage tests**

```ts
test('image-to-image source image records origin output when source comes from generated output', () => {
  const source = imageGenerationJobRepo.addSourceImage({
    job_id: childJob.id,
    file_id: parentOutput.file_id,
    slot: 1,
    url: parentOutput.url,
    origin_job_id: parentJob.id,
    origin_output_id: parentOutput.id,
  });

  assert.equal(source.origin_job_id, parentJob.id);
  assert.equal(source.origin_output_id, parentOutput.id);
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/jobs.test.ts
cd packages/frontend && node --import tsx --test src/image-generation/ImageJobQueue.test.tsx
```

Expected: fails until lineage UI/data exists.

实际 RED：

- `rtk node --import tsx --test packages/backend/src/image-generation/jobs.test.ts`：11/12 通过，新增 `image-to-image job records origin output when source file comes from generated output` 失败；`sourceImage.origin_job_id` 实际为 `null`，确认 service 尚未自动解析 generated output lineage。
- `rtk node --import tsx --test packages/frontend/src/image-generation/ImageJobQueue.test.tsx`：1/2 通过，新增 lineage 面板测试失败于 `ERR_MODULE_NOT_FOUND`，确认 `ImageLineagePanel` 尚未实现。
- 浏览器验收中额外发现图库重复展示 generated output：资源 API 返回 uploaded file id 为 `file:<id>`，而 output 使用原始 `file_id` 去重。补充 RED：`gallery excludes uploaded resources that wrap generated output file ids` 先失败于 `buildGalleryItems is not a function`。

- [x] **Step 3: Implement source resolver**

`source-images.ts`:

```ts
export function resolveImageSourceFiles(projectId: string, sourceFileIds: string[]): ImageSourceFile[] {
  return sourceFileIds.map((fileId, index) => {
    const file = fileRepo.get(fileId);
    if (!file || file.project_id !== projectId || file.deleted_at !== null) {
      throw new Error('source image is not available');
    }
    if (!file.mime_type.startsWith('image/')) {
      throw new Error('source file must be an image');
    }
    const origin = imageGenerationJobRepo.findOutputByFileId(file.id);
    return {
      slot: index + 1,
      file,
      origin_job_id: origin?.job_id ?? null,
      origin_output_id: origin?.id ?? null,
    };
  });
}
```

实际实现：

- 新增 `source-images.ts`，集中校验源图文件存在、未删除、项目归属与 `image/*` MIME，并通过 `imageGenerationJobRepo.findOutputByFileId(file.id)` 解析 `origin_job_id/origin_output_id`。
- `jobs.ts` 增加只读查询 `findOutputByFileId(fileId)`；`service.ts` 在创建 image-to-image job 时写入解析出的 origin ids，仍由 `addSourceImage` 做项目归属与 origin output/file 二次校验。

- [x] **Step 4: Implement lineage panel**

Show parent source thumbnails and child outputs. Keep it simple: a two-column list with parent -> child arrows; no canvas/minimap in this task.

实际实现：

- 新增 `ImageLineagePanel.tsx`，从 job detail 的 `source_images`、outputs 与 parent job detail 推导父源图 -> 子输出关系。
- `ImageGalleryPanel.tsx` 接入 lineage 面板，并导出 `buildGalleryItems` 供测试覆盖。
- 修复图库 output/resource 去重：对 `file:<id>` 包装与原始 `file_id` 做归一化，避免同一 generated output 在图库中重复出现。

- [x] **Step 5: Run tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/jobs.test.ts
cd packages/frontend && node --import tsx --test src/image-generation/ImageJobQueue.test.tsx
```

Expected: pass.

实际 GREEN / 验证：

- `rtk node --import tsx --test packages/backend/src/image-generation/jobs.test.ts`：12/12 通过。
- `rtk node --import tsx --test packages/frontend/src/image-generation/ImageJobQueue.test.tsx`：3/3 通过。
- `rtk node --import tsx --test packages/backend/src/image-generation/jobs.test.ts packages/backend/src/image-generation/routes.test.ts packages/backend/src/image-generation/queue.test.ts`：31/31 通过。
- `rtk node --import tsx --test packages/frontend/src/image-generation/ImageJobQueue.test.tsx packages/frontend/src/image-generation/ImageGenerationShell.test.tsx packages/frontend/src/image-generation/ImageJobForm.test.tsx`：8/8 通过。
- `rtk npm run build`：后端 `tsc`、前端 `tsc -b` 与 Vite build 通过，保留既有 chunk size warning。
- 后端 HTTP smoke：临时 SQLite + 临时 Express API + fake provider runtime，依次调用 `/api/projects`、`/image-provider-profiles`、`/image-jobs` 创建父图和 image-to-image 子任务；`GET /api/projects/:projectId/image-jobs/:childJobId` 返回 `source_images[0].origin_job_id` 等于父 job，`origin_output_id` 等于父 output，runtime 收到 1 张源图。
- 浏览器测试：临时后端 `http://127.0.0.1:17349`、前端 `http://127.0.0.1:5179`；Playwright 打开 `/projects/RcdpPc6aQlo9/images`，桌面快照确认 `图生图链路`、`browser-parent.png`、`browser-child.png`、父/子 prompt 和两张图库缩略图可见；390x844 移动宽度 DOM 检查 lineage 区域存在、宽度 314/390、`bodyScrollWidth=390`、无水平溢出。截图：`/tmp/scimage-task19-lineage-dedup-desktop.png`、`/tmp/scimage-task19-lineage-mobile.png`。React Router v7 future flag warning 为既有依赖提示，与本次改动无关。临时浏览器和服务已关闭。
- 代码审查：检查完整 diff 与新文件，`rtk git diff --check` 无空白错误；审查发现并修复图库 output/resource duplicate；确认 lineage 解析不绕过 repo 的项目归属与 origin output/file 校验。

- [x] **Step 6: Commit**

```bash
git add packages/backend/src/image-generation/source-images.ts packages/backend/src/image-generation/jobs.ts packages/backend/src/image-generation/service.ts packages/frontend/src/image-generation/ImageGalleryPanel.tsx packages/frontend/src/image-generation/ImageLineagePanel.tsx packages/backend/src/image-generation/jobs.test.ts packages/frontend/src/image-generation/ImageJobQueue.test.tsx docs/superpowers/plans/2026-06-07-SCimage图片生成集成实施计划.md
git commit -m "feat(frontend): 添加图片生成血缘关系"
```

### Task 20: 提示词库与图库分组

**Files:**
- Modify: `packages/backend/src/image-generation/jobs.ts`
- Modify: `packages/backend/src/image-generation/routes.ts`
- Create: `packages/frontend/src/image-generation/PromptBankPanel.tsx`
- Modify: `packages/frontend/src/image-generation/ImageGalleryPanel.tsx`
- Test: `packages/backend/src/image-generation/routes.test.ts`
- Test: `packages/frontend/src/image-generation/imageGenerationModel.test.ts`

- [x] **Step 1: Write failing grouping tests**

```ts
test('image gallery groups jobs by prompt', async () => {
  const response = await request(`/api/projects/${project.id}/image-jobs/groups?groupBy=prompt`);
  assert.equal(response.status, 200);
  const groups = await response.json() as Array<{ key: string; count: number }>;
  assert.equal(groups[0]?.key, 'apple');
});
```

- [x] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/routes.test.ts
cd packages/frontend && node --import tsx --test src/image-generation/imageGenerationModel.test.ts
```

Expected: fails.

实际 RED：

- `rtk node --import tsx --test packages/backend/src/image-generation/routes.test.ts`：6/8 通过，新增 `/image-jobs/groups` 与 `/image-prompt-presets` 测试分别因 404 失败。
- `rtk node --import tsx --test packages/frontend/src/image-generation/imageGenerationModel.test.ts`：失败于 `imageGenerationModel` 未导出 `buildPromptPresetPayload` 等 helper。

- [x] **Step 3: Add grouping endpoint**

Add:

```text
GET /api/projects/:projectId/image-jobs/groups?groupBy=prompt|task|session
```

Return:

```ts
type ImageJobGroup = {
  key: string;
  label: string;
  count: number;
  latest_job_id: string;
  latest_updated_at: number;
};
```

实际实现：

- `jobs.ts` 增加 `listGroupsByProject(projectId, groupBy)`，支持 `prompt|task|session`，返回 `key/label/count/latest_job_id/latest_updated_at`，按数量和最新时间排序。
- `routes.ts` 新增 `GET /api/projects/:projectId/image-jobs/groups?groupBy=prompt|task|session`，并放在 `:jobId` 动态路由之前，避免路由冲突。

- [x] **Step 4: Add prompt bank panel**

Use local persisted project-level prompt entries in a new table or `localStorage` only for UI draft. For project sharing, use a backend table:

```sql
CREATE TABLE IF NOT EXISTS image_prompt_presets (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  title TEXT NOT NULL,
  prompt TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  deleted_at INTEGER,
  FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
);
```

Panel operations:

- Add current prompt.
- Search presets.
- Insert preset into form.
- Delete preset.

实际实现：

- `db.ts` 新增 `image_prompt_presets` 表与项目索引。
- 新增 `prompt-presets.ts` 仓储，支持项目内 list/search/create/softDelete。
- `routes.ts` 新增 `GET/POST/DELETE /api/projects/:projectId/image-prompt-presets`。
- 前端新增 `PromptBankPanel.tsx`，支持保存当前 prompt、搜索、插入 preset 到生成表单、删除 preset。
- `ImageJobForm.tsx` 接入提示词库；`ImageGalleryPanel.tsx` 展示提示词分组；`ImageJobQueue.tsx` 和创建任务成功后会 invalidate 分组查询。

- [x] **Step 5: Run tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/routes.test.ts
cd packages/frontend && node --import tsx --test src/image-generation/imageGenerationModel.test.ts
```

Expected: pass.

实际 GREEN / 验证：

- `rtk node --import tsx --test packages/backend/src/image-generation/routes.test.ts`：8/8 通过。
- `rtk node --import tsx --test packages/frontend/src/image-generation/imageGenerationModel.test.ts`：5/5 通过。
- `rtk node --import tsx --test packages/backend/src/image-generation/jobs.test.ts packages/backend/src/image-generation/routes.test.ts packages/backend/src/image-generation/queue.test.ts`：33/33 通过。
- `rtk node --import tsx --test packages/frontend/src/image-generation/imageGenerationModel.test.ts packages/frontend/src/image-generation/ImageJobForm.test.tsx packages/frontend/src/image-generation/ImageGenerationShell.test.tsx packages/frontend/src/image-generation/ImageJobQueue.test.tsx`：13/13 通过。
- `rtk npm run build`：后端 `tsc`、前端 `tsc -b` 与 Vite build 通过，保留既有 chunk size warning。
- 后端 HTTP smoke：临时 SQLite + 临时 Express API，真实调用 `/api/projects/:projectId/image-jobs/groups?groupBy=prompt` 返回 `ocean poster` 分组计数 2；真实调用 `POST/GET/DELETE /image-prompt-presets` 完成创建、搜索、软删除。
- 浏览器测试：临时后端 `http://127.0.0.1:17350`、前端 `http://127.0.0.1:5180`；Playwright 打开 `/projects/hYQT0LSZ88MU/images`，确认 `提示词库`、`Ocean Preset`、`提示词分组` 可见，点击 `插入` 后主 Prompt 文本域值为 `ocean poster with clean product lighting`，分组文本显示计数 2；390x844 移动宽度 `bodyScrollWidth=390`，无水平溢出。截图：`/tmp/scimage-task20-prompt-bank-desktop.png`、`/tmp/scimage-task20-prompt-bank-mobile.png`。React Router v7 future flag warning 为既有依赖提示，与本次改动无关。临时浏览器和服务已关闭。
- 代码审查：检查完整 diff、新增 `prompt-presets.ts` 与 `PromptBankPanel.tsx`；`rtk git diff --check` 无空白错误；确认 prompt preset CRUD 为项目级作用域、删除为软删除、groups endpoint 路由顺序不会被 `:jobId` 吞掉，前端按钮均为 `type="button"` 避免嵌入生成表单后误提交。

- [x] **Step 6: Commit**

```bash
git add packages/backend/src/db.ts packages/backend/src/image-generation/jobs.ts packages/backend/src/image-generation/routes.ts packages/backend/src/image-generation/types.ts packages/backend/src/image-generation/prompt-presets.ts packages/frontend/src/image-generation/PromptBankPanel.tsx packages/frontend/src/image-generation/ImageGalleryPanel.tsx packages/frontend/src/image-generation/ImageJobForm.tsx packages/frontend/src/image-generation/ImageJobQueue.tsx packages/frontend/src/image-generation/imageGenerationModel.ts packages/frontend/src/lib/api.ts packages/frontend/src/lib/types.ts packages/backend/src/image-generation/routes.test.ts packages/frontend/src/image-generation/imageGenerationModel.test.ts docs/superpowers/plans/2026-06-07-SCimage图片生成集成实施计划.md
git commit -m "feat(frontend): 添加图片提示词库与分组"
```

### Task 21: 多 provider 兼容扩展与批量操作

**Files:**
- Modify: `packages/backend/src/image-generation/openai-compatible.ts`
- Modify: `packages/backend/src/image-generation/routes.ts`
- Modify: `packages/frontend/src/image-generation/ImageGalleryPanel.tsx`
- Test: `packages/backend/src/image-generation/openai-compatible.test.ts`
- Test: `packages/backend/src/image-generation/routes.test.ts`

- [ ] **Step 1: Write failing compatibility tests**

```ts
test('chat completions image transport extracts image url from message content', async () => {
  const result = await requestChatCompletionsImageGeneration({
    baseUrl: 'https://example.com/v1',
    apiKey: 'secret',
    model: 'image-chat-model',
    prompt: 'apple',
    quality: 'auto',
    size: 'auto',
    fetchImpl: async () => new Response(JSON.stringify({
      choices: [{
        message: {
          content: [{ type: 'image_url', image_url: { url: 'data:image/png;base64,cG5n' } }],
        },
      }],
    })),
  });

  assert.equal(result.images.length, 1);
});
```

- [ ] **Step 2: Run failing tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/openai-compatible.test.ts src/image-generation/routes.test.ts
```

Expected: fails.

- [ ] **Step 3: Add chat completions transport**

Add support for `compat_profile_id = 'chat-completions'`:

```ts
export async function requestChatCompletionsImageGeneration(input: ChatCompletionsImageRequest): Promise<ImageGenerationRuntimeResponse> {
  const response = await input.fetchImpl(`${input.baseUrl}/chat/completions`, {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${input.apiKey}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      model: input.model,
      messages: [{ role: 'user', content: input.prompt }],
      modalities: ['image', 'text'],
    }),
    signal: input.signal,
  });
  return parseChatCompletionImageResponse(response, input.fetchImpl, input.signal);
}
```

- [ ] **Step 4: Add batch gallery actions**

Routes:

```text
POST /api/projects/:projectId/image-outputs/batch/delete
POST /api/projects/:projectId/image-outputs/batch/download-manifest
```

Delete should soft-delete linked files through existing file deletion behavior and remove output references from gallery queries.

- [ ] **Step 5: Add frontend batch toolbar**

Gallery supports:

- Multi-select.
- Delete selected.
- Download selected by manifest URLs.
- Clear selection.

- [ ] **Step 6: Run tests**

```bash
cd packages/backend && node --import tsx --test src/image-generation/openai-compatible.test.ts src/image-generation/routes.test.ts
npm run build -w @openclaw-room/frontend
```

Expected: tests and build pass.

- [ ] **Step 7: Commit**

```bash
git add packages/backend/src/image-generation/openai-compatible.ts packages/backend/src/image-generation/routes.ts packages/frontend/src/image-generation/ImageGalleryPanel.tsx packages/backend/src/image-generation/openai-compatible.test.ts packages/backend/src/image-generation/routes.test.ts
git commit -m "feat: 扩展图片提供方兼容与批量操作"
```

## Final Integration Verification

Run these after each phase, and run all commands before marking the whole implementation complete:

```bash
npm run test -w @openclaw-room/backend
npm run build -w @openclaw-room/backend
npm run build -w @openclaw-room/frontend
npm run build
```

Browser smoke test:

1. Start dev server:

```bash
npm run dev
```

2. Open a project image workbench:

```text
http://localhost:5173/projects/<projectId>/images
```

3. Verify:

- Provider profile can be created without exposing saved API Key.
- Text-to-image job appears in queue.
- Running job transitions through WebSocket events.
- Completed output appears in gallery and project resources.
- Session composer opens image generation dialog.
- Session-created image job appears in transcript.
- Agent tool result appears as session evidence and resource.

## Spec Coverage Self-Review

- Independent workbench: covered by Tasks 10-14.
- Session image generation: covered by Tasks 15-16.
- Agent tool: covered by Tasks 17-18.
- Provider configuration and model discovery: covered by Tasks 2-3.
- Queue, cancel, retry, partial success: covered by Tasks 5-8.
- File/resource persistence: covered by Task 6.
- WebSocket status updates: covered by Task 9.
- Image-to-image and lineage: covered by Task 19.
- Prompt bank, gallery grouping, batch operations: covered by Tasks 20-21.
- No SCimage Vue/Python embedding: preserved by all tasks using OpenDeepSea native React/Express modules.

## Execution Notes

- Each task should be executed with TDD: write failing test, run it, implement, run it again, commit.
- Prefer one task per commit. Do not batch unrelated phase work into one commit.
- If a task reveals existing dirty changes in its target files, inspect them and preserve them; do not revert user changes.
- Do not commit provider API keys, generated images, local SQLite databases, or `.env` files.
- For real API smoke testing, use a local-only provider profile and remove or rotate credentials after verification.
