# 组合式主题体系 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将主题从 `light/dark/console/minimal` 平铺模型迁移为 `苹果风/极简风 × 亮色/暗色` 组合模型，并保持当前 `light` 视觉作为苹果风亮色基线不被误改。

**Architecture:** 主题状态使用组合值 `apple-light | apple-dark | minimal-light | minimal-dark`，通过 helper 集中处理旧值迁移、风格/明暗拆分和组合。UI 改为两个 segmented controls，CSS 保留 `:root` 作为苹果亮色默认 token，新增 `apple-dark`、`minimal-light`、`minimal-dark` token，并将当前极简 scoped 覆盖迁移到两个极简组合主题。

**Tech Stack:** React 18、TypeScript、Vite、Tailwind utilities、CSS custom properties、lucide-react。

---

## 文件结构

- Modify: `packages/frontend/src/lib/theme.ts`
  - 定义组合式主题类型、选项、旧值迁移和 helper。
- Modify: `packages/frontend/src/main.tsx`
  - 使用 `parseThemeMode` 初始化主题；根据 `getThemeTone` 设置 `dark` class、`colorScheme` 和 Toaster。
- Modify: `packages/frontend/src/components/AppShell.tsx`
  - 把单组主题按钮改成风格/明暗两组控件；移除 `控制台` 入口。
- Modify: `packages/frontend/src/index.css`
  - 保留当前 `:root` 不动；新增组合主题 token；迁移极简 scoped 覆盖；补苹果暗色视觉 token。
- Modify: `docs/superpowers/plans/2026-05-14-组合式主题体系实现计划.md`
  - 执行时勾选步骤。

## 当前工作区前置条件

当前工作区已有无关未提交/未跟踪内容，例如 `packages/frontend/tsconfig.tsbuildinfo`、`.logs/`、`REVIEW.md`、另一个未跟踪 spec。执行时不得 stage 或提交这些无关文件。

- [x] **Step 1: 查看目标文件 diff**

Run:

```bash
git diff -- packages/frontend/src/lib/theme.ts packages/frontend/src/main.tsx packages/frontend/src/components/AppShell.tsx packages/frontend/src/index.css
```

Expected:

- 能看到当前已经存在的 `minimal` 主题实现。
- 确认 `:root` 是当前苹果亮色基线，后续不重写它。

---

### Task 1: 主题 helper 与旧值迁移

**Files:**
- Modify: `packages/frontend/src/lib/theme.ts`
- Modify: `packages/frontend/src/main.tsx`

- [x] **Step 1: 替换主题 helper**

将 `packages/frontend/src/lib/theme.ts` 改为：

```ts
export type ThemeStyle = 'apple' | 'minimal';
export type ThemeTone = 'light' | 'dark';
export type ThemeMode = `${ThemeStyle}-${ThemeTone}`;
export type LegacyThemeMode = 'light' | 'dark' | 'console' | 'minimal';

export const THEME_STYLES: Array<{ value: ThemeStyle; label: string }> = [
  { value: 'apple', label: '苹果风' },
  { value: 'minimal', label: '极简风' },
];

export const THEME_TONES: Array<{ value: ThemeTone; label: string }> = [
  { value: 'light', label: '亮色' },
  { value: 'dark', label: '暗色' },
];

const THEME_MODES = new Set<ThemeMode>([
  'apple-light',
  'apple-dark',
  'minimal-light',
  'minimal-dark',
]);

export function isThemeMode(value: string | null): value is ThemeMode {
  return value !== null && THEME_MODES.has(value as ThemeMode);
}

export function parseThemeMode(value: string | null): ThemeMode {
  if (isThemeMode(value)) return value;
  if (value === 'light') return 'apple-light';
  if (value === 'dark') return 'apple-dark';
  if (value === 'minimal' || value === 'console') return 'minimal-light';
  return 'apple-light';
}

export function getThemeStyle(theme: ThemeMode): ThemeStyle {
  return theme.startsWith('minimal-') ? 'minimal' : 'apple';
}

export function getThemeTone(theme: ThemeMode): ThemeTone {
  return theme.endsWith('-dark') ? 'dark' : 'light';
}

export function createThemeMode(style: ThemeStyle, tone: ThemeTone): ThemeMode {
  return `${style}-${tone}`;
}
```

- [x] **Step 2: 更新 `main.tsx` 初始化与明暗判断**

将主题 import 改为：

```ts
import { getThemeTone, parseThemeMode, type ThemeMode } from './lib/theme';
```

将 `getInitialTheme` 改为：

```ts
function getInitialTheme(): ThemeMode {
  const storedTheme = localStorage.getItem('openclaw-room-theme');
  return parseThemeMode(storedTheme);
}
```

将 `RootApp` 内明暗处理改为：

```ts
  const tone = getThemeTone(theme);

  useEffect(() => {
    document.documentElement.dataset.theme = theme;
    document.documentElement.classList.toggle('dark', tone === 'dark');
    document.documentElement.style.colorScheme = tone;
    localStorage.setItem('openclaw-room-theme', theme);
  }, [theme, tone]);

  const toasterTheme = tone;
```

- [x] **Step 3: 运行类型检查**

Run:

```bash
npx tsc -p packages/frontend/tsconfig.json --noEmit
```

Expected:

- Exit code 0。
- 不应出现 `ThemeMode` 不匹配或 `console` 主题值残留导致的类型错误。

- [x] **Step 4: 提交 Task 1**

只提交本任务文件：

```bash
git add packages/frontend/src/lib/theme.ts packages/frontend/src/main.tsx
git commit -m "refactor(frontend): 迁移组合式主题模型"
```

---

### Task 2: 主题切换 UI 改为风格 × 明暗

**Files:**
- Modify: `packages/frontend/src/components/AppShell.tsx`

- [x] **Step 1: 调整 import**

从 lucide-react import 中移除 `MonitorCog`，保留 `Moon`、`PanelTop`、`Sun`，并新增 `Sparkles`。示例：

```ts
import {
  Bot,
  BriefcaseBusiness,
  FolderKanban,
  GitBranch,
  Home,
  Loader2,
  Moon,
  PanelTop,
  Plus,
  RefreshCw,
  Search,
  Server,
  Settings,
  Sparkles,
  SquareCheck,
  Sun,
  Wifi,
  WifiOff,
} from 'lucide-react';
```

将主题 helper import 改为：

```ts
import {
  createThemeMode,
  getThemeStyle,
  getThemeTone,
  THEME_STYLES,
  THEME_TONES,
  type ThemeMode,
  type ThemeStyle,
  type ThemeTone,
} from '../lib/theme';
```

- [x] **Step 2: 替换 `ThemeToggle` 组件**

将现有 `ThemeToggle` 函数整体替换为：

```tsx
function ThemeToggle({
  theme,
  onThemeChange,
}: {
  theme: ThemeMode;
  onThemeChange: (theme: ThemeMode) => void;
}): JSX.Element {
  const style = getThemeStyle(theme);
  const tone = getThemeTone(theme);
  const styleIcons: Record<ThemeStyle, typeof Sun> = {
    apple: Sparkles,
    minimal: PanelTop,
  };
  const toneIcons: Record<ThemeTone, typeof Sun> = {
    light: Sun,
    dark: Moon,
  };

  return (
    <div className="theme-toggle-stack mb-2" aria-label="主题样式">
      <div className="theme-toggle" aria-label="主题风格">
        {THEME_STYLES.map((option) => {
          const Icon = styleIcons[option.value];
          return (
            <button
              key={option.value}
              type="button"
              className={cn('theme-toggle-option', style === option.value && 'is-active')}
              aria-pressed={style === option.value}
              onClick={() => onThemeChange(createThemeMode(option.value, tone))}
            >
              <Icon className="h-3.5 w-3.5" strokeWidth={1.75} />
              <span>{option.label}</span>
            </button>
          );
        })}
      </div>
      <div className="theme-toggle" aria-label="主题明暗">
        {THEME_TONES.map((option) => {
          const Icon = toneIcons[option.value];
          return (
            <button
              key={option.value}
              type="button"
              className={cn('theme-toggle-option', tone === option.value && 'is-active')}
              aria-pressed={tone === option.value}
              onClick={() => onThemeChange(createThemeMode(style, option.value))}
            >
              <Icon className="h-3.5 w-3.5" strokeWidth={1.75} />
              <span>{option.label}</span>
            </button>
          );
        })}
      </div>
    </div>
  );
}
```

- [x] **Step 3: 确认控制台入口已删除**

Run:

```bash
rg -n "console|控制台|MonitorCog" packages/frontend/src/components/AppShell.tsx packages/frontend/src/lib/theme.ts
```

Expected:

- `theme.ts` 中可以出现 legacy `console` 迁移逻辑。
- `AppShell.tsx` 中不应出现 `console`、`控制台`、`MonitorCog`。

- [x] **Step 4: 运行类型检查**

Run:

```bash
npx tsc -p packages/frontend/tsconfig.json --noEmit
```

Expected:

- Exit code 0。

- [x] **Step 5: 提交 Task 2**

```bash
git add packages/frontend/src/components/AppShell.tsx
git commit -m "refactor(frontend): 拆分主题风格与明暗切换"
```

---

### Task 3: CSS token 迁移为四组合主题

**Files:**
- Modify: `packages/frontend/src/index.css`

- [x] **Step 1: 重命名 dark token 为 apple-dark**

将：

```css
  [data-theme="dark"] {
```

改为：

```css
  [data-theme="apple-dark"] {
```

并在该块中补齐苹果暗色玻璃 token：

```css
    --color-bg: #090d16;
    --color-surface: rgba(17, 25, 39, 0.72);
    --color-surface-raised: rgba(29, 39, 57, 0.78);
    --color-border: rgba(148, 163, 184, 0.18);
    --color-border-strong: rgba(148, 163, 184, 0.32);
    --sidebar-bg: linear-gradient(180deg, rgba(18, 28, 43, 0.92), rgba(12, 19, 32, 0.82));
    --main-bg: linear-gradient(145deg, rgba(18, 27, 42, 0.84), rgba(10, 16, 28, 0.74));
    --glass-bg: linear-gradient(145deg, rgba(30, 41, 59, 0.78), rgba(15, 23, 42, 0.56));
    --glass-bg-strong: linear-gradient(145deg, rgba(42, 55, 78, 0.86), rgba(18, 27, 42, 0.64));
    --glass-edge: inset 0 1px 0 rgba(255,255,255,0.08), inset 1px 0 0 rgba(255,255,255,0.04), 0 18px 54px rgba(0, 0, 0, 0.28);
```

保留当前暗色语义色和 `color-scheme: dark`。

- [x] **Step 2: 重命名 minimal token 为 minimal-light**

将：

```css
  [data-theme="minimal"] {
```

改为：

```css
  [data-theme="minimal-light"] {
```

该块内容保持当前 minimal 亮色 token，不重调。

- [x] **Step 3: 添加 minimal-dark token**

在 `minimal-light` token 块后添加：

```css
  [data-theme="minimal-dark"] {
    --color-bg: #0f1115;
    --color-surface: #15181e;
    --color-surface-raised: #1b1f27;
    --color-border: #2b313b;
    --color-border-strong: #3a4250;
    --color-fg: #e7eaf0;
    --color-fg-muted: #a6afbc;
    --color-muted: #737d8c;

    --color-primary: #6ea8ff;
    --color-primary-hover: #8bbaff;
    --color-primary-fg: #08111f;

    --color-accent: #6ea8ff;
    --color-accent-hover: #8bbaff;

    --color-success: #5cc99a;
    --color-warning: #d89a3a;
    --color-danger: #ff756d;
    --color-danger-hover: #ff918a;

    --color-overlay: rgba(5, 7, 11, 0.66);
    --color-code-bg: #11141a;
    --color-avatar-ring: #15181e;
    --color-switch-off: #202631;
    --color-switch-thumb: #f8fafc;
    --color-scrollbar-thumb: #343b47;
    --color-scrollbar-thumb-hover: #46505f;
    --shadow-raised: none;
    --shadow-dialog: 0 18px 54px rgba(0, 0, 0, 0.42);
    --shadow-command: 0 18px 54px rgba(0, 0, 0, 0.38);
    --shadow-mention: 0 12px 34px rgba(0, 0, 0, 0.34);
    --glass-bg: #15181e;
    --glass-bg-strong: #1b1f27;
    --glass-edge: inset 0 0 0 1px var(--color-border);
    --sidebar-bg: #15181e;
    --main-bg: #0f1115;
    color-scheme: dark;
  }
```

- [x] **Step 4: 迁移极简 scoped 覆盖 selector**

将每个：

```css
[data-theme="minimal"] .some-selector
```

改为：

```css
[data-theme="minimal-light"] .some-selector,
[data-theme="minimal-dark"] .some-selector
```

示例：

```css
  [data-theme="minimal-light"] .liquid-backdrop,
  [data-theme="minimal-dark"] .liquid-backdrop {
    background: var(--color-bg);
  }
```

注意：所有极简结构覆盖必须同时作用于 `minimal-light` 和 `minimal-dark`。

- [x] **Step 5: 新增主题控件 stack 样式**

在 `.theme-toggle` 附近添加：

```css
  .theme-toggle-stack {
    display: grid;
    gap: 6px;
  }
```

- [x] **Step 6: 检查旧 selector 已清理**

Run:

```bash
rg -n 'data-theme="dark"|data-theme="minimal"|\\bconsole\\b' packages/frontend/src/index.css
```

Expected:

- 不应命中 `[data-theme="dark"]` 或 `[data-theme="minimal"]`。
- 不应命中 `console`。

- [x] **Step 7: 运行构建**

Run:

```bash
npm run build
```

Expected:

- 后端 TypeScript 编译成功。
- 前端 TypeScript 和 Vite 构建成功。

- [x] **Step 8: 提交 Task 3**

```bash
git add packages/frontend/src/index.css
git commit -m "feat(frontend): 适配苹果与极简暗色主题"
```

---

### Task 4: 浏览器迁移与视觉验证

**Files:**
- Verify: frontend app

- [x] **Step 1: 启动前端**

Run:

```bash
npm run dev:frontend
```

Expected:

- Vite 输出可访问 URL，例如 `http://localhost:5173/` 或递增端口。

- [x] **Step 2: 验证旧值迁移**

在浏览器控制台或 Playwright 中依次设置旧值并刷新：

```js
localStorage.setItem('openclaw-room-theme', 'light');
location.reload();
```

Expected after reload:

```js
document.documentElement.dataset.theme === 'apple-light'
localStorage.getItem('openclaw-room-theme') === 'apple-light'
```

重复验证：

```js
localStorage.setItem('openclaw-room-theme', 'dark');
location.reload();
```

Expected:

```js
document.documentElement.dataset.theme === 'apple-dark'
localStorage.getItem('openclaw-room-theme') === 'apple-dark'
```

```js
localStorage.setItem('openclaw-room-theme', 'minimal');
location.reload();
```

Expected:

```js
document.documentElement.dataset.theme === 'minimal-light'
localStorage.getItem('openclaw-room-theme') === 'minimal-light'
```

```js
localStorage.setItem('openclaw-room-theme', 'console');
location.reload();
```

Expected:

```js
document.documentElement.dataset.theme === 'minimal-light'
localStorage.getItem('openclaw-room-theme') === 'minimal-light'
```

- [x] **Step 3: 验证组合切换**

在 UI 上执行：

- 点击 `苹果风`，再点击 `亮色`，期望 `data-theme="apple-light"`。
- 点击 `暗色`，期望 `data-theme="apple-dark"`。
- 点击 `极简风`，期望 `data-theme="minimal-dark"`。
- 点击 `亮色`，期望 `data-theme="minimal-light"`。

- [x] **Step 4: 验证苹果亮色未误改**

在 `apple-light` 下读取关键样式：

```js
const root = getComputedStyle(document.documentElement);
const sidebar = getComputedStyle(document.querySelector('.glass-sidebar'));
({
  bg: root.getPropertyValue('--color-bg').trim(),
  surface: root.getPropertyValue('--color-surface').trim(),
  sidebarRadius: sidebar.borderRadius,
  sidebarBackdrop: sidebar.backdropFilter,
})
```

Expected:

- `bg` 仍为 `#eef5fb` 或浏览器等价值。
- `surface` 仍为当前 `rgba(255, 255, 255, 0.62)`。
- `.glass-sidebar` 仍保留当前圆角和 blur，不呈现极简硬边。

- [x] **Step 5: 验证暗色强区分**

在 `apple-dark` 下检查：

- `.glass-sidebar` 有玻璃式背景/blur/阴影层次。
- `document.documentElement.style.colorScheme === 'dark'`。

在 `minimal-dark` 下检查：

- `.glass-sidebar` 背景为纯深色 surface。
- `.glass-sidebar` 圆角为 `0px` 或极低圆角。
- `box-shadow` 是细线式边框，不是大投影。
- `document.documentElement.style.colorScheme === 'dark'`。

- [ ] **Step 6: 检查页面覆盖**

执行记录：已通过 Playwright CLI 验证 Dashboard、主题控件、旧值迁移与四组合主题样式；Project 路由打开时控制台 0 error，Project/Room API 与 Room SPA 路由返回正常。额外一次性 `npx --package playwright` 页面覆盖脚本因 npm registry `ECONNRESET` 未完成，因此本项不标记为完成。

检查 Dashboard、Project、Room：

- 两组主题控件文字不溢出。
- Room 的 toolbar、聊天、composer、任务看板、inspector 都正常显示。
- 浏览器控制台没有 error。

- [x] **Step 7: 提交验证记录**

如果执行时勾选了本计划步骤，提交计划文档：

```bash
git add docs/superpowers/plans/2026-05-14-组合式主题体系实现计划.md
git commit -m "docs: 更新组合式主题执行记录"
```

---

## 自检

- Spec 覆盖：计划覆盖组合式类型、旧值迁移、UI 两组控件、移除控制台入口、四组合 CSS、苹果亮色不误改、浏览器迁移验证。
- 范围检查：计划不修改页面布局、DOM 结构、路由、API、WebSocket 或业务模型。
- 风险处理：计划明确 `:root` 为苹果亮色基线，要求浏览器验证当前 light 视觉未被极简覆盖污染。
