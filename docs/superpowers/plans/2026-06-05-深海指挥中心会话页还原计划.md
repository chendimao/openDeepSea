# 深海指挥中心会话页还原 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将当前会话页面 100% 按 Stitch 设计稿“深海指挥中心 - 右侧模块整合版 (4:3)”重建为 Deepsea Command Center 三栏工作台。

**Architecture:** 保留现有 `SessionWorkspacePayload`、命令发送和 compact overlay 行为，仅替换 `packages/frontend/src/session-ui` 的视觉骨架与样式。真实数据优先来自 active session、history records、status、context、evidence；缺失的 profile、通知、运行计时、部分工具列表和 diff 明细使用稳定假数据占位。

**Tech Stack:** React 18、TypeScript、Vite、lucide-react、CSS。

---

### Task 1: 重建会话工作台骨架

**Files:**
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
- Test: `packages/frontend/src/session-ui/SessionShell.test.tsx`

- [ ] 将 `SessionShellView` 改为 Deepsea 专用整页结构：顶部 command bar、左侧历史栏、中间 transcript、右侧整合 inspector。
- [ ] 顶部按钮继续调用现有命令：`/new`、`/compact`、`/fork history:<id>`、`/status`。
- [ ] 左侧历史使用 `payload.historyRecords`，当前会话使用 `payload.activeSession.session`。
- [ ] 中间 transcript 使用 `payload.activeSession.messages` 和 `payload.evidence`。
- [ ] 右侧模块使用 `payload.status`、`payload.activeSession.session.objective/boundaries/risks`、`payload.activeSession.runs`、`payload.activeSession.planItems`、`payload.evidence`。

### Task 2: 重写 Deepsea 视觉系统

**Files:**
- Modify: `packages/frontend/src/session-ui/session-os.css`

- [ ] 以 Stitch token 重写样式：亮色玻璃、白色半透明面板、蓝/绿/琥珀/玫红语义色、Inter 与 JetBrains Mono 字体栈。
- [ ] 桌面按 `320px / fluid / 420px` 三栏，顶部 `56px` fixed command bar。
- [ ] 保留 4:3 设计密度，所有模块使用小字号、hairline border、轻 blur、精确 spacing。
- [ ] 小屏下改为单列滚动，右侧 inspector 与历史栏按顺序堆叠，避免横向溢出。
- [ ] compact overlay 统一成 Deepsea glass 弹层。

### Task 3: 更新测试与验证

**Files:**
- Modify: `packages/frontend/src/session-ui/SessionShell.test.tsx`
- Modify if needed: `packages/frontend/src/pages/SessionWorkspacePage.test.tsx`

- [ ] 测试服务端渲染包含目标稿关键文案：`Deepsea Command`、`会话历史`、`3. 对话记录`、`当前状态`、`目标契约`、`代理运行`、`工具调用`、`会话计划`、`待提交变更`。
- [ ] 测试命令按钮仍能触发 `/new`、`/compact`、`/status`。
- [ ] 运行 `npm run build`。
- [ ] 使用浏览器打开 `http://localhost:5173/projects/b6p57AAqxbcl/rooms/cPbEiE-0ftOx` 或项目 session 路由做实际 UI smoke test。

### Task 4: 收尾

**Files:**
- Review touched files only.

- [ ] 检查 `git diff` 确认未混入无关变更。
- [ ] 如实记录构建与浏览器验证结果。
- [ ] 仅提交本次相关文件，提交格式使用 `feat(frontend): 还原深海指挥中心会话页`。
