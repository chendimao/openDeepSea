# 会话左侧项目栏交互优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将会话页左侧项目栏调整为参考图风格的紧凑项目会话树，并补齐项目菜单、会话置顶和项目拖拽排序的真实交互。

**Architecture:** 后端只扩展现有 session PATCH 能力，让 `pinned_at` 可通过已有路由更新并复用现有 active-session broadcast。前端在 `SessionWorkspacePage` 统一接入 mutation 和 payload 刷新，`SessionShell`/`SessionShellView` 只负责展示、交互回调和可测试的拖拽/展开逻辑。样式继续落在 `session-os.css`，不引入新的视觉系统。

**Tech Stack:** React 18, TypeScript, TanStack Query, Express, Zod, SQLite repos, Node test runner, Vite, CSS.

---

## 文件结构

- Modify: `packages/backend/src/session.routes.ts`
  - 允许 `PATCH /sessions/:sessionId` 接收 `pinned_at: number | null`。
- Modify: `packages/backend/src/session-workspace-view-model.ts`
  - 在 `projectSwitcher.projects` 中带出 `created_at`、`pinned_at`、`sort_order`，供前端同层排序使用。
- Modify: `packages/backend/src/session-types.ts`
  - 扩展 `SessionProjectSwitcher.projects` 项目元数据类型。
- Modify: `packages/backend/src/session.routes.test.ts`
  - 覆盖 session 置顶和取消置顶会经 HTTP PATCH 落库。
- Modify: `packages/frontend/src/lib/api.ts`
  - 扩展 `api.updateSession` 入参类型支持 `pinned_at`。
- Modify: `packages/frontend/src/lib/types.ts`
  - 扩展 `SessionProjectSwitcher.projects` 项目元数据类型。
- Modify: `packages/frontend/src/lib/api.test.ts`
  - 覆盖 `api.updateSession` 会发送 `pinned_at` 字段。
- Modify: `packages/frontend/src/session-ui/SessionShell.tsx`
  - 透传项目编辑、移除、排序、会话置顶回调。
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
  - 默认只展开当前项目。
  - 项目前不渲染折叠 icon。
  - 项目菜单精简为真实「编辑名称 / 移除」动作。
  - 会话行增加左侧置顶按钮。
  - 项目行支持同层拖拽排序。
- Modify: `packages/frontend/src/session-ui/SessionShell.test.tsx`
  - 覆盖默认展开、菜单项、置顶按钮、拖拽 helper、CSS 关键规则。
- Modify: `packages/frontend/src/pages/SessionWorkspacePage.tsx`
  - 接入项目编辑、移除、排序和会话置顶 mutation。
  - 增加项目编辑和移除确认 Dialog。
- Modify: `packages/frontend/src/pages/SessionWorkspacePage.test.tsx`
  - 覆盖项目名称更新、项目删除后 payload 更新、会话置顶 payload 更新 helper。
- Modify: `packages/frontend/src/session-ui/session-os.css`
  - 调整参考图风格字号、行高、缩进、hover 显隐、当前会话高亮和拖拽状态。

## Task 1: 后端和 API 支持会话置顶

**Files:**
- Modify: `packages/backend/src/session.routes.test.ts`
- Modify: `packages/backend/src/session.routes.ts`
- Modify: `packages/frontend/src/lib/api.ts`
- Modify: `packages/frontend/src/lib/api.test.ts`

- [ ] **Step 1: 写后端失败测试**

在 `packages/backend/src/session.routes.test.ts` 的 legacy route 测试之后添加：

```ts
test('session PATCH updates pinned_at for active session ordering', async () => {
  const project = projectRepo.create({
    name: 'session pin patch project',
    path: mkdtempSync(join(tmpdir(), 'session-pin-patch-project-')),
  });
  const session = sessionRepo.create({
    project_id: project.id,
    title: 'Pin Me',
    mode: 'code',
    provider: 'codex',
    workspace_path: project.path,
  });

  const pinnedAt = Date.now();
  const pinRes = await request(`/api/sessions/${session.id}`, {
    method: 'PATCH',
    body: JSON.stringify({ pinned_at: pinnedAt }),
  });

  assert.equal(pinRes.status, 200);
  const pinned = await pinRes.json() as { pinned_at: number | null };
  assert.equal(pinned.pinned_at, pinnedAt);
  assert.equal(sessionRepo.get(session.id)?.pinned_at, pinnedAt);

  const unpinRes = await request(`/api/sessions/${session.id}`, {
    method: 'PATCH',
    body: JSON.stringify({ pinned_at: null }),
  });

  assert.equal(unpinRes.status, 200);
  const unpinned = await unpinRes.json() as { pinned_at: number | null };
  assert.equal(unpinned.pinned_at, null);
  assert.equal(sessionRepo.get(session.id)?.pinned_at, null);
});
```

- [ ] **Step 2: 运行后端测试确认失败**

Run:

```bash
node --import tsx --test packages/backend/src/session.routes.test.ts
```

Expected: 新测试失败，响应为 `400`，因为 `pinned_at` 还未加入 session update schema。

- [ ] **Step 3: 扩展 session PATCH schema**

在 `packages/backend/src/session.routes.ts` 的 `updateSession` schema 中加入：

```ts
pinned_at: z.number().int().nullable().optional(),
```

最终 schema 片段应包含：

```ts
const parsed = z.object({
  title: z.string().trim().min(1).optional(),
  current_goal: z.string().trim().min(1).nullable().optional(),
  mode: sessionModeSchema.optional(),
  phase: z.enum([
    'idle',
    'brainstorming',
    'planning',
    'implementing',
    'debugging',
    'reviewing',
    'verifying',
    'blocked',
    'completed',
    'archived',
  ]).optional(),
  status: z.enum(['active', 'blocked', 'completed', 'archived', 'failed']).optional(),
  provider: z.enum(['claudecode', 'opencode', 'codex']).nullable().optional(),
  model: z.string().trim().min(1).nullable().optional(),
  pinned_at: z.number().int().nullable().optional(),
}).strict().safeParse(req.body ?? {});
```

- [ ] **Step 4: 写前端 API 失败测试**

在 `packages/frontend/src/lib/api.test.ts` 添加：

```ts
test('updateSession sends pinned_at patch payload', async () => {
  const originalFetch = globalThis.fetch;
  let requestedUrl = '';
  let requestedMethod = '';
  let requestedBody = '';
  globalThis.fetch = (async (input: RequestInfo | URL, init?: RequestInit) => {
    requestedUrl = String(input);
    requestedMethod = init?.method ?? 'GET';
    requestedBody = String(init?.body ?? '');
    return new Response(JSON.stringify({
      id: 'session-1',
      project_id: 'project-1',
      title: 'Session',
      current_goal: null,
      mode: 'code',
      phase: 'idle',
      status: 'active',
      provider: 'codex',
      model: 'gpt-5.5',
      workspace_path: '/tmp/project',
      worktree_path: null,
      branch_name: null,
      forked_from_session_id: null,
      forked_from_history_record_id: null,
      latest_compaction_id: null,
      latest_context_manifest_id: null,
      closed_at: null,
      pinned_at: 123,
      last_viewed_at: null,
      created_at: 1,
      updated_at: 2,
      archived_at: null,
    }), {
      status: 200,
      headers: { 'Content-Type': 'application/json' },
    });
  }) as typeof fetch;

  try {
    await api.updateSession('session-1', { pinned_at: 123 });
  } finally {
    globalThis.fetch = originalFetch;
  }

  assert.equal(requestedUrl, '/api/sessions/session-1');
  assert.equal(requestedMethod, 'PATCH');
  assert.equal(requestedBody, JSON.stringify({ pinned_at: 123 }));
});
```

- [ ] **Step 5: 运行前端 API 测试确认失败**

Run:

```bash
node --import tsx --test packages/frontend/src/lib/api.test.ts
```

Expected: TypeScript 编译失败，提示 `pinned_at` 不属于 `api.updateSession` 入参类型。

- [ ] **Step 6: 扩展 `api.updateSession` 入参类型**

在 `packages/frontend/src/lib/api.ts` 中把 `updateSession` 入参改为：

```ts
input: Partial<Pick<
  Session,
  'title' | 'current_goal' | 'mode' | 'phase' | 'status' | 'provider' | 'model' | 'pinned_at'
>>,
```

- [ ] **Step 7: 验证后端和 API 测试通过**

Run:

```bash
node --import tsx --test packages/backend/src/session.routes.test.ts packages/frontend/src/lib/api.test.ts
```

Expected: 两个测试文件通过。

- [ ] **Step 8: 提交 Task 1**

```bash
git add packages/backend/src/session.routes.ts packages/backend/src/session.routes.test.ts packages/frontend/src/lib/api.ts packages/frontend/src/lib/api.test.ts
git commit -m "fix(session): 支持会话置顶更新"
```

## Task 2: 重构左侧项目树展示和交互回调

**Files:**
- Modify: `packages/backend/src/session-workspace-view-model.ts`
- Modify: `packages/backend/src/session-types.ts`
- Modify: `packages/frontend/src/lib/types.ts`
- Modify: `packages/frontend/src/session-ui/SessionShell.tsx`
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
- Modify: `packages/frontend/src/session-ui/SessionShell.test.tsx`

- [ ] **Step 1: 写默认展开和菜单失败测试**

在 `packages/frontend/src/session-ui/SessionShell.test.tsx` 的项目树测试附近新增或替换断言：

```ts
test('SessionShell expands only the current project by default', () => {
  const payload = createPayload();
  payload.projectSwitcher.projects.push({
    id: 'project-empty',
    name: 'EmptyProject',
    path: '/workspace/empty',
    active: false,
    recentSessions: [],
  });

  const html = renderSessionShell(payload);

  assert.match(html, /aria-label="切换 OpenClaw 项目展开状态"[^>]*aria-expanded="true"/);
  assert.match(html, /aria-label="切换 AnotherProject 项目展开状态"[^>]*aria-expanded="false"/);
  assert.match(html, /SessionOS 迁移/);
  assert.doesNotMatch(html, /接口联调/);
  assert.doesNotMatch(html, /暂无活跃会话/);
});

test('SessionShell project menu contains only rename and remove actions', () => {
  const html = renderSessionShell(createPayload());

  assert.match(html, /data-project-menu-item="编辑名称"/);
  assert.match(html, /data-project-menu-item="移除"/);
  assert.doesNotMatch(html, /在“访达”中打开/);
  assert.doesNotMatch(html, /创建永久工作树/);
  assert.doesNotMatch(html, /归档聊天/);
  assert.doesNotMatch(html, /data-disabled="true"/);
});

test('SessionShell project rows do not render a collapse icon before the project name', () => {
  const html = renderSessionShell(createPayload());

  assert.doesNotMatch(html, /data-project-collapse-icon="true"/);
  assert.match(html, /<svg[^>]+lucide-folder-open[^>]*aria-hidden="true"/s);
});
```

同时更新旧测试 `SessionShell renders project row actions for the active project tree`，删除对旧占位菜单项和“新建项目会话按钮常驻”的断言。

- [ ] **Step 2: 写置顶按钮和拖拽 helper 失败测试**

在同一个测试文件新增：

```ts
test('SessionShell renders session pin buttons without opening sessions', () => {
  const payload = createPayload();
  payload.activeSession.session.pinned_at = null;
  payload.activeSessions = payload.activeSessions.map((session) =>
    session.id === payload.activeSession.session.id ? { ...session, pinned_at: null } : session
  );

  const html = renderSessionShell(payload);

  assert.match(html, /data-session-pin-button="true"/);
  assert.match(html, /aria-label="置顶会话：SessionOS 迁移"/);
  assert.match(html, /data-pinned="false"/);
});

test('buildProjectReorderInput returns same-layer reorder ids', () => {
  const now = Date.now();
  const projects = [
    { id: 'project-1', name: 'A', path: '/a', active: false, recentSessions: [], created_at: now - 3, pinned_at: null, sort_order: 1 },
    { id: 'project-2', name: 'B', path: '/b', active: false, recentSessions: [], created_at: now - 2, pinned_at: null, sort_order: 2 },
    { id: 'project-3', name: 'C', path: '/c', active: false, recentSessions: [], created_at: now - 1, pinned_at: null, sort_order: 3 },
  ];

  assert.deepEqual(buildProjectReorderInput(projects, 'project-3', 'project-1'), {
    ids: ['project-3', 'project-1', 'project-2'],
    pinned: false,
  });
});
```

更新测试文件 import，从 `SessionShellView` 增加导入：

```ts
buildProjectReorderInput,
```

- [ ] **Step 3: 运行测试确认失败**

Run:

```bash
node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx
```

Expected: 新测试失败，原因包括默认全部展开、菜单项仍是旧占位、没有置顶按钮、`buildProjectReorderInput` 未导出。

- [ ] **Step 4: 扩展 projectSwitcher 排序元数据**

在 `packages/backend/src/session-types.ts` 的 `SessionProjectSwitcher.projects` 项中加入：

```ts
created_at?: number;
pinned_at?: number | null;
sort_order?: number | null;
```

在 `packages/frontend/src/lib/types.ts` 的 `SessionProjectSwitcher.projects` 项中加入同样字段：

```ts
created_at?: number;
pinned_at?: number | null;
sort_order?: number | null;
```

在 `packages/backend/src/session-workspace-view-model.ts` 的 `buildSessionProjectSwitcher` map 中补齐：

```ts
const projects = projectRepo.list().map((project) => ({
  id: project.id,
  name: project.name,
  path: project.path,
  active: project.id === activeProjectId,
  created_at: project.created_at,
  pinned_at: project.pinned_at,
  sort_order: project.sort_order,
  recentSessions: buildRecentProjectSessions(project),
}));
```

- [ ] **Step 5: 扩展项目类型和 helper**

在 `SessionShellView.tsx` 中扩展 `ProjectSwitcherProject` 的排序字段兼容：

```ts
type ProjectSwitcherProject = SessionWorkspacePayload['projectSwitcher']['projects'][number] & {
  created_at?: number;
  pinned_at?: number | null;
  sort_order?: number | null;
};
```

添加导入：

```ts
import type { DragEvent } from 'react';
import { isPinnedItem, layerIds, reorderWithinLayer, sortPinnedItems } from '../lib/sortableItems';
```

导出拖拽 helper：

```ts
export function buildProjectReorderInput(
  projects: ProjectSwitcherProject[],
  activeId: string,
  overId: string,
): { ids: string[]; pinned: boolean } | null {
  const sortableProjects = projects.map((project, index) => ({
    ...project,
    created_at: project.created_at ?? -index,
    pinned_at: project.pinned_at ?? null,
    sort_order: project.sort_order ?? null,
  }));
  const next = reorderWithinLayer(sortableProjects, activeId, overId);
  const moved = next.find((project) => project.id === activeId);
  if (!moved) return null;
  const pinned = isPinnedItem(moved);
  const ids = layerIds(next, pinned);
  return ids.length > 0 ? { ids, pinned } : null;
}
```

- [ ] **Step 6: 扩展 `SessionShell` 和 `SessionShellView` props**

在 `SessionShell.tsx` 和 `SessionShellView.tsx` 增加并透传：

```ts
type SessionShellProject = SessionWorkspacePayload['projectSwitcher']['projects'][number];

onRenameProject?: (project: SessionShellProject) => void;
onRemoveProject?: (project: SessionShellProject) => void;
onReorderProjects?: (input: { ids: string[]; pinned: boolean }) => void;
onToggleSessionPin?: (session: ActiveSessionSummary) => void;
```

`SessionShell` 中调用 `SessionShellView` 时补齐：

```tsx
onRenameProject={onRenameProject}
onRemoveProject={onRemoveProject}
onReorderProjects={onReorderProjects}
onToggleSessionPin={onToggleSessionPin}
```

- [ ] **Step 7: 实现默认展开和项目菜单**

把 `projectActionMenuItems` 精简为：

```ts
const projectActionMenuItems: Array<{
  label: '编辑名称' | '移除';
  icon: LucideIcon;
  danger?: boolean;
}> = [
  { label: '编辑名称', icon: SquarePen },
  { label: '移除', icon: Trash2, danger: true },
];
```

把初始展开状态改为：

```ts
const [expandedProjectIds, setExpandedProjectIds] = useState<Record<string, boolean>>(() =>
  Object.fromEntries(tree.map((project) => [project.id, project.id === currentProjectId]))
);
```

计算 expanded 时使用：

```ts
const expanded = normalizedQuery
  ? true
  : expandedProjectIds[project.id] ?? project.id === currentProjectId;
```

项目按钮 aria-label 改为：

```tsx
aria-label={`切换 ${project.name} 项目展开状态`}
```

项目菜单按钮改为真实动作：

```tsx
onClick={() => {
  setOpenProjectMenuId(null);
  if (item.label === '编辑名称') onRenameProject?.(project);
  else onRemoveProject?.(project);
}}
```

删除 `disabled`、`data-disabled` 和占位项。

- [ ] **Step 8: 实现会话置顶按钮**

把 `ProjectSessionRow` 的 button 外层改为行容器，左侧按钮独立阻止冒泡：

```tsx
<div
  className="deepsea-project-session-row-wrap"
  data-current={isCurrent ? 'true' : undefined}
  data-pinned={session.pinned_at !== null ? 'true' : 'false'}
>
  <button
    type="button"
    className="deepsea-project-session-pin"
    data-session-pin-button="true"
    data-pinned={session.pinned_at !== null ? 'true' : 'false'}
    aria-label={`${session.pinned_at !== null ? '取消置顶会话' : '置顶会话'}：${session.title}`}
    aria-pressed={session.pinned_at !== null}
    onClick={(event) => {
      event.stopPropagation();
      onToggleSessionPin?.(session);
    }}
  >
    <Pin aria-hidden="true" />
  </button>
  <button
    type="button"
    aria-current={isCurrent ? 'true' : undefined}
    className="deepsea-project-session-row"
    data-current={isCurrent ? 'true' : undefined}
    data-project-session-row="true"
    data-running={session.active_run_count > 0 ? 'true' : undefined}
    data-status={session.status}
    onClick={() => onOpenSession?.(session.project_id, session.id)}
  >
    <span className="deepsea-project-session-row__title" title={session.title}>
      {formatCompactSessionTitle(session.title, 31)}
    </span>
    <time className="deepsea-project-session-row__time">{formatRelativeTime(Date.now(), session.updated_at)}</time>
  </button>
</div>
```

在 lucide import 中加入：

```ts
Pin,
```

- [ ] **Step 9: 实现项目拖拽事件**

在 `ProjectSessionTreeRail` 中新增状态：

```ts
const [draggingProjectId, setDraggingProjectId] = useState<string | null>(null);
const [dropProjectId, setDropProjectId] = useState<string | null>(null);
```

添加 handler：

```ts
const resetDragState = () => {
  setDraggingProjectId(null);
  setDropProjectId(null);
};

const handleProjectDrop = (event: DragEvent<HTMLElement>, targetProjectId: string) => {
  event.preventDefault();
  if (!draggingProjectId || draggingProjectId === targetProjectId) {
    resetDragState();
    return;
  }
  const input = buildProjectReorderInput(tree, draggingProjectId, targetProjectId);
  if (input) onReorderProjects?.(input);
  resetDragState();
};
```

给项目 section 增加：

```tsx
draggable
data-dragging={draggingProjectId === project.id ? 'true' : undefined}
data-drop-target={dropProjectId === project.id ? 'true' : undefined}
onDragStart={(event) => {
  event.dataTransfer.effectAllowed = 'move';
  event.dataTransfer.setData('text/plain', project.id);
  setDraggingProjectId(project.id);
}}
onDragOver={(event) => {
  if (!draggingProjectId || draggingProjectId === project.id) return;
  event.preventDefault();
  event.dataTransfer.dropEffect = 'move';
  setDropProjectId(project.id);
}}
onDragLeave={() => {
  if (dropProjectId === project.id) setDropProjectId(null);
}}
onDrop={(event) => handleProjectDrop(event, project.id)}
onDragEnd={resetDragState}
```

- [ ] **Step 10: 验证 Task 2 测试通过**

Run:

```bash
node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx
```

Expected: `SessionShell.test.tsx` 通过。

- [ ] **Step 11: 提交 Task 2**

```bash
git add packages/backend/src/session-workspace-view-model.ts packages/backend/src/session-types.ts packages/frontend/src/lib/types.ts packages/frontend/src/session-ui/SessionShell.tsx packages/frontend/src/session-ui/SessionShellView.tsx packages/frontend/src/session-ui/SessionShell.test.tsx
git commit -m "feat(session): 优化左侧项目树交互"
```

## Task 3: 页面层接入项目操作和会话置顶

**Files:**
- Modify: `packages/frontend/src/pages/SessionWorkspacePage.tsx`
- Modify: `packages/frontend/src/pages/SessionWorkspacePage.test.tsx`

- [ ] **Step 1: 写 payload helper 失败测试**

在 `packages/frontend/src/pages/SessionWorkspacePage.test.tsx` 新增 import：

```ts
  applyProjectSwitcherProjectPatch,
  removeProjectFromWorkspacePayload,
  updateActiveSessionPinnedAt,
```

新增测试：

```ts
test('applyProjectSwitcherProjectPatch updates project switcher and active project name', () => {
  const payload = createCommandPayload();
  const next = applyProjectSwitcherProjectPatch(payload, {
    id: 'project-1',
    name: 'openDeepSea',
    path: '/workspace/openclaw',
  });

  assert.equal(next.project.name, 'openDeepSea');
  assert.equal(next.projectSwitcher.projects[0]?.name, 'openDeepSea');
  assert.equal(next.activeSession.session.workspace_path, payload.activeSession.session.workspace_path);
});

test('removeProjectFromWorkspacePayload removes project and its active sessions', () => {
  const payload = createCommandPayload();
  const next = removeProjectFromWorkspacePayload(payload, 'project-2');

  assert.equal(next.projectSwitcher.projects.some((project) => project.id === 'project-2'), false);
  assert.equal(next.activeSessions.some((session) => session.project_id === 'project-2'), false);
});

test('updateActiveSessionPinnedAt updates active summaries and current session', () => {
  const payload = createCommandPayload();
  const next = updateActiveSessionPinnedAt(payload, 'session-1', 123);

  assert.equal(next.activeSession.session.pinned_at, 123);
  assert.equal(next.activeSessions.find((session) => session.id === 'session-1')?.pinned_at, 123);
});
```

- [ ] **Step 2: 运行页面测试确认失败**

Run:

```bash
node --import tsx --test packages/frontend/src/pages/SessionWorkspacePage.test.tsx
```

Expected: 测试编译失败，三个 helper 尚未导出。

- [ ] **Step 3: 实现 payload helper**

在 `SessionWorkspacePage.tsx` 中导出：

```ts
export function applyProjectSwitcherProjectPatch(
  payload: SessionWorkspacePayload,
  project: Pick<Project, 'id' | 'name' | 'path'>,
): SessionWorkspacePayload {
  return {
    ...payload,
    project: payload.project.id === project.id
      ? { ...payload.project, name: project.name, path: project.path }
      : payload.project,
    projectSwitcher: {
      ...payload.projectSwitcher,
      projects: payload.projectSwitcher.projects.map((item) =>
        item.id === project.id ? { ...item, name: project.name, path: project.path } : item
      ),
    },
    activeSessions: payload.activeSessions.map((session) =>
      session.project_id === project.id
        ? { ...session, project_name: project.name, project_path: project.path }
        : session
    ),
  };
}

export function removeProjectFromWorkspacePayload(
  payload: SessionWorkspacePayload,
  projectId: string,
): SessionWorkspacePayload {
  return {
    ...payload,
    projectSwitcher: {
      ...payload.projectSwitcher,
      projects: payload.projectSwitcher.projects.filter((project) => project.id !== projectId),
    },
    activeSessions: payload.activeSessions.filter((session) => session.project_id !== projectId),
  };
}

export function updateActiveSessionPinnedAt(
  payload: SessionWorkspacePayload,
  sessionId: string,
  pinnedAt: number | null,
): SessionWorkspacePayload {
  return {
    ...payload,
    activeSession: payload.activeSession.session.id === sessionId
      ? {
        ...payload.activeSession,
        session: { ...payload.activeSession.session, pinned_at: pinnedAt },
      }
      : payload.activeSession,
    activeSessions: upsertActiveSessionSummary(
      payload.activeSessions,
      {
        ...(payload.activeSessions.find((session) => session.id === sessionId) ??
          projectSessionToActiveSummary({
            session: payload.activeSession.session,
            project: payload.project,
          })),
        pinned_at: pinnedAt,
      },
    ),
  };
}
```

- [ ] **Step 4: 接入 mutation 状态和 Dialog**

在 `SessionWorkspacePage.tsx` imports 中扩展：

```ts
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { Loader2, Trash2 } from 'lucide-react';
import { Dialog, DialogContent } from '../components/ui/Dialog';
import { Input, Label } from '../components/ui/Input';
```

在组件内添加：

```ts
const queryClient = useQueryClient();
const [renameProject, setRenameProject] = useState<SessionWorkspacePayload['projectSwitcher']['projects'][number] | null>(null);
const [removeProject, setRemoveProject] = useState<SessionWorkspacePayload['projectSwitcher']['projects'][number] | null>(null);
const [renameProjectName, setRenameProjectName] = useState('');
```

添加 mutations：

```ts
const renameProjectMutation = useMutation({
  mutationFn: (input: { projectId: string; name: string }) => api.updateProject(input.projectId, { name: input.name }),
  onSuccess: (project) => {
    queryClient.invalidateQueries({ queryKey: ['projects'] });
    setWorkspacePayload((current) => current ? applyProjectSwitcherProjectPatch(current, project) : current);
    setActiveSessions((current) => current?.map((session) =>
      session.project_id === project.id ? { ...session, project_name: project.name, project_path: project.path } : session
    ) ?? current);
    setRenameProject(null);
    setRenameProjectName('');
    toast.success('项目名称已更新');
  },
  onError: (error) => toast.error(error instanceof Error ? error.message : '项目名称更新失败'),
});

const removeProjectMutation = useMutation({
  mutationFn: (projectId: string) => api.deleteProject(projectId),
  onSuccess: (_result, projectId) => {
    queryClient.invalidateQueries({ queryKey: ['projects'] });
    setWorkspacePayload((current) => current ? removeProjectFromWorkspacePayload(current, projectId) : current);
    setActiveSessions((current) => current?.filter((session) => session.project_id !== projectId) ?? current);
    setRemoveProject(null);
    toast.success('项目已移除');
    if (projectId === workspacePayload.activeSession.session.project_id) navigate('/');
  },
  onError: (error) => {
    const message = error instanceof Error ? error.message : '项目移除失败';
    toast.error(message.includes('409') ? '项目仍有运行中的智能体或工作流，请先停止或等待完成。' : message);
  },
});

const reorderProjectsMutation = useMutation({
  mutationFn: (input: { ids: string[]; pinned: boolean }) => api.reorderProjects(input),
  onSuccess: (projects) => {
    queryClient.invalidateQueries({ queryKey: ['projects'] });
    setWorkspacePayload((current) => current ? {
      ...current,
      projectSwitcher: {
        ...current.projectSwitcher,
        projects: projects.map((project) => {
          const existing = current.projectSwitcher.projects.find((item) => item.id === project.id);
          return {
            id: project.id,
            name: project.name,
            path: project.path,
            active: existing?.active ?? project.id === current.project.id,
            recentSessions: existing?.recentSessions ?? [],
            created_at: project.created_at,
            pinned_at: project.pinned_at,
            sort_order: project.sort_order,
          };
        }),
      },
    } : current);
  },
  onError: (error) => toast.error(error instanceof Error ? error.message : '项目排序失败'),
});

const toggleSessionPinMutation = useMutation({
  mutationFn: (session: ActiveSessionSummary) =>
    api.updateSession(session.id, { pinned_at: session.pinned_at === null ? Date.now() : null }),
  onSuccess: (session) => {
    setWorkspacePayload((current) => current ? updateActiveSessionPinnedAt(current, session.id, session.pinned_at) : current);
    setActiveSessions((current) => current
      ? upsertActiveSessionSummary(current, {
        id: session.id,
        project_id: session.project_id,
        project_name: current.find((item) => item.id === session.id)?.project_name ?? workspacePayload.project.name,
        project_path: current.find((item) => item.id === session.id)?.project_path ?? workspacePayload.project.path,
        title: session.title,
        status: session.status,
        phase: session.phase,
        provider: session.provider,
        model: session.model,
        pinned_at: session.pinned_at,
        updated_at: session.updated_at,
        unread_count: current.find((item) => item.id === session.id)?.unread_count ?? 0,
        active_run_count: current.find((item) => item.id === session.id)?.active_run_count ?? 0,
        latest_event_summary: current.find((item) => item.id === session.id)?.latest_event_summary ?? session.current_goal,
      })
      : current);
  },
  onError: (error) => toast.error(error instanceof Error ? error.message : '会话置顶更新失败'),
});
```

在返回 JSX 中 `SessionShell` 后增加两个 Dialog：

```tsx
<Dialog open={renameProject !== null} onOpenChange={(open) => {
  if (!open) {
    setRenameProject(null);
    setRenameProjectName('');
  }
}}>
  <DialogContent title="编辑项目名称" description={renameProject?.path}>
    <form
      onSubmit={(event) => {
        event.preventDefault();
        const name = renameProjectName.trim();
        if (!renameProject || !name) return;
        renameProjectMutation.mutate({ projectId: renameProject.id, name });
      }}
    >
      <Label htmlFor="session-project-rename">项目名称</Label>
      <Input
        id="session-project-rename"
        value={renameProjectName}
        onChange={(event) => setRenameProjectName(event.target.value)}
        autoFocus
      />
      <div className="mt-5 flex justify-end gap-2">
        <Button type="button" variant="ghost" onClick={() => setRenameProject(null)} disabled={renameProjectMutation.isPending}>
          取消
        </Button>
        <Button type="submit" disabled={renameProjectMutation.isPending || renameProjectName.trim().length === 0}>
          {renameProjectMutation.isPending ? <Loader2 className="h-3.5 w-3.5 animate-spin" /> : null}
          保存名称
        </Button>
      </div>
    </form>
  </DialogContent>
</Dialog>

<Dialog open={removeProject !== null} onOpenChange={(open) => {
  if (!open) setRemoveProject(null);
}}>
  <DialogContent
    title="移除项目"
    description={removeProject ? `将从 OpenDeepSea 中移除「${removeProject.name}」。不会删除本地项目文件夹。` : undefined}
  >
    <div className="rounded-lg border border-[var(--color-border)] bg-[var(--color-surface-raised)] px-3 py-2.5">
      <div className="text-[11px] font-medium text-[var(--color-fg-muted)]">本地项目文件夹</div>
      <div className="mt-1 break-all font-mono text-[12px] text-[var(--color-fg)]">{removeProject?.path}</div>
    </div>
    <div className="mt-5 flex justify-end gap-2">
      <Button type="button" variant="ghost" onClick={() => setRemoveProject(null)} disabled={removeProjectMutation.isPending}>
        取消
      </Button>
      <Button
        type="button"
        variant="danger"
        onClick={() => {
          if (removeProject) removeProjectMutation.mutate(removeProject.id);
        }}
        disabled={removeProjectMutation.isPending}
      >
        {removeProjectMutation.isPending ? <Loader2 className="h-3.5 w-3.5 animate-spin" /> : <Trash2 className="h-3.5 w-3.5" />}
        {removeProjectMutation.isPending ? '正在移除' : '移除'}
      </Button>
    </div>
  </DialogContent>
</Dialog>
```

- [ ] **Step 5: 给 `SessionShell` 接入回调**

在 `SessionShell` props 中加入：

```tsx
onRenameProject={(project) => {
  setRenameProject(project);
  setRenameProjectName(project.name);
}}
onRemoveProject={(project) => setRemoveProject(project)}
onReorderProjects={(input) => reorderProjectsMutation.mutate(input)}
onToggleSessionPin={(session) => toggleSessionPinMutation.mutate(session)}
```

- [ ] **Step 6: 验证页面测试通过**

Run:

```bash
node --import tsx --test packages/frontend/src/pages/SessionWorkspacePage.test.tsx
```

Expected: `SessionWorkspacePage.test.tsx` 通过。

- [ ] **Step 7: 提交 Task 3**

```bash
git add packages/frontend/src/pages/SessionWorkspacePage.tsx packages/frontend/src/pages/SessionWorkspacePage.test.tsx
git commit -m "feat(session): 接入项目栏真实操作"
```

## Task 4: 调整参考图风格 CSS 并做视觉验证

**Files:**
- Modify: `packages/frontend/src/session-ui/session-os.css`
- Modify: `packages/frontend/src/session-ui/SessionShell.test.tsx`

- [ ] **Step 1: 写 CSS 规则测试**

在 `SessionShell.test.tsx` 添加：

```ts
test('SessionShell project rail CSS hides actions until interaction and keeps compact rows', () => {
  assert.match(sessionOsCss, /\.deepsea-project-node__actions\s*\{[^}]*opacity:\s*0/s);
  assert.match(sessionOsCss, /\.deepsea-project-node:hover\s+\.deepsea-project-node__actions/s);
  assert.match(sessionOsCss, /\.deepsea-project-session-row-wrap:hover\s+\.deepsea-project-session-pin/s);
  assert.match(sessionOsCss, /\.deepsea-project-session-row\s*\{[^}]*min-height:\s*32px/s);
  assert.match(sessionOsCss, /\.deepsea-project-session-row__time\s*\{[^}]*min-width:\s*52px/s);
});
```

- [ ] **Step 2: 运行 CSS 测试确认失败**

Run:

```bash
node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx
```

Expected: CSS 规则测试失败，因为 actions 还没有默认透明，置顶按钮样式不存在，时间列没有固定宽度。

- [ ] **Step 3: 调整 CSS**

在 `session-os.css` 中替换项目树相关规则：

```css
.deepsea-history__list.deepsea-project-tree {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 18px 8px;
}
.deepsea-project-tree-heading {
  margin-bottom: 18px;
  padding: 0 14px;
}
.deepsea-project-tree-heading > span {
  color: rgba(67, 70, 84, 0.48);
  font-size: 18px;
  font-weight: 500;
  line-height: 24px;
}
.deepsea-project-node {
  min-height: 34px;
  border-radius: 9px;
}
.deepsea-project-node:hover,
.deepsea-project-node:focus-within {
  background: rgba(255, 255, 255, 0.28);
}
.deepsea-project-node__button {
  min-height: 34px;
  gap: 10px;
  padding: 4px 14px;
}
.deepsea-project-node__button svg {
  width: 20px;
  height: 20px;
  color: rgba(67, 70, 84, 0.66);
}
.deepsea-project-node__label strong {
  font-size: 17px;
  font-weight: 500;
  line-height: 24px;
}
.deepsea-project-node__actions {
  opacity: 0;
  pointer-events: none;
  transition: opacity 140ms ease-out;
}
.deepsea-project-node:hover .deepsea-project-node__actions,
.deepsea-project-node:focus-within .deepsea-project-node__actions,
.deepsea-project-node__actions:has(.deepsea-project-node__icon-button[aria-expanded="true"]) {
  opacity: 1;
  pointer-events: auto;
}
.deepsea-project-tree-section[data-dragging="true"] {
  opacity: 0.55;
}
.deepsea-project-tree-section[data-drop-target="true"] .deepsea-project-node {
  background: rgba(255, 255, 255, 0.48);
  box-shadow: inset 0 0 0 1px rgba(67, 70, 84, 0.16);
}
.deepsea-project-node__sessions {
  gap: 0;
  margin: 8px 0 8px 30px;
}
.deepsea-project-session-row-wrap {
  display: grid;
  grid-template-columns: 28px minmax(0, 1fr);
  align-items: center;
  min-width: 0;
}
.deepsea-project-session-pin {
  display: grid;
  width: 22px;
  height: 22px;
  place-items: center;
  border: 0;
  border-radius: 6px;
  background: transparent;
  color: rgba(67, 70, 84, 0.42);
  opacity: 0;
  padding: 0;
  transition: opacity 140ms ease-out, background 140ms ease-out, color 140ms ease-out;
}
.deepsea-project-session-pin svg {
  width: 14px;
  height: 14px;
}
.deepsea-project-session-row-wrap:hover .deepsea-project-session-pin,
.deepsea-project-session-pin:focus-visible,
.deepsea-project-session-pin[data-pinned="true"] {
  opacity: 1;
}
.deepsea-project-session-pin:hover,
.deepsea-project-session-pin[data-pinned="true"] {
  background: rgba(255, 255, 255, 0.42);
  color: rgba(67, 70, 84, 0.88);
}
.deepsea-project-session-row {
  min-height: 32px;
  border: 0;
  border-radius: 8px;
  padding: 5px 10px;
}
.deepsea-project-session-row[data-current="true"] {
  background: rgba(67, 70, 84, 0.08);
  box-shadow: none;
}
.deepsea-project-session-row__title {
  font-size: 15px;
  line-height: 20px;
}
.deepsea-project-session-row__time {
  min-width: 52px;
  color: rgba(67, 70, 84, 0.46);
  font-size: 14px;
  line-height: 18px;
  text-align: right;
}
```

- [ ] **Step 4: 验证 CSS 和前端定向测试**

Run:

```bash
node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx packages/frontend/src/pages/SessionWorkspacePage.test.tsx
```

Expected: 两个测试文件通过。

- [ ] **Step 5: 启动开发服务做浏览器检查**

如果没有运行中的服务，启动：

```bash
npm run dev
```

Expected: 后端和前端启动，前端通常为 `http://localhost:5173`。

打开真实会话页，验证：

- 当前项目展开，其他项目折叠。
- 项目名前只有文件夹图标，没有折叠 icon。
- 项目更多按钮只在 hover/focus/menu open 时显示。
- 项目菜单只有「编辑名称」「移除」。
- 会话 hover 左侧出现置顶按钮。
- 会话标题与时间列不重叠。
- 拖拽项目后刷新仍保持顺序。

如果使用 Playwright 截图，保存到：

```text
output/playwright/session-sidebar-project-rail-final.png
```

- [ ] **Step 6: 提交 Task 4**

```bash
git add packages/frontend/src/session-ui/session-os.css packages/frontend/src/session-ui/SessionShell.test.tsx
git commit -m "style(session): 调整左侧项目栏密度"
```

## Task 5: 最终回归、计划勾选和收尾提交

**Files:**
- Modify: `docs/superpowers/plans/2026-06-08-会话左侧项目栏交互优化实施计划.md`

- [ ] **Step 1: 运行最终测试套件**

Run:

```bash
node --import tsx --test packages/backend/src/session.routes.test.ts packages/frontend/src/lib/api.test.ts packages/frontend/src/session-ui/SessionShell.test.tsx packages/frontend/src/pages/SessionWorkspacePage.test.tsx
```

Expected: 全部通过。

- [ ] **Step 2: 运行完整构建**

Run:

```bash
npm run build
```

Expected: backend TypeScript 编译成功，frontend Vite build 成功。Vite large chunk warning 可接受，不能有 TypeScript 或构建失败。

- [ ] **Step 3: 运行 diff 检查**

Run:

```bash
git diff --check
```

Expected: 无输出，退出码 0。

- [ ] **Step 4: 勾选计划执行记录**

执行完成后，将本计划中已完成步骤从 `- [ ]` 改为 `- [x]`。不要勾选未执行命令。

- [ ] **Step 5: 提交计划勾选**

```bash
git add docs/superpowers/plans/2026-06-08-会话左侧项目栏交互优化实施计划.md
git commit -m "docs(session): 更新左侧项目栏实施记录"
```

- [ ] **Step 6: 输出最终交付摘要**

最终回复包含：

- 已实现的交互摘要。
- 已运行的验证命令和结果。
- 截图路径（如果生成）。
- 提交列表。
