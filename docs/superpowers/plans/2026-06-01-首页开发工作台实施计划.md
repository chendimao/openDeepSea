# 首页开发工作台 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将“开发”首页重构为左侧项目列表、右侧群聊 tab 与群聊主功能区一体化的开发工作台，并为项目和群聊提供置顶与同层拖动排序持久化。

**Architecture:** 后端先补齐项目和群聊排序字段、仓储排序规则与批量排序接口；前端再新增开发工作台页面并复用现有群聊主功能。`RoomPage` 的重逻辑应抽为 `RoomWorkbench`，路由 `/`、`/projects/:projectId`、`/projects/:projectId/rooms/:roomId` 统一进入同一工作台。

**Tech Stack:** Node.js、Express、better-sqlite3、React 18、TypeScript、React Router、TanStack Query、lucide-react、原生 HTML drag/drop。

---

## File Structure

- Modify: `packages/backend/src/db.ts`
  - 为 `projects` 增加 `pinned_at`、`sort_order` 迁移。
  - 为 `rooms` 增加 `sort_order` 迁移。
  - 增加排序索引。
- Modify: `packages/backend/src/types.ts`
  - 扩展 `Project`、`Room` 类型字段。
- Modify: `packages/backend/src/repos/projects.ts`
  - 改造 `list()` 排序。
  - 扩展 `update()` 支持 `pinned_at`。
  - 新增 `reorder(ids, pinned)`。
- Modify: `packages/backend/src/repos/rooms.ts`
  - 改造 `listByProject()` 排序。
  - 扩展 `update()` 支持 `sort_order`。
  - 新增 `reorder(projectId, ids, pinned)`。
- Modify: `packages/backend/src/routes.ts`
  - 扩展项目和房间 PATCH schema。
  - 新增项目/群聊批量排序接口。
- Modify: `packages/backend/src/repos/rooms.test.ts`
  - 覆盖群聊置顶、同层排序和创建时间兜底。
- Create: `packages/backend/src/repos/projects.test.ts`
  - 覆盖项目置顶、同层排序和创建时间兜底。
- Modify: `packages/backend/src/projects.routes.test.ts`
  - 覆盖项目 PATCH 与批量排序接口。
- Create: `packages/backend/src/rooms.routes.test.ts`
  - 覆盖群聊 PATCH 与批量排序接口。
- Modify: `packages/frontend/src/lib/types.ts`
  - 扩展前端 `Project`、`Room` 类型。
- Modify: `packages/frontend/src/lib/api.ts`
  - 增加项目 update、项目 reorder、群聊 reorder API。
  - 扩展 `updateRoom()` 入参。
- Create: `packages/frontend/src/lib/sortableItems.ts`
  - 前端统一排序与同层拖动重排辅助函数。
- Create: `packages/frontend/src/lib/sortableItems.test.ts`
  - 覆盖排序和跨层拖动阻止逻辑。
- Create: `packages/frontend/src/components/ProjectRail.tsx`
  - 左侧项目列表、搜索、置顶、删除、同层拖动排序。
- Modify: `packages/frontend/src/components/RoomTabsBar.tsx`
  - 支持同层拖动排序。
  - 使用统一排序 helper。
- Create: `packages/frontend/src/components/room/RoomWorkbench.tsx`
  - 从 `RoomPage` 抽出的群聊主功能区。
- Modify: `packages/frontend/src/pages/RoomPage.tsx`
  - 缩减为兼容包装或直接移交给 `RoomWorkbench`。
- Create: `packages/frontend/src/pages/DevelopmentWorkspacePage.tsx`
  - 新开发首页工作台路由页。
- Modify: `packages/frontend/src/main.tsx`
  - 将 `/`、`/projects/:projectId`、`/projects/:projectId/rooms/:roomId` 指向 `DevelopmentWorkspacePage`。
- Modify: `packages/frontend/src/components/AppShell.tsx`
  - 让“开发”菜单在项目和群聊路由下保持 active。
- Modify: `packages/frontend/src/index.css`
  - 新增开发工作台、项目列表、排序拖动状态样式。
- Modify: `packages/frontend/src/pages/RoomPage.test.ts`
  - 调整已有断言以适配 `RoomWorkbench` 抽离。

---

### Task 1: Backend Sorting Schema And Repos

**Files:**
- Modify: `packages/backend/src/db.ts`
- Modify: `packages/backend/src/types.ts`
- Modify: `packages/backend/src/repos/projects.ts`
- Modify: `packages/backend/src/repos/rooms.ts`
- Create: `packages/backend/src/repos/projects.test.ts`
- Modify: `packages/backend/src/repos/rooms.test.ts`

- [ ] **Step 1: Add failing project repo tests**

Create `packages/backend/src/repos/projects.test.ts`:

```ts
import assert from 'node:assert/strict';
import { mkdtempSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import test from 'node:test';

process.env.OPENCLAW_ROOM_DB = join(mkdtempSync(join(tmpdir(), 'openclaw-room-projects-')), 'test.db');

const { projectRepo } = await import('./projects.js');

function createProject(name: string) {
  return projectRepo.create({
    name,
    path: mkdtempSync(join(tmpdir(), `${name}-`)),
  });
}

test('projectRepo list puts pinned projects first and uses sort_order within each layer', () => {
  const normalA = createProject('normal-a');
  const normalB = createProject('normal-b');
  const pinnedA = createProject('pinned-a');
  const pinnedB = createProject('pinned-b');

  projectRepo.update(normalA.id, { sort_order: 20 });
  projectRepo.update(normalB.id, { sort_order: 10 });
  projectRepo.update(pinnedA.id, { pinned_at: 100, sort_order: 2 });
  projectRepo.update(pinnedB.id, { pinned_at: 200, sort_order: 1 });

  assert.deepEqual(projectRepo.list().map((project) => project.id), [
    pinnedB.id,
    pinnedA.id,
    normalB.id,
    normalA.id,
  ]);
});

test('projectRepo reorder only updates projects from the requested layer', () => {
  const normalA = createProject('normal-layer-a');
  const normalB = createProject('normal-layer-b');
  const pinned = createProject('pinned-layer');
  projectRepo.update(pinned.id, { pinned_at: 100 });

  assert.throws(() => projectRepo.reorder([normalA.id, pinned.id], false), /project layer mismatch/);

  projectRepo.reorder([normalB.id, normalA.id], false);
  assert.deepEqual(projectRepo.list().filter((project) => !project.pinned_at).map((project) => project.id), [
    normalB.id,
    normalA.id,
  ]);
});
```

- [ ] **Step 2: Add failing room repo tests**

Append to `packages/backend/src/repos/rooms.test.ts`:

```ts
test('roomRepo listByProject uses pinned layer and sort_order before created_at fallback', () => {
  const project = createProject('rooms-manual-sort');
  const normalA = roomRepo.create({ project_id: project.id, name: '普通 A', ensureDefaultPlanner: false });
  const normalB = roomRepo.create({ project_id: project.id, name: '普通 B', ensureDefaultPlanner: false });
  const pinnedA = roomRepo.create({ project_id: project.id, name: '置顶 A', ensureDefaultPlanner: false });
  const pinnedB = roomRepo.create({ project_id: project.id, name: '置顶 B', ensureDefaultPlanner: false });

  roomRepo.update(normalA.id, { sort_order: 20 });
  roomRepo.update(normalB.id, { sort_order: 10 });
  roomRepo.update(pinnedA.id, { pinned_at: 100, sort_order: 2 });
  roomRepo.update(pinnedB.id, { pinned_at: 200, sort_order: 1 });

  assert.deepEqual(roomRepo.listByProject(project.id).map((room) => room.id), [
    pinnedB.id,
    pinnedA.id,
    normalB.id,
    normalA.id,
  ]);
});

test('roomRepo reorder rejects rooms from another project or layer', () => {
  const project = createProject('rooms-reorder');
  const otherProject = createProject('rooms-reorder-other');
  const normalA = roomRepo.create({ project_id: project.id, name: '普通 A', ensureDefaultPlanner: false });
  const normalB = roomRepo.create({ project_id: project.id, name: '普通 B', ensureDefaultPlanner: false });
  const pinned = roomRepo.create({ project_id: project.id, name: '置顶', ensureDefaultPlanner: false });
  const otherRoom = roomRepo.create({ project_id: otherProject.id, name: '其他项目', ensureDefaultPlanner: false });
  roomRepo.update(pinned.id, { pinned_at: 100 });

  assert.throws(() => roomRepo.reorder(project.id, [normalA.id, pinned.id], false), /room layer mismatch/);
  assert.throws(() => roomRepo.reorder(project.id, [normalA.id, otherRoom.id], false), /room project mismatch/);

  roomRepo.reorder(project.id, [normalB.id, normalA.id], false);
  assert.deepEqual(roomRepo.listByProject(project.id).filter((room) => !room.pinned_at).map((room) => room.id), [
    normalB.id,
    normalA.id,
  ]);
});
```

- [ ] **Step 3: Run repo tests and verify failure**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/repos/projects.test.ts src/repos/rooms.test.ts
```

Expected: fail with missing `sort_order`, `pinned_at` on projects, and missing `reorder` methods.

- [ ] **Step 4: Add database fields and indexes**

In `packages/backend/src/db.ts`, update `CREATE TABLE IF NOT EXISTS projects`:

```sql
  pinned_at INTEGER,
  sort_order INTEGER,
```

Add migration near the existing room column migrations:

```ts
const projectColumns = db.prepare('PRAGMA table_info(projects)').all() as Array<{ name: string }>;
const projectColumnNames = new Set(projectColumns.map((column) => column.name));
if (!projectColumnNames.has('pinned_at')) {
  db.exec('ALTER TABLE projects ADD COLUMN pinned_at INTEGER');
}
if (!projectColumnNames.has('sort_order')) {
  db.exec('ALTER TABLE projects ADD COLUMN sort_order INTEGER');
}
if (!roomColumnNames.has('sort_order')) {
  db.exec('ALTER TABLE rooms ADD COLUMN sort_order INTEGER');
}
db.exec('CREATE INDEX IF NOT EXISTS idx_projects_sort ON projects(pinned_at DESC, sort_order ASC, created_at DESC)');
db.exec('CREATE INDEX IF NOT EXISTS idx_rooms_project_sort ON rooms(project_id, pinned_at DESC, sort_order ASC, created_at DESC)');
```

If `roomColumnNames` is declared later than this block, place the room `sort_order` migration next to the existing `last_opened_at` and `pinned_at` room migrations.

- [ ] **Step 5: Extend backend types**

In `packages/backend/src/types.ts`, add to `Project`:

```ts
pinned_at: number | null;
sort_order: number | null;
```

Add to `Room`:

```ts
sort_order: number | null;
```

- [ ] **Step 6: Implement project sorting repo methods**

In `packages/backend/src/repos/projects.ts`, replace `list()` ordering:

```ts
list(): Project[] {
  return db
    .prepare(`
      SELECT * FROM projects
      ORDER BY
        pinned_at IS NULL ASC,
        sort_order IS NULL ASC,
        sort_order ASC,
        created_at DESC
    `)
    .all() as Project[];
},
```

Change `update()` signature and implementation:

```ts
update(
  id: string,
  patch: Partial<Pick<Project, 'name' | 'description'>> & {
    pinned_at?: number | null;
    sort_order?: number | null;
  },
): Project | undefined {
  const existing = this.get(id);
  if (!existing) return undefined;

  const setClauses: string[] = [];
  const values: Array<string | number | null> = [];

  if (patch.name !== undefined) {
    setClauses.push('name = ?');
    values.push(patch.name);
  }
  if (patch.description !== undefined) {
    setClauses.push('description = ?');
    values.push(patch.description);
  }
  if (patch.pinned_at !== undefined) {
    setClauses.push('pinned_at = ?');
    values.push(patch.pinned_at);
  }
  if (patch.sort_order !== undefined) {
    setClauses.push('sort_order = ?');
    values.push(patch.sort_order);
  }

  if (setClauses.length === 0) return existing;
  setClauses.push('updated_at = ?');
  values.push(now());

  db.prepare(`UPDATE projects SET ${setClauses.join(', ')} WHERE id = ?`).run(...values, id);
  return this.get(id);
},
```

Add `reorder()`:

```ts
reorder(ids: string[], pinned: boolean): Project[] {
  const uniqueIds = [...new Set(ids)];
  if (uniqueIds.length !== ids.length) throw new Error('duplicate project ids');
  if (uniqueIds.length === 0) return this.list();

  const placeholders = uniqueIds.map(() => '?').join(', ');
  const rows = db.prepare(`SELECT id, pinned_at FROM projects WHERE id IN (${placeholders})`).all(...uniqueIds) as Array<{
    id: string;
    pinned_at: number | null;
  }>;
  if (rows.length !== uniqueIds.length) throw new Error('project not found');
  for (const row of rows) {
    if (Boolean(row.pinned_at) !== pinned) throw new Error('project layer mismatch');
  }

  const updateOrder = db.transaction((orderedIds: string[]) => {
    orderedIds.forEach((id, index) => {
      db.prepare('UPDATE projects SET sort_order = ?, updated_at = ? WHERE id = ?').run(index + 1, now(), id);
    });
  });
  updateOrder(uniqueIds);
  return this.list();
},
```

- [ ] **Step 7: Implement room sorting repo methods**

In `packages/backend/src/repos/rooms.ts`, change `listByProject()` ordering:

```ts
ORDER BY
  pinned_at IS NULL ASC,
  sort_order IS NULL ASC,
  sort_order ASC,
  created_at DESC
```

Extend `create()` insert columns:

```ts
`INSERT INTO rooms (id, project_id, name, description, created_at, last_opened_at, pinned_at, sort_order)
 VALUES (?, ?, ?, ?, ?, ?, NULL, NULL)`
```

Extend `update()` patch:

```ts
sort_order?: number | null;
```

and add:

```ts
if (patch.sort_order !== undefined) {
  setClauses.push('sort_order = ?');
  values.push(patch.sort_order);
}
```

Add `reorder()`:

```ts
reorder(projectId: string, ids: string[], pinned: boolean): Room[] {
  const uniqueIds = [...new Set(ids)];
  if (uniqueIds.length !== ids.length) throw new Error('duplicate room ids');
  if (uniqueIds.length === 0) return this.listByProject(projectId);

  const placeholders = uniqueIds.map(() => '?').join(', ');
  const rows = db.prepare(`SELECT id, project_id, pinned_at FROM rooms WHERE id IN (${placeholders})`).all(...uniqueIds) as Array<{
    id: string;
    project_id: string;
    pinned_at: number | null;
  }>;
  if (rows.length !== uniqueIds.length) throw new Error('room not found');
  for (const row of rows) {
    if (row.project_id !== projectId) throw new Error('room project mismatch');
    if (Boolean(row.pinned_at) !== pinned) throw new Error('room layer mismatch');
  }

  const updateOrder = db.transaction((orderedIds: string[]) => {
    orderedIds.forEach((id, index) => {
      db.prepare('UPDATE rooms SET sort_order = ? WHERE id = ?').run(index + 1, id);
    });
  });
  updateOrder(uniqueIds);
  return this.listByProject(projectId);
},
```

- [ ] **Step 8: Run repo tests**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/repos/projects.test.ts src/repos/rooms.test.ts
```

Expected: pass.

- [ ] **Step 9: Commit backend repo sorting**

```bash
git add packages/backend/src/db.ts packages/backend/src/types.ts packages/backend/src/repos/projects.ts packages/backend/src/repos/rooms.ts packages/backend/src/repos/projects.test.ts packages/backend/src/repos/rooms.test.ts
git commit -m "feat(backend): 持久化项目和群聊排序"
```

---

### Task 2: Backend Sorting Routes

**Files:**
- Modify: `packages/backend/src/routes.ts`
- Modify: `packages/backend/src/projects.routes.test.ts`
- Create: `packages/backend/src/rooms.routes.test.ts`

- [ ] **Step 1: Add failing route tests**

Append to `packages/backend/src/projects.routes.test.ts`:

```ts
test('patch project updates pinned_at', async () => {
  const { project } = createProjectFixture('patch-pin');

  const res = await request(`/api/projects/${project.id}`, {
    method: 'PATCH',
    body: JSON.stringify({ pinned_at: 123 }),
  });

  assert.equal(res.status, 200);
  const body = await res.json() as { pinned_at: number };
  assert.equal(body.pinned_at, 123);
});

test('reorder projects persists normal layer order', async () => {
  const first = createProjectFixture('reorder-project-first').project;
  const second = createProjectFixture('reorder-project-second').project;

  const res = await request('/api/projects/reorder', {
    method: 'PUT',
    body: JSON.stringify({ ids: [second.id, first.id], pinned: false }),
  });

  assert.equal(res.status, 200);
  const body = await res.json() as Array<{ id: string }>;
  assert.deepEqual(body.filter((project) => project.id === first.id || project.id === second.id).map((project) => project.id), [
    second.id,
    first.id,
  ]);
});
```

Create `packages/backend/src/rooms.routes.test.ts`:

```ts
import assert from 'node:assert/strict';
import { mkdtempSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import test from 'node:test';

process.env.OPENCLAW_ROOM_DB = join(mkdtempSync(join(tmpdir(), 'openclaw-room-rooms-routes-')), 'test.db');

const { projectRepo } = await import('./repos/projects.js');
const { roomRepo } = await import('./repos/rooms.js');
const { router } = await import('./routes.js');
const express = (await import('express')).default;

const app = express();
app.use(express.json());
app.use('/api', router);

async function request(path: string, init: RequestInit = {}): Promise<Response> {
  const server = app.listen(0);
  try {
    const address = server.address();
    assert(address && typeof address === 'object');
    return await fetch(`http://127.0.0.1:${address.port}${path}`, {
      ...init,
      headers: { 'Content-Type': 'application/json', ...(init.headers ?? {}) },
    });
  } finally {
    await new Promise<void>((resolve) => server.close(() => resolve()));
  }
}

function createProjectFixture(name: string) {
  const project = projectRepo.create({
    name,
    path: mkdtempSync(join(tmpdir(), `openclaw-room-room-route-${name}-`)),
  });
  const room = roomRepo.create({ project_id: project.id, name: `${name} Room`, ensureDefaultPlanner: false });
  return { project, room };
}

test('patch room updates sort_order', async () => {
  const { room } = createProjectFixture('patch-sort');

  const res = await request(`/api/rooms/${room.id}`, {
    method: 'PATCH',
    body: JSON.stringify({ sort_order: 7 }),
  });

  assert.equal(res.status, 200);
  const body = await res.json() as { sort_order: number };
  assert.equal(body.sort_order, 7);
});

test('reorder rooms persists normal layer order', async () => {
  const { project, room: first } = createProjectFixture('reorder-room-first');
  const second = roomRepo.create({ project_id: project.id, name: 'second room', ensureDefaultPlanner: false });

  const res = await request(`/api/projects/${project.id}/rooms/reorder`, {
    method: 'PUT',
    body: JSON.stringify({ ids: [second.id, first.id], pinned: false }),
  });

  assert.equal(res.status, 200);
  const body = await res.json() as Array<{ id: string }>;
  assert.deepEqual(body.map((room) => room.id), [second.id, first.id]);
});
```

- [ ] **Step 2: Run route tests and verify failure**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/projects.routes.test.ts src/rooms.routes.test.ts
```

Expected: fail with unsupported `pinned_at`, `sort_order`, and missing reorder routes.

- [ ] **Step 3: Implement project route schema and reorder route**

In `packages/backend/src/routes.ts`, change project patch schema:

```ts
const schema = z.object({
  name: z.string().min(1).optional(),
  description: z.string().nullable().optional(),
  pinned_at: z.number().int().nullable().optional(),
  sort_order: z.number().int().nullable().optional(),
}).strict();
```

Add before `router.get('/projects/:id', ...)`:

```ts
router.put('/projects/reorder', (req, res) => {
  const schema = z.object({
    ids: z.array(z.string().min(1)),
    pinned: z.boolean(),
  }).strict();
  const parsed = schema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  try {
    const projects = projectRepo.reorder(parsed.data.ids, parsed.data.pinned);
    res.json(projects.map((project) => ({ ...project, stats: projectRepo.stats(project.id) })));
  } catch (err) {
    const message = (err as Error).message;
    if (message === 'project not found') return res.status(404).json({ error: message });
    if (message === 'project layer mismatch' || message === 'duplicate project ids') {
      return res.status(400).json({ error: message });
    }
    throw err;
  }
});
```

- [ ] **Step 4: Implement room route schema and reorder route**

In `packages/backend/src/routes.ts`, extend room patch schema:

```ts
sort_order: z.number().int().nullable().optional(),
```

Add near room routes:

```ts
router.put('/projects/:projectId/rooms/reorder', (req, res) => {
  const schema = z.object({
    ids: z.array(z.string().min(1)),
    pinned: z.boolean(),
  }).strict();
  const parsed = schema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  if (!projectRepo.get(req.params.projectId)) return res.status(404).json({ error: 'project not found' });
  try {
    res.json(roomRepo.reorder(req.params.projectId, parsed.data.ids, parsed.data.pinned));
  } catch (err) {
    const message = (err as Error).message;
    if (message === 'room not found') return res.status(404).json({ error: message });
    if (
      message === 'room project mismatch' ||
      message === 'room layer mismatch' ||
      message === 'duplicate room ids'
    ) {
      return res.status(400).json({ error: message });
    }
    throw err;
  }
});
```

- [ ] **Step 5: Run route tests**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/projects.routes.test.ts src/rooms.routes.test.ts
```

Expected: pass.

- [ ] **Step 6: Commit backend routes**

```bash
git add packages/backend/src/routes.ts packages/backend/src/projects.routes.test.ts packages/backend/src/rooms.routes.test.ts
git commit -m "feat(backend): 添加排序接口"
```

---

### Task 3: Frontend API And Sorting Helpers

**Files:**
- Modify: `packages/frontend/src/lib/types.ts`
- Modify: `packages/frontend/src/lib/api.ts`
- Create: `packages/frontend/src/lib/sortableItems.ts`
- Create: `packages/frontend/src/lib/sortableItems.test.ts`

- [ ] **Step 1: Add sorting helper tests**

Create `packages/frontend/src/lib/sortableItems.test.ts`:

```ts
import assert from 'node:assert/strict';
import test from 'node:test';
import { reorderWithinLayer, sortPinnedItems } from './sortableItems';

type Item = {
  id: string;
  created_at: number;
  pinned_at?: number | null;
  sort_order?: number | null;
};

test('sortPinnedItems puts pinned first and sorts each layer by sort_order then created_at desc', () => {
  const items: Item[] = [
    { id: 'normal-old', created_at: 1, sort_order: null },
    { id: 'normal-new', created_at: 4, sort_order: null },
    { id: 'pinned-second', created_at: 3, pinned_at: 10, sort_order: 2 },
    { id: 'pinned-first', created_at: 2, pinned_at: 20, sort_order: 1 },
  ];

  assert.deepEqual(sortPinnedItems(items).map((item) => item.id), [
    'pinned-first',
    'pinned-second',
    'normal-new',
    'normal-old',
  ]);
});

test('reorderWithinLayer reorders only matching pinned layer', () => {
  const items: Item[] = [
    { id: 'pinned', created_at: 1, pinned_at: 1 },
    { id: 'a', created_at: 2 },
    { id: 'b', created_at: 3 },
  ];

  assert.deepEqual(reorderWithinLayer(items, 'a', 'b').map((item) => item.id), ['pinned', 'b', 'a']);
  assert.deepEqual(reorderWithinLayer(items, 'pinned', 'a').map((item) => item.id), ['pinned', 'b', 'a']);
});
```

- [ ] **Step 2: Run helper test and verify failure**

Run:

```bash
node --import tsx --test packages/frontend/src/lib/sortableItems.test.ts
```

Expected: fail because `sortableItems.ts` does not exist.

- [ ] **Step 3: Extend frontend types and API**

In `packages/frontend/src/lib/types.ts`, add to `Project`:

```ts
pinned_at?: number | null;
sort_order?: number | null;
```

Add to `Room`:

```ts
sort_order?: number | null;
```

In `packages/frontend/src/lib/api.ts`, add:

```ts
updateProject: (
  id: string,
  input: { name?: string; description?: string | null; pinned_at?: number | null; sort_order?: number | null },
) =>
  request<Project>(`/projects/${id}`, {
    method: 'PATCH',
    body: JSON.stringify(input),
  }),
reorderProjects: (input: { ids: string[]; pinned: boolean }) =>
  request<Project[]>(`/projects/reorder`, {
    method: 'PUT',
    body: JSON.stringify(input),
  }),
reorderRooms: (projectId: string, input: { ids: string[]; pinned: boolean }) =>
  request<Room[]>(`/projects/${projectId}/rooms/reorder`, {
    method: 'PUT',
    body: JSON.stringify(input),
  }),
```

Extend `updateRoom()` input:

```ts
input: { name?: string; last_opened_at?: number | null; pinned_at?: number | null; sort_order?: number | null },
```

- [ ] **Step 4: Implement sorting helper**

Create `packages/frontend/src/lib/sortableItems.ts`:

```ts
export type SortablePinnedItem = {
  id: string;
  created_at: number;
  pinned_at?: number | null;
  sort_order?: number | null;
};

export function isPinnedItem(item: SortablePinnedItem): boolean {
  return Boolean(item.pinned_at);
}

export function sortPinnedItems<T extends SortablePinnedItem>(items: T[]): T[] {
  return [...items].sort((a, b) => {
    const aPinned = isPinnedItem(a);
    const bPinned = isPinnedItem(b);
    if (aPinned !== bPinned) return aPinned ? -1 : 1;

    const aOrder = a.sort_order ?? null;
    const bOrder = b.sort_order ?? null;
    if (aOrder !== null && bOrder !== null && aOrder !== bOrder) return aOrder - bOrder;
    if (aOrder !== null) return -1;
    if (bOrder !== null) return 1;
    return b.created_at - a.created_at;
  });
}

export function reorderWithinLayer<T extends SortablePinnedItem>(
  items: T[],
  activeId: string,
  overId: string,
): T[] {
  const sorted = sortPinnedItems(items);
  const active = sorted.find((item) => item.id === activeId);
  const over = sorted.find((item) => item.id === overId);
  if (!active || !over) return sorted;
  if (isPinnedItem(active) !== isPinnedItem(over)) return sorted;

  const pinned = isPinnedItem(active);
  const layer = sorted.filter((item) => isPinnedItem(item) === pinned);
  const otherLayer = sorted.filter((item) => isPinnedItem(item) !== pinned);
  const from = layer.findIndex((item) => item.id === activeId);
  const to = layer.findIndex((item) => item.id === overId);
  if (from < 0 || to < 0 || from === to) return sorted;

  const nextLayer = [...layer];
  const [moved] = nextLayer.splice(from, 1);
  nextLayer.splice(to, 0, moved);
  return pinned ? [...nextLayer, ...otherLayer] : [...otherLayer, ...nextLayer];
}

export function layerIds<T extends SortablePinnedItem>(items: T[], pinned: boolean): string[] {
  return sortPinnedItems(items)
    .filter((item) => isPinnedItem(item) === pinned)
    .map((item) => item.id);
}
```

- [ ] **Step 5: Run helper test**

Run:

```bash
node --import tsx --test packages/frontend/src/lib/sortableItems.test.ts
```

Expected: pass.

- [ ] **Step 6: Commit frontend data layer**

```bash
git add packages/frontend/src/lib/types.ts packages/frontend/src/lib/api.ts packages/frontend/src/lib/sortableItems.ts packages/frontend/src/lib/sortableItems.test.ts
git commit -m "feat(frontend): 添加排序数据辅助"
```

---

### Task 4: Extract RoomWorkbench

**Files:**
- Create: `packages/frontend/src/components/room/RoomWorkbench.tsx`
- Modify: `packages/frontend/src/pages/RoomPage.tsx`
- Modify: `packages/frontend/src/pages/RoomPage.test.ts`

- [ ] **Step 1: Identify RoomPage boundaries before editing**

Read:

```bash
sed -n '1,620p' packages/frontend/src/pages/RoomPage.tsx
sed -n '620,1240p' packages/frontend/src/pages/RoomPage.tsx
```

Confirm these responsibilities:

- URL params and route wrapper belong in `RoomPage` or future `DevelopmentWorkspacePage`.
- Query, WebSocket, task state, chat state, render grid and composer belong in `RoomWorkbench`.
- `RoomTabsBar` should be removed from extracted workbench because the new page owns the top-level tab bar.

- [ ] **Step 2: Create RoomWorkbench component by moving RoomPage internals**

Create `packages/frontend/src/components/room/RoomWorkbench.tsx` with:

```tsx
import { useCallback, useEffect, useMemo, useRef, useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { Bot, ClipboardList, GitBranch } from 'lucide-react';
import type { LucideIcon } from 'lucide-react';
import { toast } from 'sonner';
import { api } from '../../lib/api';
import { roomSocket, type WsServerEvent } from '../../lib/ws';
import { parseMessageMetadata } from '../../lib/messageMetadata';
import type {
  Agent,
  AgentRun,
  Message,
  Room,
  RoomAgent,
  Task,
  MessageLayer,
  WorkflowRun,
} from '../../lib/types';
import { useI18n } from '../../lib/i18n';
import { recordRecentRoomVisit } from '../../lib/recentRooms';
import { cn } from '../../lib/utils';
```

Then move the current `RoomPage` implementation after `const { projectId = '', roomId = '' } = useParams();` into:

```tsx
export function RoomWorkbench({
  projectId,
  roomId,
}: {
  projectId: string;
  roomId: string;
}): JSX.Element {
  // moved RoomPage query/state/render logic
}
```

Inside the moved JSX, remove the outer `RoomTabsBar` block. The returned root should start with:

```tsx
return (
  <div className="workspace-root" data-testid="room-workbench">
    <div className={cn('workspace-grid task-os-grid', showMemoryPanel && 'has-inspector')}>
      ...
    </div>
  </div>
);
```

Keep helper functions from `RoomPage` in `RoomWorkbench.tsx` if they are used only by the workbench.

- [ ] **Step 3: Reduce RoomPage to compatibility wrapper**

In `packages/frontend/src/pages/RoomPage.tsx`, replace the component with:

```tsx
import { useParams } from 'react-router-dom';
import { RoomWorkbench } from '../components/room/RoomWorkbench';

export function RoomPage(): JSX.Element {
  const { projectId = '', roomId = '' } = useParams();
  return <RoomWorkbench projectId={projectId} roomId={roomId} />;
}
```

Delete imports no longer needed from `RoomPage.tsx`.

- [ ] **Step 4: Adjust RoomPage test selector**

In `packages/frontend/src/pages/RoomPage.test.ts`, replace assertions for `data-testid="room-page"` with `data-testid="room-workbench"` when the test renders `RoomPage`.

- [ ] **Step 5: Run targeted frontend tests**

Run:

```bash
node --import tsx --test packages/frontend/src/pages/RoomPage.test.ts packages/frontend/src/pages/roomPageLogic.test.ts
```

Expected: pass.

- [ ] **Step 6: Commit RoomWorkbench extraction**

```bash
git add packages/frontend/src/components/room/RoomWorkbench.tsx packages/frontend/src/pages/RoomPage.tsx packages/frontend/src/pages/RoomPage.test.ts
git commit -m "refactor(frontend): 抽离群聊工作区"
```

---

### Task 5: Development Workspace Page And Project Rail

**Files:**
- Create: `packages/frontend/src/components/ProjectRail.tsx`
- Create: `packages/frontend/src/pages/DevelopmentWorkspacePage.tsx`
- Modify: `packages/frontend/src/main.tsx`
- Modify: `packages/frontend/src/components/AppShell.tsx`
- Modify: `packages/frontend/src/index.css`

- [ ] **Step 1: Create ProjectRail without drag first**

Create `packages/frontend/src/components/ProjectRail.tsx`:

```tsx
import * as DropdownMenu from '@radix-ui/react-dropdown-menu';
import { Ellipsis, FolderOpen, Pin, PinOff, Plus, Search, Trash2 } from 'lucide-react';
import { useMemo, useState } from 'react';
import type { Project } from '../lib/types';
import { sortPinnedItems } from '../lib/sortableItems';
import { cn, truncate } from '../lib/utils';
import { Input } from './ui/Input';

export function ProjectRail({
  projects,
  activeProjectId,
  busyProjectId,
  onSelectProject,
  onCreateProject,
  onTogglePin,
  onDeleteProject,
}: {
  projects: Project[];
  activeProjectId?: string;
  busyProjectId?: string | null;
  onSelectProject: (project: Project) => void;
  onCreateProject: () => void;
  onTogglePin: (project: Project) => void;
  onDeleteProject: (project: Project) => void;
}): JSX.Element {
  const [query, setQuery] = useState('');
  const visibleProjects = useMemo(() => {
    const normalized = query.trim().toLowerCase();
    const sorted = sortPinnedItems(projects);
    if (!normalized) return sorted;
    return sorted.filter((project) => project.name.toLowerCase().includes(normalized));
  }, [projects, query]);

  return (
    <aside className="project-rail" aria-label="项目列表">
      <div className="project-rail-header">
        <span>项目</span>
        <button type="button" className="project-rail-icon-button" onClick={onCreateProject} aria-label="新增项目">
          <Plus className="h-4 w-4" />
        </button>
      </div>
      <div className="project-rail-search">
        <Search className="h-3.5 w-3.5" />
        <Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="搜索项目" />
      </div>
      <div className="project-rail-list">
        {visibleProjects.map((project) => (
          <ProjectRailItem
            key={project.id}
            project={project}
            active={project.id === activeProjectId}
            busy={busyProjectId === project.id}
            onSelect={() => onSelectProject(project)}
            onTogglePin={() => onTogglePin(project)}
            onDelete={() => onDeleteProject(project)}
          />
        ))}
      </div>
    </aside>
  );
}

function ProjectRailItem({
  project,
  active,
  busy,
  onSelect,
  onTogglePin,
  onDelete,
}: {
  project: Project;
  active: boolean;
  busy: boolean;
  onSelect: () => void;
  onTogglePin: () => void;
  onDelete: () => void;
}): JSX.Element {
  return (
    <div className={cn('project-rail-item', active && 'is-active', busy && 'is-busy')}>
      <button type="button" className="project-rail-select" onClick={onSelect}>
        <FolderOpen className="h-4 w-4" />
        <span>{truncate(project.name, 28)}</span>
        {project.pinned_at ? <Pin className="h-3.5 w-3.5" /> : null}
      </button>
      <DropdownMenu.Root>
        <DropdownMenu.Trigger asChild>
          <button type="button" className="project-rail-menu-button" aria-label={`${project.name} 操作`}>
            <Ellipsis className="h-4 w-4" />
          </button>
        </DropdownMenu.Trigger>
        <DropdownMenu.Portal>
          <DropdownMenu.Content align="end" sideOffset={6} className="project-rail-menu">
            <DropdownMenu.Item className="project-rail-menu-item" onSelect={onTogglePin}>
              {project.pinned_at ? <PinOff className="h-3.5 w-3.5" /> : <Pin className="h-3.5 w-3.5" />}
              {project.pinned_at ? '取消置顶' : '置顶'}
            </DropdownMenu.Item>
            <DropdownMenu.Item className="project-rail-menu-item is-danger" onSelect={onDelete}>
              <Trash2 className="h-3.5 w-3.5" />
              删除
            </DropdownMenu.Item>
          </DropdownMenu.Content>
        </DropdownMenu.Portal>
      </DropdownMenu.Root>
    </div>
  );
}
```

- [ ] **Step 2: Create DevelopmentWorkspacePage**

Create `packages/frontend/src/pages/DevelopmentWorkspacePage.tsx`:

```tsx
import { useEffect, useMemo, useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { useNavigate, useParams } from 'react-router-dom';
import { toast } from 'sonner';
import { api } from '../lib/api';
import type { Project, Room } from '../lib/types';
import { sortPinnedItems } from '../lib/sortableItems';
import { ProjectRail } from '../components/ProjectRail';
import { RoomTabsBar } from '../components/RoomTabsBar';
import { RoomWorkbench } from '../components/room/RoomWorkbench';
import { WorkspaceEmptyState } from '../components/WorkspaceEmptyState';
import { Button } from '../components/ui/Button';
import { CreateProjectDialog } from '../components/CreateProjectDialog';
import { CreateRoomDialog } from './ProjectPage';

export function DevelopmentWorkspacePage(): JSX.Element {
  const { projectId, roomId } = useParams();
  const navigate = useNavigate();
  const queryClient = useQueryClient();
  const [createProjectOpen, setCreateProjectOpen] = useState(false);
  const { data: projects = [] } = useQuery({ queryKey: ['projects'], queryFn: api.listProjects });
  const sortedProjects = useMemo(() => sortPinnedItems(projects), [projects]);
  const activeProject = sortedProjects.find((project) => project.id === projectId) ?? sortedProjects[0] ?? null;
  const activeProjectId = activeProject?.id ?? '';
  const { data: rooms = [] } = useQuery({
    queryKey: ['rooms', activeProjectId],
    queryFn: () => api.listRooms(activeProjectId),
    enabled: Boolean(activeProjectId),
  });
  const sortedRooms = useMemo(() => sortPinnedItems(rooms), [rooms]);
  const activeRoom = sortedRooms.find((room) => room.id === roomId) ?? sortedRooms[0] ?? null;

  const updateProject = useMutation({
    mutationFn: ({ id, patch }: { id: string; patch: { pinned_at?: number | null } }) => api.updateProject(id, patch),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['projects'] }),
    onError: (err) => toast.error((err as Error).message),
  });
  const updateRoom = useMutation({
    mutationFn: ({ id, patch }: { id: string; patch: { name?: string; pinned_at?: number | null } }) => api.updateRoom(id, patch),
    onSuccess: (room) => {
      queryClient.invalidateQueries({ queryKey: ['rooms', room.project_id] });
      queryClient.invalidateQueries({ queryKey: ['room', room.id] });
    },
    onError: (err) => toast.error((err as Error).message),
  });
  const createRoom = useMutation({
    mutationFn: () => api.createRoom(activeProjectId, { name: '新群聊' }),
    onSuccess: (room) => {
      queryClient.invalidateQueries({ queryKey: ['rooms', activeProjectId] });
      navigate(`/projects/${activeProjectId}/rooms/${room.id}`, { replace: false });
    },
    onError: (err) => toast.error((err as Error).message),
  });
  const deleteRoom = useMutation({
    mutationFn: (room: Room) => api.deleteRoom(room.id).then(() => room),
    onSuccess: (deletedRoom) => {
      const remaining = sortPinnedItems(rooms.filter((room) => room.id !== deletedRoom.id));
      queryClient.invalidateQueries({ queryKey: ['rooms', activeProjectId] });
      const nextRoom = remaining[0];
      navigate(nextRoom ? `/projects/${activeProjectId}/rooms/${nextRoom.id}` : `/projects/${activeProjectId}`, { replace: true });
    },
    onError: (err) => toast.error((err as Error).message),
  });
  const deleteProject = useMutation({
    mutationFn: (project: Project) => api.deleteProject(project.id).then(() => project),
    onSuccess: (deletedProject) => {
      const remaining = sortPinnedItems(projects.filter((project) => project.id !== deletedProject.id));
      queryClient.invalidateQueries({ queryKey: ['projects'] });
      navigate(remaining[0] ? `/projects/${remaining[0].id}` : '/', { replace: true });
    },
    onError: (err) => toast.error((err as Error).message),
  });

  useEffect(() => {
    if (sortedProjects.length === 0) return;
    if (!projectId || !sortedProjects.some((project) => project.id === projectId)) {
      navigate(`/projects/${sortedProjects[0].id}`, { replace: true });
    }
  }, [navigate, projectId, sortedProjects]);

  useEffect(() => {
    if (!activeProjectId || rooms.length === 0) return;
    if (!roomId || !rooms.some((room) => room.id === roomId)) {
      navigate(`/projects/${activeProjectId}/rooms/${sortedRooms[0].id}`, { replace: true });
    }
  }, [activeProjectId, navigate, roomId, rooms, sortedRooms]);

  if (projects.length === 0) {
    return (
      <div className="development-workspace-empty">
        <WorkspaceEmptyState
          title="还没有项目"
          description="创建第一个项目后，开发工作台会在这里显示项目群聊。"
          action={<Button onClick={() => setCreateProjectOpen(true)}>新增项目</Button>}
        />
        <CreateProjectDialog open={createProjectOpen} onOpenChange={setCreateProjectOpen} />
      </div>
    );
  }

  return (
    <div className="development-workspace">
      <ProjectRail
        projects={projects}
        activeProjectId={activeProjectId}
        busyProjectId={updateProject.isPending ? updateProject.variables?.id ?? null : deleteProject.isPending ? deleteProject.variables?.id ?? null : null}
        onSelectProject={(project) => navigate(`/projects/${project.id}`)}
        onCreateProject={() => setCreateProjectOpen(true)}
        onTogglePin={(project) => updateProject.mutate({ id: project.id, patch: { pinned_at: project.pinned_at ? null : Date.now() } })}
        onDeleteProject={(project) => {
          if (window.confirm(`删除项目「${project.name}」？此操作不可撤销。`)) deleteProject.mutate(project);
        }}
      />
      <section className="development-workspace-main">
        {activeProject && rooms.length === 0 ? (
          <WorkspaceEmptyState
            title="这个项目还没有群聊"
            description="创建群聊后可以开始任务协作。"
            action={<CreateRoomDialog projectId={activeProject.id} buttonText="新增群聊" />}
          />
        ) : activeProject && activeRoom ? (
          <>
            <RoomTabsBar
              projectId={activeProject.id}
              roomId={activeRoom.id}
              rooms={rooms}
              busyRoomId={updateRoom.isPending ? updateRoom.variables?.id ?? null : deleteRoom.isPending ? deleteRoom.variables?.id ?? null : null}
              creating={createRoom.isPending}
              onCreateRoom={() => createRoom.mutate()}
              onRenameRoom={(room, name) => updateRoom.mutateAsync({ id: room.id, patch: { name } }).then(() => undefined)}
              onTogglePin={(room) => updateRoom.mutate({ id: room.id, patch: { pinned_at: room.pinned_at ? null : Date.now() } })}
              onDeleteRoom={(room) => {
                if (window.confirm(`删除群聊「${room.name}」？此操作不可撤销。`)) deleteRoom.mutate(room);
              }}
            />
            <RoomWorkbench projectId={activeProject.id} roomId={activeRoom.id} />
          </>
        ) : null}
      </section>
      <CreateProjectDialog open={createProjectOpen} onOpenChange={setCreateProjectOpen} />
    </div>
  );
}
```

Before implementing this exact import, export `CreateRoomDialog` from `packages/frontend/src/pages/ProjectPage.tsx` or move it to `packages/frontend/src/components/CreateRoomDialog.tsx`. Prefer moving it to a component if TypeScript import cycles or page coupling appears.

- [ ] **Step 3: Wire routes**

In `packages/frontend/src/main.tsx`, import `DevelopmentWorkspacePage` and replace:

```tsx
<Route path="/" element={<DashboardPage />} />
<Route path="/projects/:projectId" element={<ProjectPage />} />
<Route path="/projects/:projectId/rooms/:roomId" element={<RoomPage />} />
```

with:

```tsx
<Route path="/" element={<DevelopmentWorkspacePage />} />
<Route path="/projects/:projectId" element={<DevelopmentWorkspacePage />} />
<Route path="/projects/:projectId/rooms/:roomId" element={<DevelopmentWorkspacePage />} />
```

Remove unused imports for `DashboardPage`, `ProjectPage`, and `RoomPage` only if no route still references them.

- [ ] **Step 4: Keep Development nav active**

In `packages/frontend/src/components/AppShell.tsx`, add:

```ts
const isDevelopmentRoute = location.pathname === '/' || location.pathname.startsWith('/projects/');
```

Change the development `SidebarLink`:

```tsx
<SidebarLink to="/" active={isDevelopmentRoute} icon={Home} label={t('shell.nav.development')} exact className="shell-public-link" />
```

- [ ] **Step 5: Add layout CSS**

Append to `packages/frontend/src/index.css` near workspace styles:

```css
.development-workspace {
  display: grid;
  grid-template-columns: minmax(260px, 28%) minmax(0, 1fr);
  height: 100%;
  min-height: 0;
  overflow: hidden;
}

.project-rail {
  display: flex;
  min-height: 0;
  flex-direction: column;
  border-right: 1px solid var(--color-border);
  background: var(--color-surface);
}

.project-rail-header,
.project-rail-search {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 12px;
}

.project-rail-list {
  min-height: 0;
  overflow-y: auto;
  padding: 6px;
}

.project-rail-item {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 30px;
  align-items: center;
  gap: 4px;
  border-radius: 8px;
  color: var(--color-fg-muted);
}

.project-rail-item.is-active {
  background: var(--color-surface-raised);
  color: var(--color-fg);
}

.project-rail-select,
.project-rail-menu-button,
.project-rail-icon-button {
  display: inline-flex;
  align-items: center;
  border: 0;
  background: transparent;
  color: inherit;
}

.project-rail-select {
  min-width: 0;
  gap: 8px;
  padding: 9px 8px;
  text-align: left;
}

.project-rail-select span {
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.project-rail-menu-button,
.project-rail-icon-button {
  justify-content: center;
  width: 30px;
  height: 30px;
  border-radius: 7px;
}

.project-rail-menu {
  z-index: 50;
  min-width: 150px;
  border: 1px solid var(--color-border);
  border-radius: 8px;
  background: var(--color-surface);
  padding: 4px;
  box-shadow: var(--shadow-dialog);
}

.project-rail-menu-item {
  display: flex;
  cursor: pointer;
  align-items: center;
  gap: 8px;
  border-radius: 6px;
  padding: 8px 10px;
  font-size: 12px;
  outline: none;
}

.project-rail-menu-item.is-danger {
  color: var(--color-danger);
}

.development-workspace-main {
  display: flex;
  min-width: 0;
  min-height: 0;
  flex-direction: column;
  overflow: hidden;
}

.development-workspace-main .workspace-root {
  min-height: 0;
  flex: 1;
}

.development-workspace-empty {
  display: grid;
  height: 100%;
  place-items: center;
}
```

- [ ] **Step 6: Run TypeScript-focused frontend tests**

Run:

```bash
node --import tsx --test packages/frontend/src/lib/sortableItems.test.ts packages/frontend/src/pages/RoomPage.test.ts
```

Expected: pass. Do not run `npm run build` in this task.

- [ ] **Step 7: Commit workspace route shell**

```bash
git add packages/frontend/src/components/ProjectRail.tsx packages/frontend/src/pages/DevelopmentWorkspacePage.tsx packages/frontend/src/main.tsx packages/frontend/src/components/AppShell.tsx packages/frontend/src/index.css
git commit -m "feat(frontend): 重构开发首页工作台"
```

---

### Task 6: Drag Sorting UI For Projects And Rooms

**Files:**
- Modify: `packages/frontend/src/components/ProjectRail.tsx`
- Modify: `packages/frontend/src/components/RoomTabsBar.tsx`
- Modify: `packages/frontend/src/pages/DevelopmentWorkspacePage.tsx`
- Modify: `packages/frontend/src/index.css`

- [ ] **Step 1: Add drag props to ProjectRail**

Extend `ProjectRail` props:

```ts
onReorderProjects: (ids: string[], pinned: boolean) => void;
```

Add local drag state:

```ts
const [draggingId, setDraggingId] = useState<string | null>(null);
```

When rendering project items, pass `draggable`, `dragging`, `onDragStart`, `onDragOver`, `onDrop`, and `onDragEnd`. On drop:

```ts
const next = reorderWithinLayer(visibleProjects, draggingId, project.id);
const pinned = Boolean(next.find((item) => item.id === draggingId)?.pinned_at);
onReorderProjects(layerIds(next, pinned), pinned);
setDraggingId(null);
```

Ignore drops where `draggingId` is null.

- [ ] **Step 2: Add project reorder mutation**

In `DevelopmentWorkspacePage`, add:

```ts
const reorderProjects = useMutation({
  mutationFn: ({ ids, pinned }: { ids: string[]; pinned: boolean }) => api.reorderProjects({ ids, pinned }),
  onMutate: async ({ ids, pinned }) => {
    await queryClient.cancelQueries({ queryKey: ['projects'] });
    const previous = queryClient.getQueryData<Project[]>(['projects']);
    queryClient.setQueryData<Project[]>(['projects'], (current) => {
      if (!current) return current;
      const order = new Map(ids.map((id, index) => [id, index + 1]));
      return current.map((project) =>
        Boolean(project.pinned_at) === pinned && order.has(project.id)
          ? { ...project, sort_order: order.get(project.id) ?? project.sort_order }
          : project,
      );
    });
    return { previous };
  },
  onError: (err, _variables, context) => {
    queryClient.setQueryData(['projects'], context?.previous);
    toast.error((err as Error).message);
  },
  onSuccess: (projects) => queryClient.setQueryData(['projects'], projects),
});
```

Pass to `ProjectRail`:

```tsx
onReorderProjects={(ids, pinned) => reorderProjects.mutate({ ids, pinned })}
```

- [ ] **Step 3: Add drag props to RoomTabsBar**

Extend `RoomTabsBarProps`:

```ts
onReorderRooms: (ids: string[], pinned: boolean) => void;
```

Use the same `draggingId` pattern. On drop:

```ts
const next = reorderWithinLayer(sortedRooms, draggingId, room.id);
const pinned = Boolean(next.find((item) => item.id === draggingId)?.pinned_at);
onReorderRooms(layerIds(next, pinned), pinned);
setDraggingId(null);
```

Keep right-click context menu behavior unchanged.

- [ ] **Step 4: Add room reorder mutation**

In `DevelopmentWorkspacePage`, add:

```ts
const reorderRooms = useMutation({
  mutationFn: ({ ids, pinned }: { ids: string[]; pinned: boolean }) =>
    api.reorderRooms(activeProjectId, { ids, pinned }),
  onMutate: async ({ ids, pinned }) => {
    await queryClient.cancelQueries({ queryKey: ['rooms', activeProjectId] });
    const previous = queryClient.getQueryData<Room[]>(['rooms', activeProjectId]);
    queryClient.setQueryData<Room[]>(['rooms', activeProjectId], (current) => {
      if (!current) return current;
      const order = new Map(ids.map((id, index) => [id, index + 1]));
      return current.map((room) =>
        Boolean(room.pinned_at) === pinned && order.has(room.id)
          ? { ...room, sort_order: order.get(room.id) ?? room.sort_order }
          : room,
      );
    });
    return { previous };
  },
  onError: (err, _variables, context) => {
    queryClient.setQueryData(['rooms', activeProjectId], context?.previous);
    toast.error((err as Error).message);
  },
  onSuccess: (rooms) => queryClient.setQueryData(['rooms', activeProjectId], rooms),
});
```

Pass to `RoomTabsBar`:

```tsx
onReorderRooms={(ids, pinned) => reorderRooms.mutate({ ids, pinned })}
```

- [ ] **Step 5: Add drag CSS states**

In `packages/frontend/src/index.css`:

```css
.project-rail-item[draggable="true"],
.room-tab-wrapper[draggable="true"] {
  cursor: grab;
}

.project-rail-item.is-dragging,
.room-tab-wrapper.is-dragging {
  opacity: 0.55;
}

.project-rail-item.is-drop-target,
.room-tab-wrapper.is-drop-target {
  outline: 1px solid var(--color-accent);
  outline-offset: -1px;
}
```

- [ ] **Step 6: Run helper test and backend route tests**

Run:

```bash
node --import tsx --test packages/frontend/src/lib/sortableItems.test.ts
npm run test -w @openclaw-room/backend -- src/projects.routes.test.ts src/rooms.routes.test.ts
```

Expected: pass.

- [ ] **Step 7: Commit drag sorting UI**

```bash
git add packages/frontend/src/components/ProjectRail.tsx packages/frontend/src/components/RoomTabsBar.tsx packages/frontend/src/pages/DevelopmentWorkspacePage.tsx packages/frontend/src/index.css
git commit -m "feat(frontend): 支持项目和群聊拖动排序"
```

---

### Task 7: Final Verification And Push

**Files:**
- Modify only if verification finds regressions.

- [ ] **Step 1: Run backend targeted tests**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/repos/projects.test.ts src/repos/rooms.test.ts src/projects.routes.test.ts src/rooms.routes.test.ts
```

Expected: pass.

- [ ] **Step 2: Run frontend targeted tests**

Run:

```bash
node --import tsx --test packages/frontend/src/lib/sortableItems.test.ts packages/frontend/src/pages/RoomPage.test.ts packages/frontend/src/pages/roomPageLogic.test.ts
```

Expected: pass.

- [ ] **Step 3: Run final build once**

Run:

```bash
npm run build
```

Expected: backend TypeScript build and frontend Vite build pass.

- [ ] **Step 4: Start dev server for browser QA**

Run:

```bash
npm run dev
```

Expected: frontend available at `http://localhost:5173` and backend at port `7330`. If port `5173` is busy, use the Vite URL printed by the command.

- [ ] **Step 5: Browser QA routes**

Use browser QA on:

```text
http://localhost:5173/
http://localhost:5173/projects/ZwgdJRslFpih
http://localhost:5173/projects/ZwgdJRslFpih/rooms/vcHtzxxovkbl
```

Verify:

- 公共 Header 显示，并且“开发”菜单 active。
- `/` 自动进入排序第一项目。
- `/projects/:projectId` 自动进入该项目排序第一群聊。
- `/projects/:projectId/rooms/:roomId` 直接进入指定群聊。
- 左侧项目列表可搜索、置顶、取消置顶、同层拖动排序。
- 群聊 tab 可新增、重命名、置顶、取消置顶、删除、同层拖动排序。
- 刷新后项目和群聊排序仍然保持。
- 群聊主区域保持左侧任务工作区 60%、右侧聊天区域 40%。
- 极简主题没有 Apple 玻璃液化主题的视觉样式外溢。

- [ ] **Step 6: Review diff**

Run:

```bash
git diff --stat HEAD
git diff HEAD
```

Check:

- 没有重新引入全局页面 tab。
- 没有改动无关气泡目录文件。
- 没有提交 SQLite 数据、截图、`.codegraph/`、`.playwright-mcp/`、`output/`。

- [ ] **Step 7: Final commit if QA fixes were needed**

If Step 4-6 required fixes:

```bash
git add packages/frontend/src packages/backend/src
git commit -m "fix(frontend): 修复开发工作台验收问题"
```

If no fixes were needed, skip this commit.

- [ ] **Step 8: Push**

Run:

```bash
git push
```

Expected: current branch pushed to remote.
