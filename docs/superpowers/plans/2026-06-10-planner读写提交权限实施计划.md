# Planner 读写提交权限 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让内置 session planner 默认具备项目根目录读写、shell 和 commit 工具能力，并保证旧默认只读数据自动升级。

**Architecture:** 权限边界仍集中在内置 agent template 和 `agentRepo.ensureBuiltInAgents()` 迁移逻辑。`resolveSessionPlannerRuntime()` 不引入新分支，只从全局内置 planner runtime profile 读取最终权限并写入 snapshot。

**Tech Stack:** TypeScript, Node test runner, SQLite repository layer, ACP runtime profile.

---

### Task 1: 核对内置 planner 默认权限

**Files:**
- Inspect: `packages/backend/src/agent-templates.ts`
- Inspect: `packages/backend/src/agent-templates.test.ts`
- Inspect: `packages/backend/src/crew-templates.test.ts`

- [x] **Step 1: 确认 planner template 使用读写提交边界**

检查 `packages/backend/src/agent-templates.ts` 中 `planner` 是否等价于：

```ts
{
  acp_permission_mode: 'workspace-write',
  tool_policy: { allowed: ['read_files', 'write_files', 'run_shell', 'commit'] },
  workspace_policy: { read: ['.'], write: ['.'] },
  memory_scope: 'agent',
}
```

- [x] **Step 2: 如缺失则补齐最小实现**

如果 template 未满足 Step 1，更新 `planner` 使用专用边界：

```ts
const PLANNER_WRITE_RUNTIME_BOUNDARY = {
  runtime_backend: 'acp',
  tool_policy: { allowed: ['read_files', 'write_files', 'run_shell', 'commit'] },
  workspace_policy: { read: ['.'], write: ['.'] },
  memory_scope: 'agent',
} satisfies Pick<BuiltInAgentTemplate, 'runtime_backend' | 'tool_policy' | 'workspace_policy' | 'memory_scope'>;
```

并让 planner 设置：

```ts
acp_permission_mode: 'workspace-write',
...PLANNER_WRITE_RUNTIME_BOUNDARY,
```

- [x] **Step 3: 同步模板测试断言**

`packages/backend/src/agent-templates.test.ts` 中 planner 断言应为：

```ts
assert.equal(templates['planner']?.acp_permission_mode, 'workspace-write');
assert.deepEqual(templates['planner']?.tool_policy, {
  allowed: ['read_files', 'write_files', 'run_shell', 'commit'],
});
assert.deepEqual(templates['planner']?.workspace_policy, { read: ['.'], write: ['.'] });
assert.equal(templates['planner']?.memory_scope, 'agent');
```

`packages/backend/src/crew-templates.test.ts` 中房间模板 planner 断言也使用同一边界。

- [x] **Step 4: 运行模板相关定向测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/agent-templates.test.ts src/crew-templates.test.ts
```

Expected: 两个测试文件全部通过。

### Task 2: 核对旧默认只读 planner 自动升级

**Files:**
- Inspect/Modify: `packages/backend/src/repos/agents.ts`
- Inspect/Modify: `packages/backend/src/repos/agents.test.ts`

- [x] **Step 1: 确认旧只读边界识别函数存在**

`packages/backend/src/repos/agents.ts` 应包含：

```ts
function isPlannerReadOnlyDefaultBoundary(agent: AgentWithRuntimeProfileVersion): boolean {
  return agent.builtin_key === 'planner'
    && agent.default_acp_permission_mode === 'read-only'
    && agent.default_runtime_backend === 'acp'
    && agent.default_memory_scope === 'room'
    && agent.default_tool_policy.allowed.join('\n') === ['read_files'].join('\n')
    && agent.default_workspace_policy.read.join('\n') === ['.'].join('\n')
    && agent.default_workspace_policy.write.length === 0;
}
```

- [x] **Step 2: 确认 seed 迁移会刷新 planner**

`resolveBuiltInRuntimeBoundary()` 应把 `shouldUpgradePlanner` 合入 permission、tool policy、workspace policy、memory scope 的刷新条件：

```ts
const shouldUpgradePlanner = isPlannerReadOnlyDefaultBoundary(existing);
const shouldRefreshPermission = shouldRefreshAll || shouldUpgradeTechnicalWriter || shouldUpgradePlanner;
const shouldRefreshToolPolicy = isLegacyToolPolicy(existing.default_tool_policy) ||
  shouldUpgradeTechnicalWriter ||
  shouldUpgradePlanner;
const shouldRefreshWorkspacePolicy = isLegacyWorkspacePolicy(existing.default_workspace_policy) ||
  shouldUpgradeTechnicalWriter ||
  shouldUpgradePlanner;
const shouldRefreshMemoryScope = existing.default_memory_scope === LEGACY_RUNTIME_BOUNDARY.default_memory_scope
  || shouldUpgradeTechnicalWriter
  || shouldUpgradePlanner;
```

- [x] **Step 3: 确认迁移测试覆盖旧只读 planner**

`packages/backend/src/repos/agents.test.ts` 应包含将 planner 写回旧默认只读边界，再调用 `agentRepo.ensureBuiltInAgents()` 的测试，并断言：

```ts
assert.equal(afterSeed?.default_acp_permission_mode, 'workspace-write');
assert.deepEqual(afterSeed?.default_tool_policy, {
  allowed: ['read_files', 'write_files', 'run_shell', 'commit'],
});
assert.deepEqual(afterSeed?.default_workspace_policy, { read: ['.'], write: ['.'] });
assert.equal(afterSeed?.default_memory_scope, 'agent');
```

- [x] **Step 4: 运行 repository 定向测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/repos/agents.test.ts
```

Expected: `repos/agents.test.ts` 全部通过。

### Task 3: 补齐 session planner runtime 断言

**Files:**
- Modify: `packages/backend/src/session-planner-runtime.test.ts`

- [x] **Step 1: 增加 commit tool 断言**

在 `resolveSessionPlannerRuntime gives the session planner workspace write permission` 测试中补充：

```ts
assert.ok(runtime.toolPolicy.allowed.includes('commit'));
assert.equal(runtime.memoryScope, 'agent');
```

- [x] **Step 2: 增加 snapshot 权限断言**

在 `resolveSessionPlannerRuntime builds a stable runtime snapshot` 测试中补充：

```ts
assert.equal(snapshot.permission_mode, runtime.permissionMode);
assert.equal(snapshot.memory_scope, runtime.memoryScope);
```

- [x] **Step 3: 运行 session planner runtime 定向测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/session-planner-runtime.test.ts
```

Expected: `session-planner-runtime.test.ts` 全部通过。

### Task 4: 最终验证、审查和提交

**Files:**
- Review: `packages/backend/src/agent-templates.ts`
- Review: `packages/backend/src/repos/agents.ts`
- Review: `packages/backend/src/agent-templates.test.ts`
- Review: `packages/backend/src/repos/agents.test.ts`
- Review: `packages/backend/src/session-planner-runtime.test.ts`
- Review: `packages/backend/src/crew-templates.test.ts`
- Review: `docs/superpowers/specs/2026-06-10-planner读写提交权限设计.md`
- Review: `docs/superpowers/plans/2026-06-10-planner读写提交权限实施计划.md`

- [x] **Step 1: 运行完整相关定向测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/agent-templates.test.ts src/crew-templates.test.ts src/repos/agents.test.ts src/session-planner-runtime.test.ts
```

Expected: 四个测试文件全部通过。

- [x] **Step 2: 运行最低构建门禁**

Run:

```bash
npm run build
```

Expected: 后端 TypeScript 编译和前端 Vite build 退出码为 0。

- [x] **Step 3: 做最终代码审查**

检查 diff 只包含 planner 权限、对应测试和计划文档，不包含当前工作区已有的 frontend、截图、临时目录或删除文件。

- [x] **Step 4: 提交本任务实现**

Run:

```bash
git add packages/backend/src/session-planner-runtime.test.ts docs/superpowers/plans/2026-06-10-planner读写提交权限实施计划.md
git commit -m "test(planner): 补齐读写提交权限验证"
```

Expected: 只提交本任务相关文件。
