# 项目聊天室记忆机制 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 OpenClaw Room 增加可管理、可审计、可注入到 Agent 上下文的项目/聊天室/智能体/任务记忆机制。

**Architecture:** OpenClaw Room 自己维护显式 `memory_entries` 记忆层，作为产品级项目/聊天室记忆来源；OpenClaw 原生 memory/Active Memory 保持为底层可选能力，不作为本轮主存储。后端负责记忆持久化、API、上下文格式化与 prompt 注入；前端负责在聊天室和任务详情中展示、创建、编辑、删除记忆；workflow 完成后自动沉淀任务总结记忆。

**Tech Stack:** Node.js、Express、better-sqlite3、zod、React 18、TypeScript、TanStack Query、WebSocket、Vite、TailwindCSS、lucide-react。

---

## 文件结构

- Modify: `packages/backend/src/db.ts`，新增 `memory_entries` 表和索引。
- Modify: `packages/backend/src/types.ts`，新增后端 `MemoryEntry`、`MemoryScope`、`MemoryType`、`MemorySourceType` 类型。
- Create: `packages/backend/src/repos/memory.ts`，封装记忆 CRUD、上下文查询和自动任务总结去重。
- Create: `packages/backend/src/memory/context.ts`，把记忆条目格式化为可注入 prompt 的短上下文块。
- Create: `packages/backend/src/memory/context.test.ts`，覆盖记忆格式化、截断和空记忆行为。
- Create: `packages/backend/src/repos/memory.test.ts`，覆盖记忆创建、查询、过滤和自动总结去重。
- Modify: `packages/backend/src/routes.ts`，新增项目记忆 API 与消息保存为记忆 API。
- Modify: `packages/backend/src/dispatcher.ts`，普通聊天 agent run 前注入项目/聊天室/智能体/任务相关记忆。
- Modify: `packages/backend/src/workflows/prompts.ts`，workflow prompt 支持 `memoryContext` 字段。
- Modify: `packages/backend/src/workflows/orchestrator.ts`，workflow 阶段 prompt 注入记忆，并在验收通过后写入任务总结记忆。
- Modify: `packages/frontend/src/lib/types.ts`，新增前端记忆类型、标签映射和表单输入类型。
- Modify: `packages/frontend/src/lib/api.ts`，新增记忆 CRUD API。
- Create: `packages/frontend/src/components/MemoryPanel.tsx`，通用记忆列表、创建、编辑、删除、置顶组件。
- Modify: `packages/frontend/src/pages/RoomPage.tsx`，聊天室侧栏加入记忆管理入口，并支持从消息保存为记忆。
- Modify: `packages/frontend/src/components/TaskDetailPanel.tsx`，任务详情侧栏展示任务相关记忆和“保存任务总结”入口。
- Modify: `packages/frontend/src/components/MessageContent.tsx`，保留消息展示职责；保存入口放在 `RoomPage` 消息气泡操作区。
- Modify: `docs/superpowers/plans/2026-05-14-项目聊天室记忆机制.md`，执行时逐项勾选。

## Task 1: 后端记忆数据模型与仓储

**Files:**
- Modify: `packages/backend/src/db.ts`
- Modify: `packages/backend/src/types.ts`
- Create: `packages/backend/src/repos/memory.ts`
- Create: `packages/backend/src/repos/memory.test.ts`

- [x] **Step 1: 编写仓储测试**

创建 `packages/backend/src/repos/memory.test.ts`：

```ts
import assert from 'node:assert/strict';
import { mkdtempSync, mkdirSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import test from 'node:test';

process.env.OPENCLAW_ROOM_DB = join(mkdtempSync(join(tmpdir(), 'openclaw-room-memory-')), 'test.db');

const projectDir = join(tmpdir(), `openclaw-room-memory-project-${Date.now()}`);
mkdirSync(projectDir, { recursive: true });

const { projectRepo } = await import('./projects.js');
const { roomAgentRepo, roomRepo } = await import('./rooms.js');
const { taskRepo } = await import('./tasks.js');
const { memoryRepo } = await import('./memory.js');

test('memoryRepo stores and filters project, room, agent, and task memories for prompt context', () => {
  const project = projectRepo.create({ name: 'Memory Test', path: projectDir });
  const room = roomRepo.create({ project_id: project.id, name: 'Feature Room' });
  const otherRoom = roomRepo.create({ project_id: project.id, name: 'Other Room' });
  const agent = roomAgentRepo.add({ room_id: room.id, agent_id: 'architect', agent_name: 'Architect' });
  const task = taskRepo.create({ project_id: project.id, room_id: room.id, title: 'Memory task' });

  const projectMemory = memoryRepo.create({
    project_id: project.id,
    scope: 'project',
    memory_type: 'decision',
    title: 'Use explicit memory',
    content: 'Project memories must be injected before agent prompts.',
    pinned: true,
  });
  const roomMemory = memoryRepo.create({
    project_id: project.id,
    room_id: room.id,
    scope: 'room',
    memory_type: 'fact',
    title: 'Room purpose',
    content: 'This room coordinates memory feature work.',
  });
  const agentMemory = memoryRepo.create({
    project_id: project.id,
    room_id: room.id,
    room_agent_id: agent.id,
    scope: 'agent',
    memory_type: 'preference',
    title: 'Architect preference',
    content: 'Architect prefers short implementation plans.',
  });
  const taskMemory = memoryRepo.create({
    project_id: project.id,
    room_id: room.id,
    task_id: task.id,
    scope: 'task',
    memory_type: 'task_summary',
    title: 'Task summary',
    content: 'The task created the memory repository.',
  });
  memoryRepo.create({
    project_id: project.id,
    room_id: otherRoom.id,
    scope: 'room',
    memory_type: 'fact',
    title: 'Other room',
    content: 'This should not be injected into the feature room.',
  });

  const entries = memoryRepo.listForRoomContext({
    projectId: project.id,
    roomId: room.id,
    roomAgentId: agent.id,
    taskId: task.id,
  });

  assert.deepEqual(entries.map((entry) => entry.id), [
    projectMemory.id,
    taskMemory.id,
    agentMemory.id,
    roomMemory.id,
  ]);
});

test('memoryRepo upserts one automatic task summary per task and source', () => {
  const project = projectRepo.list()[0];
  const room = roomRepo.listByProject(project.id)[0];
  const task = taskRepo.create({ project_id: project.id, room_id: room.id, title: 'Auto summary task' });

  const first = memoryRepo.upsertTaskSummary({
    project_id: project.id,
    room_id: room.id,
    task_id: task.id,
    title: 'Auto summary task',
    content: 'First accepted summary.',
    source_id: 'workflow-1',
  });
  const second = memoryRepo.upsertTaskSummary({
    project_id: project.id,
    room_id: room.id,
    task_id: task.id,
    title: 'Auto summary task',
    content: 'Updated accepted summary.',
    source_id: 'workflow-1',
  });

  assert.equal(first.id, second.id);
  assert.equal(second.content, 'Updated accepted summary.');
  assert.equal(second.source_type, 'workflow');
  assert.equal(second.memory_type, 'task_summary');
});
```

- [x] **Step 2: 运行测试确认失败**

Run: `npm run test -w @openclaw-room/backend -- src/repos/memory.test.ts`

Expected: FAIL，错误包含 `Cannot find module './memory.js'` 或 `memory_entries` 表不存在。

- [x] **Step 3: 扩展后端类型**

在 `packages/backend/src/types.ts` 的 `TaskArtifactType` 后加入：

```ts
export type MemoryScope = 'project' | 'room' | 'agent' | 'task';
export type MemoryType = 'decision' | 'fact' | 'preference' | 'lesson' | 'task_summary' | 'artifact_summary';
export type MemorySourceType = 'manual' | 'message' | 'workflow' | 'task';

export interface MemoryEntry {
  id: string;
  project_id: string;
  room_id: string | null;
  room_agent_id: string | null;
  task_id: string | null;
  scope: MemoryScope;
  memory_type: MemoryType;
  title: string;
  content: string;
  source_type: MemorySourceType;
  source_id: string | null;
  pinned: 0 | 1;
  created_at: number;
  updated_at: number;
}
```

- [x] **Step 4: 新增数据库表**

在 `packages/backend/src/db.ts` 的 `task_artifacts` 索引之后、`tasks` 表之前加入：

```sql
CREATE TABLE IF NOT EXISTS memory_entries (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  room_id TEXT,
  room_agent_id TEXT,
  task_id TEXT,
  scope TEXT NOT NULL,
  memory_type TEXT NOT NULL,
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  source_type TEXT NOT NULL DEFAULT 'manual',
  source_id TEXT,
  pinned INTEGER NOT NULL DEFAULT 0,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE,
  FOREIGN KEY (room_id) REFERENCES rooms(id) ON DELETE CASCADE,
  FOREIGN KEY (room_agent_id) REFERENCES room_agents(id) ON DELETE SET NULL,
  FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE SET NULL
);
CREATE INDEX IF NOT EXISTS idx_memory_project ON memory_entries(project_id, pinned, updated_at);
CREATE INDEX IF NOT EXISTS idx_memory_room ON memory_entries(room_id, pinned, updated_at);
CREATE INDEX IF NOT EXISTS idx_memory_agent ON memory_entries(room_agent_id, pinned, updated_at);
CREATE INDEX IF NOT EXISTS idx_memory_task ON memory_entries(task_id, updated_at);
CREATE UNIQUE INDEX IF NOT EXISTS idx_memory_task_source
  ON memory_entries(task_id, source_type, source_id)
  WHERE task_id IS NOT NULL AND source_id IS NOT NULL;
```

- [x] **Step 5: 创建仓储实现**

创建 `packages/backend/src/repos/memory.ts`：

```ts
import { nanoid } from 'nanoid';
import { db, now } from '../db.js';
import type { MemoryEntry, MemoryScope, MemorySourceType, MemoryType } from '../types.js';

export interface MemoryListFilters {
  projectId?: string;
  roomId?: string;
  roomAgentId?: string;
  taskId?: string;
  limit?: number;
}

export const memoryRepo = {
  list(filters: MemoryListFilters): MemoryEntry[] {
    const clauses: string[] = [];
    const params: Array<string | number> = [];
    if (filters.projectId) {
      clauses.push('project_id = ?');
      params.push(filters.projectId);
    }
    if (filters.roomId) {
      clauses.push('(room_id = ? OR scope = ?)');
      params.push(filters.roomId, 'project');
    }
    if (filters.roomAgentId) {
      clauses.push('(room_agent_id = ? OR room_agent_id IS NULL)');
      params.push(filters.roomAgentId);
    }
    if (filters.taskId) {
      clauses.push('(task_id = ? OR task_id IS NULL)');
      params.push(filters.taskId);
    }
    const where = clauses.length > 0 ? `WHERE ${clauses.join(' AND ')}` : '';
    const limit = Math.max(1, Math.min(filters.limit ?? 100, 200));
    return db
      .prepare(
        `SELECT * FROM memory_entries ${where}
         ORDER BY pinned DESC, updated_at DESC
         LIMIT ?`,
      )
      .all(...params, limit) as MemoryEntry[];
  },

  listForRoomContext(args: {
    projectId: string;
    roomId: string;
    roomAgentId?: string | null;
    taskId?: string | null;
    limit?: number;
  }): MemoryEntry[] {
    const limit = Math.max(1, Math.min(args.limit ?? 12, 30));
    return db
      .prepare(
        `SELECT * FROM memory_entries
         WHERE project_id = ?
           AND (
             scope = 'project'
             OR room_id = ?
             OR (? IS NOT NULL AND room_agent_id = ?)
             OR (? IS NOT NULL AND task_id = ?)
           )
         ORDER BY pinned DESC, updated_at DESC
         LIMIT ?`,
      )
      .all(
        args.projectId,
        args.roomId,
        args.roomAgentId ?? null,
        args.roomAgentId ?? null,
        args.taskId ?? null,
        args.taskId ?? null,
        limit,
      ) as MemoryEntry[];
  },

  get(id: string): MemoryEntry | undefined {
    return db.prepare('SELECT * FROM memory_entries WHERE id = ?').get(id) as MemoryEntry | undefined;
  },

  create(input: {
    project_id: string;
    room_id?: string | null;
    room_agent_id?: string | null;
    task_id?: string | null;
    scope: MemoryScope;
    memory_type: MemoryType;
    title: string;
    content: string;
    source_type?: MemorySourceType;
    source_id?: string | null;
    pinned?: boolean;
  }): MemoryEntry {
    const id = nanoid(16);
    const ts = now();
    db.prepare(
      `INSERT INTO memory_entries (
        id, project_id, room_id, room_agent_id, task_id, scope, memory_type, title,
        content, source_type, source_id, pinned, created_at, updated_at
      )
       VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
    ).run(
      id,
      input.project_id,
      input.room_id ?? null,
      input.room_agent_id ?? null,
      input.task_id ?? null,
      input.scope,
      input.memory_type,
      input.title,
      input.content,
      input.source_type ?? 'manual',
      input.source_id ?? null,
      input.pinned ? 1 : 0,
      ts,
      ts,
    );
    return this.get(id)!;
  },

  update(
    id: string,
    patch: Partial<Pick<MemoryEntry, 'memory_type' | 'title' | 'content'>> & { pinned?: boolean },
  ): MemoryEntry | undefined {
    const existing = this.get(id);
    if (!existing) return undefined;
    const next = {
      ...existing,
      ...patch,
      pinned: patch.pinned === undefined ? existing.pinned : patch.pinned ? 1 : 0,
      updated_at: now(),
    };
    db.prepare(
      `UPDATE memory_entries
       SET memory_type = ?, title = ?, content = ?, pinned = ?, updated_at = ?
       WHERE id = ?`,
    ).run(next.memory_type, next.title, next.content, next.pinned, next.updated_at, id);
    return this.get(id);
  },

  upsertTaskSummary(input: {
    project_id: string;
    room_id: string;
    task_id: string;
    title: string;
    content: string;
    source_id: string;
  }): MemoryEntry {
    const existing = db
      .prepare(
        `SELECT * FROM memory_entries
         WHERE task_id = ? AND source_type = 'workflow' AND source_id = ?`,
      )
      .get(input.task_id, input.source_id) as MemoryEntry | undefined;
    if (existing) {
      return this.update(existing.id, {
        title: input.title,
        content: input.content,
        memory_type: 'task_summary',
      })!;
    }
    return this.create({
      project_id: input.project_id,
      room_id: input.room_id,
      task_id: input.task_id,
      scope: 'task',
      memory_type: 'task_summary',
      title: input.title,
      content: input.content,
      source_type: 'workflow',
      source_id: input.source_id,
    });
  },

  delete(id: string): boolean {
    return db.prepare('DELETE FROM memory_entries WHERE id = ?').run(id).changes > 0;
  },
};
```

- [x] **Step 6: 运行仓储测试**

Run: `npm run test -w @openclaw-room/backend -- src/repos/memory.test.ts`

Expected: PASS，输出包含 `# pass 2`。

- [x] **Step 7: 提交 Task 1**

```bash
git add packages/backend/src/db.ts packages/backend/src/types.ts packages/backend/src/repos/memory.ts packages/backend/src/repos/memory.test.ts
git commit -m "feat(backend): 新增项目聊天室记忆存储"
```

## Task 2: 后端记忆 API

**Files:**
- Modify: `packages/backend/src/routes.ts`
- Create: `packages/backend/src/memory/routes.test.ts`

- [x] **Step 1: 编写 API 测试**

创建 `packages/backend/src/memory/routes.test.ts`：

```ts
import assert from 'node:assert/strict';
import { mkdtempSync, mkdirSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import test from 'node:test';

process.env.OPENCLAW_ROOM_DB = join(mkdtempSync(join(tmpdir(), 'openclaw-room-memory-routes-')), 'test.db');

const projectDir = join(tmpdir(), `openclaw-room-memory-routes-project-${Date.now()}`);
mkdirSync(projectDir, { recursive: true });

const { projectRepo } = await import('../repos/projects.js');
const { roomRepo } = await import('../repos/rooms.js');
const { taskRepo } = await import('../repos/tasks.js');
const { router } = await import('../routes.js');

const express = (await import('express')).default;
const app = express();
app.use(express.json());
app.use('/api', router);

async function request(path: string, init: RequestInit = {}) {
  const server = app.listen(0);
  try {
    const address = server.address();
    assert(address && typeof address === 'object');
    return await fetch(`http://127.0.0.1:${address.port}${path}`, {
      ...init,
      headers: { 'Content-Type': 'application/json', ...(init.headers ?? {}) },
    });
  } finally {
    await new Promise<void>((resolve) => server.close(() => resolve()));
  }
}

test('memory routes create, list, update, and delete project memory', async () => {
  const project = projectRepo.create({ name: 'API Memory', path: projectDir });
  const room = roomRepo.create({ project_id: project.id, name: 'API Room' });
  const task = taskRepo.create({ project_id: project.id, room_id: room.id, title: 'API task' });

  const createRes = await request(`/api/projects/${project.id}/memories`, {
    method: 'POST',
    body: JSON.stringify({
      scope: 'task',
      memory_type: 'decision',
      title: 'Remember API decision',
      content: 'The API exposes project-scoped memory CRUD.',
      room_id: room.id,
      task_id: task.id,
      pinned: true,
    }),
  });
  assert.equal(createRes.status, 201);
  const created = await createRes.json() as { id: string; title: string; pinned: 0 | 1 };
  assert.equal(created.title, 'Remember API decision');
  assert.equal(created.pinned, 1);

  const listRes = await request(`/api/projects/${project.id}/memories?roomId=${room.id}&taskId=${task.id}`);
  assert.equal(listRes.status, 200);
  const listed = await listRes.json() as Array<{ id: string }>;
  assert.deepEqual(listed.map((entry) => entry.id), [created.id]);

  const patchRes = await request(`/api/memories/${created.id}`, {
    method: 'PATCH',
    body: JSON.stringify({
      memory_type: 'lesson',
      title: 'Remember API lesson',
      content: 'The API validates related project resources.',
      pinned: false,
    }),
  });
  assert.equal(patchRes.status, 200);
  const patched = await patchRes.json() as { memory_type: string; pinned: 0 | 1 };
  assert.equal(patched.memory_type, 'lesson');
  assert.equal(patched.pinned, 0);

  const deleteRes = await request(`/api/memories/${created.id}`, { method: 'DELETE' });
  assert.equal(deleteRes.status, 204);
});
```

- [x] **Step 2: 运行测试确认失败**

Run: `npm run test -w @openclaw-room/backend -- src/memory/routes.test.ts`

Expected: FAIL，错误包含 `404` 或 `Cannot POST /api/projects/.../memories`。

- [x] **Step 3: 增加 routes import 和 schema**

在 `packages/backend/src/routes.ts` 顶部加入：

```ts
import { memoryRepo } from './repos/memory.js';
```

把类型 import 改为：

```ts
import type { AcpBackend, MemoryScope, MessageRoutingMode, TaskInteractionMode, WorkflowRole } from './types.js';
```

在 settings schema 之后加入：

```ts
const memoryInputSchema = z.object({
  scope: z.enum(['project', 'room', 'agent', 'task']),
  memory_type: z.enum(['decision', 'fact', 'preference', 'lesson', 'task_summary', 'artifact_summary']),
  title: z.string().min(1).max(160),
  content: z.string().min(1).max(12000),
  room_id: z.string().nullable().optional(),
  room_agent_id: z.string().nullable().optional(),
  task_id: z.string().nullable().optional(),
  source_type: z.enum(['manual', 'message', 'workflow', 'task']).optional(),
  source_id: z.string().nullable().optional(),
  pinned: z.boolean().optional(),
});

function validateMemoryScope(input: {
  projectId: string;
  scope: MemoryScope;
  room_id?: string | null;
  room_agent_id?: string | null;
  task_id?: string | null;
}): { ok: true } | { ok: false; status: number; error: string } {
  if (!projectRepo.get(input.projectId)) return { ok: false, status: 404, error: 'project not found' };
  if (input.scope === 'room' && !input.room_id) return { ok: false, status: 400, error: 'room_id is required' };
  if (input.scope === 'agent' && !input.room_agent_id) return { ok: false, status: 400, error: 'room_agent_id is required' };
  if (input.scope === 'task' && !input.task_id) return { ok: false, status: 400, error: 'task_id is required' };
  if (input.room_id) {
    const room = roomRepo.get(input.room_id);
    if (!room || room.project_id !== input.projectId) return { ok: false, status: 400, error: 'room_id is invalid' };
  }
  if (input.room_agent_id) {
    const agent = roomAgentRepo.get(input.room_agent_id);
    if (!agent) return { ok: false, status: 400, error: 'room_agent_id is invalid' };
    const room = roomRepo.get(agent.room_id);
    if (!room || room.project_id !== input.projectId) return { ok: false, status: 400, error: 'room_agent_id is invalid' };
  }
  if (input.task_id) {
    const task = taskRepo.get(input.task_id);
    if (!task || task.project_id !== input.projectId) return { ok: false, status: 400, error: 'task_id is invalid' };
  }
  return { ok: true };
}
```

- [x] **Step 4: 增加 API 路由**

在 `router.get('/projects/:id'...)` 之后加入：

```ts
router.get('/projects/:projectId/memories', (req, res) => {
  if (!projectRepo.get(req.params.projectId)) return res.status(404).json({ error: 'project not found' });
  res.json(memoryRepo.list({
    projectId: req.params.projectId,
    roomId: typeof req.query.roomId === 'string' ? req.query.roomId : undefined,
    roomAgentId: typeof req.query.roomAgentId === 'string' ? req.query.roomAgentId : undefined,
    taskId: typeof req.query.taskId === 'string' ? req.query.taskId : undefined,
  }));
});

router.post('/projects/:projectId/memories', (req, res) => {
  const parsed = memoryInputSchema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  const scopeCheck = validateMemoryScope({
    projectId: req.params.projectId,
    scope: parsed.data.scope,
    room_id: parsed.data.room_id,
    room_agent_id: parsed.data.room_agent_id,
    task_id: parsed.data.task_id,
  });
  if (!scopeCheck.ok) return res.status(scopeCheck.status).json({ error: scopeCheck.error });
  const memory = memoryRepo.create({
    project_id: req.params.projectId,
    room_id: parsed.data.room_id ?? null,
    room_agent_id: parsed.data.room_agent_id ?? null,
    task_id: parsed.data.task_id ?? null,
    scope: parsed.data.scope,
    memory_type: parsed.data.memory_type,
    title: parsed.data.title,
    content: parsed.data.content,
    source_type: parsed.data.source_type ?? 'manual',
    source_id: parsed.data.source_id ?? null,
    pinned: parsed.data.pinned ?? false,
  });
  res.status(201).json(memory);
});

router.patch('/memories/:id', (req, res) => {
  const schema = memoryInputSchema.pick({
    memory_type: true,
    title: true,
    content: true,
    pinned: true,
  }).partial();
  const parsed = schema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  const updated = memoryRepo.update(req.params.id, parsed.data);
  if (!updated) return res.status(404).json({ error: 'not found' });
  res.json(updated);
});

router.delete('/memories/:id', (req, res) => {
  const ok = memoryRepo.delete(req.params.id);
  res.status(ok ? 204 : 404).end();
});
```

- [x] **Step 5: 运行 API 测试**

Run: `npm run test -w @openclaw-room/backend -- src/memory/routes.test.ts`

Expected: PASS，输出包含 `# pass 1`。

- [x] **Step 6: 提交 Task 2**

```bash
git add packages/backend/src/routes.ts packages/backend/src/memory/routes.test.ts
git commit -m "feat(backend): 暴露项目聊天室记忆API"
```

## Task 3: 记忆上下文格式化与普通聊天注入

**Files:**
- Create: `packages/backend/src/memory/context.ts`
- Create: `packages/backend/src/memory/context.test.ts`
- Modify: `packages/backend/src/dispatcher.ts`

- [x] **Step 1: 编写上下文格式化测试**

创建 `packages/backend/src/memory/context.test.ts`：

```ts
import assert from 'node:assert/strict';
import test from 'node:test';
import { appendMemoryContext, formatMemoryContext } from './context.js';
import type { MemoryEntry } from '../types.js';

function memory(overrides: Partial<MemoryEntry>): MemoryEntry {
  return {
    id: 'mem-1',
    project_id: 'project-1',
    room_id: null,
    room_agent_id: null,
    task_id: null,
    scope: 'project',
    memory_type: 'decision',
    title: 'Default title',
    content: 'Default content',
    source_type: 'manual',
    source_id: null,
    pinned: 0,
    created_at: 1,
    updated_at: 1,
    ...overrides,
  };
}

test('formatMemoryContext returns an empty string when no memories exist', () => {
  assert.equal(formatMemoryContext([]), '');
});

test('formatMemoryContext renders bounded labeled memory block', () => {
  const output = formatMemoryContext([
    memory({ memory_type: 'decision', scope: 'project', pinned: 1, title: 'Use memory', content: 'Inject memory into prompts.' }),
    memory({ id: 'mem-2', memory_type: 'lesson', scope: 'room', title: 'Room lesson', content: 'Keep entries short.' }),
  ]);

  assert.match(output, /项目\/聊天室记忆：/);
  assert.match(output, /1\. \[决策；project；置顶\] Use memory/);
  assert.match(output, /2\. \[经验；room\] Room lesson/);
});

test('appendMemoryContext keeps the original prompt when no memories exist', () => {
  assert.equal(appendMemoryContext('Hello', []), 'Hello');
});

test('appendMemoryContext prepends memory before current request', () => {
  const output = appendMemoryContext('用户问题', [
    memory({ title: 'Known fact', content: 'The project is local-first.' }),
  ]);

  assert.match(output, /^项目\/聊天室记忆：/);
  assert.match(output, /当前请求：\n用户问题$/);
});
```

- [x] **Step 2: 运行测试确认失败**

Run: `npm run test -w @openclaw-room/backend -- src/memory/context.test.ts`

Expected: FAIL，错误包含 `Cannot find module './context.js'`。

- [x] **Step 3: 创建上下文格式化模块**

创建 `packages/backend/src/memory/context.ts`：

```ts
import type { MemoryEntry } from '../types.js';

const TYPE_LABEL: Record<MemoryEntry['memory_type'], string> = {
  decision: '决策',
  fact: '事实',
  preference: '偏好',
  lesson: '经验',
  task_summary: '任务总结',
  artifact_summary: '产物摘要',
};

export function formatMemoryContext(entries: MemoryEntry[]): string {
  if (entries.length === 0) return '';
  const lines = entries.map((entry, index) => {
    const pin = entry.pinned ? '；置顶' : '';
    return `${index + 1}. [${TYPE_LABEL[entry.memory_type]}；${entry.scope}${pin}] ${entry.title}\n${entry.content}`;
  });
  return ['项目/聊天室记忆：', ...lines].join('\n');
}

export function appendMemoryContext(prompt: string, entries: MemoryEntry[]): string {
  const memory = formatMemoryContext(entries);
  if (!memory) return prompt;
  return [memory, '', '当前请求：', prompt].join('\n');
}
```

- [x] **Step 4: 修改普通聊天调度注入记忆**

在 `packages/backend/src/dispatcher.ts` 顶部加入：

```ts
import { appendMemoryContext } from './memory/context.js';
import { memoryRepo } from './repos/memory.js';
```

把 `respondAsAgent` 开头改为：

```ts
export async function respondAsAgent(args: {
  agent: RoomAgent;
  projectPath: string;
  roomId: string;
  prompt: string;
  taskId?: string | null;
  workflowRunId?: string | null;
  workflowStepId?: string | null;
  workflowStage?: WorkflowStage | null;
  onRunCreated?: (run: AgentRun) => Promise<void> | void;
  onFinished?: (result: { run: AgentRun; message: Message; status: AgentRunStatus }) => Promise<void> | void;
}): Promise<void> {
  const { agent, projectPath, roomId } = args;
  const room = roomRepo.get(roomId);
  const prompt = room
    ? appendMemoryContext(
        args.prompt,
        memoryRepo.listForRoomContext({
          projectId: room.project_id,
          roomId,
          roomAgentId: agent.id,
          taskId: args.taskId,
        }),
      )
    : args.prompt;
  const backend = agent.acp_enabled && agent.acp_backend ? agent.acp_backend : 'openclaw';
```

- [x] **Step 5: 运行上下文测试和后端测试**

Run: `npm run test -w @openclaw-room/backend -- src/memory/context.test.ts`

Expected: PASS，输出包含 `# pass 4`。

Run: `npm run test -w @openclaw-room/backend`

Expected: PASS，所有后端测试通过。

- [x] **Step 6: 提交 Task 3**

```bash
git add packages/backend/src/memory/context.ts packages/backend/src/memory/context.test.ts packages/backend/src/dispatcher.ts
git commit -m "feat(backend): 向智能体注入聊天室记忆"
```

## Task 4: Workflow 记忆注入与自动任务总结

**Files:**
- Modify: `packages/backend/src/workflows/prompts.ts`
- Modify: `packages/backend/src/workflows/orchestrator.ts`
- Create: `packages/backend/src/workflows/memory.test.ts`

- [x] **Step 1: 编写 workflow prompt 记忆测试**

创建 `packages/backend/src/workflows/memory.test.ts`：

```ts
import assert from 'node:assert/strict';
import test from 'node:test';
import { buildStagePrompt } from './prompts.js';

test('buildStagePrompt includes memory context when provided', () => {
  const prompt = buildStagePrompt('analysis', {
    projectName: 'Memory Project',
    projectPath: '/tmp/memory-project',
    room: {
      id: 'room-1',
      project_id: 'project-1',
      name: 'Memory Room',
      description: null,
      created_at: 1,
    },
    task: {
      id: 'task-1',
      room_id: 'room-1',
      project_id: 'project-1',
      parent_task_id: null,
      title: 'Build memory',
      description: 'Add memory context',
      status: 'todo',
      priority: 'normal',
      interaction_mode: 'ask_user',
      assigned_agent_id: null,
      created_at: 1,
      updated_at: 1,
      completed_at: null,
    },
    agents: [],
    artifacts: [],
    memoryContext: '项目/聊天室记忆：\n1. [决策；project] Use explicit memory\nInject it.',
  });

  assert.match(prompt, /项目\/聊天室记忆：/);
  assert.match(prompt, /Use explicit memory/);
});
```

- [x] **Step 2: 运行测试确认失败**

Run: `npm run test -w @openclaw-room/backend -- src/workflows/memory.test.ts`

Expected: FAIL，`memoryContext` 没有出现在 prompt 中。

- [x] **Step 3: 扩展 workflow prompt context**

在 `packages/backend/src/workflows/prompts.ts` 的 `PromptContext` 加入：

```ts
  memoryContext?: string;
```

把 `baseContext` 改为：

```ts
function baseContext(context: PromptContext): string {
  return [
    `项目：${context.projectName}`,
    `路径：${context.projectPath}`,
    `聊天室：${context.room.name}`,
    `任务：${context.task.title}`,
    `描述：${context.task.description ?? '无'}`,
    '',
    '可用智能体：',
    formatAgents(context.agents),
    '',
    context.memoryContext || '项目/聊天室记忆：暂无相关记忆。',
  ].join('\n');
}
```

- [x] **Step 4: 修改 orchestrator 注入记忆**

在 `packages/backend/src/workflows/orchestrator.ts` 顶部加入：

```ts
import { formatMemoryContext } from '../memory/context.js';
import { memoryRepo } from '../repos/memory.js';
```

在 `startAgentStage` 和 `startAgentStageWithAgent` 调用 `buildStagePrompt` 时加入：

```ts
    memoryContext: formatMemoryContext(memoryRepo.listForRoomContext({
      projectId: context.project.id,
      roomId: context.room.id,
      roomAgentId: agent.id,
      taskId: task.id,
    })),
```

- [x] **Step 5: 验收通过后写入任务总结记忆**

在 `finishAcceptance` 的 `if (verdict.verdict === 'pass')` 分支中，在 `updateTaskStatus(run.task_id, 'done')` 之后加入：

```ts
    const task = requireTask(run.task_id);
    memoryRepo.upsertTaskSummary({
      project_id: run.project_id,
      room_id: run.room_id,
      task_id: run.task_id,
      title: `任务完成：${task.title}`,
      content: [
        `任务：${task.title}`,
        `验收结论：通过`,
        '',
        '验收摘要：',
        output,
      ].join('\n'),
      source_id: run.id,
    });
```

- [x] **Step 6: 运行 workflow 记忆测试和后端测试**

Run: `npm run test -w @openclaw-room/backend -- src/workflows/memory.test.ts`

Expected: PASS，输出包含 `# pass 1`。

Run: `npm run test -w @openclaw-room/backend`

Expected: PASS，所有后端测试通过。

- [x] **Step 7: 提交 Task 4**

```bash
git add packages/backend/src/workflows/prompts.ts packages/backend/src/workflows/orchestrator.ts packages/backend/src/workflows/memory.test.ts
git commit -m "feat(workflow): 记住完成任务摘要"
```

## Task 5: 前端记忆类型与 API

**Files:**
- Modify: `packages/frontend/src/lib/types.ts`
- Modify: `packages/frontend/src/lib/api.ts`

- [ ] **Step 1: 增加前端类型**

在 `packages/frontend/src/lib/types.ts` 的基础类型区域加入：

```ts
export type MemoryScope = 'project' | 'room' | 'agent' | 'task';
export type MemoryType = 'decision' | 'fact' | 'preference' | 'lesson' | 'task_summary' | 'artifact_summary';
export type MemorySourceType = 'manual' | 'message' | 'workflow' | 'task';

export interface MemoryEntry {
  id: string;
  project_id: string;
  room_id: string | null;
  room_agent_id: string | null;
  task_id: string | null;
  scope: MemoryScope;
  memory_type: MemoryType;
  title: string;
  content: string;
  source_type: MemorySourceType;
  source_id: string | null;
  pinned: 0 | 1;
  created_at: number;
  updated_at: number;
}

export interface MemoryInput {
  scope: MemoryScope;
  memory_type: MemoryType;
  title: string;
  content: string;
  room_id?: string | null;
  room_agent_id?: string | null;
  task_id?: string | null;
  source_type?: MemorySourceType;
  source_id?: string | null;
  pinned?: boolean;
}
```

在 label 区加入：

```ts
export const MEMORY_SCOPE_LABEL: Record<MemoryScope, string> = {
  project: '项目',
  room: '聊天室',
  agent: '智能体',
  task: '任务',
};

export const MEMORY_TYPE_LABEL: Record<MemoryType, string> = {
  decision: '决策',
  fact: '事实',
  preference: '偏好',
  lesson: '经验',
  task_summary: '任务总结',
  artifact_summary: '产物摘要',
};
```

- [ ] **Step 2: 增加 API 类型 import**

在 `packages/frontend/src/lib/api.ts` 的 import type 列表加入：

```ts
  MemoryEntry,
  MemoryInput,
```

- [ ] **Step 3: 增加 API 方法**

在 `api` 对象中 `getProject` 附近加入：

```ts
  listMemories: (
    projectId: string,
    filters: { roomId?: string; roomAgentId?: string; taskId?: string } = {},
  ) => {
    const params = new URLSearchParams();
    if (filters.roomId) params.set('roomId', filters.roomId);
    if (filters.roomAgentId) params.set('roomAgentId', filters.roomAgentId);
    if (filters.taskId) params.set('taskId', filters.taskId);
    const query = params.toString();
    return request<MemoryEntry[]>(`/projects/${projectId}/memories${query ? `?${query}` : ''}`);
  },
  createMemory: (projectId: string, input: MemoryInput) =>
    request<MemoryEntry>(`/projects/${projectId}/memories`, { method: 'POST', body: JSON.stringify(input) }),
  updateMemory: (
    id: string,
    input: Partial<Pick<MemoryInput, 'memory_type' | 'title' | 'content' | 'pinned'>>,
  ) =>
    request<MemoryEntry>(`/memories/${id}`, { method: 'PATCH', body: JSON.stringify(input) }),
  deleteMemory: (id: string) => request<void>(`/memories/${id}`, { method: 'DELETE' }),
```

- [ ] **Step 4: 运行前端类型检查**

Run: `npm run build -w @openclaw-room/frontend`

Expected: PASS，Vite build 成功。

- [ ] **Step 5: 提交 Task 5**

```bash
git add packages/frontend/src/lib/types.ts packages/frontend/src/lib/api.ts
git commit -m "feat(frontend): 增加记忆类型和API"
```

## Task 6: 前端通用记忆面板

**Files:**
- Create: `packages/frontend/src/components/MemoryPanel.tsx`

- [ ] **Step 1: 创建记忆面板组件**

创建 `packages/frontend/src/components/MemoryPanel.tsx`：

```tsx
import { FormEvent, useMemo, useState } from 'react';
import { Pin, Plus, Save, Trash2, X } from 'lucide-react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { toast } from 'sonner';
import { api } from '../lib/api';
import {
  MEMORY_SCOPE_LABEL,
  MEMORY_TYPE_LABEL,
  MemoryEntry,
  MemoryInput,
  MemoryScope,
  MemoryType,
  RoomAgent,
  Task,
} from '../lib/types';
import { cn } from '../lib/utils';
import { Button } from './ui/Button';
import { Input } from './ui/Input';

interface MemoryPanelProps {
  projectId: string;
  roomId?: string;
  roomAgents?: RoomAgent[];
  task?: Task;
  defaultScope?: MemoryScope;
  compact?: boolean;
}

const MEMORY_TYPES: MemoryType[] = ['decision', 'fact', 'preference', 'lesson', 'task_summary', 'artifact_summary'];

export function MemoryPanel({
  projectId,
  roomId,
  roomAgents = [],
  task,
  defaultScope = roomId ? 'room' : 'project',
  compact = false,
}: MemoryPanelProps) {
  const queryClient = useQueryClient();
  const [editing, setEditing] = useState<MemoryEntry | null>(null);
  const [showForm, setShowForm] = useState(false);
  const filters = useMemo(() => ({ roomId, taskId: task?.id }), [roomId, task?.id]);
  const { data: memories = [] } = useQuery({
    queryKey: ['memories', projectId, filters],
    queryFn: () => api.listMemories(projectId, filters),
  });
  const invalidate = () => queryClient.invalidateQueries({ queryKey: ['memories', projectId] });
  const createMutation = useMutation({
    mutationFn: (input: MemoryInput) => api.createMemory(projectId, input),
    onSuccess: () => {
      toast.success('记忆已保存');
      setShowForm(false);
      invalidate();
    },
  });
  const updateMutation = useMutation({
    mutationFn: ({ id, input }: { id: string; input: Partial<MemoryInput> }) => api.updateMemory(id, input),
    onSuccess: () => {
      toast.success('记忆已更新');
      setEditing(null);
      invalidate();
    },
  });
  const deleteMutation = useMutation({
    mutationFn: api.deleteMemory,
    onSuccess: () => {
      toast.success('记忆已删除');
      invalidate();
    },
  });

  return (
    <section className={cn('space-y-3', compact && 'text-[12px]')}>
      <div className="flex items-center justify-between gap-2">
        <div>
          <h3 className="font-display text-sm font-semibold text-[var(--color-fg)]">记忆</h3>
          <p className="text-[11px] text-[var(--color-fg-muted)]">会注入后续智能体上下文</p>
        </div>
        <Button size="sm" variant="secondary" onClick={() => setShowForm(true)}>
          <Plus className="h-4 w-4" />
          新增
        </Button>
      </div>

      {(showForm || editing) && (
        <MemoryForm
          projectId={projectId}
          roomId={roomId}
          roomAgents={roomAgents}
          task={task}
          defaultScope={defaultScope}
          value={editing}
          onCancel={() => {
            setShowForm(false);
            setEditing(null);
          }}
          onSubmit={(input) => {
            if (editing) updateMutation.mutate({ id: editing.id, input });
            else createMutation.mutate(input);
          }}
        />
      )}

      <div className="space-y-2">
        {memories.length === 0 ? (
          <div className="rounded-md border border-dashed border-[var(--color-border)] p-3 text-[12px] text-[var(--color-fg-muted)]">
            暂无记忆
          </div>
        ) : (
          memories.map((memory) => (
            <article key={memory.id} className="rounded-md border border-[var(--color-border)] bg-[var(--color-surface)] p-3">
              <div className="flex items-start justify-between gap-2">
                <div className="min-w-0">
                  <div className="flex flex-wrap items-center gap-1.5">
                    <span className="rounded border border-[var(--color-border)] px-1.5 py-0.5 text-[10px] text-[var(--color-fg-muted)]">
                      {MEMORY_SCOPE_LABEL[memory.scope]}
                    </span>
                    <span className="rounded border border-[var(--color-border)] px-1.5 py-0.5 text-[10px] text-[var(--color-fg-muted)]">
                      {MEMORY_TYPE_LABEL[memory.memory_type]}
                    </span>
                    {memory.pinned ? <Pin className="h-3 w-3 text-[var(--color-accent)]" /> : null}
                  </div>
                  <h4 className="mt-1 break-words text-[13px] font-medium text-[var(--color-fg)]">{memory.title}</h4>
                </div>
                <div className="flex shrink-0 items-center gap-1">
                  <Button size="icon" variant="ghost" onClick={() => setEditing(memory)} aria-label="编辑记忆">
                    <Save className="h-4 w-4" />
                  </Button>
                  <Button size="icon" variant="ghost" onClick={() => deleteMutation.mutate(memory.id)} aria-label="删除记忆">
                    <Trash2 className="h-4 w-4" />
                  </Button>
                </div>
              </div>
              <p className="mt-2 whitespace-pre-wrap break-words text-[12px] leading-5 text-[var(--color-fg-muted)]">
                {memory.content}
              </p>
            </article>
          ))
        )}
      </div>
    </section>
  );
}

function MemoryForm({
  roomId,
  roomAgents,
  task,
  defaultScope,
  value,
  onCancel,
  onSubmit,
}: {
  projectId: string;
  roomId?: string;
  roomAgents: RoomAgent[];
  task?: Task;
  defaultScope: MemoryScope;
  value: MemoryEntry | null;
  onCancel: () => void;
  onSubmit: (input: MemoryInput) => void;
}) {
  const [scope, setScope] = useState<MemoryScope>(value?.scope ?? defaultScope);
  const [memoryType, setMemoryType] = useState<MemoryType>(value?.memory_type ?? 'fact');
  const [title, setTitle] = useState(value?.title ?? '');
  const [content, setContent] = useState(value?.content ?? '');
  const [roomAgentId, setRoomAgentId] = useState(value?.room_agent_id ?? '');
  const [pinned, setPinned] = useState(Boolean(value?.pinned));

  const submit = (event: FormEvent) => {
    event.preventDefault();
    onSubmit({
      scope,
      memory_type: memoryType,
      title: title.trim(),
      content: content.trim(),
      room_id: scope === 'room' || scope === 'agent' || scope === 'task' ? roomId ?? null : null,
      room_agent_id: scope === 'agent' ? roomAgentId || null : null,
      task_id: scope === 'task' ? task?.id ?? null : null,
      pinned,
    });
  };

  return (
    <form className="space-y-2 rounded-md border border-[var(--color-border)] bg-[var(--color-surface-subtle)] p-3" onSubmit={submit}>
      <div className="grid grid-cols-2 gap-2">
        <select className="h-9 rounded-md border border-[var(--color-border)] bg-[var(--color-surface)] px-2 text-sm" value={scope} onChange={(event) => setScope(event.target.value as MemoryScope)}>
          <option value="project">项目</option>
          {roomId ? <option value="room">聊天室</option> : null}
          {roomAgents.length > 0 ? <option value="agent">智能体</option> : null}
          {task ? <option value="task">任务</option> : null}
        </select>
        <select className="h-9 rounded-md border border-[var(--color-border)] bg-[var(--color-surface)] px-2 text-sm" value={memoryType} onChange={(event) => setMemoryType(event.target.value as MemoryType)}>
          {MEMORY_TYPES.map((type) => <option key={type} value={type}>{MEMORY_TYPE_LABEL[type]}</option>)}
        </select>
      </div>
      {scope === 'agent' ? (
        <select className="h-9 w-full rounded-md border border-[var(--color-border)] bg-[var(--color-surface)] px-2 text-sm" value={roomAgentId} onChange={(event) => setRoomAgentId(event.target.value)} required>
          <option value="">选择智能体</option>
          {roomAgents.map((agent) => <option key={agent.id} value={agent.id}>{agent.agent_name}</option>)}
        </select>
      ) : null}
      <Input value={title} onChange={(event) => setTitle(event.target.value)} placeholder="标题" required />
      <textarea className="min-h-24 w-full resize-y rounded-md border border-[var(--color-border)] bg-[var(--color-surface)] px-3 py-2 text-sm text-[var(--color-fg)] outline-none" value={content} onChange={(event) => setContent(event.target.value)} placeholder="记忆内容" required />
      <label className="flex items-center gap-2 text-[12px] text-[var(--color-fg-muted)]">
        <input type="checkbox" checked={pinned} onChange={(event) => setPinned(event.target.checked)} />
        置顶
      </label>
      <div className="flex justify-end gap-2">
        <Button type="button" variant="ghost" onClick={onCancel}>
          <X className="h-4 w-4" />
          取消
        </Button>
        <Button type="submit">
          <Save className="h-4 w-4" />
          保存
        </Button>
      </div>
    </form>
  );
}
```

- [ ] **Step 2: 运行前端 build**

Run: `npm run build -w @openclaw-room/frontend`

Expected: PASS，TypeScript 和 Vite 构建成功。

- [ ] **Step 3: 提交 Task 6**

```bash
git add packages/frontend/src/components/MemoryPanel.tsx
git commit -m "feat(frontend): 新增记忆管理面板"
```

## Task 7: 聊天室和任务详情接入记忆 UI

**Files:**
- Modify: `packages/frontend/src/pages/RoomPage.tsx`
- Modify: `packages/frontend/src/components/TaskDetailPanel.tsx`

- [ ] **Step 1: RoomPage 引入 MemoryPanel**

在 `packages/frontend/src/pages/RoomPage.tsx` 顶部加入：

```ts
import { MemoryPanel } from '../components/MemoryPanel';
```

在 `RoomPage` 的右侧栏任务区域下方加入：

```tsx
{project ? (
  <div className="border-t border-[var(--color-border)] p-4">
    <MemoryPanel
      projectId={project.id}
      roomId={roomId}
      roomAgents={agents}
      defaultScope="room"
      compact
    />
  </div>
) : null}
```

- [ ] **Step 2: 聊天消息保存为记忆**

在 `ChatPane` props 中加入：

```ts
projectId?: string;
```

在 `ChatPane` 内增加 mutation：

```ts
const saveMessageMemory = useMutation({
  mutationFn: (message: Message) =>
    api.createMemory(projectId!, {
      scope: 'room',
      memory_type: message.sender_type === 'agent' ? 'lesson' : 'fact',
      title: message.content.slice(0, 80) || '聊天记忆',
      content: message.content,
      room_id: roomId,
      source_type: 'message',
      source_id: message.id,
    }),
  onSuccess: () => {
    toast.success('已保存为记忆');
    queryClient.invalidateQueries({ queryKey: ['memories', projectId] });
  },
});
```

在每个消息气泡操作区加入：

```tsx
{projectId && message.content.trim() ? (
  <Button
    size="sm"
    variant="ghost"
    onClick={() => saveMessageMemory.mutate(message)}
  >
    保存为记忆
  </Button>
) : null}
```

- [ ] **Step 3: TaskDetailPanel 接入任务记忆**

修改 `packages/frontend/src/components/TaskDetailPanel.tsx` 的 props：

```ts
export function TaskDetailPanel({
  task,
  agents,
  onClose,
  projectId,
}: {
  task: Task;
  agents: RoomAgent[];
  onClose: () => void;
  projectId: string;
}) {
```

在文件顶部加入：

```ts
import { MemoryPanel } from './MemoryPanel';
```

在详情内容区域加入：

```tsx
<div className="border-t border-[var(--color-border)] pt-4">
  <MemoryPanel
    projectId={projectId}
    roomId={task.room_id}
    roomAgents={agents}
    task={task}
    defaultScope="task"
    compact
  />
</div>
```

- [ ] **Step 4: RoomPage 传递 projectId 给 TaskDetailPanel**

把 `TaskDetailPanel` 调用改为：

```tsx
{selectedTask && project ? (
  <TaskDetailPanel
    task={selectedTask}
    agents={agents}
    projectId={project.id}
    onClose={() => setSelectedTaskId(null)}
  />
) : null}
```

- [ ] **Step 5: 运行前端 build**

Run: `npm run build -w @openclaw-room/frontend`

Expected: PASS，TypeScript 和 Vite 构建成功。

- [ ] **Step 6: 提交 Task 7**

```bash
git add packages/frontend/src/pages/RoomPage.tsx packages/frontend/src/components/TaskDetailPanel.tsx
git commit -m "feat(frontend): 在聊天室和任务详情管理记忆"
```

## Task 8: 最终验证、代码审查和收尾

**Files:**
- Modify: `docs/superpowers/plans/2026-05-14-项目聊天室记忆机制.md`

- [ ] **Step 1: 运行后端测试**

Run: `npm run test -w @openclaw-room/backend`

Expected: PASS，所有后端测试通过，至少包含 memory repo、memory routes、memory context、workflow memory 测试。

- [ ] **Step 2: 运行整仓 build**

Run: `npm run build`

Expected: PASS，后端 `tsc`、前端 `tsc -b` 和 Vite build 成功。

- [ ] **Step 3: 人工代码审查清单**

检查以下具体风险：

```text
1. memory_entries 的 project_id、room_id、room_agent_id、task_id 校验是否防止跨项目引用。
2. 普通聊天和 workflow prompt 是否只注入当前 project/room/agent/task 相关记忆。
3. 自动任务总结是否只在 acceptance pass 后写入。
4. 前端删除记忆是否只调用 DELETE /memories/:id，没有误删 task/message。
5. 记忆内容是否有长度上限，避免单条记忆撑爆 prompt。
```

- [ ] **Step 4: 更新计划勾选**

把本文件中已经完成的步骤从 `- [ ]` 改为 `- [x]`，只勾选真实完成且验证过的步骤。

- [ ] **Step 5: 提交最终计划勾选**

```bash
git add docs/superpowers/plans/2026-05-14-项目聊天室记忆机制.md
git commit -m "docs: 更新项目聊天室记忆机制计划进度"
```

## Self-Review

**Spec coverage:** 本计划覆盖显式记忆存储、API、普通聊天 prompt 注入、workflow prompt 注入、任务完成自动沉淀、前端管理入口、消息保存为记忆和任务详情记忆查看。OpenClaw 原生 memory 同步未纳入本轮，因为本轮目标是 OpenClaw Room 的项目/聊天室级可控记忆层。

**Placeholder scan:** 已检查计划中没有占位式步骤。每个实现步骤包含具体文件、代码片段、命令和预期结果。

**Type consistency:** 后端和前端统一使用 `MemoryEntry`、`MemoryScope`、`MemoryType`、`MemorySourceType`、`memory_type`、`room_agent_id`、`source_type`、`source_id` 字段。API 路径统一为 `GET/POST /projects/:projectId/memories`、`PATCH/DELETE /memories/:id`。
