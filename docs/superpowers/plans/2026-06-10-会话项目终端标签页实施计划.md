# 会话项目终端标签页 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在会话页面中间工作区的「文件浏览器」右侧新增固定「项目终端」标签页，首次切入时懒启动拥有当前项目目录本机 shell 权限的模拟终端。

**Architecture:** 前端在 `SessionCenterWorkspace` 的 FlexLayout model 中增加第三个固定 tab，使用 `enableRenderOnDemand` 保证终端首次可见时才渲染。终端 UI 复用 `TerminalPanel`，通过 `profile="project_shell"` 和 `projectId` 接入已有后端 terminal 服务；后端只补回归测试，验证 `project_shell` 的 cwd 来自项目路径。

**Tech Stack:** React 18、TypeScript、FlexLayout React、xterm、Express、node-pty、Node test runner。

---

## Scope Check

本计划只覆盖一个功能面：会话中间工作区新增项目终端 tab。它不改 Skills 页安装终端、不新增终端后端 API、不做命令白名单或目录沙盒。

## File Structure

- Modify: `packages/frontend/src/session-ui/SessionCenterWorkspace.tsx`
  - 扩展 pane 类型、FlexLayout tab model、tab id 映射。
  - 新增项目终端 wrapper，懒加载 `TerminalPanel`。
  - 保持 SSR fallback 不初始化 xterm。
- Modify: `packages/frontend/src/session-ui/SessionCenterWorkspace.test.tsx`
  - 覆盖 SSR tab 文案、pane 映射、`project_shell` 接入、`enableRenderOnDemand`。
- Modify: `packages/frontend/src/session-ui/session-os.css`
  - 增加项目终端容器样式，确保终端填满 tab 内容区。
- Modify: `packages/backend/src/terminal/routes.test.ts`
  - 增加 `project_shell + projectId` 的 cwd 回归测试。

## Task 1: 前端测试先锁定第三个固定 tab

**Files:**
- Modify: `packages/frontend/src/session-ui/SessionCenterWorkspace.test.tsx`
- Test: `packages/frontend/src/session-ui/SessionCenterWorkspace.test.tsx`

- [x] **Step 1: 写入前端失败测试**

将 `packages/frontend/src/session-ui/SessionCenterWorkspace.test.tsx` 顶部 import 改为：

```ts
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import test from 'node:test';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import React from 'react';
import { renderToStaticMarkup } from 'react-dom/server';
import { getSessionCenterWorkspacePaneForTabId, SessionCenterWorkspace } from './SessionCenterWorkspace';

const source = readFileSync(new URL('./SessionCenterWorkspace.tsx', import.meta.url), 'utf8');
const sessionOsCss = readFileSync(new URL('./session-os.css', import.meta.url), 'utf8');
```

将第一个测试名称和断言改为：

```ts
test('SessionCenterWorkspace renders fixed transcript, file browser, and project terminal tabs', () => {
  const queryClient = new QueryClient();
  const html = renderToStaticMarkup(
    <QueryClientProvider client={queryClient}>
      <SessionCenterWorkspace
        projectId="project-1"
        workspaceRootPath="/workspace/project-1"
        transcript={<div data-test-transcript="true">Transcript</div>}
      />
    </QueryClientProvider>,
  );

  assert.match(html, /对话记录/);
  assert.match(html, /文件浏览器/);
  assert.match(html, /项目终端/);
  assert.match(html, /data-test-transcript="true"/);
});
```

将 pane 映射测试改为：

```ts
test('SessionCenterWorkspace maps center tabs to workspace panes', () => {
  assert.equal(getSessionCenterWorkspacePaneForTabId('session-transcript-tab'), 'transcript');
  assert.equal(getSessionCenterWorkspacePaneForTabId('session-file-browser-tab'), 'file-browser');
  assert.equal(getSessionCenterWorkspacePaneForTabId('session-project-terminal-tab'), 'project-terminal');
  assert.equal(getSessionCenterWorkspacePaneForTabId('unknown-tab'), null);
});
```

追加两个源码/样式断言测试：

```ts
test('SessionCenterWorkspace wires project terminal through the project shell profile', () => {
  assert.match(source, /React\.lazy/);
  assert.match(source, /TerminalPanel/);
  assert.match(source, /profile="project_shell"/);
  assert.match(source, /projectId=\{projectId\}/);
  assert.match(source, /title="项目终端"/);
  assert.match(source, /enableRenderOnDemand:\s*true/);
});

test('session workspace styles make the project terminal fill the tab content', () => {
  assert.match(sessionOsCss, /deepsea-project-terminal/);
  assert.match(sessionOsCss, /deepsea-project-terminal-panel/);
  assert.match(sessionOsCss, /height:\s*100%/);
  assert.match(sessionOsCss, /min-height:\s*0/);
});
```

- [x] **Step 2: 运行测试确认失败**

Run:

```bash
rtk npm --workspace packages/frontend exec node -- --import tsx --test src/session-ui/SessionCenterWorkspace.test.tsx
```

Expected: FAIL。失败点至少包含「项目终端」未出现或 `session-project-terminal-tab` 映射缺失。

## Task 2: 实现前端项目终端 tab

**Files:**
- Modify: `packages/frontend/src/session-ui/SessionCenterWorkspace.tsx`
- Modify: `packages/frontend/src/session-ui/session-os.css`
- Test: `packages/frontend/src/session-ui/SessionCenterWorkspace.test.tsx`

- [x] **Step 1: 更新 React imports 并懒加载 TerminalPanel**

在 `packages/frontend/src/session-ui/SessionCenterWorkspace.tsx` 中将 React import 改为：

```ts
import React, { Suspense, useMemo, useRef } from 'react';
```

在 import 区下方增加懒加载组件：

```ts
const LazyTerminalPanel = React.lazy(() => import('../components/TerminalPanel').then((module) => ({
  default: module.TerminalPanel,
})));
```

- [x] **Step 2: 扩展 pane 类型**

将类型改为：

```ts
export type SessionCenterWorkspacePane = 'transcript' | 'file-browser' | 'project-terminal';
```

- [x] **Step 3: SSR fallback 增加第三个 tab 文案**

在 SSR tabs 中追加：

```tsx
<span>项目终端</span>
```

结果应为：

```tsx
<div className="deepsea-center-workspace__ssr-tabs" aria-hidden="true">
  <span>对话记录</span>
  <span>文件浏览器</span>
  <span>项目终端</span>
</div>
```

- [x] **Step 4: factory 渲染项目终端**

在 factory 中 `file-browser` 分支后追加：

```tsx
if (component === 'project-terminal') {
  return <ProjectTerminalWorkspace projectId={projectId} />;
}
```

- [x] **Step 5: 新增项目终端 wrapper**

在 `SessionCenterWorkspace` 组件后、`createSessionCenterModel` 前增加：

```tsx
function ProjectTerminalWorkspace({ projectId }: { projectId: string }): JSX.Element {
  return (
    <div className="deepsea-project-terminal" aria-label="项目终端工作区">
      <Suspense fallback={<div className="deepsea-project-terminal-loading">正在准备项目终端...</div>}>
        <LazyTerminalPanel
          profile="project_shell"
          projectId={projectId}
          title="项目终端"
          className="deepsea-project-terminal-panel"
        />
      </Suspense>
    </div>
  );
}
```

- [x] **Step 6: FlexLayout model 增加第三个固定 tab**

在 `children` 数组的「文件浏览器」tab 后追加：

```ts
{
  type: 'tab',
  id: 'session-project-terminal-tab',
  name: '项目终端',
  component: 'project-terminal',
  enableClose: false,
  enableDrag: false,
  enableRenderOnDemand: true,
},
```

该 `enableRenderOnDemand: true` 是懒启动 shell 的关键约束。

- [x] **Step 7: 更新 tab id 映射**

将 mapper 改为：

```ts
export function getSessionCenterWorkspacePaneForTabId(tabId: string): SessionCenterWorkspacePane | null {
  if (tabId === 'session-transcript-tab') return 'transcript';
  if (tabId === 'session-file-browser-tab') return 'file-browser';
  if (tabId === 'session-project-terminal-tab') return 'project-terminal';
  return null;
}
```

- [x] **Step 8: 增加会话页终端样式**

在 `packages/frontend/src/session-ui/session-os.css` 的 `deepsea-center-workspace`/`deepsea-file-browser` 基础尺寸选择器中加入：

```css
.deepsea-project-terminal,
```

在 `.deepsea-center-workspace .flexlayout__tab` 规则后追加：

```css
.deepsea-project-terminal {
  display: flex;
  height: 100%;
  min-width: 0;
  min-height: 0;
  overflow: hidden;
  background: #071018;
}
.deepsea-project-terminal-panel {
  height: 100%;
  min-height: 0;
  flex: 1 1 auto;
  border: 0;
  border-radius: 0;
  box-shadow: none;
}
.deepsea-project-terminal-panel > div:last-child {
  min-height: 0;
}
.deepsea-project-terminal-loading {
  display: flex;
  flex: 1 1 auto;
  align-items: center;
  justify-content: center;
  min-height: 0;
  background: #071018;
  color: #d7e5ee;
  font-size: 12px;
  font-weight: 700;
}
```

- [x] **Step 9: 运行前端定向测试确认通过**

Run:

```bash
rtk npm --workspace packages/frontend exec node -- --import tsx --test src/session-ui/SessionCenterWorkspace.test.tsx
```

Expected: PASS。

- [x] **Step 10: 提交前端实现**

Run:

```bash
rtk git add packages/frontend/src/session-ui/SessionCenterWorkspace.tsx packages/frontend/src/session-ui/SessionCenterWorkspace.test.tsx packages/frontend/src/session-ui/session-os.css
rtk git commit -m "feat(session): 增加项目终端标签页"
```

Expected: commit 只包含上述三个文件。

## Task 3: 补后端 project shell cwd 回归测试

**Files:**
- Modify: `packages/backend/src/terminal/routes.test.ts`
- Test: `packages/backend/src/terminal/routes.test.ts`

- [x] **Step 1: 导入 projectRepo**

在 `const { router } = await import('../routes.js');` 后追加：

```ts
const { projectRepo } = await import('../repos/projects.js');
```

- [x] **Step 2: 添加 project shell cwd 测试**

在 `terminal routes create, get, and kill skills install sessions` 测试后追加：

```ts
test('terminal routes create project shell sessions in the project directory', async () => {
  const projectPath = mkdtempSync(join(tmpdir(), 'opendeepsea-terminal-project-shell-'));
  const project = projectRepo.create({ name: 'Terminal Project Shell', path: projectPath });

  const createRes = await request('/api/terminals', {
    method: 'POST',
    body: JSON.stringify({
      profile: 'project_shell',
      projectId: project.id,
      cols: 80,
      rows: 24,
    }),
  });
  assert.equal(createRes.status, 201);
  const created = await createRes.json() as {
    id: string;
    profile: string;
    cwd: string;
    status: string;
  };
  assert.equal(created.profile, 'project_shell');
  assert.equal(created.cwd, projectPath);
  assert.equal(created.status, 'running');

  const killRes = await request(`/api/terminals/${created.id}/kill`, { method: 'POST' });
  assert.equal(killRes.status, 204);
});
```

- [x] **Step 3: 运行后端定向测试**

Run:

```bash
rtk npm --workspace packages/backend exec node -- --import tsx --test src/terminal/routes.test.ts
```

Expected: PASS。该测试主要是回归覆盖；如果失败，先检查 `projectRepo.create` 是否拿到真实目录，以及 `terminalService.create` 是否被成功 kill。

- [x] **Step 4: 提交后端测试**

Run:

```bash
rtk git add packages/backend/src/terminal/routes.test.ts
rtk git commit -m "test(session): 覆盖项目终端工作目录"
```

Expected: commit 只包含 `packages/backend/src/terminal/routes.test.ts`。

## Task 4: 集成验证与最终收口

**Files:**
- Verify: `packages/frontend/src/session-ui/SessionCenterWorkspace.tsx`
- Verify: `packages/frontend/src/session-ui/SessionCenterWorkspace.test.tsx`
- Verify: `packages/frontend/src/session-ui/session-os.css`
- Verify: `packages/backend/src/terminal/routes.test.ts`

- [x] **Step 1: 运行前端和后端定向测试**

Run:

```bash
rtk npm --workspace packages/frontend exec node -- --import tsx --test src/session-ui/SessionCenterWorkspace.test.tsx
rtk npm --workspace packages/backend exec node -- --import tsx --test src/terminal/routes.test.ts src/terminal/socket-controller.test.ts
```

Expected: PASS。

- [x] **Step 2: 运行全量 build 门禁**

Run:

```bash
rtk npm run build
```

Expected: PASS。若因为当前工作区既有非本任务改动失败，记录失败文件和错误，不把失败归因为项目终端改动。

- [x] **Step 3: 检查实现满足显式需求**

Run:

```bash
rtk rg -n "项目终端|project-terminal|session-project-terminal-tab|project_shell|enableRenderOnDemand" packages/frontend/src/session-ui packages/backend/src/terminal
```

Expected:

- `SessionCenterWorkspace.tsx` 包含「项目终端」、`session-project-terminal-tab`、`project-terminal`、`profile="project_shell"`、`enableRenderOnDemand: true`。
- `SessionCenterWorkspace.test.tsx` 覆盖 tab 文案、映射和接入断言。
- `routes.test.ts` 覆盖 `project_shell` 的项目 cwd。

- [x] **Step 4: 检查本任务提交与工作区**

Run:

```bash
rtk git log --oneline -3
rtk git status --short
```

Expected:

- 最近提交包含本计划的前端实现和后端测试提交。
- `git status --short` 可能仍显示进入本任务前已经存在的未提交改动；不要回滚或纳入这些无关改动。

- [x] **Step 5: 最终代码审查**

检查以下点：

- `TerminalPanel` 没有被复制实现。
- `skills_install` 行为没有变化。
- `project_shell` 只通过会话页项目终端暴露。
- `enableRenderOnDemand: true` 存在，确保 shell 不会在初始进入会话页时启动。
- `isSessionInspectorVisibleForWorkspacePane` 仍只对 `transcript` 返回 true。

如果发现问题，修复后重新执行 Task 4 Step 1 和 Step 2。

## Self-Review

- Spec coverage:
  - 固定第三个 tab：Task 1、Task 2 覆盖。
  - 首次切入懒启动：Task 2 的 `enableRenderOnDemand: true` 覆盖。
  - 复用 Skills 页模拟终端：Task 2 复用 `TerminalPanel` 覆盖。
  - 当前项目目录权限和 cwd：Task 2 传 `projectId`，Task 3 验证 cwd。
  - 不新增后端 API、不改 `skills_install`：Task 3 仅补测试，Task 4 审查。
- Placeholder scan:
  - 未使用占位符或未完成标记。
  - 每个代码修改步骤都给出具体代码块。
- Type consistency:
  - `SessionCenterWorkspacePane`、`project-terminal`、`session-project-terminal-tab` 在测试、实现和映射中一致。
  - `TerminalPanel` props 使用现有 `profile`、`projectId`、`title`、`className`。
