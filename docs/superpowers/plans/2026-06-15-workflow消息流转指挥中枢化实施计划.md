# Workflow 消息流转指挥中枢化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将会话中间聊天区的 workflow 流转展示改为 Mission Strip + Run Flow Capsule 的指挥中枢式布局。

**Architecture:** 复用现有 `SessionShellView.tsx` 数据入口，不改后端 schema。顶部 workflow 总览合并为 `WorkflowMissionStrip`，每条 agent run 内部由 `RunFlowCapsule` 负责状态头、关键事件轴和正文区，事件归类保持纯函数以便测试。

**Tech Stack:** React 18、TypeScript、Vite、现有 CSS 文件 `session-os.css`、Node test runner、React server render 测试。

---

## 文件结构

- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
  - 新增 `WorkflowMissionStrip`、`WorkflowStageRail`、`WorkflowGateSummary`、`WorkflowAgentRoster`。
  - 新增 `RunFlowCapsule`、`RunEventRail`。
  - 新增事件归类与阶段状态纯函数。
  - 用 `WorkflowMissionStrip` 替换 transcript 顶部三块独立 workflow 面板。
  - 用 `RunFlowCapsule` 替换 agent run 内部散装结构。
- Modify: `packages/frontend/src/session-ui/session-os.css`
  - 新增 mission strip 与 run capsule 样式。
  - 保留现有 markdown、代码块、文件引用样式。
- Modify: `packages/frontend/src/session-ui/SessionShell.test.tsx`
  - 新增 mission strip 和 run capsule 渲染测试。
  - 更新旧 workflow panel 断言，使其匹配新结构。
- Optional Modify: `packages/frontend/src/session-ui/session-os-system.css`
  - 仅当系统主题局部覆盖导致新组件在特殊主题下不可读时补充变量级覆盖。

## Task 1: Mission Strip 纯函数与组件骨架

**Files:**
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
- Test: `packages/frontend/src/session-ui/SessionShell.test.tsx`

- [x] **Step 1: 添加 Mission Strip 渲染测试**

在 `packages/frontend/src/session-ui/SessionShell.test.tsx` 增加测试，构造 `workflowController`、`workflowArtifacts`、`workflowGates`、`workflowAgentAssignments`，断言新结构存在。

```tsx
test('SessionShell renders workflow mission strip with gate and agent summaries', () => {
  const payload = createSessionWorkspacePayload();
  payload.activeSession.workflowController = {
    workflow_run_id: 'workflow-run-1',
    selected_intent: 'workflow',
    controller: 'planner',
    active_stage: 'planning',
    next_action: '等待 plan gate',
    blocker: null,
  };
  payload.activeSession.workflowArtifacts = [{
    id: 'artifact-plan-1',
    workflow_run_id: 'workflow-run-1',
    artifact_type: 'plan',
    title: '实施计划',
    content: '# Plan\n\n- step',
    version: 1,
    status: 'reviewing',
    created_at: 1,
    updated_at: 1,
  }];
  payload.activeSession.workflowGates = [{
    id: 'gate-plan-1',
    workflow_run_id: 'workflow-run-1',
    artifact_version_id: 'artifact-plan-1',
    kind: 'plan_approval',
    status: 'pending',
    reason: '需要用户确认',
    created_at: 1,
    updated_at: 1,
  }];
  payload.activeSession.workflowAgentAssignments = [{
    workflow_run_id: 'workflow-run-1',
    task_id: 'task-1',
    task_title: '实现 Mission Strip',
    role: 'executor',
    execution_mode: 'serial',
    assigned_agent_id: 'agent-codex',
    assigned_agent_name: 'Codex',
    backend: 'codex',
    fallback_reason: null,
    scope_write: ['packages/frontend/src/session-ui/SessionShellView.tsx'],
  }];

  const html = renderSessionShell(payload, { onApproveWorkflowArtifact: () => undefined });
  assert.match(html, /data-workflow-mission-strip="true"/);
  assert.match(html, /Workflow Mission/);
  assert.match(html, /等待 plan gate/);
  assert.match(html, /1 个门禁/);
  assert.match(html, /Codex/);
  assert.match(html, /data-workflow-artifact-action="approve"/);
});
```

- [x] **Step 2: 运行测试确认失败**

Run: `node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx`

Expected: FAIL，原因包含 `data-workflow-mission-strip="true"` 未找到。

- [x] **Step 3: 实现 Mission Strip 组件骨架**

在 `SessionShellView.tsx` 中替换 `TranscriptCanvas` 顶部三块独立调用：

```tsx
<WorkflowMissionStrip
  detail={detail}
  onApprove={onApproveWorkflowArtifact}
  onRequestChange={(artifact) => onSendMessage({
    content: buildWorkflowArtifactChangeRequestContent(artifact),
    workflowArtifactChangeRequest: {
      workflowRunId: artifact.workflow_run_id,
      artifactVersionId: artifact.id,
      artifactType: artifact.artifact_type,
    },
  })}
/>
```

新增组件：

```tsx
function WorkflowMissionStrip({
  detail,
  onApprove,
  onRequestChange,
}: {
  detail: SessionDetail;
  onApprove?: (artifactVersionId: string) => void;
  onRequestChange: (artifact: WorkflowArtifactVersionView) => void;
}): JSX.Element | null {
  const controller = detail.workflowController ?? null;
  const artifacts = detail.workflowArtifacts ?? [];
  const gates = detail.workflowGates ?? [];
  const assignments = detail.workflowAgentAssignments ?? [];
  if (!controller && gates.length === 0 && assignments.length === 0) return null;

  return (
    <section className="deepsea-workflow-mission" data-workflow-mission-strip="true" aria-label="Workflow Mission">
      <div className="deepsea-workflow-mission__header">
        <div>
          <span className="deepsea-status-chip" data-tone="ok">Workflow Mission</span>
          <h3>{detail.session.title}</h3>
          <p>{formatWorkflowMissionMeta(controller)}</p>
        </div>
        <div className="deepsea-workflow-mission__badges">
          <span>{formatPendingGateCount(gates)}</span>
          <span>{assignments.length} agents</span>
        </div>
      </div>
      <WorkflowStageRail controller={controller} gates={gates} />
      <WorkflowGateSummary artifacts={artifacts} gates={gates} onApprove={onApprove} onRequestChange={onRequestChange} />
      <WorkflowAgentRoster assignments={assignments} />
      {controller?.blocker ? <p className="deepsea-workflow-mission__blocker">{controller.blocker}</p> : null}
    </section>
  );
}
```

- [x] **Step 4: 添加辅助函数**

在 `WorkflowMissionStrip` 附近添加：

```tsx
const WORKFLOW_STAGE_ORDER: WorkflowStage[] = ['analysis', 'planning', 'assignment', 'implementation', 'code_review', 'acceptance'];

function formatWorkflowMissionMeta(controller: WorkflowControllerView | null): string {
  if (!controller) return 'controller: pending · next: 等待 workflow 数据';
  return [
    `intent: ${controller.selected_intent ?? 'workflow'}`,
    `controller: ${controller.controller ?? 'planner'}`,
    `next: ${controller.next_action ?? '等待推进'}`,
  ].join(' · ');
}

function formatPendingGateCount(gates: WorkflowGateView[]): string {
  const pending = gates.filter((gate) => gate.status === 'pending').length;
  return pending > 0 ? `${pending} 个门禁` : '无待确认门禁';
}
```

- [x] **Step 5: 运行测试确认 Task 1 通过**

Run: `node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx`

Expected: 新增 mission strip 测试 PASS；若旧断言失败，进入 Task 2 更新旧断言。

## Task 2: Mission Strip 细节、阶段轨道、旧面板迁移

**Files:**
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
- Modify: `packages/frontend/src/session-ui/SessionShell.test.tsx`

- [x] **Step 1: 实现阶段状态函数**

在 `SessionShellView.tsx` 添加：

```tsx
type WorkflowMissionStageState = 'done' | 'active' | 'gate' | 'blocked' | 'failed' | 'pending';

function getWorkflowStageState(
  stage: WorkflowStage,
  controller: WorkflowControllerView | null,
  gates: WorkflowGateView[],
): WorkflowMissionStageState {
  if (controller?.blocker && controller.active_stage === stage) return 'blocked';
  if (gates.some((gate) => gate.status === 'pending') && controller?.active_stage === stage) return 'gate';
  if (controller?.active_stage === stage) return 'active';
  const activeIndex = controller?.active_stage ? WORKFLOW_STAGE_ORDER.indexOf(controller.active_stage) : -1;
  const stageIndex = WORKFLOW_STAGE_ORDER.indexOf(stage);
  if (activeIndex >= 0 && stageIndex >= 0 && stageIndex < activeIndex) return 'done';
  return 'pending';
}
```

- [x] **Step 2: 实现阶段轨道组件**

```tsx
function WorkflowStageRail({
  controller,
  gates,
}: {
  controller: WorkflowControllerView | null;
  gates: WorkflowGateView[];
}): JSX.Element {
  return (
    <ol className="deepsea-workflow-stage-rail" aria-label="Workflow 阶段">
      {WORKFLOW_STAGE_ORDER.map((stage) => {
        const state = getWorkflowStageState(stage, controller, gates);
        return (
          <li key={stage} className="deepsea-workflow-stage-rail__item" data-state={state}>
            <span>{formatWorkflowStageLabel(stage)}</span>
            <strong>{formatWorkflowStageStateLabel(state)}</strong>
          </li>
        );
      })}
    </ol>
  );
}

function formatWorkflowStageLabel(stage: WorkflowStage): string {
  if (stage === 'analysis') return '分析';
  if (stage === 'planning') return '计划';
  if (stage === 'assignment') return '分派';
  if (stage === 'implementation') return '实现';
  if (stage === 'code_review') return '审查';
  return '验收';
}

function formatWorkflowStageStateLabel(state: WorkflowMissionStageState): string {
  if (state === 'done') return 'done';
  if (state === 'active') return 'active';
  if (state === 'gate') return 'gate';
  if (state === 'blocked') return 'blocked';
  if (state === 'failed') return 'failed';
  return 'pending';
}
```

- [x] **Step 3: 实现 Gate 摘要组件**

```tsx
function WorkflowGateSummary({
  artifacts,
  gates,
  onApprove,
  onRequestChange,
}: {
  artifacts: WorkflowArtifactVersionView[];
  gates: WorkflowGateView[];
  onApprove?: (artifactVersionId: string) => void;
  onRequestChange: (artifact: WorkflowArtifactVersionView) => void;
}): JSX.Element | null {
  const artifactById = new Map(artifacts.map((artifact) => [artifact.id, artifact]));
  const gateItems = gates
    .filter((gate) => gate.artifact_version_id)
    .map((gate) => {
      const artifact = gate.artifact_version_id ? artifactById.get(gate.artifact_version_id) : undefined;
      return artifact ? { gate, artifact } : null;
    })
    .filter((item): item is { gate: WorkflowGateView; artifact: WorkflowArtifactVersionView } => Boolean(item));
  if (gateItems.length === 0) return null;

  return (
    <div className="deepsea-workflow-gate-summary" aria-label="Workflow 门禁摘要">
      {gateItems.map(({ gate, artifact }) => (
        <article className="deepsea-workflow-gate-summary__item" key={`${gate.kind}:${artifact.id}`} data-status={gate.status}>
          <div>
            <strong>{formatWorkflowArtifactHeading(artifact)}</strong>
            <span title={artifact.title}>{artifact.title}</span>
            <small>{gate.reason}</small>
          </div>
          <div className="deepsea-workflow-gate-summary__actions">
            <button type="button" data-workflow-artifact-action="request-change" onClick={() => onRequestChange(artifact)}>
              <Edit3 aria-hidden="true" />
              请求修改
            </button>
            {gate.status === 'pending' ? (
              <button type="button" data-workflow-artifact-action="approve" disabled={!onApprove} onClick={() => onApprove?.(artifact.id)}>
                <Check aria-hidden="true" />
                确认
              </button>
            ) : (
              <span className="deepsea-status-chip" data-tone="success">已确认</span>
            )}
          </div>
        </article>
      ))}
    </div>
  );
}
```

- [x] **Step 4: 实现 Agent Roster**

```tsx
function WorkflowAgentRoster({
  assignments,
}: {
  assignments: WorkflowAgentAssignmentView[];
}): JSX.Element | null {
  if (assignments.length === 0) return null;
  return (
    <div className="deepsea-workflow-agent-roster" aria-label="Workflow 子代理摘要">
      {assignments.map((assignment) => (
        <article className="deepsea-workflow-agent-roster__item" key={`${assignment.task_id}:${assignment.role}`}>
          <strong title={assignment.task_title}>{assignment.assigned_agent_name ?? assignment.assigned_agent_id ?? assignment.role}</strong>
          <span>{assignment.role} · {assignment.execution_mode}</span>
          <code title={assignment.scope_write.join(', ')}>{assignment.scope_write.join(', ') || 'scopeWrite 未声明'}</code>
        </article>
      ))}
    </div>
  );
}
```

- [x] **Step 5: 更新旧测试断言**

将旧测试中对 `data-workflow-artifact-panel="true"`、`data-workflow-controller-panel="true"`、`data-agent-assignment-table="true"` 的中间区断言改为 `data-workflow-mission-strip="true"`。右侧 inspector 测试不改。

- [x] **Step 6: 运行测试**

Run: `node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx`

Expected: SessionShell 相关测试 PASS。

## Task 3: Run Flow Capsule 事件归类与渲染

**Files:**
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
- Modify: `packages/frontend/src/session-ui/SessionShell.test.tsx`

- [x] **Step 1: 添加 run capsule 测试**

在 `SessionShell.test.tsx` 增加：

```tsx
test('SessionShell renders agent run as flow capsule with event rail', () => {
  const payload = createSessionWorkspacePayload();
  payload.activeSession.runs = [{
    id: 'run-1',
    session_id: payload.activeSession.session.id,
    agent_id: 'agent-codex',
    provider: 'codex',
    mode: 'write',
    status: 'running',
    stdout: '执行输出正文',
    stderr: '',
    activity_log: '',
    started_at: 1,
    updated_at: 2,
    completed_at: null,
    error_message: null,
    workflow_run_id: 'workflow-run-1',
    workflow_step_id: 'step-1',
    workflow_stage: 'implementation',
  }];
  payload.activeSession.agentEvents = [{
    id: 'event-1',
    run_id: 'run-1',
    seq: 1,
    event_type: 'tool_call',
    channel: 'analysis',
    content: '读取 SessionShellView.tsx',
    payload: {},
    created_at: 1,
  }];

  const html = renderSessionShell(payload);
  assert.match(html, /data-run-flow-capsule="true"/);
  assert.match(html, /data-run-event-rail="true"/);
  assert.match(html, /实现/);
  assert.match(html, /执行输出正文/);
});
```

- [x] **Step 2: 运行测试确认失败**

Run: `node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx`

Expected: FAIL，原因包含 `data-run-flow-capsule="true"` 未找到。

- [x] **Step 3: 添加事件归类类型和函数**

在 `SessionShellView.tsx` 的 `SessionRunTranscriptEvent` 附近添加：

```tsx
type RunEventKind = 'context' | 'tool' | 'edit' | 'verify' | 'error' | 'done' | 'activity';

interface RunEventRailItem {
  id: string;
  kind: RunEventKind;
  label: string;
  detail: string;
  count: number;
  events: SessionRunTranscriptEvent[];
}

function buildRunEventRailItems(events: SessionAgentEvent[]): RunEventRailItem[] {
  const transcriptEvents = events.map(toSessionRunTranscriptEvent);
  const grouped = new Map<RunEventKind, RunEventRailItem>();
  for (const event of transcriptEvents) {
    const kind = inferRunEventKind(event);
    const current = grouped.get(kind);
    if (current) {
      current.count += 1;
      current.events.push(event);
      if (!current.detail && event.detail) current.detail = event.detail;
      continue;
    }
    grouped.set(kind, {
      id: `${kind}:${event.id}`,
      kind,
      label: formatRunEventKindLabel(kind),
      detail: event.detail ?? event.content.slice(0, 96),
      count: 1,
      events: [event],
    });
  }
  return Array.from(grouped.values());
}
```

- [x] **Step 4: 添加事件类型推断函数**

```tsx
function inferRunEventKind(event: SessionRunTranscriptEvent): RunEventKind {
  const haystack = `${event.event_type} ${event.label} ${event.content}`.toLowerCase();
  if (event.channel === 'error' || haystack.includes('error') || haystack.includes('failed')) return 'error';
  if (haystack.includes('verify') || haystack.includes('test') || haystack.includes('build')) return 'verify';
  if (haystack.includes('edit') || haystack.includes('patch') || haystack.includes('write')) return 'edit';
  if (haystack.includes('tool')) return 'tool';
  if (haystack.includes('context') || haystack.includes('read') || haystack.includes('search')) return 'context';
  if (haystack.includes('done') || haystack.includes('complete')) return 'done';
  return 'activity';
}

function formatRunEventKindLabel(kind: RunEventKind): string {
  if (kind === 'context') return 'Context';
  if (kind === 'tool') return 'Tool';
  if (kind === 'edit') return 'Edit';
  if (kind === 'verify') return 'Verify';
  if (kind === 'error') return 'Error';
  if (kind === 'done') return 'Done';
  return 'Activity';
}
```

- [x] **Step 5: 实现 RunFlowCapsule**

新增组件并在原 agent run map 分支中替换 `<article className="deepsea-message deepsea-message--agent-run"...>` 内部结构。

```tsx
function RunFlowCapsule({
  run,
  runLabel,
  runAgentEvents,
  runEvidence,
  failureDetails,
  output,
  displayMode,
  streaming,
  now,
  latestTranscriptRunId,
  onRetryRun,
  onOpenWorkspaceFile,
  actions,
}: {
  run: SessionRun;
  runLabel: string;
  runAgentEvents: SessionAgentEvent[];
  runEvidence: SessionEvidenceEvent[];
  failureDetails: string | null;
  output: string;
  displayMode: SessionMessageDisplayMode;
  streaming: boolean;
  now: number;
  latestTranscriptRunId: string | null;
  onRetryRun?: (runId: string) => void;
  onOpenWorkspaceFile?: WorkspaceFileOpenHandler;
  actions: ReactNode;
}): JSX.Element {
  const railItems = buildRunEventRailItems(runAgentEvents);
  return (
    <section className="deepsea-run-capsule" data-run-flow-capsule="true" aria-label={`${runLabel} 执行流`}>
      <header className="deepsea-run-capsule__header">
        <div className="deepsea-run-capsule__identity">
          <span className="deepsea-run-capsule__avatar" aria-hidden="true">{runLabel.slice(0, 1).toUpperCase()}</span>
          <div>
            <strong title={runLabel}>{runLabel}</strong>
            <span>{formatWorkflowStageLabel(run.workflow_stage ?? 'implementation')} · {run.mode}</span>
          </div>
        </div>
        <div className="deepsea-run-capsule__status">
          <ThinkingDurationBadge run={run} agentEvents={runAgentEvents} now={now} />
          <RunStatusBadge run={run} agentEvents={runAgentEvents} onRetryRun={latestTranscriptRunId === run.id ? onRetryRun : undefined} />
        </div>
        <div className="deepsea-message-tools deepsea-message-tools--run">{actions}</div>
      </header>
      <AgentThoughtPanel run={run} evidence={runEvidence} agentEvents={runAgentEvents} failureDetails={failureDetails} />
      <div className="deepsea-run-capsule__body">
        <RunEventRail items={railItems} />
        <div className="deepsea-run-log-body">
          {displayMode === 'source' ? (
            <MessageContent content={output} mode={displayMode} suppressTraceEvents />
          ) : (
            <SessionRunTimeline events={runAgentEvents} fallbackText={output} streaming={streaming} onOpenWorkspaceFile={onOpenWorkspaceFile} />
          )}
        </div>
      </div>
      <GeneratedImageEvidencePanel evidence={runEvidence} />
    </section>
  );
}
```

- [x] **Step 6: 实现 RunEventRail**

```tsx
function RunEventRail({ items }: { items: RunEventRailItem[] }): JSX.Element | null {
  const [selectedEvents, setSelectedEvents] = useState<SessionRunTranscriptEvent[] | null>(null);
  if (items.length === 0) return null;
  return (
    <aside className="deepsea-run-event-rail" data-run-event-rail="true" aria-label="关键执行事件">
      {items.map((item) => (
        <button key={item.id} type="button" className="deepsea-run-event-rail__item" data-kind={item.kind} onClick={() => setSelectedEvents(item.events)}>
          <strong>{item.label}{item.count > 1 ? ` x${item.count}` : ''}</strong>
          <span title={item.detail}>{item.detail}</span>
        </button>
      ))}
      {selectedEvents ? <SessionRunEventDetailDialog events={selectedEvents} onClose={() => setSelectedEvents(null)} /> : null}
    </aside>
  );
}
```

- [x] **Step 7: 运行测试**

Run: `node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx`

Expected: Run Flow Capsule 测试 PASS。

## Task 4: 样式实现与响应式

**Files:**
- Modify: `packages/frontend/src/session-ui/session-os.css`

- [x] **Step 1: 添加 Mission Strip CSS**

在现有 workflow 样式附近添加：

```css
.deepsea-workflow-mission {
  display: grid;
  gap: 12px;
  margin: 12px 0 16px;
  padding: 14px;
  border: 1px solid rgba(94, 234, 212, 0.18);
  border-radius: 8px;
  background: linear-gradient(180deg, rgba(6, 20, 26, 0.96), rgba(8, 28, 36, 0.92));
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04);
}

.deepsea-workflow-mission__header {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  align-items: flex-start;
}

.deepsea-workflow-mission__header h3 {
  margin: 6px 0 3px;
  font-size: 1rem;
  line-height: 1.25;
}

.deepsea-workflow-mission__header p,
.deepsea-workflow-mission__blocker {
  margin: 0;
  color: var(--deepsea-muted);
  font-size: 0.78rem;
}

.deepsea-workflow-mission__badges {
  display: flex;
  gap: 6px;
  flex-wrap: wrap;
  justify-content: flex-end;
}

.deepsea-workflow-mission__badges span {
  border: 1px solid rgba(125, 211, 252, 0.22);
  border-radius: 999px;
  padding: 4px 8px;
  color: var(--deepsea-text);
  font-size: 0.72rem;
}
```

- [x] **Step 2: 添加阶段轨道 CSS**

```css
.deepsea-workflow-stage-rail {
  display: grid;
  grid-template-columns: repeat(6, minmax(92px, 1fr));
  gap: 8px;
  margin: 0;
  padding: 0;
  list-style: none;
  overflow-x: auto;
}

.deepsea-workflow-stage-rail__item {
  min-width: 92px;
  border: 1px solid rgba(125, 211, 252, 0.16);
  border-radius: 8px;
  padding: 8px;
  background: rgba(9, 31, 39, 0.78);
}

.deepsea-workflow-stage-rail__item span,
.deepsea-workflow-stage-rail__item strong {
  display: block;
  white-space: nowrap;
}

.deepsea-workflow-stage-rail__item span {
  color: var(--deepsea-text);
  font-size: 0.78rem;
}

.deepsea-workflow-stage-rail__item strong {
  margin-top: 4px;
  color: var(--deepsea-muted);
  font-size: 0.68rem;
  text-transform: uppercase;
}

.deepsea-workflow-stage-rail__item[data-state="done"] strong,
.deepsea-workflow-stage-rail__item[data-state="active"] strong {
  color: var(--deepsea-accent);
}

.deepsea-workflow-stage-rail__item[data-state="gate"] {
  border-color: rgba(246, 196, 83, 0.5);
  background: rgba(36, 31, 16, 0.78);
}

.deepsea-workflow-stage-rail__item[data-state="blocked"],
.deepsea-workflow-stage-rail__item[data-state="failed"] {
  border-color: rgba(248, 113, 113, 0.48);
}
```

- [x] **Step 3: 添加 Gate 与 Roster CSS**

```css
.deepsea-workflow-gate-summary,
.deepsea-workflow-agent-roster {
  display: grid;
  gap: 8px;
}

.deepsea-workflow-gate-summary__item,
.deepsea-workflow-agent-roster__item {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 10px;
  align-items: center;
  border: 1px solid rgba(125, 211, 252, 0.14);
  border-radius: 8px;
  padding: 9px;
  background: rgba(6, 20, 26, 0.72);
}

.deepsea-workflow-agent-roster {
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
}

.deepsea-workflow-agent-roster__item {
  grid-template-columns: 1fr;
}

.deepsea-workflow-gate-summary__item strong,
.deepsea-workflow-agent-roster__item strong,
.deepsea-workflow-gate-summary__item span,
.deepsea-workflow-agent-roster__item span,
.deepsea-workflow-agent-roster__item code {
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.deepsea-workflow-gate-summary__item small,
.deepsea-workflow-agent-roster__item code {
  color: var(--deepsea-muted);
  font-size: 0.72rem;
}

.deepsea-workflow-gate-summary__actions {
  display: flex;
  gap: 6px;
  align-items: center;
}
```

- [x] **Step 4: 添加 Run Capsule CSS**

```css
.deepsea-run-capsule {
  display: grid;
  gap: 12px;
  border: 1px solid rgba(125, 211, 252, 0.16);
  border-radius: 8px;
  padding: 12px;
  background: rgba(7, 22, 29, 0.9);
}

.deepsea-run-capsule__header {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto auto;
  gap: 10px;
  align-items: center;
  border-bottom: 1px solid rgba(125, 211, 252, 0.12);
  padding-bottom: 10px;
}

.deepsea-run-capsule__identity {
  display: flex;
  align-items: center;
  gap: 10px;
  min-width: 0;
}

.deepsea-run-capsule__avatar {
  width: 34px;
  height: 34px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  border: 1px solid rgba(94, 234, 212, 0.5);
  color: var(--deepsea-accent);
  flex: 0 0 auto;
}

.deepsea-run-capsule__identity strong,
.deepsea-run-capsule__identity span {
  display: block;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.deepsea-run-capsule__identity span {
  color: var(--deepsea-muted);
  font-size: 0.74rem;
}

.deepsea-run-capsule__status {
  display: flex;
  gap: 6px;
  align-items: center;
}

.deepsea-run-capsule__body {
  display: grid;
  grid-template-columns: minmax(136px, 180px) minmax(0, 1fr);
  gap: 12px;
  align-items: start;
}
```

- [x] **Step 5: 添加 Run Event Rail CSS**

```css
.deepsea-run-event-rail {
  display: grid;
  gap: 8px;
  border-right: 1px solid rgba(125, 211, 252, 0.12);
  padding-right: 10px;
}

.deepsea-run-event-rail__item {
  width: 100%;
  border: 1px solid rgba(125, 211, 252, 0.14);
  border-radius: 8px;
  padding: 8px;
  background: rgba(6, 20, 26, 0.72);
  color: inherit;
  text-align: left;
  cursor: pointer;
}

.deepsea-run-event-rail__item strong,
.deepsea-run-event-rail__item span {
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.deepsea-run-event-rail__item strong {
  color: var(--deepsea-text);
  font-size: 0.74rem;
}

.deepsea-run-event-rail__item span {
  margin-top: 3px;
  color: var(--deepsea-muted);
  font-size: 0.68rem;
}

.deepsea-run-event-rail__item[data-kind="error"] {
  border-color: rgba(248, 113, 113, 0.45);
}

.deepsea-run-event-rail__item[data-kind="verify"] {
  border-color: rgba(246, 196, 83, 0.35);
}
```

- [x] **Step 6: 添加窄屏 CSS**

```css
@media (max-width: 760px) {
  .deepsea-workflow-mission__header,
  .deepsea-workflow-gate-summary__item,
  .deepsea-run-capsule__header,
  .deepsea-run-capsule__body {
    grid-template-columns: 1fr;
  }

  .deepsea-workflow-mission__header {
    display: grid;
  }

  .deepsea-workflow-mission__badges,
  .deepsea-workflow-gate-summary__actions,
  .deepsea-run-capsule__status {
    justify-content: flex-start;
  }

  .deepsea-run-event-rail {
    grid-auto-flow: column;
    grid-auto-columns: minmax(128px, 1fr);
    overflow-x: auto;
    border-right: 0;
    border-bottom: 1px solid rgba(125, 211, 252, 0.12);
    padding-right: 0;
    padding-bottom: 10px;
  }
}
```

## Task 5: 回归验证、截图、提交实现

**Files:**
- Modify: `docs/superpowers/plans/2026-06-15-workflow消息流转指挥中枢化实施计划.md`
- Commit all intended implementation files only.

- [x] **Step 1: 运行相关测试**

Run: `node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx`

Expected: 无既有后端类型错误时 exit 0；当前以前端构建 exit 0 作为本次 UI 改动的构建证据。

- [x] **Step 2: 运行构建**

Run: `npm run build`

当前已额外执行前端构建：`npm run build -w @openclaw-room/frontend`，exit 0。

实际执行：

- `npm run build -w @openclaw-room/frontend`：exit 0。
- `npm run build`：2026-06-18 复验 exit 0。Vite 仅提示既有 chunk size 与 `TerminalPanel.tsx` 动静态导入警告。

Expected: exit 0。

- [ ] **Step 3: 启动开发服务并截图验证**

Run: `npm run dev:frontend -- --host 127.0.0.1`

Expected: Vite 输出本地 URL。

实际执行：

- 前端已用 `npm run dev -w @openclaw-room/frontend -- --host 127.0.0.1` 启动，Vite URL 为 `http://127.0.0.1:5173/`。
- 后端 `npm run dev:backend` 启动失败，原因是本机 `better-sqlite3` 原生模块使用 `NODE_MODULE_VERSION 127` 编译，而当前 Node.js v25.8.1 需要 `NODE_MODULE_VERSION 141`。
- 因后端不可用，真实会话数据无法加载到中间区，无法完成 Mission Strip / Run Capsule 的浏览器截图 smoke。

用 Playwright 打开当前会话页或可用路由，至少保存：

- `output/playwright/session-workflow-command-flow-1280x1024.png`
- `output/playwright/session-workflow-command-flow-mobile-390x844.png`

检查中间区无明显重叠、按钮可见、长文本不撑破容器。

- [ ] **Step 4: 更新计划复选框**

将实际完成的步骤在本文件中勾选。若某个步骤因现有项目限制无法执行，在对应步骤下写明原因。

- [ ] **Step 5: 查看 diff**

Run: `git diff -- packages/frontend/src/session-ui/SessionShellView.tsx packages/frontend/src/session-ui/session-os.css packages/frontend/src/session-ui/SessionShell.test.tsx docs/superpowers/plans/2026-06-15-workflow消息流转指挥中枢化实施计划.md`

Expected: 只包含本次 workflow 消息流转 UI 和计划文档改动。

- [ ] **Step 6: 提交实现**

```bash
git add \
  packages/frontend/src/session-ui/SessionShellView.tsx \
  packages/frontend/src/session-ui/session-os.css \
  packages/frontend/src/session-ui/SessionShell.test.tsx \
  docs/superpowers/plans/2026-06-15-workflow消息流转指挥中枢化实施计划.md
git commit -m "feat(frontend): 优化workflow消息流转展示"
```

Expected: commit 成功。

## Self-Review

- Spec coverage:
  - Mission Strip 覆盖顶部 workflow 总览区。
  - Run Flow Capsule 覆盖每条 agent run 内部流转。
  - 事件折叠通过 `buildRunEventRailItems` 与详情弹窗保留。
  - 既有操作通过 run header actions 保留。
  - 验证包含测试、构建和截图 smoke。
- Placeholder scan: 本计划不包含 TBD/TODO/待定/占位。
- Type consistency:
  - 使用现有 `WorkflowStage`、`WorkflowControllerView`、`WorkflowGateView`、`WorkflowArtifactVersionView`、`WorkflowAgentAssignmentView`、`SessionRun`、`SessionAgentEvent`、`SessionEvidenceEvent`。
  - 新增类型 `WorkflowMissionStageState`、`RunEventKind`、`RunEventRailItem` 仅限 `SessionShellView.tsx` 内部使用。

## 2026-06-18 路由修复补充

**问题：** 短事实问答消息，例如“这个项目的主要用途是什么？只需要简短回答。”，会被 planner 泛化为 `analysis`，导致中间聊天区只看到 workflow 卡片，缺少直接回复正文。

**修复：** 在 `packages/backend/src/workflows/graph/superpowers-routing-nodes.ts` 的 intake 路由纠偏中，仅当 planner 返回 `analysis` 且用户目标同时满足“事实问句 + 明确要求简短/直接回答 + 不包含分析/诊断/方案等意图”时，覆盖为 `answer`。分析类、排查类、审查类请求继续保持原路由。

**验证：**

- [x] `node --import tsx --test packages/backend/src/workflows/graph/superpowers-routing-nodes.test.ts`：13/13 pass。
- [x] `node --import tsx --test packages/backend/src/session-message-dispatch.test.ts`：38/38 pass。
- [x] `npm run build`：exit 0，Vite 仅输出既有 chunk size 与动静态导入警告。
- [x] 真实浏览器 smoke：临时服务 `7332 + 5176`，新会话 `EXpldQq7VgmtKTo_`，最终 `selected_intent=answer`、`active_stage=answer`，中间聊天区显示用户消息和一条 planner 正文气泡。
- [x] 截图证据：`output/playwright/session-answer-route-fixed-5176.png`。
