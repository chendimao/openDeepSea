# 会话聊天正文字号与流式计时 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 让会话聊天正文小一号，并让智能体流式回复时显示等待光标、思考计时按真实 answer 事件停止。

**Architecture:** 只在前端会话页内实现。`SessionShellView.tsx` 负责活动 run 判断、秒级 tick、基于 agent event 的 thinking duration 和流式光标渲染；`session-os.css` 负责正文密度与光标动画；`SessionShell.test.tsx` 用 node:test 覆盖计算、渲染和 CSS 约束。

**Tech Stack:** React 18、TypeScript、Vite、CSS、Node.js `node --import tsx --test`、npm workspaces。

---

## 文件结构

- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
  - 增加 `isRunLive`、`firstAnswerEvent` 等小 helper。
  - `TranscriptCanvas` 仅在 live run 存在时启动 1 秒 tick。
  - `ThinkingDurationBadge` 接收该 run 的 agent events 和 tick `now`。
  - `SessionRunTimeline` 接收 `streaming`，并复用 `MessageContent` 的通用流式光标。
- Modify: `packages/frontend/src/session-ui/session-os.css`
  - 聊天正文和 Markdown 内容从 `14px / 20px` 降到 `13px / 19px`。
  - Markdown 标题、段落、列表同步降一档。
  - 覆盖 `.deepsea-run-log-body .streaming-cursor` 的会话页颜色和 blink animation。
- Modify: `packages/frontend/src/session-ui/SessionShell.test.tsx`
  - 在现有测试文件中追加本需求测试。
  - 保留当前文件中已有的项目栏断言改动，不回退。
- Create or keep untracked: `docs/superpowers/specs/2026-06-08-会话聊天正文字号与流式计时设计.md`
  - 作为本次需求规格来源。
- Create: `docs/superpowers/plans/2026-06-08-会话聊天正文字号与流式计时实施计划.md`
  - 本实施计划。

---

### Task 1: 写失败测试

**Files:**
- Modify: `packages/frontend/src/session-ui/SessionShell.test.tsx`

- [x] **Step 1: 追加 thinking duration 真实 answer 事件测试**

在现有 `getSessionRunThinkingDuration formats active and completed durations` 测试附近追加：

```ts
test('getSessionRunThinkingDuration stops at the first answer event', () => {
  const run = {
    status: 'running' as const,
    started_at: 1_000,
    updated_at: 60_000,
    completed_at: null,
  };

  assert.deepEqual(getSessionRunThinkingDuration(run, [
    createAgentEvent({
      id: 'thinking-before-answer',
      seq: 1,
      channel: 'thinking',
      event_type: 'reasoning_delta',
      content: '分析中',
      created_at: 5_000,
    }),
    createAgentEvent({
      id: 'answer-first',
      seq: 2,
      channel: 'answer',
      event_type: 'agent_message_chunk',
      content: '开始回复。',
      created_at: 8_000,
    }),
    createAgentEvent({
      id: 'answer-second',
      seq: 3,
      channel: 'answer',
      event_type: 'agent_message_chunk',
      content: '继续回复。',
      created_at: 20_000,
    }),
  ], 90_000), { label: '思考 7s', active: false });
});
```

- [x] **Step 2: 追加 paused 不再继续计时测试**

```ts
test('getSessionRunThinkingDuration does not keep paused runs active', () => {
  assert.deepEqual(getSessionRunThinkingDuration({
    status: 'paused',
    started_at: 1_000,
    updated_at: 12_000,
    completed_at: null,
  }, [], 90_000), { label: '思考 11s', active: false });
});
```

- [x] **Step 3: 追加流式光标渲染测试**

```ts
test('SessionShell renders a blinking cursor while a run is streaming', () => {
  const payload = createPayload();
  const run = payload.activeSession.runs[0]!;
  run.status = 'running';
  run.stdout = '开始回复。';
  run.started_at = 1_000;
  run.updated_at = 8_000;
  run.completed_at = null;
  payload.activeSession.agentEvents = [
    createAgentEvent({
      id: 'answer',
      seq: 1,
      channel: 'answer',
      event_type: 'agent_message_chunk',
      content: '开始回复。',
      created_at: 8_000,
    }),
  ];

  const html = renderSessionShell(payload);

  assert.match(html, /streaming-cursor/);
});
```

- [x] **Step 4: 追加 completed 不显示光标测试**

```ts
test('SessionShell hides the streaming cursor after a run completes', () => {
  const payload = createPayload();
  const run = payload.activeSession.runs[0]!;
  run.status = 'completed';
  run.stdout = '已完成。';
  run.started_at = 1_000;
  run.updated_at = 8_000;
  run.completed_at = 8_000;
  payload.activeSession.agentEvents = [
    createAgentEvent({
      id: 'answer',
      seq: 1,
      channel: 'answer',
      event_type: 'agent_message_chunk',
      content: '已完成。',
      created_at: 8_000,
    }),
  ];

  const html = renderSessionShell(payload);

  assert.doesNotMatch(html, /streaming-cursor/);
});
```

- [x] **Step 5: 追加 CSS 正文字号和光标样式测试**

```ts
test('SessionShell CSS makes only chat body text one size smaller and defines streaming cursor', () => {
  assert.match(sessionOsCss, /\.deepsea-message-body,[\s\S]*font-size:\s*13px/);
  assert.match(sessionOsCss, /\.deepsea-run-log-body,[\s\S]*line-height:\s*19px/);
  assert.match(sessionOsCss, /\.deepsea-message-body \.message-content,[\s\S]*font-size:\s*13px/);
  assert.match(sessionOsCss, /\.deepsea-message-body \.markdown-preview p,[\s\S]*font-size:\s*13px/);
  assert.match(sessionOsCss, /\.deepsea-run-log-body \.streaming-cursor\s*\{/);
  assert.match(sessionOsCss, /@keyframes deepsea-cursor-blink/);
});
```

- [x] **Step 6: 运行测试确认失败**

Run:

```bash
node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx
```

Expected: FAIL，原因包括 `getSessionRunThinkingDuration` 形参不支持 agent events、paused 仍 active、光标 CSS/DOM 不存在、字号仍为 `14px / 20px`。

---

### Task 2: 实现真实思考计时和秒级 tick

**Files:**
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`

- [x] **Step 1: 增加 live run helper**

在 `runOutputText` 前后增加：

```ts
function isRunLive(status: SessionRun['status']): boolean {
  return status === 'queued' || status === 'running' || status === 'retrying';
}
```

- [x] **Step 2: TranscriptCanvas 增加 now tick**

在 `const timeline = buildTranscriptTimeline(detail).slice(-36);` 后增加：

```ts
const hasLiveRun = timeline.some((item) => item.kind === 'run' && isRunLive(item.run.status));
const [nowTick, setNowTick] = useState(() => Date.now());
```

在 scroll effects 附近增加：

```ts
useEffect(() => {
  if (!hasLiveRun) return undefined;
  setNowTick(Date.now());
  const timer = window.setInterval(() => setNowTick(Date.now()), 1_000);
  return () => window.clearInterval(timer);
}, [hasLiveRun]);
```

- [x] **Step 3: 传入 agent events 和 now**

把：

```tsx
<ThinkingDurationBadge run={item.run} />
```

改为：

```tsx
<ThinkingDurationBadge run={item.run} agentEvents={runAgentEvents} now={nowTick} />
```

- [x] **Step 4: 改造 ThinkingDurationBadge**

把签名改为：

```ts
function ThinkingDurationBadge({
  run,
  agentEvents,
  now,
}: {
  run: SessionRun;
  agentEvents: SessionAgentEvent[];
  now: number;
}): JSX.Element | null {
  const duration = getSessionRunThinkingDuration(run, agentEvents, now);
  if (!duration) return null;
  return (
    <span className="deepsea-thinking-duration" data-active={duration.active ? 'true' : 'false'}>
      <Timer aria-hidden="true" />
      {duration.label}
    </span>
  );
}
```

- [x] **Step 5: 改造 getSessionRunThinkingDuration**

把函数改为：

```ts
export function getSessionRunThinkingDuration(
  run: Pick<SessionRun, 'status' | 'started_at' | 'updated_at' | 'completed_at'>,
  agentEvents: SessionAgentEvent[] = [],
  now = Date.now(),
): { label: string; active: boolean } | null {
  if (!Number.isFinite(run.started_at) || run.started_at <= 0) return null;
  const answerAt = firstAnswerEvent(agentEvents)?.created_at ?? null;
  const live = isRunLive(run.status);
  const active = live && answerAt === null;
  const endAt = answerAt ?? (active ? now : run.completed_at ?? run.updated_at ?? now);
  const durationMs = Math.max(0, endAt - run.started_at);
  return {
    label: `${active ? '思考中' : '思考'} ${formatSessionDuration(durationMs)}`,
    active,
  };
}
```

- [x] **Step 6: 增加 firstAnswerEvent**

在 `isAnswerTextEvent` 附近增加：

```ts
function firstAnswerEvent(events: SessionAgentEvent[]): SessionAgentEvent | null {
  return [...events]
    .filter(isAnswerTextEvent)
    .sort((left, right) => left.created_at - right.created_at || left.seq - right.seq)[0] ?? null;
}
```

- [x] **Step 7: 运行测试**

Run:

```bash
node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx
```

Expected: thinking duration 相关测试 PASS，光标和 CSS 测试仍 FAIL。

---

### Task 3: 实现流式光标

**Files:**
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`

- [x] **Step 1: 给 SessionRunTimeline 传 streaming**

把 preview 分支改为：

```tsx
<SessionRunTimeline
  events={runAgentEvents}
  fallbackText={output}
  streaming={isRunLive(item.run.status)}
/>
```

- [x] **Step 2: 扩展 SessionRunTimeline 参数**

把函数签名改为：

```ts
function SessionRunTimeline({
  events,
  fallbackText,
  streaming,
}: {
  events: SessionAgentEvent[];
  fallbackText: string;
  streaming: boolean;
}): JSX.Element {
```

- [x] **Step 3: 渲染最后文本后的 cursor**

函数体改为：

```tsx
const items = buildSessionRunTranscriptItems(events, fallbackText);
const lastTextItemId = [...items].reverse().find((item) => item.type === 'text')?.id ?? null;
return (
  <div className="deepsea-run-timeline">
    {items.map((item) => item.type === 'text' ? (
      <div key={item.id} className="deepsea-run-timeline__text">
        <MessageContent
          content={item.text}
          mode="preview"
          streaming={streaming && item.id === lastTextItemId}
          suppressTraceEvents
        />
      </div>
    ) : (
      <div key={item.id} className="deepsea-run-timeline__event">
        <span>[{item.label}]</span>
        {item.detail && <small>{item.detail}</small>}
      </div>
    ))}
  </div>
);
```

- [x] **Step 4: 复用 MessageContent 的 StreamingCursor**

不新增会话页专属 DOM 组件，避免光标作为块级兄弟节点换行。`MessageContent` 已在纯文本和 Markdown 渲染路径中处理 `streaming` 光标。

- [x] **Step 5: 运行测试**

Run:

```bash
node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx
```

Expected: 光标 DOM 测试 PASS，CSS 测试仍 FAIL。

---

### Task 4: 调整 CSS 字号与光标动画

**Files:**
- Modify: `packages/frontend/src/session-ui/session-os.css`

- [x] **Step 1: 正文基础字号降一号**

把聊天正文基础规则改为：

```css
.deepsea-message p,
.deepsea-message-body,
.deepsea-run-log-body,
.deepsea-run-log p {
  margin: 0;
  color: var(--deepsea-text);
  font-size: 13px;
  line-height: 19px;
  overflow-wrap: anywhere;
  white-space: pre-wrap;
}
```

- [x] **Step 2: message-content 和 markdown-preview 降一号**

把对应规则的 `font-size` 和 `line-height` 改为：

```css
  font-size: 13px;
  line-height: 19px;
```

- [x] **Step 3: Markdown 标题降一档**

把 h1/h2/h3+ 改为：

```css
.deepsea-message-body .markdown-preview h1,
.deepsea-run-log-body .markdown-preview h1 {
  font-size: 15px;
}
.deepsea-message-body .markdown-preview h2,
.deepsea-run-log-body .markdown-preview h2 {
  font-size: 14px;
}
.deepsea-message-body .markdown-preview h3,
.deepsea-run-log-body .markdown-preview h3,
.deepsea-message-body .markdown-preview h4,
.deepsea-run-log-body .markdown-preview h4,
.deepsea-message-body .markdown-preview h5,
.deepsea-run-log-body .markdown-preview h5,
.deepsea-message-body .markdown-preview h6,
.deepsea-run-log-body .markdown-preview h6 {
  font-size: 13px;
}
```

- [x] **Step 4: Markdown 段落和列表降一号**

把 p 和 li 规则改为：

```css
  font-size: 13px;
  line-height: 19px;
```

- [x] **Step 5: 增加光标样式**

在 run timeline 相关 CSS 附近增加：

```css
.deepsea-run-log-body .streaming-cursor {
  display: inline-block;
  width: 7px;
  height: 15px;
  margin-left: 3px;
  transform: translateY(2px);
  border-radius: 2px;
  background: var(--deepsea-ok);
  animation: deepsea-cursor-blink 1s steps(2, start) infinite;
}

@keyframes deepsea-cursor-blink {
  0%,
  45% {
    opacity: 1;
  }
  46%,
  100% {
    opacity: 0;
  }
}
```

- [x] **Step 6: 运行测试**

Run:

```bash
node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx
```

Expected: 本文件测试 PASS。

---

### Task 5: 最终验证、审查与提交

**Files:**
- Verify all modified files.

- [x] **Step 1: 运行定向测试**

Run:

```bash
node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx
```

Expected: PASS。

- [x] **Step 2: 运行构建**

Run:

```bash
npm run build
```

Expected: PASS。

- [x] **Step 3: 自审 diff**

Run:

```bash
git diff -- packages/frontend/src/session-ui/SessionShellView.tsx packages/frontend/src/session-ui/session-os.css packages/frontend/src/session-ui/SessionShell.test.tsx docs/superpowers/specs/2026-06-08-会话聊天正文字号与流式计时设计.md docs/superpowers/plans/2026-06-08-会话聊天正文字号与流式计时实施计划.md
```

Check:

- 没有回退已有项目栏测试改动。
- 没有修改后端或协议。
- 光标只在 live run 显示。
- thinking duration 使用第一条 answer event 停止。

- [x] **Step 4: 提交**

只提交本任务相关文件；若 `SessionShell.test.tsx` 中仍包含用户已有项目栏断言改动，需要在最终说明中明确同文件已有改动被保留。

Run:

```bash
git add packages/frontend/src/session-ui/SessionShellView.tsx packages/frontend/src/session-ui/session-os.css packages/frontend/src/session-ui/SessionShell.test.tsx docs/superpowers/specs/2026-06-08-会话聊天正文字号与流式计时设计.md docs/superpowers/plans/2026-06-08-会话聊天正文字号与流式计时实施计划.md
git commit -m "fix(session): 优化聊天流式计时显示"
```

Expected: commit succeeds.

---

## 自审记录

- 规格覆盖：正文降字号、流式光标、真实思考计时、paused 不增长、测试和构建验证均有任务覆盖。
- 占位符扫描：计划不包含 TBD/TODO/待定项。
- 类型一致性：`getSessionRunThinkingDuration(run, agentEvents, now)`、`SessionRunTimeline({ events, fallbackText, streaming })` 与测试调用一致。

## 实际验证记录

- `node --import tsx --test --test-name-pattern "getSessionRunThinkingDuration|blinking cursor|streaming cursor|chat body text|ACP tool records|agent thought above" packages/frontend/src/session-ui/SessionShell.test.tsx`：8/8 PASS。
- `node --import tsx --test packages/frontend/src/session-ui/SessionShell.test.tsx`：60/60 PASS。
- `npm run build`：PASS；Vite 输出现有大 chunk warning。
- `git diff --cached --check`：PASS，无空白错误。
- 收尾审查：暂存区仅包含会话聊天正文字号、流式光标、思考计时及对应设计/计划文档；未暂存的文件上传、Markdown 样式补充、截图与临时目录未纳入本次提交。
