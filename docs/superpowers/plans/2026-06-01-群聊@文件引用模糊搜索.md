# 群聊 `@` 文件引用模糊搜索 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把群聊输入框的 `@` 从「提及智能体」改为「文件引用模糊搜索」（项目文件库 + 工作区磁盘文件），消息统一由兜底智能体（planner）回复，并支持系统/项目两层可配置的工作区排除目录。

**Architecture:** 前端 `@` 触发器改为异步搜索两路文件源，选中插入带 `kind` 前缀的 chip；发送时项目库文件走 `fileIds`、磁盘文件走新增 `fileRefs`（路径数组）。后端消息入口校验 `fileRefs` 路径安全并写入 `metadata.file_refs`，派发时读盘注入 prompt/imagePaths；群聊派发不再按正文解析提及，空 mentions 自然落到 `fallback_reply`→planner。排除目录新增为 settings 列（JSON 数组），系统层∪项目层∪默认黑名单。

**Tech Stack:** TypeScript / React 18 / Vite / Express / better-sqlite3 / zod / node:test（`node --import tsx --test`）/ npm workspaces。

**Spec:** `docs/superpowers/specs/2026-06-01-群聊@文件引用模糊搜索设计.md`

---

## 文件结构总览

**后端新增/修改：**
- 修改 `packages/backend/src/db.ts` — settings 表新增 `workspace_excluded_dirs` 列 + 迁移
- 修改 `packages/backend/src/types.ts` — `ScopedSettings` / `EffectiveSettings` / `MessageMetadata` 增字段
- 修改 `packages/backend/src/repos/settings.ts` — 读写 `workspace_excluded_dirs` + `resolveWorkspaceExcludedDirs`
- 修改 `packages/backend/src/workspace-files.ts` — `isIgnoredWorkspacePath` / `searchWorkspaceFiles` / `listWorkspaceDirectory` 接收 `extraIgnoredDirs`
- 修改 `packages/backend/src/routes.ts` — 消息 schema 增 `fileRefs`、写入 metadata、停用正文提及解析、workspace 路由传入排除目录、settings zod 增字段
- 新增 `packages/backend/src/workspace-file-refs.ts` — 由 `file_refs` 构建 prompt 注入与 imagePaths
- 修改 `packages/backend/src/dispatcher.ts` — 注入 `file_refs` 内容
- 修改 `packages/backend/src/uploads.ts` 无需改（attachment 不变）
- 测试：`workspace-file-refs.test.ts`、`workspace-files.test.ts`（追加）、`repos/settings.test.ts`（追加）、`mentions`/dispatch 行为测试追加于合适文件

**前端新增/修改：**
- 修改 `packages/frontend/src/lib/api.ts` — `searchWorkspaceFiles`、`listProjectFiles` 支持 `q`、`sendMessage` 增 `fileRefs`、settings 入参增 `workspace_excluded_dirs`
- 修改 `packages/frontend/src/lib/types.ts` — `EffectiveSettings` / `WorkspaceSearchResponse` 等增字段
- 重写 `packages/frontend/src/components/RichMessageComposer.triggers.ts` — `@` 文件搜索 + `buildFileSuggestions` 纯函数 + chip value 编解码
- 修改 `packages/frontend/src/components/RichMessageComposer.tsx` — props/onSend、handleSubmit 分流、triggers 构建
- 修改 `packages/frontend/src/components/room/RoomWorkbench.tsx` — onSend 透传 `fileRefs`（去掉 mentions）
- 修改 `packages/frontend/src/components/SettingsDialogs.tsx` — 系统/项目表单新增排除目录编辑器
- 测试：`RichMessageComposer.triggers.test.ts`

**配置数据变更（无代码）：**
- 将房间 `vcHtzxxovkbl` 设置为 `fallback_reply` + `planner`

---

## 阶段 A：后端「可配置排除目录」设置

### Task A1：settings 表新增列 + 迁移

**Files:**
- Modify: `packages/backend/src/db.ts`（参照现有 `PRAGMA table_info` 迁移范式，行 715-722 / 925-939）

- [ ] **Step 1: 在 settings 建表语句中加入新列**

在 `db.ts` 的 `CREATE TABLE IF NOT EXISTS settings (...)`（约行 30-45）中，于 `superpowers_bootstrap_owner` 行之后、`langchain_planner_model` 之前插入一列：

```sql
  workspace_excluded_dirs TEXT,
```

- [ ] **Step 2: 为已存在的 db 文件补列（幂等迁移）**

在 db.ts 中已有列迁移代码附近（如 projects/tasks 迁移块之后），追加：

```typescript
const settingsColumns = db.prepare('PRAGMA table_info(settings)').all() as { name: string }[];
const settingsColumnNames = new Set(settingsColumns.map((column) => column.name));
if (!settingsColumnNames.has('workspace_excluded_dirs')) {
  db.exec('ALTER TABLE settings ADD COLUMN workspace_excluded_dirs TEXT');
}
```

- [ ] **Step 3: 构建后端验证编译通过**

Run: `cd packages/backend && npx tsc --noEmit`
Expected: 无新增类型错误（此步仅 SQL/JS，无类型变更）。

- [ ] **Step 4: Commit**

```bash
git add packages/backend/src/db.ts
git commit -m "feat(backend): settings表新增workspace_excluded_dirs列"
```

### Task A2：类型与 repo 读写 `workspace_excluded_dirs`

**Files:**
- Modify: `packages/backend/src/types.ts`（`ScopedSettings` 行 903-913、`EffectiveSettings` 行 915-922）
- Modify: `packages/backend/src/repos/settings.ts`（`upsertScoped` 行 103-168、`getScoped`、`resolveForProject`、`resolveForRoom` 行 712-762、`DEFAULT_SETTINGS` 行 21-28）
- Test: `packages/backend/src/repos/settings.test.ts`

- [ ] **Step 1: 写失败测试**

在 `settings.test.ts` 末尾追加：

```typescript
test('settingsRepo unions workspace_excluded_dirs across system and project', () => {
  const projectPath = mkdtempSync(join(tmpdir(), 'openclaw-room-settings-excluded-'));
  const project = projectRepo.create({ name: 'Excluded Dirs', path: projectPath });

  assert.deepEqual(settingsRepo.resolveWorkspaceExcludedDirs(project.id), []);

  settingsRepo.updateSystem({ workspace_excluded_dirs: ['vendor'] });
  settingsRepo.updateProject(project.id, { workspace_excluded_dirs: ['tmp', 'vendor'] });

  assert.deepEqual(
    settingsRepo.resolveWorkspaceExcludedDirs(project.id).sort(),
    ['tmp', 'vendor'],
  );
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/backend && node --import tsx --test src/repos/settings.test.ts`
Expected: FAIL（`resolveWorkspaceExcludedDirs` / `updateSystem(... workspace_excluded_dirs)` 不存在）。

- [ ] **Step 3: 扩展类型**

`types.ts` `ScopedSettings`（行 903-913）在 `superpowers_bootstrap_owner` 行后加：

```typescript
  workspace_excluded_dirs: string | null;
```

`types.ts` `EffectiveSettings`（行 915-922）在 `superpowers_bootstrap_owner` 行后加：

```typescript
  workspace_excluded_dirs: string[];
```

- [ ] **Step 4: DEFAULT_SETTINGS 增默认值**

`repos/settings.ts` `DEFAULT_SETTINGS`（行 21-28）加：

```typescript
  workspace_excluded_dirs: [],
```

- [ ] **Step 5: repo 读写实现**

在 `repos/settings.ts`：

1) `getScoped` 把行 `workspace_excluded_dirs` 读出（它是 SELECT * 映射，无需改 SQL；若有显式列映射，加入该列）。`emptyScoped` 增 `workspace_excluded_dirs: null`。

2) `upsertScoped` 的 `patch` 类型增 `workspace_excluded_dirs?: string[] | null;`，并在函数体加：

```typescript
  const workspaceExcludedDirs =
    patch.workspace_excluded_dirs === undefined
      ? existing.workspace_excluded_dirs
      : patch.workspace_excluded_dirs === null
        ? null
        : JSON.stringify(normalizeExcludedDirs(patch.workspace_excluded_dirs));
```

在 INSERT 列与 VALUES、`ON CONFLICT DO UPDATE SET` 中加入 `workspace_excluded_dirs`，并在 `.run(...)` 实参末尾按列顺序加 `workspaceExcludedDirs`。

3) 文件顶部加纯函数（去空白、去重、去默认黑名单冗余可选）：

```typescript
function normalizeExcludedDirs(dirs: string[]): string[] {
  const cleaned = dirs
    .map((dir) => dir.trim())
    .filter((dir) => dir.length > 0);
  return Array.from(new Set(cleaned));
}

function parseExcludedDirs(raw: string | null): string[] {
  if (!raw) return [];
  try {
    const parsed = JSON.parse(raw) as unknown;
    if (!Array.isArray(parsed)) return [];
    return normalizeExcludedDirs(parsed.filter((item): item is string => typeof item === 'string'));
  } catch {
    return [];
  }
}
```

- [ ] **Step 6: effective 解析带上字段**

`resolveForProject` 的 `effective` 返回对象加（系统层∪项目层）：

```typescript
        workspace_excluded_dirs: Array.from(new Set([
          ...parseExcludedDirs(system.workspace_excluded_dirs),
          ...parseExcludedDirs(projectSettings?.workspace_excluded_dirs ?? null),
        ])),
```

`resolveForRoom` 的 `effective`（行 712-762 内）继承自 project（房间层不参与排除目录），加：

```typescript
        workspace_excluded_dirs: projectResolution.effective.workspace_excluded_dirs,
```

> 注：`system` 为 `SystemSettings`，需确认其类型也含 `workspace_excluded_dirs: string | null`。若 `SystemSettings` 与 `ScopedSettings` 分开定义，给 `SystemSettings` 也加该字段，并在系统设置读取处映射该列。

- [ ] **Step 7: 新增便捷方法 `resolveWorkspaceExcludedDirs`**

在 `settingsRepo` 对象中加：

```typescript
  resolveWorkspaceExcludedDirs(projectId: string): string[] {
    return this.resolveForProject(projectId)?.effective.workspace_excluded_dirs ?? [];
  },
```

并确保 `updateSystem` / `updateProject` 的 patch 类型透传 `workspace_excluded_dirs?: string[] | null`（它们内部调用 `upsertScoped`）。

- [ ] **Step 8: 运行测试确认通过**

Run: `cd packages/backend && node --import tsx --test src/repos/settings.test.ts`
Expected: PASS（含新增用例与原有用例）。

- [ ] **Step 9: Commit**

```bash
git add packages/backend/src/types.ts packages/backend/src/repos/settings.ts packages/backend/src/repos/settings.test.ts
git commit -m "feat(backend): settings读写并合并workspace排除目录"
```

### Task A3：workspace 文件搜索/列目录支持额外排除目录

**Files:**
- Modify: `packages/backend/src/workspace-files.ts`（`IGNORED_DIRECTORIES` 行 24、`isIgnoredWorkspacePath` 行 203-212、`listWorkspaceDirectory` 行 214、`searchWorkspaceFiles` 行 339、调用点 行 235/409）
- Test: `packages/backend/src/workspace-files.test.ts`

- [ ] **Step 1: 写失败测试**

在 `workspace-files.test.ts` 追加：

```typescript
test('searchWorkspaceFiles excludes extra ignored dirs', async () => {
  const root = mkdtempSync(join(tmpdir(), 'openclaw-room-ws-exclude-'));
  mkdirSync(join(root, 'vendor'), { recursive: true });
  writeFileSync(join(root, 'vendor', 'target.txt'), 'hit');
  writeFileSync(join(root, 'target.txt'), 'hit');

  const withExtra = await searchWorkspaceFiles(root, 'target', '', ['vendor']);
  assert.ok(withExtra.entries.every((entry) => !entry.path.startsWith('vendor/')));

  const withoutExtra = await searchWorkspaceFiles(root, 'target', '', []);
  assert.ok(withoutExtra.entries.some((entry) => entry.path.startsWith('vendor/')));
});
```

（顶部补 `import { mkdirSync, writeFileSync } from 'node:fs';` 若缺。）

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/backend && node --import tsx --test src/workspace-files.test.ts`
Expected: FAIL（`searchWorkspaceFiles` 第 4 参不存在）。

- [ ] **Step 3: 给 `isIgnoredWorkspacePath` 增可选额外目录**

```typescript
export function isIgnoredWorkspacePath(inputPath: string, extraIgnoredDirs: string[] = []): boolean {
  const normalized = normalizeIgnoredPath(inputPath);
  if (!normalized) return false;
  const extra = new Set(extraIgnoredDirs);
  const segments = normalized.split('/');
  for (const segment of segments) {
    if (IGNORED_DIRECTORIES.has(segment) || extra.has(segment)) return true;
  }
  const baseName = segments[segments.length - 1] ?? '';
  return isIgnoredFileName(baseName);
}
```

- [ ] **Step 4: 透传到列目录与搜索**

`listWorkspaceDirectory` 签名改为：

```typescript
export async function listWorkspaceDirectory(projectPath: string, inputPath = '', extraIgnoredDirs: string[] = []): Promise<WorkspaceDirectoryEntry[]>
```

行 235 调用改为 `if (isIgnoredWorkspacePath(relativeEntryPath, extraIgnoredDirs)) continue;`

`searchWorkspaceFiles` 签名改为：

```typescript
export async function searchWorkspaceFiles(
  projectPath: string,
  query: string,
  inputPath = '',
  extraIgnoredDirs: string[] = [],
): Promise<WorkspaceSearchResponse>
```

行 409 调用改为 `if (isIgnoredWorkspacePath(relativeEntryPath, extraIgnoredDirs)) continue;`（若递归遍历内部还有其它 `isIgnoredWorkspacePath` 调用，一并传入 `extraIgnoredDirs`）。

- [ ] **Step 5: 运行测试确认通过**

Run: `cd packages/backend && node --import tsx --test src/workspace-files.test.ts`
Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add packages/backend/src/workspace-files.ts packages/backend/src/workspace-files.test.ts
git commit -m "feat(backend): workspace搜索支持额外排除目录"
```

### Task A4：workspace 路由注入项目级排除目录

**Files:**
- Modify: `packages/backend/src/routes.ts`（`/workspace/tree` 行 988-1005、`/workspace/file` 行 1007-1023、`/workspace/search` 行 1025-1044）

- [ ] **Step 1: tree 与 search 路由传入排除目录**

`/workspace/tree` 处理体改为：

```typescript
    const extraIgnoredDirs = settingsRepo.resolveWorkspaceExcludedDirs(project.id);
    const entries = await listWorkspaceDirectory(project.path, workspacePath, extraIgnoredDirs);
    res.json({ path: workspacePath, entries });
```

`/workspace/search` 处理体改为：

```typescript
    const extraIgnoredDirs = settingsRepo.resolveWorkspaceExcludedDirs(project.id);
    const result = await searchWorkspaceFiles(project.path, parsed.data.q, parsed.data.path ?? '', extraIgnoredDirs);
    res.json(result);
```

（`/workspace/file` 单文件预览不需排除目录，不改。确认 `settingsRepo` 已在 routes.ts 顶部导入。）

- [ ] **Step 2: 构建验证**

Run: `cd packages/backend && npx tsc --noEmit`
Expected: 无类型错误。

- [ ] **Step 3: Commit**

```bash
git add packages/backend/src/routes.ts
git commit -m "feat(backend): workspace路由按项目设置排除目录"
```

---

## 阶段 B：后端 `fileRefs` 入库与派发注入

### Task B1：消息 metadata 支持 `file_refs` + buildUserMessageMetadata

**Files:**
- Modify: `packages/backend/src/types.ts`（`MessageMetadata` 行 782-804）
- Modify: `packages/backend/src/routes.ts`（`buildUserMessageMetadata` 行 2036-2062）

- [ ] **Step 1: MessageMetadata 增字段**

`MessageMetadata`（行 782-804）的 `attachments?` 附近加：

```typescript
  file_refs?: string[];
```

- [ ] **Step 2: buildUserMessageMetadata 接收 fileRefs**

签名与体改为：

```typescript
function buildUserMessageMetadata(input: {
  roomId: string;
  attachments: MessageMetadata['attachments'];
  fileRefs?: string[];
  replyToMessageId?: string;
}): MessageMetadata | undefined {
  const metadata: MessageMetadata = {};
  if (input.attachments && input.attachments.length > 0) {
    metadata.attachments = input.attachments;
  }
  if (input.fileRefs && input.fileRefs.length > 0) {
    metadata.file_refs = input.fileRefs;
  }
  // ...原 reply_to 逻辑保持不变...
```

- [ ] **Step 3: 构建验证**

Run: `cd packages/backend && npx tsc --noEmit`
Expected: 无类型错误。

- [ ] **Step 4: Commit**

```bash
git add packages/backend/src/types.ts packages/backend/src/routes.ts
git commit -m "feat(backend): 消息metadata支持file_refs"
```

### Task B2：消息入口解析并校验 `fileRefs`

**Files:**
- Modify: `packages/backend/src/routes.ts`（`jsonMessageSchema` 行 1749-1757、`multipartMessageSchema` 行 1759-1767、`handleJsonMessage` 行 1842-1906、`handleMultipartMessage` 行 1908+）
- Test: `packages/backend/src/routes`（新增或就近的 routes 测试文件，如 `task-router.routes.test.ts` 同目录新建 `room-message-filerefs.routes.test.ts`）

- [ ] **Step 1: schema 增 fileRefs**

`jsonMessageSchema` 加：

```typescript
  fileRefs: z.array(z.string()).optional(),
```

`multipartMessageSchema` 加：

```typescript
  fileRefs: z.string().optional(),
```

- [ ] **Step 2: 增路径安全解析工具**

在 routes.ts 适当位置（靠近 `parseMultipartFileIds`）加：

```typescript
function sanitizeFileRefs(rawRefs: string[]): string[] {
  const normalized = rawRefs
    .map((ref) => {
      try {
        return normalizeWorkspacePath(ref);
      } catch {
        return '';
      }
    })
    .filter((ref) => ref.length > 0);
  return dedupeIds(normalized);
}

function parseMultipartFileRefs(raw: string | undefined): string[] {
  if (!raw) return [];
  let parsed: unknown;
  try {
    parsed = JSON.parse(raw);
  } catch {
    throw new Error('invalid fileRefs');
  }
  if (!Array.isArray(parsed) || parsed.some((item) => typeof item !== 'string')) {
    throw new Error('invalid fileRefs');
  }
  return sanitizeFileRefs(parsed as string[]);
}
```

（确认 `normalizeWorkspacePath` 已从 `./workspace-files.js` 导入。）

- [ ] **Step 3: handleJsonMessage 接入**

在 `const fileIds = dedupeIds(...)` 之后加：

```typescript
  const fileRefs = sanitizeFileRefs(parsed.data.fileRefs ?? []);
```

把空判断改为 `if (content.length === 0 && fileIds.length === 0 && fileRefs.length === 0)`。

`buildUserMessageMetadata({...})` 调用加 `fileRefs,`。

- [ ] **Step 4: handleMultipartMessage 接入**

解析 fileIds 之后加（仿照其 try/catch 风格）：

```typescript
    let fileRefs: string[];
    try {
      fileRefs = parseMultipartFileRefs(parsed.data.fileRefs);
    } catch (err) {
      await cleanupProjectUploadedFiles(files);
      res.status(400).json({ error: (err as Error).message });
      return;
    }
```

空判断加入 `fileRefs.length === 0`；`buildUserMessageMetadata({...})` 调用加 `fileRefs,`。

- [ ] **Step 5: 写测试（路径安全）**

新建 `packages/backend/src/room-message-filerefs.routes.test.ts`，参照现有 `*.routes.test.ts` 启动 app/supertest 风格（若现有 routes 测试用 `node:http` + fetch，则照搬其 setup）。核心断言：

```typescript
// POST 一条带 fileRefs: ['a.txt', '../escape.txt'] 的 JSON 消息
// 期望：返回 201；读取该消息 metadata.file_refs === ['a.txt']（越界路径被剔除）
```

> 实现时复用该目录下已有 routes 测试的 app 初始化样板（同目录 `task-router.routes.test.ts` 提供范式）。

- [ ] **Step 6: 运行测试确认通过**

Run: `cd packages/backend && node --import tsx --test src/room-message-filerefs.routes.test.ts`
Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add packages/backend/src/routes.ts packages/backend/src/room-message-filerefs.routes.test.ts
git commit -m "feat(backend): 消息入口校验并存储fileRefs路径"
```

### Task B3：新增 `workspace-file-refs.ts` 注入构建模块

**Files:**
- Create: `packages/backend/src/workspace-file-refs.ts`
- Create: `packages/backend/src/workspace-file-refs.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
import assert from 'node:assert/strict';
import test from 'node:test';
import { mkdtempSync, writeFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { buildWorkspaceFileRefContext } from './workspace-file-refs';

test('buildWorkspaceFileRefContext injects text content and resolves image paths', async () => {
  const root = mkdtempSync(join(tmpdir(), 'openclaw-room-fileref-'));
  writeFileSync(join(root, 'note.txt'), 'hello world');

  const result = await buildWorkspaceFileRefContext(root, ['note.txt', 'pic.png']);

  assert.ok(result.promptAddition.includes('note.txt'));
  assert.ok(result.promptAddition.includes('hello world'));
  assert.deepEqual(result.imagePaths, [join(root, 'pic.png')]);
});

test('buildWorkspaceFileRefContext ignores traversal paths', async () => {
  const root = mkdtempSync(join(tmpdir(), 'openclaw-room-fileref-safe-'));
  const result = await buildWorkspaceFileRefContext(root, ['../secret.txt']);
  assert.equal(result.promptAddition, '');
  assert.deepEqual(result.imagePaths, []);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/backend && node --import tsx --test src/workspace-file-refs.test.ts`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现模块**

```typescript
import { join } from 'node:path';
import { normalizeWorkspacePath, readWorkspaceFilePreview } from './workspace-files.js';

const IMAGE_EXTENSIONS = new Set(['.png', '.jpg', '.jpeg', '.gif', '.webp', '.bmp', '.svg']);

function isImagePath(path: string): boolean {
  const dot = path.lastIndexOf('.');
  if (dot < 0) return false;
  return IMAGE_EXTENSIONS.has(path.slice(dot).toLowerCase());
}

export interface WorkspaceFileRefContext {
  promptAddition: string;
  imagePaths: string[];
}

export async function buildWorkspaceFileRefContext(
  projectPath: string,
  fileRefs: string[],
): Promise<WorkspaceFileRefContext> {
  const blocks: string[] = [];
  const imagePaths: string[] = [];

  for (const ref of fileRefs) {
    let safePath: string;
    try {
      safePath = normalizeWorkspacePath(ref);
    } catch {
      continue;
    }
    if (!safePath) continue;

    if (isImagePath(safePath)) {
      imagePaths.push(join(projectPath, safePath));
      continue;
    }

    try {
      const preview = await readWorkspaceFilePreview(projectPath, safePath);
      const truncatedNote = preview.truncated ? '\n…(truncated)' : '';
      blocks.push(`引用文件 ${safePath}:\n\`\`\`\n${preview.content}${truncatedNote}\n\`\`\``);
    } catch {
      continue;
    }
  }

  return {
    promptAddition: blocks.join('\n\n'),
    imagePaths,
  };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/backend && node --import tsx --test src/workspace-file-refs.test.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add packages/backend/src/workspace-file-refs.ts packages/backend/src/workspace-file-refs.test.ts
git commit -m "feat(backend): 新增workspace文件引用注入构建"
```

### Task B4：dispatcher 注入 `file_refs`

**Files:**
- Modify: `packages/backend/src/dispatcher.ts`（`dispatchUserMessage` 行 238-293）

- [ ] **Step 1: 读取 metadata.file_refs 工具**

在 dispatcher.ts 加（靠近 `getMessageTaskId`）：

```typescript
function getMessageFileRefs(metadata: string | null): string[] {
  if (!metadata) return [];
  try {
    const parsed = JSON.parse(metadata) as { file_refs?: unknown };
    if (!Array.isArray(parsed.file_refs)) return [];
    return parsed.file_refs.filter((item): item is string => typeof item === 'string');
  } catch {
    return [];
  }
}
```

顶部 import 加：

```typescript
import { buildWorkspaceFileRefContext } from './workspace-file-refs.js';
```

- [ ] **Step 2: 注入 prompt 与 imagePaths**

在 `dispatchUserMessage` 内、构建 `promptWithAttachments` 与 `imagePaths`（行 258-265）之后插入：

```typescript
  const fileRefContext = await buildWorkspaceFileRefContext(project.path, getMessageFileRefs(userMessage.metadata));
  const finalPrompt = fileRefContext.promptAddition
    ? `${promptWithAttachments}\n\n${fileRefContext.promptAddition}`
    : promptWithAttachments;
  const finalImagePaths = [...imagePaths, ...fileRefContext.imagePaths];
```

把后续 `resolveInitialTargets({ ..., prompt: promptWithAttachments, imagePaths })` 改为 `prompt: finalPrompt, imagePaths: finalImagePaths`；`runTargets({ ..., imagePaths })` 改为 `imagePaths: finalImagePaths`。

- [ ] **Step 3: 构建验证**

Run: `cd packages/backend && npx tsc --noEmit`
Expected: 无类型错误。

- [ ] **Step 4: Commit**

```bash
git add packages/backend/src/dispatcher.ts
git commit -m "feat(backend): 派发时注入file_refs内容与图片"
```

### Task B5：群聊派发停用正文提及解析

**Files:**
- Modify: `packages/backend/src/routes.ts`（`dispatchUserMessageFromRoute` 行 2095-2115）
- Test: `packages/backend/src/routes`（就近 routes 测试，可并入 B2 的测试文件）

- [ ] **Step 1: 写失败测试**

在 `room-message-filerefs.routes.test.ts` 追加：

```typescript
// 房间含一个 agent_name 为 'planner' 的智能体，且路由模式 fallback_reply
// 发送 content 含字面量 '@planner 你好'，但不带 mentions
// 期望：dispatchUserMessage 收到的 mentionedAgentRoomIds 为空（不因正文@命中）
```

> 用 `messageRouteDeps.dispatchUserMessage` 注入 spy 捕获入参（routes.ts 已支持该依赖注入点，见行 1904 的 `messageRouteDeps.dispatchUserMessage ?? dispatchUserMessage`）。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/backend && node --import tsx --test src/room-message-filerefs.routes.test.ts`
Expected: FAIL（当前 `resolveMentionedAgentRoomIds` 会按正文解析命中 planner）。

- [ ] **Step 3: 改为只用显式 mentions**

`dispatchUserMessageFromRoute`（行 2095-2115）体改为不再调用 `resolveMentionedAgentRoomIds`：

```typescript
function dispatchUserMessageFromRoute(input: {
  roomId: string;
  userMessageId: string;
  content: string;
  mentions?: string[];
}): void {
  const userMessage = messageRepo.get(input.userMessageId);
  if (!userMessage) return;
  const mentionedAgentRoomIds = dedupeIds(input.mentions ?? []);
  void (messageRouteDeps.dispatchUserMessage ?? dispatchUserMessage)({
    roomId: input.roomId,
    userMessage,
    mentionedAgentRoomIds,
  });
}
```

（`resolveMentionedAgentRoomIds` 模块与 `mentions.ts` 保留，仅此处不再调用；若该 import 在 routes.ts 变为未使用，删除该 import 行以通过 lint。）

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/backend && node --import tsx --test src/room-message-filerefs.routes.test.ts`
Expected: PASS。

- [ ] **Step 5: 全量后端测试**

Run: `cd packages/backend && npm test`
Expected: 全绿（注意原 `mentions.test.ts` 仍应通过，因模块未删）。

- [ ] **Step 6: Commit**

```bash
git add packages/backend/src/routes.ts packages/backend/src/room-message-filerefs.routes.test.ts
git commit -m "feat(backend): 群聊派发不再按正文解析提及"
```

---

## 阶段 C：前端 API 与类型

### Task C1：前端类型与 API 封装

**Files:**
- Modify: `packages/frontend/src/lib/types.ts`（`EffectiveSettings` 行 500-507；新增 workspace 搜索类型）
- Modify: `packages/frontend/src/lib/api.ts`（`request`/`workspaceRequest` 区、`listProjectFiles` 行 533-541、`sendMessage` 行 729-771、`updateSystemSettings` 行 246-260、`updateProjectSettings` 行 294-304）

- [ ] **Step 1: 前端类型扩展**

`types.ts` `EffectiveSettings`（行 500-507）加：

```typescript
  workspace_excluded_dirs: string[];
```

并新增 workspace 搜索类型：

```typescript
export interface WorkspaceSearchResult {
  path: string;
  name: string;
  type: 'file';
}

export interface WorkspaceSearchResponse {
  entries: WorkspaceSearchResult[];
  truncated: boolean;
}
```

- [ ] **Step 2: listProjectFiles 支持 q**

`api.ts` `listProjectFiles`（行 533-541）改为：

```typescript
listProjectFiles: (
  projectId: string,
  filters: { sourceType?: ProjectFile['source_type']; q?: string } = {},
) => {
  const params = new URLSearchParams();
  if (filters.sourceType) params.set('sourceType', filters.sourceType);
  if (filters.q) params.set('q', filters.q);
  const query = params.toString();
  return request<ProjectFile[]>(`/projects/${projectId}/files${query ? `?${query}` : ''}`);
},
```

- [ ] **Step 3: 新增 searchWorkspaceFiles（带本地 token）**

在 api 对象中加（复用 `workspaceRequest`，行 134-140 已存在且会带 `X-OpenDeepSea-Local-Token`）：

```typescript
searchWorkspaceFiles: (projectId: string, query: string) =>
  workspaceRequest<WorkspaceSearchResponse>(
    `/projects/${projectId}/workspace/search?q=${encodeURIComponent(query)}`,
  ),
```

（确认 `WorkspaceSearchResponse` 已 import；若 `workspaceRequest` 当前为模块内私有函数，确保 api 对象内可访问。）

- [ ] **Step 4: sendMessage 增 fileRefs、去 mentions 依赖**

`sendMessage`（行 729-771）：入参类型加 `fileRefs?: string[];`；FormData 分支在 fileIds 之后加：

```typescript
    if (input.fileRefs && input.fileRefs.length > 0) {
      form.append('fileRefs', JSON.stringify(input.fileRefs));
    }
```

JSON 分支 body 加 `fileRefs: input.fileRefs,`。`mentions` 字段保留（兼容），但调用方将不再传。

- [ ] **Step 5: settings 入参加排除目录**

`updateSystemSettings`（行 246-260）入参加 `workspace_excluded_dirs?: string[];`
`updateProjectSettings`（行 294-304）入参加 `workspace_excluded_dirs?: string[] | null;`

- [ ] **Step 6: 类型构建验证**

Run: `cd packages/frontend && npx tsc --noEmit`
Expected: 无新增类型错误。

- [ ] **Step 7: Commit**

```bash
git add packages/frontend/src/lib/types.ts packages/frontend/src/lib/api.ts
git commit -m "feat(frontend): API支持workspace搜索与fileRefs"
```

---

## 阶段 D：前端 `@` 文件搜索改造

### Task D1：触发器改为文件搜索 + 纯函数合并

**Files:**
- Rewrite: `packages/frontend/src/components/RichMessageComposer.triggers.ts`
- Create: `packages/frontend/src/components/RichMessageComposer.triggers.test.ts`

- [ ] **Step 1: 写失败测试（纯函数 buildFileSuggestions + chip value 编解码）**

`RichMessageComposer.triggers.test.ts`：

```typescript
import assert from 'node:assert/strict';
import test from 'node:test';
import { buildFileSuggestions, encodeFileChipValue, parseFileChipValue } from './RichMessageComposer.triggers';
import type { ProjectFile } from '../lib/types';

const projectFile = {
  id: 'f1',
  original_name: 'report.md',
  mime_type: 'text/markdown',
} as ProjectFile;

test('buildFileSuggestions merges project and workspace, dedups, limits', () => {
  const suggestions = buildFileSuggestions(
    [projectFile],
    [{ path: 'src/report.md', name: 'report.md', type: 'file' }],
    'report',
  );
  assert.equal(suggestions[0].value, 'project:f1');
  assert.ok(suggestions.some((s) => s.value === 'workspace:src/report.md'));
  assert.ok(suggestions.length <= 8);
});

test('encode/parse file chip value round-trips', () => {
  assert.equal(encodeFileChipValue('project', 'f1'), 'project:f1');
  assert.deepEqual(parseFileChipValue('project:f1'), { kind: 'project', ref: 'f1' });
  assert.deepEqual(parseFileChipValue('workspace:src/a.ts'), { kind: 'workspace', ref: 'src/a.ts' });
  assert.equal(parseFileChipValue('garbage'), null);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/frontend && node --import tsx --test src/components/RichMessageComposer.triggers.test.ts`
Expected: FAIL（导出不存在）。若报 `tsx` 不可用，先 `cd packages/frontend && npx tsx --version` 确认；不可用则在该命令前加 `--import tsx` 等价方式或从根目录 `npx tsx`。

- [ ] **Step 3: 重写 triggers 文件**

```typescript
import type { ProjectFile, WorkspaceSearchResult } from '../lib/types';
import type { TriggerConfig, TriggerSuggestion } from './prompt-area/types';
import { api } from '../lib/api';

export const FILE_TRIGGER = '@';
const SUGGESTION_LIMIT = 8;

export interface ComposerTriggerLabels {
  fileMenuAria: string;
  fileEmpty: string;
}

export type FileChipKind = 'project' | 'workspace';

export function encodeFileChipValue(kind: FileChipKind, ref: string): string {
  return `${kind}:${ref}`;
}

export function parseFileChipValue(value: string): { kind: FileChipKind; ref: string } | null {
  const sep = value.indexOf(':');
  if (sep < 0) return null;
  const kind = value.slice(0, sep);
  const ref = value.slice(sep + 1);
  if ((kind !== 'project' && kind !== 'workspace') || !ref) return null;
  return { kind, ref };
}

export function buildFileSuggestions(
  projectFiles: ProjectFile[],
  workspaceEntries: WorkspaceSearchResult[],
  query: string,
): TriggerSuggestion[] {
  const needle = query.trim().toLowerCase();
  const seen = new Set<string>();
  const suggestions: TriggerSuggestion[] = [];

  for (const file of projectFiles) {
    if (needle && !file.original_name.toLowerCase().includes(needle)) continue;
    const value = encodeFileChipValue('project', file.id);
    if (seen.has(value)) continue;
    seen.add(value);
    suggestions.push({
      value,
      label: file.original_name,
      description: '项目文件',
      data: { kind: 'project', file },
    });
  }

  for (const entry of workspaceEntries) {
    const value = encodeFileChipValue('workspace', entry.path);
    if (seen.has(value)) continue;
    seen.add(value);
    suggestions.push({
      value,
      label: entry.name,
      description: entry.path,
      data: { kind: 'workspace', path: entry.path, name: entry.name },
    });
  }

  return suggestions.slice(0, SUGGESTION_LIMIT);
}

interface BuildComposerTriggersInput {
  projectId: string;
  labels: ComposerTriggerLabels;
}

export function buildComposerTriggers({
  projectId,
  labels,
}: BuildComposerTriggersInput): TriggerConfig[] {
  return [
    {
      char: FILE_TRIGGER,
      position: 'any',
      mode: 'dropdown',
      accessibilityLabel: labels.fileMenuAria,
      searchDebounceMs: 200,
      onSearch: async (query) => {
        const [projectFiles, workspace] = await Promise.all([
          api.listProjectFiles(projectId, query ? { q: query } : {}).catch(() => [] as ProjectFile[]),
          query
            ? api.searchWorkspaceFiles(projectId, query).catch(() => ({ entries: [], truncated: false }))
            : Promise.resolve({ entries: [], truncated: false }),
        ]);
        return buildFileSuggestions(projectFiles, workspace.entries, query);
      },
      onSelect: (suggestion) => suggestion.label,
      emptyMessage: labels.fileEmpty,
    },
  ];
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/frontend && node --import tsx --test src/components/RichMessageComposer.triggers.test.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add packages/frontend/src/components/RichMessageComposer.triggers.ts packages/frontend/src/components/RichMessageComposer.triggers.test.ts
git commit -m "feat(frontend): @触发器改为文件模糊搜索"
```

### Task D2：Composer 发送分流 + 移除 mentions

**Files:**
- Modify: `packages/frontend/src/components/RichMessageComposer.tsx`（props 行 27-49、triggers 构建 行 78-84、handleSubmit 行 188-216）

- [ ] **Step 1: 更新 triggers 构建**

行 78-84 改为：

```typescript
const triggers = useMemo(() => buildComposerTriggers({
  projectId,
  labels: {
    fileMenuAria: t('composer.fileMenuAria'),
    fileEmpty: t('composer.fileEmpty'),
  },
}), [projectId, t]);
```

并更新顶部 import：`import { buildComposerTriggers, FILE_TRIGGER, parseFileChipValue } from './RichMessageComposer.triggers';`（移除 `AGENT_TRIGGER` 及 `findTypedMentionRoomAgentIds` 的 import 若来自该文件）。

- [ ] **Step 2: onSend 类型改造**

`RichMessageComposerProps.onSend`（行 44-49）改为：

```typescript
  onSend: (input: {
    content: string;
    files?: File[];
    fileIds?: string[];
    fileRefs?: string[];
    replyToMessageId?: string;
  }) => void;
```

（删除 `mentions?: string[];`。）

- [ ] **Step 3: handleSubmit 分流**

行 188-216 的 `mentionedRoomAgentIds` 段整体替换为基于 chip value 的文件分流：

```typescript
  const fileChips = getChipsByTrigger(segments, FILE_TRIGGER)
    .map((chip) => parseFileChipValue(chip.value))
    .filter((parsed): parsed is { kind: 'project' | 'workspace'; ref: string } => parsed !== null);

  const chipProjectFileIds = fileChips
    .filter((chip) => chip.kind === 'project')
    .map((chip) => chip.ref);
  const fileRefs = fileChips
    .filter((chip) => chip.kind === 'workspace')
    .map((chip) => chip.ref)
    .filter((ref, index, refs) => refs.indexOf(ref) === index);
```

`fileIds` 计算合并 chip 项与附件项并去重：

```typescript
  const fileIds = [
    ...attachmentsRef.current
      .filter((attachment): attachment is Extract<ComposerAttachment, { kind: 'project' }> => attachment.kind === 'project')
      .map((attachment) => attachment.file.id),
    ...chipProjectFileIds,
  ].filter((id, index, ids) => ids.indexOf(id) === index);
```

`onSend({...})` 改为：

```typescript
  onSend({
    content,
    files: localFiles.length > 0 ? localFiles : undefined,
    fileIds: fileIds.length > 0 ? fileIds : undefined,
    fileRefs: fileRefs.length > 0 ? fileRefs : undefined,
    replyToMessageId: getExplicitReplyToMessageId(replyTarget),
  });
```

（确认 `getChipsByTrigger` 仍从原模块 import；删除现已无引用的 `agents`、`AGENT_TRIGGER`、`findTypedMentionRoomAgentIds`。若 `agents` prop 在组件其它处仍被使用则保留 prop，仅移除提及相关用法。）

- [ ] **Step 4: 处理 agents prop**

若 `agents` 在 D2 后于本组件不再使用：从 `RichMessageComposerProps`（行 27-49）移除 `agents: RoomAgent[];`，并在调用方 RoomWorkbench 停止传入（见 Task D3）。若仍被用于占位提示等，保留。按编译结果决定。

- [ ] **Step 5: 类型构建验证**

Run: `cd packages/frontend && npx tsc --noEmit`
Expected: 仅剩 RoomWorkbench 调用方因 onSend/props 变更而需在 D3 修复的错误（预期）。

- [ ] **Step 6: Commit**

```bash
git add packages/frontend/src/components/RichMessageComposer.tsx
git commit -m "feat(frontend): composer发送按文件类型分流fileIds与fileRefs"
```

### Task D3：RoomWorkbench 透传 fileRefs

**Files:**
- Modify: `packages/frontend/src/components/room/RoomWorkbench.tsx`（onSend 处理与 `api.sendMessage` 调用，约行 839-868；`<RichMessageComposer .../>` 渲染处）

- [ ] **Step 1: 定位并改造 onSend 处理**

找到传给 `<RichMessageComposer onSend={...}>` 的处理函数（原从 `input.mentions` 取值）。改为不再读 `mentions`，新增透传 `fileRefs`，调用 `api.sendMessage(roomId, { content, files, fileIds, fileRefs, replyToMessageId, activeTaskId })`。

具体：把 onSend 入参类型与 `api.sendMessage(...)` 调用中的 `mentions` 删除，加入 `fileRefs: input.fileRefs`。

- [ ] **Step 2: 移除 agents 传入（若 D2 移除了该 prop）**

若 Task D2 从 composer 移除了 `agents` prop，则在 `<RichMessageComposer .../>` 处删除 `agents={...}`。否则保留。

- [ ] **Step 3: 类型构建验证**

Run: `cd packages/frontend && npx tsc --noEmit`
Expected: 无类型错误（前端整体通过）。

- [ ] **Step 4: 前端构建**

Run: `cd packages/frontend && npm run build`
Expected: 构建成功。

- [ ] **Step 5: Commit**

```bash
git add packages/frontend/src/components/room/RoomWorkbench.tsx
git commit -m "feat(frontend): 群聊发送透传fileRefs去除mentions"
```

---

## 阶段 E：前端设置 UI 排除目录编辑器

### Task E1：系统/项目设置新增排除目录编辑器

**Files:**
- Modify: `packages/frontend/src/components/SettingsDialogs.tsx`（`SystemSettingsForm` 行 281-620、`ProjectSettingsForm` 行 997-1097；保存 patch 组装处 行 456-464 / 1037-1045）

- [ ] **Step 1: 新增可复用目录标签编辑器组件**

在 `SettingsDialogs.tsx` 内（或同目录新建小组件）加手写 useState 版标签编辑器：

```typescript
function ExcludedDirsEditor({
  value,
  onChange,
  label,
}: {
  value: string[];
  onChange: (next: string[]) => void;
  label: string;
}): JSX.Element {
  const [draft, setDraft] = useState('');
  const add = () => {
    const trimmed = draft.trim();
    if (!trimmed || value.includes(trimmed)) {
      setDraft('');
      return;
    }
    onChange([...value, trimmed]);
    setDraft('');
  };
  return (
    <div className="settings-field">
      <label className="settings-label">{label}</label>
      <div className="excluded-dirs-tags">
        {value.map((dir) => (
          <span key={dir} className="excluded-dirs-tag">
            {dir}
            <button type="button" onClick={() => onChange(value.filter((item) => item !== dir))}>×</button>
          </span>
        ))}
      </div>
      <div className="excluded-dirs-input">
        <input
          value={draft}
          onChange={(event) => setDraft(event.currentTarget.value)}
          onKeyDown={(event) => {
            if (event.key === 'Enter') {
              event.preventDefault();
              add();
            }
          }}
          placeholder="vendor"
        />
        <button type="button" onClick={add}>添加</button>
      </div>
    </div>
  );
}
```

（样式类名沿用项目既有 `settings-field` 等；若无对应 class，可加最小 CSS 到 `index.css`，非必需。）

- [ ] **Step 2: SystemSettingsForm 接入**

在 `SystemSettingsForm` 加本地 state（仿行 298-304 风格）：

```typescript
const [workspaceExcludedDirs, setWorkspaceExcludedDirs] = useState<string[]>(value.workspace_excluded_dirs ?? []);
```

在表单 JSX 合适位置渲染：

```typescript
<ExcludedDirsEditor
  value={workspaceExcludedDirs}
  onChange={setWorkspaceExcludedDirs}
  label={t('settings.workspaceExcludedDirs')}
/>
```

保存 patch（行 456-464）加 `workspace_excluded_dirs: workspaceExcludedDirs,`。

- [ ] **Step 3: ProjectSettingsForm 接入**

加 state：

```typescript
const [workspaceExcludedDirs, setWorkspaceExcludedDirs] = useState<string[]>(own?.workspace_excluded_dirs ?? []);
```

渲染同上（label 可复用同 key 或加 `settings.workspaceExcludedDirsProject`）。保存 patch（行 1037-1045）加 `workspace_excluded_dirs: workspaceExcludedDirs,`。

> 注：项目设置读取的 `own` 为 `ScopedSettings | null`，其 `workspace_excluded_dirs` 在前端类型为 `string[]`（后端 effective 已解析为数组返回；若 `getProjectSettings` 返回的是 `ScopedSettings` 原始 JSON 字符串，则改读 `settings.effective.workspace_excluded_dirs` 或在前端解析）。实现时以 `getProjectSettings` 实际返回结构为准，必要时取 `settings.effective.workspace_excluded_dirs`。

- [ ] **Step 4: i18n 文案**

在前端语言资源（与 `settings.*` 同处）加键 `settings.workspaceExcludedDirs`（中/英），文案如「工作区排除目录」/「Workspace excluded folders」。

- [ ] **Step 5: 类型构建 + 构建验证**

Run: `cd packages/frontend && npx tsc --noEmit && npm run build`
Expected: 通过。

- [ ] **Step 6: Commit**

```bash
git add packages/frontend/src/components/SettingsDialogs.tsx packages/frontend/src/index.css
git commit -m "feat(frontend): 设置新增工作区排除目录编辑器"
```

---

## 阶段 F：配置数据变更与端到端验证

### Task F1：将目标房间设为 fallback_reply + planner

**Files:** 无（运行时数据操作）

- [ ] **Step 1: 启动后端（若未运行）**

Run: `cd /Users/chendimao/WWW/openclaw-room && npm run dev:backend`（或整体 `npm run dev`）

- [ ] **Step 2: 写入房间路由设置**

Run（默认本地 token 为 `openclaw-room-dev-token`，端口以实际后端为准，假设 3000；非 workspace 路由通常不需本地 token，带上无害）：

```bash
curl -X PATCH http://localhost:3000/rooms/vcHtzxxovkbl/settings \
  -H 'Content-Type: application/json' \
  -H 'X-OpenDeepSea-Local-Token: openclaw-room-dev-token' \
  -d '{"message_routing_mode":"fallback_reply","fallback_agent_id":"planner"}'
```

Expected: 200，返回的 `effective.message_routing_mode` 为 `fallback_reply`、`effective.fallback_agent_id` 为 `planner`。

- [ ] **Step 3: 确认**

Run:

```bash
curl http://localhost:3000/rooms/vcHtzxxovkbl/settings -H 'X-OpenDeepSea-Local-Token: openclaw-room-dev-token'
```

Expected: `effective` 显示 `fallback_reply` + `planner`。

### Task F2：端到端冒烟

**Files:** 无

- [ ] **Step 1: 全量后端测试**

Run: `cd packages/backend && npm test`
Expected: 全绿。

- [ ] **Step 2: 前端构建**

Run: `cd packages/frontend && npm run build`
Expected: 成功。

- [ ] **Step 3: 浏览器手动验证**

打开 `http://localhost:5173/projects/ZwgdJRslFpih/rooms/vcHtzxxovkbl`：
1. 输入 `@` → 出现文件搜索下拉（不再是智能体）。
2. 输入关键字 → 同时出现项目库文件与工作区磁盘文件（排除 `node_modules`）。
3. 选中项目库文件发送 → planner 回复，且消息携带该文件附件。
4. 选中工作区磁盘文件发送 → planner 回复，且 prompt 含该文件内容（后端日志 / 回复体现）。
5. 普通文本（不 `@`）发送 → planner 回复。
6. 在项目设置加排除目录 `vendor` → `@` 搜索不再返回 `vendor/` 下文件。

- [ ] **Step 4: 验证报告**

将上述 6 项结果如实记录（通过/失败 + 证据）。

---

## 完成标准

- [ ] 后端 `npm test` 全绿（含新增：排除目录并集、workspace 额外排除、fileRefs 路径安全、file_refs 注入、群聊不按正文提及）。
- [ ] 前端 `npx tsc --noEmit` 与 `npm run build` 通过；新增 triggers 纯函数测试通过。
- [ ] 浏览器 6 项冒烟全部通过。
- [ ] 目标房间路由确认为 `fallback_reply` + `planner`。
- [ ] 执行 superpowers:requesting-code-review 与 verification-before-completion 后再宣告完成。
