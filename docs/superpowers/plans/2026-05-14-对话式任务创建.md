# 对话式任务创建 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让手动创建任务、`/task` 快捷创建和 workflow 关键流转都在聊天记录中形成可追溯对话，同时保持任务和 workflow 的结构化状态。

**Architecture:** 后端新增任务对话服务，统一负责创建任务、写入用户意图消息、写入系统任务事件和广播 WebSocket；任务表增加来源字段，消息 metadata 承载任务事件信息。前端通过新 API 创建任务，聊天区解析任务事件 metadata 并以轻量样式渲染。

**Tech Stack:** Node.js, Express, better-sqlite3, TypeScript, React 18, Vite, TanStack Query, Tailwind CSS, Vitest/node:test style tests where existing backend tests use `node:test`.

---

## File Structure

- Modify: `packages/backend/src/db.ts`
  - 给 `tasks` 表增加 `source_message_id`、`created_from` 可空字段和兼容旧库的 `ALTER TABLE` 迁移。
- Modify: `packages/backend/src/types.ts`
  - 增加 `TaskCreatedFrom`、`TaskEventType`、任务事件 metadata 类型，并扩展 `Task`、`MessageMetadata`。
- Modify: `packages/backend/src/repos/tasks.ts`
  - 支持任务来源字段写入、读取和更新状态不受影响。
- Create: `packages/backend/src/task-conversation.ts`
  - 提供 `createTaskWithConversation` 和 `recordTaskEvent`。
- Create: `packages/backend/src/task-conversation.test.ts`
  - 覆盖手动创建任务写聊天、普通任务字段、任务事件 metadata。
- Modify: `packages/backend/src/routes.ts`
  - 新增对话式任务创建路由；保留旧任务路由但改为走统一服务；状态更新写任务事件。
- Modify: `packages/backend/src/workflows/orchestrator.ts`
  - workflow 创建、计划等待、分配子任务、阻塞、完成、取消等关键节点调用 `recordTaskEvent`。
- Modify: `packages/frontend/src/lib/types.ts`
  - 同步任务来源字段和任务事件 metadata 类型。
- Modify: `packages/frontend/src/lib/messageMetadata.ts`
  - 解析并净化任务事件 metadata，同时保留附件解析。
- Modify: `packages/frontend/src/lib/api.ts`
  - 新增 `createTaskWithConversation`，保留 `createTask` 用于兼容或 workflow 内部需求。
- Modify: `packages/frontend/src/components/CreateTaskDialog.tsx`
  - 手动创建任务改走对话式任务 API，成功后刷新消息和任务。
- Modify: `packages/frontend/src/pages/RoomPage.tsx`
  - `/task` 快捷命令改走对话式任务 API；WebSocket 收到任务事件消息后自然进入聊天流；系统任务事件用轻量行渲染。
- Test/verify: `npm test -- --runInBand` if available, otherwise targeted `node --test` backend tests and `npm run build`.

---

## Task 1: 数据模型和类型扩展

**Files:**
- Modify: `packages/backend/src/db.ts`
- Modify: `packages/backend/src/types.ts`
- Modify: `packages/backend/src/repos/tasks.ts`
- Modify: `packages/frontend/src/lib/types.ts`

- [ ] **Step 1: Extend backend shared types**

In `packages/backend/src/types.ts`, add these type exports near the task/message type definitions:

```ts
export type TaskCreatedFrom = 'manual' | 'chat_plan' | 'slash_command' | 'workflow_assignment';

export type TaskEventType =
  | 'plan_proposed'
  | 'task_created'
  | 'task_updated'
  | 'task_status_changed'
  | 'workflow_started'
  | 'workflow_stage_changed'
  | 'workflow_plan_ready'
  | 'workflow_assignment_created'
  | 'workflow_blocked'
  | 'workflow_completed'
  | 'workflow_cancelled';

export interface MessageTaskEventMetadata {
  task_id?: string;
  task_title?: string;
  workflow_run_id?: string;
  workflow_step_id?: string;
  event_type?: TaskEventType;
  origin?: TaskCreatedFrom;
}
```

Then change `MessageMetadata` from:

```ts
export interface MessageMetadata {
  attachments?: MessageAttachmentMetadata[];
}
```

to:

```ts
export interface MessageMetadata extends MessageTaskEventMetadata {
  attachments?: MessageAttachmentMetadata[];
}
```

Extend `Task`:

```ts
export interface Task {
  id: string;
  room_id: string;
  project_id: string;
  parent_task_id: string | null;
  title: string;
  description: string | null;
  status: TaskStatus;
  priority: TaskPriority;
  interaction_mode: TaskInteractionMode;
  assigned_agent_id: string | null;
  source_message_id: string | null;
  created_from: TaskCreatedFrom | null;
  created_at: number;
  updated_at: number;
  completed_at: number | null;
}
```

- [ ] **Step 2: Add SQLite columns and idempotent migration**

In `packages/backend/src/db.ts`, update the `tasks` table definition wherever it appears in the file so the column list includes:

```sql
  source_message_id TEXT,
  created_from TEXT,
```

immediately before `created_at`.

After the main `db.exec(...)` schema block, add an idempotent migration helper if the file does not already have one:

```ts
function ensureColumn(table: string, column: string, definition: string): void {
  const columns = db.prepare(`PRAGMA table_info(${table})`).all() as Array<{ name: string }>;
  if (columns.some((item) => item.name === column)) return;
  db.prepare(`ALTER TABLE ${table} ADD COLUMN ${column} ${definition}`).run();
}

ensureColumn('tasks', 'source_message_id', 'TEXT');
ensureColumn('tasks', 'created_from', 'TEXT');
```

Expected: new databases get the columns from `CREATE TABLE`; old databases get them through `ALTER TABLE`.

- [ ] **Step 3: Update task repository create input**

In `packages/backend/src/repos/tasks.ts`, update the import:

```ts
import type { Task, TaskCreatedFrom, TaskInteractionMode, TaskPriority, TaskStatus } from '../types.js';
```

Extend the `create` input:

```ts
source_message_id?: string | null;
created_from?: TaskCreatedFrom | null;
```

Update the `INSERT` SQL:

```ts
`INSERT INTO tasks (
  id, room_id, project_id, parent_task_id, title, description, status,
  priority, interaction_mode, assigned_agent_id, source_message_id, created_from, created_at, updated_at
)
 VALUES (?, ?, ?, ?, ?, ?, 'todo', ?, ?, ?, ?, ?, ?, ?)`
```

Update `.run(...)` arguments:

```ts
input.assigned_agent_id ?? null,
input.source_message_id ?? null,
input.created_from ?? null,
ts,
ts,
```

- [ ] **Step 4: Mirror frontend shared types**

In `packages/frontend/src/lib/types.ts`, add:

```ts
export type TaskCreatedFrom = 'manual' | 'chat_plan' | 'slash_command' | 'workflow_assignment';

export type TaskEventType =
  | 'plan_proposed'
  | 'task_created'
  | 'task_updated'
  | 'task_status_changed'
  | 'workflow_started'
  | 'workflow_stage_changed'
  | 'workflow_plan_ready'
  | 'workflow_assignment_created'
  | 'workflow_blocked'
  | 'workflow_completed'
  | 'workflow_cancelled';
```

Change `MessageMetadata` to:

```ts
export interface MessageMetadata {
  attachments: MessageAttachmentMetadata[];
  task_id?: string;
  task_title?: string;
  workflow_run_id?: string;
  workflow_step_id?: string;
  event_type?: TaskEventType;
  origin?: TaskCreatedFrom;
}
```

Extend `Task`:

```ts
  source_message_id: string | null;
  created_from: TaskCreatedFrom | null;
```

Place the new fields before `created_at`.

- [ ] **Step 5: Run build to catch type/schema errors**

Run:

```bash
npm run build
```

Expected: TypeScript either passes or fails only on references that later tasks intentionally introduce. If it fails from these type changes, fix this task before proceeding.

- [ ] **Step 6: Commit**

```bash
git add packages/backend/src/db.ts packages/backend/src/types.ts packages/backend/src/repos/tasks.ts packages/frontend/src/lib/types.ts
git commit -m "feat: 扩展任务来源字段"
```

---

## Task 2: 后端任务对话服务

**Files:**
- Create: `packages/backend/src/task-conversation.ts`
- Create: `packages/backend/src/task-conversation.test.ts`
- Modify: `packages/backend/src/types.ts` if Task 1 missed exported request/response helper types

- [ ] **Step 1: Create task conversation service**

Create `packages/backend/src/task-conversation.ts`:

```ts
import { messageRepo } from './repos/messages.js';
import { roomRepo } from './repos/rooms.js';
import { settingsRepo } from './repos/settings.js';
import { taskRepo } from './repos/tasks.js';
import { wsHub } from './ws-hub.js';
import type {
  Message,
  MessageMetadata,
  Task,
  TaskCreatedFrom,
  TaskEventType,
  TaskInteractionMode,
  TaskPriority,
} from './types.js';

export interface CreateTaskWithConversationInput {
  roomId: string;
  actor?: {
    sender_id?: string;
    sender_name?: string;
  };
  taskInput: {
    title: string;
    description?: string | null;
    priority?: TaskPriority;
    interaction_mode?: TaskInteractionMode;
    assigned_agent_id?: string | null;
    parent_task_id?: string | null;
  };
  origin: TaskCreatedFrom;
  sourceMessageId?: string | null;
  userFacingContent?: string;
  createUserMessage?: boolean;
}

export interface CreateTaskWithConversationResult {
  task: Task;
  userMessage: Message | null;
  systemMessage: Message;
}

export function createTaskWithConversation(input: CreateTaskWithConversationInput): CreateTaskWithConversationResult {
  const room = roomRepo.get(input.roomId);
  if (!room) throw new Error('room not found');

  const title = input.taskInput.title.trim();
  if (!title) throw new Error('title is required');

  const shouldCreateUserMessage = input.createUserMessage !== false && !input.sourceMessageId;
  const userMessage = shouldCreateUserMessage
    ? createUserIntentMessage({
        roomId: input.roomId,
        senderId: input.actor?.sender_id ?? 'user',
        senderName: input.actor?.sender_name ?? 'You',
        content: input.userFacingContent ?? `创建任务：${title}`,
        origin: input.origin,
      })
    : null;

  const sourceMessageId = input.sourceMessageId ?? userMessage?.id ?? null;
  const interactionMode =
    input.taskInput.interaction_mode ?? settingsRepo.resolveForRoom(input.roomId)?.effective.interaction_mode ?? 'ask_user';

  const task = taskRepo.create({
    room_id: input.roomId,
    project_id: room.project_id,
    parent_task_id: input.taskInput.parent_task_id ?? undefined,
    title,
    description: input.taskInput.description ?? undefined,
    priority: input.taskInput.priority,
    interaction_mode: interactionMode,
    assigned_agent_id: input.taskInput.assigned_agent_id ?? undefined,
    source_message_id: sourceMessageId,
    created_from: input.origin,
  });

  wsHub.broadcast(input.roomId, { type: 'task:created', task });

  const systemMessage = recordTaskEvent({
    roomId: input.roomId,
    taskId: task.id,
    taskTitle: task.title,
    eventType: 'task_created',
    origin: input.origin,
    content: buildTaskCreatedMessage(task),
  });

  return { task, userMessage, systemMessage };
}

export function recordTaskEvent(input: {
  roomId: string;
  taskId: string;
  taskTitle?: string;
  workflowRunId?: string | null;
  workflowStepId?: string | null;
  eventType: TaskEventType;
  origin?: TaskCreatedFrom;
  content: string;
}): Message {
  const metadata: MessageMetadata = {
    task_id: input.taskId,
    task_title: input.taskTitle,
    workflow_run_id: input.workflowRunId ?? undefined,
    workflow_step_id: input.workflowStepId ?? undefined,
    event_type: input.eventType,
    origin: input.origin,
  };
  const message = messageRepo.create({
    room_id: input.roomId,
    sender_type: 'system',
    sender_id: 'system',
    sender_name: 'System',
    content: input.content,
    message_type: 'system',
    metadata,
  });
  wsHub.broadcast(input.roomId, { type: 'message:new', roomId: input.roomId, message });
  return message;
}

function createUserIntentMessage(input: {
  roomId: string;
  senderId: string;
  senderName: string;
  content: string;
  origin: TaskCreatedFrom;
}): Message {
  const message = messageRepo.create({
    room_id: input.roomId,
    sender_type: 'user',
    sender_id: input.senderId,
    sender_name: input.senderName,
    content: input.content,
    message_type: 'text',
    metadata: { origin: input.origin },
  });
  wsHub.broadcast(input.roomId, { type: 'message:new', roomId: input.roomId, message });
  return message;
}

function buildTaskCreatedMessage(task: Task): string {
  const parts = [`已创建任务 #${task.id}：${task.title}`];
  parts.push(`优先级 ${task.priority}`);
  parts.push(task.assigned_agent_id ? `已指派 ${task.assigned_agent_id}` : '未指派');
  return parts.join('，');
}
```

- [ ] **Step 2: Write backend service tests**

Create `packages/backend/src/task-conversation.test.ts`:

```ts
import { strict as assert } from 'node:assert';
import test from 'node:test';
import { db } from './db.js';
import { createTaskWithConversation, recordTaskEvent } from './task-conversation.js';

function insertProjectAndRoom(): { projectId: string; roomId: string } {
  const projectId = `project-${Date.now()}-${Math.random()}`;
  const roomId = `room-${Date.now()}-${Math.random()}`;
  const ts = Date.now();
  db.prepare(
    `INSERT INTO projects (id, name, path, description, message_routing_mode, fallback_agent_id, created_at, updated_at)
     VALUES (?, ?, ?, NULL, 'mentions_only', NULL, ?, ?)`,
  ).run(projectId, projectId, `/tmp/${projectId}`, ts, ts);
  db.prepare(
    `INSERT INTO rooms (id, project_id, name, description, created_at)
     VALUES (?, ?, 'Room', NULL, ?)`,
  ).run(roomId, projectId, ts);
  return { projectId, roomId };
}

test('createTaskWithConversation creates user message, task, and system task event', () => {
  const { roomId } = insertProjectAndRoom();
  const result = createTaskWithConversation({
    roomId,
    origin: 'manual',
    actor: { sender_id: 'user', sender_name: 'You' },
    taskInput: { title: '修复登录错误', priority: 'high' },
  });

  assert.equal(result.userMessage?.content, '创建任务：修复登录错误');
  assert.equal(result.task.title, '修复登录错误');
  assert.equal(result.task.priority, 'high');
  assert.equal(result.task.created_from, 'manual');
  assert.equal(result.task.source_message_id, result.userMessage?.id);
  assert.equal(result.systemMessage.sender_type, 'system');
  assert.equal(result.systemMessage.message_type, 'system');

  const metadata = JSON.parse(result.systemMessage.metadata ?? '{}') as Record<string, unknown>;
  assert.equal(metadata.event_type, 'task_created');
  assert.equal(metadata.task_id, result.task.id);
  assert.equal(metadata.task_title, result.task.title);
  assert.equal(metadata.origin, 'manual');
});

test('recordTaskEvent persists workflow metadata on a system message', () => {
  const { roomId } = insertProjectAndRoom();
  const task = createTaskWithConversation({
    roomId,
    origin: 'slash_command',
    taskInput: { title: '补充测试' },
  }).task;

  const message = recordTaskEvent({
    roomId,
    taskId: task.id,
    taskTitle: task.title,
    workflowRunId: 'workflow-1',
    workflowStepId: 'step-1',
    eventType: 'workflow_stage_changed',
    content: '任务进入分析阶段',
  });

  const metadata = JSON.parse(message.metadata ?? '{}') as Record<string, unknown>;
  assert.equal(metadata.task_id, task.id);
  assert.equal(metadata.workflow_run_id, 'workflow-1');
  assert.equal(metadata.workflow_step_id, 'step-1');
  assert.equal(metadata.event_type, 'workflow_stage_changed');
});
```

- [ ] **Step 3: Run targeted backend test**

Run:

```bash
node --test packages/backend/src/task-conversation.test.ts
```

Expected: both tests pass. If TypeScript execution is not configured for direct `node --test`, run the repository's existing backend test command used by current tests; if none exists, run `npm run build` and document the missing direct test runner in the final handoff.

- [ ] **Step 4: Commit**

```bash
git add packages/backend/src/task-conversation.ts packages/backend/src/task-conversation.test.ts packages/backend/src/types.ts
git commit -m "feat: 添加任务对话服务"
```

---

## Task 3: 路由接入和普通任务更新事件

**Files:**
- Modify: `packages/backend/src/routes.ts`
- Modify: `packages/frontend/src/lib/api.ts`

- [ ] **Step 1: Import task conversation service**

In `packages/backend/src/routes.ts`, add:

```ts
import { createTaskWithConversation, recordTaskEvent } from './task-conversation.js';
```

- [ ] **Step 2: Add conversation task route schema**

Near the existing task route schemas, add:

```ts
const taskCreateSchema = z.object({
  title: z.string().min(1),
  description: z.string().optional(),
  priority: z.enum(['low', 'normal', 'high', 'urgent']).optional(),
  interaction_mode: z.enum(['ask_user', 'auto_recommended']).optional(),
  assigned_agent_id: z.string().optional(),
  parent_task_id: z.string().optional(),
});

const conversationTaskCreateSchema = taskCreateSchema.extend({
  origin: z.enum(['manual', 'slash_command', 'chat_plan']).default('manual'),
  sender_id: z.string().default('user'),
  sender_name: z.string().optional(),
  user_message: z.string().optional(),
  source_message_id: z.string().nullable().optional(),
});
```

- [ ] **Step 3: Add new API route**

Before the existing `router.post('/rooms/:roomId/tasks', ...)`, add:

```ts
router.post('/rooms/:roomId/tasks/conversation', (req, res) => {
  const parsed = conversationTaskCreateSchema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  try {
    const result = createTaskWithConversation({
      roomId: req.params.roomId,
      actor: {
        sender_id: parsed.data.sender_id,
        sender_name: parsed.data.sender_name,
      },
      origin: parsed.data.origin,
      sourceMessageId: parsed.data.source_message_id ?? null,
      userFacingContent: parsed.data.user_message,
      taskInput: {
        title: parsed.data.title,
        description: parsed.data.description,
        priority: parsed.data.priority,
        interaction_mode: parsed.data.interaction_mode,
        assigned_agent_id: parsed.data.assigned_agent_id,
        parent_task_id: parsed.data.parent_task_id,
      },
    });
    res.status(201).json(result);
  } catch (err) {
    const message = (err as Error).message;
    res.status(message === 'room not found' ? 404 : 400).json({ error: message });
  }
});
```

- [ ] **Step 4: Update legacy task creation route to use shared schema**

Replace the inline schema in `router.post('/rooms/:roomId/tasks', ...)` with `taskCreateSchema`. Keep this route as a pure task route for compatibility, but pass default source fields:

```ts
const task = taskRepo.create({
  room_id: req.params.roomId,
  project_id: room.project_id,
  ...parsed.data,
  interaction_mode:
    parsed.data.interaction_mode ?? settingsRepo.resolveForRoom(req.params.roomId)?.effective.interaction_mode,
  created_from: parsed.data.parent_task_id ? 'workflow_assignment' : null,
});
```

Do not write chat messages in this legacy route; the new frontend path will use `/tasks/conversation`.

- [ ] **Step 5: Record task status update events**

In `router.patch('/tasks/:id', ...)`, store the previous task before updating:

```ts
const before = task;
```

After broadcasting `task:updated`, add:

```ts
if (task && parsed.data.status && before.status !== task.status) {
  recordTaskEvent({
    roomId: task.room_id,
    taskId: task.id,
    taskTitle: task.title,
    eventType: 'task_status_changed',
    content: `任务「${task.title}」状态变更为 ${task.status}`,
  });
}
```

Ensure this block only runs after `task` is truthy.

- [ ] **Step 6: Add frontend API method**

In `packages/frontend/src/lib/api.ts`, add this method next to `createTask`:

```ts
createTaskWithConversation: (
  roomId: string,
  input: {
    title: string;
    description?: string;
    priority?: Task['priority'];
    interaction_mode?: Task['interaction_mode'];
    assigned_agent_id?: string;
    parent_task_id?: string;
    origin?: 'manual' | 'slash_command' | 'chat_plan';
    sender_id?: string;
    sender_name?: string;
    user_message?: string;
    source_message_id?: string | null;
  },
) =>
  request<{ task: Task; userMessage: Message | null; systemMessage: Message }>(
    `/rooms/${roomId}/tasks/conversation`,
    { method: 'POST', body: JSON.stringify(input) },
  ),
```

- [ ] **Step 7: Run build**

Run:

```bash
npm run build
```

Expected: build passes.

- [ ] **Step 8: Commit**

```bash
git add packages/backend/src/routes.ts packages/frontend/src/lib/api.ts
git commit -m "feat: 接入对话式任务路由"
```

---

## Task 4: workflow 关键事件写入聊天

**Files:**
- Modify: `packages/backend/src/workflows/orchestrator.ts`

- [ ] **Step 1: Import task event helper**

In `packages/backend/src/workflows/orchestrator.ts`, add:

```ts
import { recordTaskEvent } from '../task-conversation.js';
```

- [ ] **Step 2: Add safe workflow event wrapper**

Near existing broadcast helper functions, add:

```ts
function recordWorkflowTaskEvent(input: {
  run: WorkflowRun;
  task: Task;
  workflowStepId?: string | null;
  eventType:
    | 'workflow_started'
    | 'workflow_stage_changed'
    | 'workflow_plan_ready'
    | 'workflow_assignment_created'
    | 'workflow_blocked'
    | 'workflow_completed'
    | 'workflow_cancelled';
  content: string;
}): void {
  try {
    recordTaskEvent({
      roomId: input.run.room_id,
      taskId: input.task.id,
      taskTitle: input.task.title,
      workflowRunId: input.run.id,
      workflowStepId: input.workflowStepId ?? null,
      eventType: input.eventType,
      content: input.content,
    });
  } catch (err) {
    console.warn(`[workflow] failed to record task event: ${(err as Error).message}`);
  }
}
```

- [ ] **Step 3: Record workflow start**

In `workflowOrchestrator.start`, after `broadcastWorkflow('workflow:created', run);`, add:

```ts
recordWorkflowTaskEvent({
  run,
  task,
  eventType: 'workflow_started',
  content: `任务「${task.title}」已启动开发闭环`,
});
```

- [ ] **Step 4: Record each agent stage start**

In `startAgentStageWithAgent`, after broadcasting the created step and updating the run, add:

```ts
recordWorkflowTaskEvent({
  run,
  task,
  workflowStepId: step.id,
  eventType: 'workflow_stage_changed',
  content: `任务「${task.title}」进入 ${stage} 阶段`,
});
```

Use the same block in `startAgentStage` if the file has both functions creating workflow steps for stages.

- [ ] **Step 5: Record plan ready**

In the planning completion branch where a `plan` artifact is created and the run becomes `awaiting_approval`, add after `broadcastWorkflow('workflow:updated', updated);`:

```ts
recordWorkflowTaskEvent({
  run: updated,
  task: requireTask(updated.task_id),
  workflowStepId: step.id,
  eventType: 'workflow_plan_ready',
  content: `任务「${requireTask(updated.task_id).title}」计划已生成，等待确认`,
});
```

Store `const updatedTask = requireTask(updated.task_id);` before calling the helper to avoid calling `requireTask` twice.

- [ ] **Step 6: Record assignment creation and mark child source**

In `assignFromPlan`, update child task creation:

```ts
const child = taskRepo.create({
  room_id: task.room_id,
  project_id: task.project_id,
  parent_task_id: task.id,
  title: item.title,
  description: `${item.description}\n\n验收点：\n${item.acceptance.map((point) => `- ${point}`).join('\n')}`,
  priority: item.priority,
  assigned_agent_id: assigned?.id,
  created_from: 'workflow_assignment',
});
```

After the assignment artifact is broadcast, add:

```ts
recordWorkflowTaskEvent({
  run,
  task,
  workflowStepId: step.id,
  eventType: 'workflow_assignment_created',
  content: `已根据计划为任务「${task.title}」创建 ${plan.tasks.length} 个子任务`,
});
```

- [ ] **Step 7: Record blocked, cancelled, completed**

In `block(run, error)`, after workflow update broadcast, fetch task and record:

```ts
const task = taskRepo.get(run.task_id);
if (updated && task) {
  recordWorkflowTaskEvent({
    run: updated,
    task,
    eventType: 'workflow_blocked',
    content: `任务「${task.title}」已阻塞：${error}`,
  });
}
```

In `workflowOrchestrator.cancel`, after `broadcastWorkflow('workflow:updated', updated);`, record:

```ts
const task = requireTask(updated.task_id);
recordWorkflowTaskEvent({
  run: updated,
  task,
  eventType: 'workflow_cancelled',
  content: `任务「${task.title}」开发闭环已取消`,
});
```

In the acceptance success branch where workflow becomes `completed`, after broadcast, record:

```ts
const completedTask = requireTask(updated.task_id);
recordWorkflowTaskEvent({
  run: updated,
  task: completedTask,
  eventType: 'workflow_completed',
  content: `任务「${completedTask.title}」完成，验收通过`,
});
```

- [ ] **Step 8: Run build**

Run:

```bash
npm run build
```

Expected: build passes.

- [ ] **Step 9: Commit**

```bash
git add packages/backend/src/workflows/orchestrator.ts
git commit -m "feat: 记录任务流转聊天事件"
```

---

## Task 5: 前端任务事件解析与渲染

**Files:**
- Modify: `packages/frontend/src/lib/messageMetadata.ts`
- Modify: `packages/frontend/src/pages/RoomPage.tsx`

- [ ] **Step 1: Parse task event metadata**

In `packages/frontend/src/lib/messageMetadata.ts`, keep attachment sanitization and update `parseMessageMetadata` return:

```ts
const taskEvent = sanitizeTaskEventMetadata(parsed);

return { attachments, ...taskEvent };
```

Add:

```ts
const taskEventTypes = new Set([
  'plan_proposed',
  'task_created',
  'task_updated',
  'task_status_changed',
  'workflow_started',
  'workflow_stage_changed',
  'workflow_plan_ready',
  'workflow_assignment_created',
  'workflow_blocked',
  'workflow_completed',
  'workflow_cancelled',
]);

const taskOrigins = new Set(['manual', 'chat_plan', 'slash_command', 'workflow_assignment']);

function sanitizeTaskEventMetadata(value: Record<string, unknown>) {
  return {
    task_id: typeof value.task_id === 'string' ? value.task_id : undefined,
    task_title: typeof value.task_title === 'string' ? value.task_title : undefined,
    workflow_run_id: typeof value.workflow_run_id === 'string' ? value.workflow_run_id : undefined,
    workflow_step_id: typeof value.workflow_step_id === 'string' ? value.workflow_step_id : undefined,
    event_type:
      typeof value.event_type === 'string' && taskEventTypes.has(value.event_type)
        ? value.event_type
        : undefined,
    origin:
      typeof value.origin === 'string' && taskOrigins.has(value.origin)
        ? value.origin
        : undefined,
  };
}
```

- [ ] **Step 2: Render task event system messages as compact rows**

In `packages/frontend/src/pages/RoomPage.tsx`, update `MessageBubble` after metadata is parsed:

```ts
const isTaskEvent = isSystem && Boolean(metadata.event_type && metadata.task_id);
```

Before the current `if (isSystem) { return ... }`, add:

```tsx
if (isTaskEvent) {
  return (
    <div className="flex justify-center py-1">
      <button
        type="button"
        className="task-event-row"
        onClick={() => {
          // Task selection is wired in a later UI refinement; keep this as a non-destructive affordance.
        }}
        title={metadata.task_title ?? metadata.task_id}
      >
        <CheckSquare className="h-3.5 w-3.5" strokeWidth={1.8} />
        <span className="truncate">{message.content}</span>
      </button>
    </div>
  );
}
```

If `CheckSquare` is not imported from `lucide-react`, add it to the existing import list.

- [ ] **Step 3: Add CSS for compact task event**

In `packages/frontend/src/index.css`, add a compact style near message styles:

```css
.task-event-row {
  display: inline-flex;
  max-width: min(720px, 100%);
  align-items: center;
  gap: 0.375rem;
  border: 1px solid var(--color-border);
  border-radius: 999px;
  background: var(--color-surface-raised);
  color: var(--color-fg-muted);
  padding: 0.35rem 0.7rem;
  font-size: 11.5px;
  line-height: 1.25;
}

.task-event-row:hover {
  color: var(--color-fg);
  border-color: var(--color-border-strong);
}
```

- [ ] **Step 4: Run build**

Run:

```bash
npm run build
```

Expected: build passes and no JSX/type errors remain.

- [ ] **Step 5: Commit**

```bash
git add packages/frontend/src/lib/messageMetadata.ts packages/frontend/src/pages/RoomPage.tsx packages/frontend/src/index.css
git commit -m "feat: 渲染任务聊天事件"
```

---

## Task 6: 前端创建任务入口改为对话式

**Files:**
- Modify: `packages/frontend/src/components/CreateTaskDialog.tsx`
- Modify: `packages/frontend/src/pages/RoomPage.tsx`

- [ ] **Step 1: Update manual task dialog mutation**

In `packages/frontend/src/components/CreateTaskDialog.tsx`, replace:

```ts
api.createTask(roomId, {
```

with:

```ts
api.createTaskWithConversation(roomId, {
```

Add:

```ts
origin: 'manual',
user_message: `创建任务：${title.trim()}`,
```

inside the input object.

In `onSuccess`, add:

```ts
queryClient.invalidateQueries({ queryKey: ['messages', roomId] });
```

right after invalidating `room-tasks`.

- [ ] **Step 2: Update `/task` command path**

In `packages/frontend/src/pages/RoomPage.tsx`, replace `createTaskFromCommand` mutation:

```ts
const createTaskFromCommand = useMutation({
  mutationFn: (title: string) => api.createTask(roomId, { title }),
```

with:

```ts
const createTaskFromCommand = useMutation({
  mutationFn: (title: string) =>
    api.createTaskWithConversation(roomId, {
      title,
      origin: 'slash_command',
      user_message: `/task ${title}`,
    }),
```

In `onSuccess`, add:

```ts
queryClient.invalidateQueries({ queryKey: ['messages', roomId] });
```

before or after invalidating tasks.

- [ ] **Step 3: Confirm no ordinary chat path creates tasks**

Inspect `handleSend` in `RoomPage.tsx` and keep this behavior:

```ts
if (taskMatch?.[1]?.trim()) {
  ...
  createTaskFromCommand.mutate(taskMatch[1].trim());
  return;
}
send.mutate({ content, mentions: input.mentions, files });
```

Do not add automatic task creation to the normal `send.mutate` path.

- [ ] **Step 4: Run build**

Run:

```bash
npm run build
```

Expected: build passes.

- [ ] **Step 5: Commit**

```bash
git add packages/frontend/src/components/CreateTaskDialog.tsx packages/frontend/src/pages/RoomPage.tsx
git commit -m "feat: 通过对话创建任务"
```

---

## Task 7: 端到端验证和文档勾选

**Files:**
- Modify: `docs/superpowers/plans/2026-05-14-对话式任务创建.md`

- [ ] **Step 1: Run full build**

Run:

```bash
npm run build
```

Expected: build passes.

- [ ] **Step 2: Run available backend tests**

Run:

```bash
npm test
```

Expected: if a test script exists, all tests pass. If `npm test` is missing, run:

```bash
node --test packages/backend/src/*.test.ts packages/backend/src/**/*.test.ts
```

Expected: either tests pass under the repository's configured loader or the command reveals that TypeScript direct test execution is not configured. Report the exact result in the final handoff.

- [ ] **Step 3: Manual smoke test**

Start dev server:

```bash
npm run dev
```

Expected: backend and frontend start. Open the frontend URL shown by Vite, usually `http://localhost:5173`.

Smoke flow:

1. Open an existing project and room.
2. Send a normal chat message. Expected: chat message appears; task count does not increase.
3. Use `/task 验证对话式任务创建`. Expected: chat shows the command/user intent and a system task event; task board shows the new task.
4. Click “新建任务” and create `手动任务聊天记录验证`. Expected: chat shows user intent and system task event; task board shows the new task.
5. Change a task status. Expected: chat shows a compact system task event for status change.

- [ ] **Step 4: Mark this plan checklist**

Edit this plan file and mark completed steps as `[x]` for all tasks that were actually completed.

- [ ] **Step 5: Final commit**

```bash
git add docs/superpowers/plans/2026-05-14-对话式任务创建.md
git commit -m "docs: 更新对话式任务创建执行记录"
```

---

## Self-Review Checklist

- Spec coverage:
  - 普通聊天不创建任务：Task 6 Step 3.
  - 手动创建任务写聊天：Task 2, Task 3, Task 6.
  - `/task` 写聊天：Task 3, Task 6.
  - 任务来源字段：Task 1.
  - 任务事件 metadata：Task 1, Task 2, Task 5.
  - workflow 关键阶段写聊天：Task 4.
  - 前端轻量任务事件渲染：Task 5.
  - 验证方式：Task 7.
- Placeholder scan: no placeholder markers or vague implementation-only steps are intended.
- Type consistency:
  - Backend and frontend both use `TaskCreatedFrom` and `TaskEventType`.
  - Service methods are named `createTaskWithConversation` and `recordTaskEvent` consistently.
  - API route returns `{ task, userMessage, systemMessage }`, matching frontend API type.
