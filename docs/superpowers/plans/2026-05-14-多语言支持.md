# 多语言支持 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 OpenClaw Room 前端添加 `zh` / `en` 双语界面支持，默认中文，并持久保存用户选择。

**Architecture:** 在前端新增轻量 i18n Context，集中维护字典和格式化工具；组件通过 `useI18n()` 获取 `t()`、`locale`、`setLocale()` 和 `formatRelativeTime()`。不改后端 API、数据库 schema 或用户生成内容。

**Tech Stack:** React 18、TypeScript、Vite、Tailwind、localStorage、现有 UI primitives。

---

## File Structure

- Create: `packages/frontend/src/lib/i18n.tsx`
  - 定义 `Locale = 'zh' | 'en'`、`DEFAULT_LOCALE`、`isLocale()`、`getInitialLocale()`、`I18nProvider`、`useI18n()`、`t()` 和 `formatRelativeTime()`。
  - 维护 `zhMessages` 和 `enMessages`，中文字典作为 key 类型来源。
- Modify: `packages/frontend/src/main.tsx`
  - 包裹 `I18nProvider`。
- Modify: `packages/frontend/src/components/AppShell.tsx`
  - 迁移侧边栏、主题标签、OpenClaw 网关文案。
  - 增加语言切换控件。
- Modify: `packages/frontend/src/lib/types.ts`
  - 保留数据类型，移除或停止使用静态中文 label 常量。
- Modify: `packages/frontend/src/lib/utils.ts`
  - 保留 `relativeTime()` 兼容旧调用；新 UI 迁移到 `formatRelativeTime()`。
- Modify: `packages/frontend/src/pages/DashboardPage.tsx`
  - 迁移项目总览文案和相对时间。
- Modify: `packages/frontend/src/pages/ProjectPage.tsx`
  - 迁移项目详情、群聊列表、创建群聊文案。
- Modify: `packages/frontend/src/pages/RoomPage.tsx`
  - 迁移房间页工具栏、聊天空状态、composer placeholder、toast 和路由策略提示。
- Modify: `packages/frontend/src/components/CreateProjectDialog.tsx`
  - 迁移创建项目弹窗。
- Modify: `packages/frontend/src/components/CreateTaskDialog.tsx`
  - 迁移创建任务弹窗。
- Modify: `packages/frontend/src/components/AddAgentDialog.tsx`
  - 迁移邀请 Agent 弹窗。
- Modify: `packages/frontend/src/components/CommandMenu.tsx`
  - 迁移全局命令菜单。
- Modify: `packages/frontend/src/components/SettingsDialogs.tsx`
  - 迁移系统、项目、群聊设置。
- Modify: `packages/frontend/src/components/ProjectRoutingDialog.tsx`
  - 迁移项目消息路由弹窗。
- Modify: `packages/frontend/src/components/TaskBoard.tsx`
  - 迁移任务看板、任务卡状态与优先级标签。
- Modify: `packages/frontend/src/components/TaskDetailPanel.tsx`
  - 迁移任务详情和工作流操作文案。
- Modify: `packages/frontend/src/components/WorkflowTimeline.tsx`
  - 迁移工作流阶段、决策、操作文案。
- Modify: `packages/frontend/src/components/AgentRunPanel.tsx`
  - 迁移执行状态卡操作文案。
- Modify: `packages/frontend/src/components/RichMessageComposer.tsx`
  - 迁移附件、发送按钮、错误提示、aria 文案。
- Modify: `packages/frontend/src/components/MessageContent.tsx`
  - 迁移 Markdown 预览/原文/复制文案。
- Modify: `packages/frontend/src/components/AgentMentionMenu.tsx`
  - 迁移 mention 菜单空状态和 aria 文案。
- Modify: `packages/frontend/src/components/LobsterMark.tsx`
  - 迁移品牌 aria-label。

## Implementation Notes

- 本计划集中修改前端，避免触碰 `packages/backend`。
- 当前工作区已有无关未提交改动，执行时只暂存本任务相关文件。
- 不新增依赖。
- 不翻译项目名、房间名、任务标题、任务描述、聊天消息、agent 输出、provider 品牌名。
- 英文文案尽量短，避免侧边栏、按钮和任务卡溢出。

---

### Task 1: Add i18n Foundation

**Files:**
- Create: `packages/frontend/src/lib/i18n.tsx`
- Modify: `packages/frontend/src/main.tsx`

- [ ] **Step 1: Create i18n module**

Add `packages/frontend/src/lib/i18n.tsx` with this structure:

```tsx
import { createContext, useContext, useEffect, useMemo, useState, type ReactNode } from 'react';

export type Locale = 'zh' | 'en';

export const DEFAULT_LOCALE: Locale = 'zh';
const STORAGE_KEY = 'openclaw-room-locale';

const zhMessages = {
  'app.name': '深海指挥中心',
  'app.tagline': '管理本地项目、群聊与智能体协作上下文',
  'language.label': '界面语言',
  'language.zh': '中文',
  'language.en': 'EN',
  'common.cancel': '取消',
  'common.create': '创建',
  'common.save': '保存',
  'common.apply': '应用',
  'common.close': '关闭',
  'common.settings': '设置',
  'common.loading': '加载中…',
  'common.none': '暂无',
  'common.unassigned': '未指派',
  'common.delete': '删除',
  'common.retry': '重试',
  'common.refresh': '刷新',
  'common.error': '错误',
  'time.justNow': '刚刚',
  'time.minutesAgo': '{count}分钟前',
  'time.hoursAgo': '{count}小时前',
  'time.daysAgo': '{count}天前',
  'time.dateLocale': 'zh-CN',
} as const;

const enMessages: Record<keyof typeof zhMessages, string> = {
  'app.name': 'Deep Sea Command',
  'app.tagline': 'Manage local projects, rooms, and agent collaboration context',
  'language.label': 'Language',
  'language.zh': '中文',
  'language.en': 'EN',
  'common.cancel': 'Cancel',
  'common.create': 'Create',
  'common.save': 'Save',
  'common.apply': 'Apply',
  'common.close': 'Close',
  'common.settings': 'Settings',
  'common.loading': 'Loading...',
  'common.none': 'None',
  'common.unassigned': 'Unassigned',
  'common.delete': 'Delete',
  'common.retry': 'Retry',
  'common.refresh': 'Refresh',
  'common.error': 'Error',
  'time.justNow': 'just now',
  'time.minutesAgo': '{count} min ago',
  'time.hoursAgo': '{count} hr ago',
  'time.daysAgo': '{count} days ago',
  'time.dateLocale': 'en-US',
};

const dictionaries = {
  zh: zhMessages,
  en: enMessages,
};

export type MessageKey = keyof typeof zhMessages;
type Params = Record<string, string | number>;

type I18nContextValue = {
  locale: Locale;
  setLocale: (locale: Locale) => void;
  t: (key: MessageKey, params?: Params) => string;
  formatRelativeTime: (timestamp: number) => string;
};

const I18nContext = createContext<I18nContextValue | null>(null);

export function isLocale(value: unknown): value is Locale {
  return value === 'zh' || value === 'en';
}

export function getInitialLocale(): Locale {
  const storedLocale = localStorage.getItem(STORAGE_KEY);
  return isLocale(storedLocale) ? storedLocale : DEFAULT_LOCALE;
}

export function I18nProvider({ children }: { children: ReactNode }): JSX.Element {
  const [locale, setLocale] = useState<Locale>(getInitialLocale);

  useEffect(() => {
    localStorage.setItem(STORAGE_KEY, locale);
    document.documentElement.lang = locale;
  }, [locale]);

  const value = useMemo<I18nContextValue>(() => {
    const translate = (key: MessageKey, params?: Params) => {
      const template = dictionaries[locale][key] ?? dictionaries.zh[key];
      if (!params) return template;
      return template.replace(/\{(\w+)\}/g, (match, name) =>
        Object.prototype.hasOwnProperty.call(params, name) ? String(params[name]) : match,
      );
    };

    return {
      locale,
      setLocale,
      t: translate,
      formatRelativeTime: (timestamp: number) => formatRelativeTimeForLocale(timestamp, locale, translate),
    };
  }, [locale]);

  return <I18nContext.Provider value={value}>{children}</I18nContext.Provider>;
}

export function useI18n(): I18nContextValue {
  const value = useContext(I18nContext);
  if (!value) throw new Error('useI18n must be used within I18nProvider');
  return value;
}

function formatRelativeTimeForLocale(
  timestamp: number,
  locale: Locale,
  t: (key: MessageKey, params?: Params) => string,
): string {
  const diff = Date.now() - timestamp;
  const sec = Math.floor(diff / 1000);
  if (sec < 60) return t('time.justNow');
  const min = Math.floor(sec / 60);
  if (min < 60) return t('time.minutesAgo', { count: min });
  const hr = Math.floor(min / 60);
  if (hr < 24) return t('time.hoursAgo', { count: hr });
  const day = Math.floor(hr / 24);
  if (day < 7) return t('time.daysAgo', { count: day });
  return new Date(timestamp).toLocaleDateString(locale === 'zh' ? 'zh-CN' : 'en-US');
}
```

- [ ] **Step 2: Wrap app with provider**

In `packages/frontend/src/main.tsx`, import `I18nProvider` and wrap the router tree:

```tsx
import { I18nProvider } from './lib/i18n';
```

```tsx
return (
  <I18nProvider>
    <QueryClientProvider client={queryClient}>
      <BrowserRouter>
        <AppShell theme={theme} onThemeChange={setTheme}>
          <Routes>
            <Route path="/" element={<DashboardPage />} />
            <Route path="/projects/:projectId" element={<ProjectPage />} />
            <Route path="/projects/:projectId/rooms/:roomId" element={<RoomPage />} />
            <Route path="*" element={<Navigate to="/" replace />} />
          </Routes>
        </AppShell>
        <Toaster theme={toasterTheme} position="bottom-right" richColors />
      </BrowserRouter>
    </QueryClientProvider>
  </I18nProvider>
);
```

- [ ] **Step 3: Build after foundation**

Run:

```bash
npm run build
```

Expected: TypeScript and Vite compile successfully. If translation keys are incomplete, TypeScript should fail before runtime.

- [ ] **Step 4: Commit foundation**

```bash
git add packages/frontend/src/lib/i18n.tsx packages/frontend/src/main.tsx
git commit -m "feat(frontend): 添加多语言基础设施"
```

---

### Task 2: Add Language Switcher and Shell Translations

**Files:**
- Modify: `packages/frontend/src/lib/i18n.tsx`
- Modify: `packages/frontend/src/components/AppShell.tsx`
- Modify: `packages/frontend/src/components/LobsterMark.tsx`

- [ ] **Step 1: Add shell dictionary keys**

Extend the dictionaries in `packages/frontend/src/lib/i18n.tsx` with keys for:

```ts
'shell.sidebar.aria': '项目工作区',
'shell.subtitle': 'local agent console',
'shell.newProject': '新增项目',
'shell.systemSettings': '系统设置',
'shell.searchCommand': '搜索 / 快速命令',
'shell.nav.development': '开发',
'shell.nav.roadmap': '路线',
'shell.nav.tasks': '任务',
'shell.nav.workflow': '工作流',
'shell.nav.files': '文件',
'shell.currentProject': '当前项目',
'shell.recentProjects': '最近项目',
'shell.noProjects': '暂无项目',
'shell.selectProject': '选择项目',
'shell.selectProjectDescription': '打开项目后显示工作区摘要。',
'shell.metric.rooms': '群聊',
'shell.metric.tasks': '任务',
'shell.metric.inProgress': '进行中',
'shell.metric.done': '已完成',
'theme.label': '主题样式',
'theme.light': '浅色',
'theme.dark': '深色',
'theme.console': '控制台',
'theme.minimal': '极简',
'gateway.label': 'OpenClaw 网关',
'gateway.dialogTitle': '本机 OpenClaw',
'gateway.dialogDescription': '网关运行状态与本机可用 Agent 信息',
'gateway.checking': '检查中',
'gateway.online': '网关在线',
'gateway.running': '网关运行中',
'gateway.offline': '网关离线',
'gateway.serviceStatus': '服务状态',
'gateway.connected': '已连接',
'gateway.disconnected': '未连接',
'gateway.readingAgents': '正在读取本机 OpenClaw agents 列表',
'gateway.readAgentsFailed': '无法读取本机 OpenClaw agents。{error}',
'gateway.noAgents': '当前本机 OpenClaw 配置中没有可用 Agent。',
'gateway.refresh': '刷新 OpenClaw 信息',
```

Add English values with the same keys, using concise labels such as `Workspace`, `New project`, `System settings`, `Search / commands`, `Current project`, `Recent projects`, `No projects`, `Light`, `Dark`, `Console`, `Minimal`, `Gateway online`, `Gateway offline`.

- [ ] **Step 2: Add LanguageToggle component**

In `AppShell.tsx`, import `useI18n` and render a switcher near `ThemeToggle`:

```tsx
function LanguageToggle(): JSX.Element {
  const { locale, setLocale, t } = useI18n();
  const options: Array<{ value: Locale; label: string }> = [
    { value: 'zh', label: t('language.zh') },
    { value: 'en', label: t('language.en') },
  ];

  return (
    <div className="theme-toggle mb-2" aria-label={t('language.label')}>
      {options.map((option) => (
        <button
          key={option.value}
          type="button"
          className={cn('theme-toggle-option', locale === option.value && 'is-active')}
          aria-pressed={locale === option.value}
          onClick={() => setLocale(option.value)}
        >
          <span>{option.label}</span>
        </button>
      ))}
    </div>
  );
}
```

Import type:

```tsx
import { useI18n, type Locale } from '../lib/i18n';
```

- [ ] **Step 3: Replace shell hardcoded labels**

In `AppShell`, `ProjectSidebar`, `ThemeToggle`, and `OpenClawGatewayDialog`, call `const { t } = useI18n();` and replace hardcoded strings with keys from Step 1.

Example:

```tsx
<aside className="app-sidebar" aria-label={t('shell.sidebar.aria')}>
```

```tsx
<div className="truncate font-display text-[16px] font-semibold leading-tight">{t('app.name')}</div>
```

```tsx
<ThemeToggle theme={theme} onThemeChange={onThemeChange} />
<LanguageToggle />
<OpenClawGatewayDialog ... />
```

- [ ] **Step 4: Update LobsterMark aria label**

In `LobsterMark.tsx`, use `useI18n()`:

```tsx
const { t } = useI18n();
```

Then set:

```tsx
aria-label={t('app.name')}
```

- [ ] **Step 5: Build and commit shell migration**

Run:

```bash
npm run build
```

Expected: build succeeds and language switcher is type-safe.

Commit:

```bash
git add packages/frontend/src/lib/i18n.tsx packages/frontend/src/components/AppShell.tsx packages/frontend/src/components/LobsterMark.tsx
git commit -m "feat(frontend): 添加界面语言切换"
```

---

### Task 3: Migrate Shared Labels and Relative Time

**Files:**
- Modify: `packages/frontend/src/lib/i18n.tsx`
- Modify: `packages/frontend/src/lib/types.ts`
- Modify: `packages/frontend/src/lib/utils.ts`
- Modify consumers that import static label maps.

- [ ] **Step 1: Add enum label helpers to i18n module**

Add dictionary keys for task status, priority, interaction mode, routing mode, agent run status, workflow role, workflow status, and workflow stage.

Use key naming:

```ts
'task.status.todo': '待办',
'task.status.in_progress': '进行中',
'task.status.review': '待审查',
'task.status.done': '已完成',
'task.status.failed': '失败',
'task.priority.low': '低',
'task.priority.normal': '普通',
'task.priority.high': '高',
'task.priority.urgent': '紧急',
'task.interaction.ask_user': '需要决策时询问我',
'task.interaction.auto_recommended': '使用推荐选项自动继续',
'routing.mentions_only': '只响应 @',
'routing.fallback_reply': '兜底回复',
'routing.fallback_route': '兜底调度',
'agentRun.status.queued': '排队中',
'agentRun.status.running': '运行中',
'agentRun.status.completed': '已完成',
'agentRun.status.failed': '失败',
'agentRun.status.cancelled': '已取消',
'agentRun.status.interrupted': '已中断',
'workflow.role.analyst': '分析',
'workflow.role.planner': '规划',
'workflow.role.coordinator': '协调',
'workflow.role.executor': '执行',
'workflow.role.reviewer': '代码审查',
'workflow.role.acceptor': '功能验收',
'workflow.status.draft': '未启动',
'workflow.status.running': '运行中',
'workflow.status.awaiting_decision': '等待决策',
'workflow.status.awaiting_approval': '等待确认',
'workflow.status.blocked': '阻塞',
'workflow.status.cancelled': '已取消',
'workflow.status.completed': '已完成',
'workflow.status.failed': '失败',
'workflow.stage.analysis': '分析',
'workflow.stage.planning': '计划',
'workflow.stage.assignment': '分配',
'workflow.stage.implementation': '执行',
'workflow.stage.code_review': '代码审查',
'workflow.stage.acceptance': '功能验收',
```

Add English values such as `To do`, `In progress`, `Review`, `Done`, `Failed`, `Low`, `Normal`, `High`, `Urgent`, `Ask me when decisions are needed`, `Auto-continue with recommended choices`, `Mentions only`, `Fallback reply`, `Fallback routing`.

- [ ] **Step 2: Add typed label functions**

Import frontend types into `i18n.tsx` with type-only imports:

```tsx
import type {
  AgentRunStatus,
  MessageRoutingMode,
  Task,
  TaskInteractionMode,
  WorkflowRole,
  WorkflowStage,
  WorkflowStatus,
} from './types';
```

Expose helpers on context:

```ts
taskStatusLabel: (status: Task['status']) => string;
taskPriorityLabel: (priority: Task['priority']) => string;
interactionModeLabel: (mode: TaskInteractionMode) => string;
routingModeLabel: (mode: MessageRoutingMode) => string;
agentRunStatusLabel: (status: AgentRunStatus) => string;
workflowRoleLabel: (role: WorkflowRole) => string;
workflowStatusLabel: (status: WorkflowStatus) => string;
workflowStageLabel: (stage: WorkflowStage) => string;
```

Implementation pattern:

```ts
taskStatusLabel: (status) => translate(`task.status.${status}` as MessageKey),
```

- [ ] **Step 3: Stop using static Chinese label maps**

In consumers, replace imports like:

```ts
import { TASK_PRIORITY_LABEL, TASK_STATUS_LABEL } from '../lib/types';
```

with:

```ts
import { useI18n } from '../lib/i18n';
```

Then replace:

```tsx
TASK_STATUS_LABEL[status]
```

with:

```tsx
taskStatusLabel(status)
```

Remove static label constant exports from `types.ts` after all imports are gone.

- [ ] **Step 4: Replace relative time in migrated components**

For every component touched in later tasks, replace:

```tsx
relativeTime(value)
```

with:

```tsx
formatRelativeTime(value)
```

Keep `relativeTime()` in `utils.ts` until `rg "relativeTime\\(" packages/frontend/src` shows no UI consumers remain or only compatibility callers remain.

- [ ] **Step 5: Verify static label imports are gone**

Run:

```bash
rg -n "TASK_STATUS_LABEL|TASK_PRIORITY_LABEL|TASK_INTERACTION_MODE_LABEL|MESSAGE_ROUTING_MODE_LABEL|AGENT_RUN_STATUS_LABEL|WORKFLOW_ROLE_LABEL|WORKFLOW_STATUS_LABEL|WORKFLOW_STAGE_LABEL" packages/frontend/src
```

Expected: no output after migration is complete.

- [ ] **Step 6: Build and commit shared labels**

Run:

```bash
npm run build
```

Expected: build succeeds.

Commit:

```bash
git add packages/frontend/src/lib/i18n.tsx packages/frontend/src/lib/types.ts packages/frontend/src/lib/utils.ts packages/frontend/src
git commit -m "refactor(frontend): 国际化共享标签"
```

---

### Task 4: Migrate Pages

**Files:**
- Modify: `packages/frontend/src/lib/i18n.tsx`
- Modify: `packages/frontend/src/pages/DashboardPage.tsx`
- Modify: `packages/frontend/src/pages/ProjectPage.tsx`
- Modify: `packages/frontend/src/pages/RoomPage.tsx`

- [ ] **Step 1: Add page dictionary keys**

Add keys for dashboard, project page, room page, routing hints, and command errors.

Required Chinese keys:

```ts
'dashboard.newProject': '新建项目',
'dashboard.allProjects': '所有项目',
'dashboard.projectCount': '{count} 个项目',
'dashboard.loading': '加载中…',
'dashboard.emptyTitle': '还没有项目',
'dashboard.emptyDescription': '添加一个本地代码目录后，可以为它创建群聊、分配 agent，并跟踪协作任务。',
'dashboard.addFirstProject': '添加第一个项目',
'project.loading': '加载项目…',
'project.settings': '项目设置',
'project.stats.rooms': '群聊 {count}',
'project.stats.tasks': '任务 {count}',
'project.stats.done': '已完成 {count}',
'project.stats.inProgress': '进行中 {count}',
'project.rooms': '群聊',
'project.emptyRoomsTitle': '还没有群聊',
'project.emptyRoomsDescription': '为项目创建一个协作主题，后续可以邀请 agent、发送消息并拆分任务。',
'project.createFirstRoom': '创建第一个群聊',
'project.roomDeleted': '已删除',
'project.deleteRoom': '删除群聊',
'project.newRoom': '新建群聊',
'project.roomCreated': '群聊已创建',
'project.newRoomDescription': '为这个项目开启一个新的协作主题',
'project.roomName': '群聊名称',
'project.roomDescription': '主题描述 (可选)',
'project.roomDescriptionPlaceholder': '这个群聊的目标是...',
'project.creating': '创建中…',
'room.backToProject': '返回项目',
'room.defaultName': '开发闭环看板',
'room.viewLabel': '工作区视图',
'room.tab.chat': '聊天',
'room.tab.tasks': '任务',
'room.tab.workflow': '工作流',
'room.tab.files': '文件',
'room.tab.agent': 'Agent',
'room.tab.settings': '设置',
'room.newTask': '新建任务',
'room.roomSettings': '群聊设置',
'room.inviteAgent': '邀请 Agent',
'room.chatAria': 'AI Agent 协作流',
'room.noAgents': '暂无 agent',
'room.configureAgent': '配置 {name}',
'room.taskCreated': '任务已创建',
'room.retryStageDone': '已重试当前阶段',
'room.taskCommandNoAttachments': '/task 命令不能携带附件，请先移除附件',
'room.emptyMessagesTitle': '还没有消息',
'room.emptyMessagesNoAgents': '先添加一个 agent，再开始对话或创建任务。',
'room.emptyMessagesWithAgents': '发送第一条消息，或使用 /task 快速创建协作任务。',
'room.composerNoAgents': '先邀请一个 agent 才能开始对话...',
'room.composerReady': '发送消息、@agent 定向，或 /task 创建任务',
'room.currentUser': '你',
'room.routing.mentionsOnly': '当前策略：只有被 @ 的智能体会回复；无 @ 时不会触发回复。',
'room.routing.fallbackMissing': '当前策略：无 @ 时交给 {agentId}；当前群聊尚未邀请它，因此不会触发兜底。',
'room.routing.fallbackReply': '当前策略：无 @ 时由 {agentName} 回复。',
'room.routing.fallbackRoute': '当前策略：无 @ 时由 {agentName} 分析并 @ 相关智能体协作。',
'room.routing.fallbackAgent': '兜底智能体',
```

Add English values for every key.

- [ ] **Step 2: Migrate DashboardPage**

In `DashboardPage.tsx`:

```tsx
const { t, formatRelativeTime } = useI18n();
```

Replace all static strings and `relativeTime(p.updated_at)`.

Use count key:

```tsx
{t('dashboard.projectCount', { count: projects.length })}
```

- [ ] **Step 3: Migrate ProjectPage**

In `ProjectPage.tsx`, use `useI18n()` in `ProjectPage`, `RoomItem`, and `CreateRoomDialog`. Replace toast success, labels, placeholders, aria labels, button text, empty state, stats, and `relativeTime(room.created_at)`.

- [ ] **Step 4: Migrate RoomPage**

In `RoomPage.tsx`, use `useI18n()` in `RoomPage`, `AgentStrip`, `ChatColumn`, `MessageBubble`, and `routingHint()`.

Change `routingHint()` signature:

```ts
function routingHint(
  mode: 'mentions_only' | 'fallback_reply' | 'fallback_route',
  fallbackAgentId: string | null,
  fallbackAgent: RoomAgent | undefined,
  t: ReturnType<typeof useI18n>['t'],
): string
```

Call:

```tsx
routingHint(routingMode, fallbackAgentId, agents.find((agent) => agent.agent_id === fallbackAgentId), t)
```

- [ ] **Step 5: Verify page strings**

Run:

```bash
rg -n "[\\p{Han}]" packages/frontend/src/pages
```

Expected: no hardcoded Chinese UI strings remain in page files except comments or user-facing fallback content that must stay dynamic.

- [ ] **Step 6: Build and commit page migration**

Run:

```bash
npm run build
```

Expected: build succeeds.

Commit:

```bash
git add packages/frontend/src/lib/i18n.tsx packages/frontend/src/pages
git commit -m "refactor(frontend): 国际化页面文案"
```

---

### Task 5: Migrate Dialogs, Task, Workflow, and Chat Components

**Files:**
- Modify: `packages/frontend/src/lib/i18n.tsx`
- Modify: `packages/frontend/src/components/CreateProjectDialog.tsx`
- Modify: `packages/frontend/src/components/CreateTaskDialog.tsx`
- Modify: `packages/frontend/src/components/AddAgentDialog.tsx`
- Modify: `packages/frontend/src/components/CommandMenu.tsx`
- Modify: `packages/frontend/src/components/SettingsDialogs.tsx`
- Modify: `packages/frontend/src/components/ProjectRoutingDialog.tsx`
- Modify: `packages/frontend/src/components/TaskBoard.tsx`
- Modify: `packages/frontend/src/components/TaskDetailPanel.tsx`
- Modify: `packages/frontend/src/components/WorkflowTimeline.tsx`
- Modify: `packages/frontend/src/components/AgentRunPanel.tsx`
- Modify: `packages/frontend/src/components/RichMessageComposer.tsx`
- Modify: `packages/frontend/src/components/MessageContent.tsx`
- Modify: `packages/frontend/src/components/AgentMentionMenu.tsx`

- [ ] **Step 1: Add component dictionary keys**

Add keys grouped by component:

```ts
'createProject.title': '添加本地项目',
'createProject.description': '把已有的代码目录纳入深海指挥中心管理',
'createProject.name': '项目名称',
'createProject.path': '本地路径 (绝对路径)',
'createProject.projectDescription': '描述 (可选)',
'createProject.descriptionPlaceholder': '一句话说明这个项目...',
'createProject.adding': '添加中…',
'createProject.submit': '添加项目',
'createProject.success': '项目已添加',
'createTask.title': '新建任务',
'createTask.description': '把群聊里的协作事项沉淀为可追踪任务',
'createTask.taskTitle': '标题',
'createTask.titlePlaceholder': '修复登录错误 / 拆分认证模块',
'createTask.taskDescription': '描述 (可选)',
'createTask.descriptionPlaceholder': '补充任务背景、验收标准或相关上下文',
'createTask.priority': '优先级',
'createTask.assignee': '指派 Agent (可选)',
'createTask.interaction': '交互策略',
'createTask.inheritInteraction': '使用当前设置默认值',
'createTask.creating': '创建中…',
'createTask.submit': '创建任务',
'createTask.success': '任务已创建',
'addAgent.button': '邀请 Agent',
'addAgent.title': '邀请 Agent 加入群聊',
'addAgent.description': '从本机 OpenClaw 配置中选择 Agent',
'addAgent.loading': '读取中',
'addAgent.reading': '正在读取本机 OpenClaw agents 列表',
'addAgent.failedManual': '无法读取 OpenClaw agents，将使用手动输入。{error}',
'addAgent.noAgentsManual': '本机 OpenClaw 配置中没有可用 agent，请手动输入 Agent ID。',
'addAgent.collapseManual': '收起手动输入',
'addAgent.expandManual': '手动输入 Agent ID',
'addAgent.displayName': '显示名称',
'addAgent.role': '角色 (可选)',
'addAgent.inviting': '邀请中…',
'addAgent.submit': '邀请',
'addAgent.success': 'Agent 已加入群聊',
'command.title': '全局命令',
'command.description': '搜索项目、跳转页面或创建新项目',
'command.placeholder': '搜索项目或命令...',
'command.noResults': '没有匹配结果',
'command.navigation': '导航',
'command.backToDashboard': '返回项目总览',
'command.createProject': '创建项目',
'command.projects': '项目 ({count})',
'settings.systemTitle': '系统设置',
'settings.systemDescription': '配置全局默认行为。项目和群聊可以对重复设置项单独覆盖。',
'settings.projectTitle': '项目设置',
'settings.roomTitle': '群聊设置',
'settings.saveSystem': '保存系统设置',
'settings.saveProject': '保存项目设置',
'settings.saveRoom': '保存群聊设置',
'settings.defaultRouting': '默认消息路由',
'settings.defaultInteraction': '默认交互策略',
'settings.projectInfo': '项目信息',
'settings.projectPath': '项目路径',
'settings.projectDescription': '项目描述',
'settings.projectRouting': '项目消息路由',
'settings.projectInteraction': '项目交互策略',
'settings.roomInfo': '群聊信息',
'settings.roomName': '群聊名称',
'settings.roomDescription': '群聊描述',
'settings.roomRouting': '群聊消息路由',
'settings.roomInteraction': '群聊交互策略',
'settings.inheritParent': '继承上级设置',
'settings.inheritSystem': '继承系统: {value}',
'settings.inheritUpper': '继承上级: {value}',
'settings.fallbackAgent': '兜底智能体',
'settings.selectFallback': '选择兜底智能体',
'settings.inheritedOrInvisible': '{agentId}（继承或当前不可见）',
'settings.agentIdPlaceholder': '输入 agent_id',
'settings.resetInherited': '重置重复项为继承',
'settings.resolutionTitle': '重复设置项生效状态',
'settings.routing': '消息路由',
'settings.interaction': '交互策略',
'settings.roomResolutionDescription': '群聊覆盖项目和系统；未覆盖的设置继续向上继承。',
'settings.projectResolutionDescription': '项目覆盖系统；未覆盖的设置继续使用系统默认值。',
'settings.source': '来源：{source}',
'settings.scope.room': '群聊级',
'settings.scope.project': '项目级',
'settings.scope.system': '系统级',
'taskBoard.title': '任务看板',
'taskBoard.empty': '暂无任务',
'taskBoard.markFailed': '标记失败',
'taskDetail.waiting': '等待任务选择',
'taskDetail.emptyTitle': '暂无任务',
'taskDetail.emptyDescription': '创建或选择任务后，这里会显示开发闭环、计划和执行产物。',
'taskDetail.basicInfo': '基础信息',
'taskDetail.taskNumber': '任务编号',
'taskDetail.status': '状态',
'taskDetail.priority': '优先级',
'taskDetail.assignee': '指派人',
'taskDetail.description': '描述',
'taskDetail.noDescription': '暂无描述',
'taskDetail.workflow': '开发闭环',
'taskDetail.starting': '启动中…',
'taskDetail.start': '启动闭环',
'taskDetail.created': '创建',
'taskDetail.completed': '完成',
'taskDetail.notCompleted': '未完成',
'taskDetail.canceling': '取消中…',
'taskDetail.cancel': '取消',
'taskDetail.updated': '任务已更新',
'taskDetail.workflowStarted': '开发闭环已启动',
'taskDetail.planApproved': '计划已确认',
'taskDetail.decisionSubmitted': '决策已提交',
'taskDetail.workflowCanceled': '开发闭环已取消',
'workflow.notStarted': '尚未启动开发闭环',
'workflow.approvePlan': '确认计划',
'workflow.awaitingDecision': '待决策',
'workflow.recommended': '推荐',
'workflow.submitDecision': '提交决策并继续',
'workflow.system': '系统',
'agentRun.status': '执行状态',
'agentRun.cancel': '取消执行',
'agentRun.retryStage': '重试当前阶段',
'agentRun.cancelSuccess': '执行已取消',
'composer.attachments': '待发送附件',
'composer.removeAttachment': '移除 {name}',
'composer.chooseAttachment': '选择附件',
'composer.sendMessage': '发送消息',
'composer.send': '发送',
'composer.taskNoAttachments': '/task 命令不能携带附件，请先移除附件',
'message.markdownMode': 'Markdown 显示模式',
'message.preview': '预览',
'message.source': '原文',
'message.copy': '复制',
'message.copied': '已复制',
'mention.selectAgent': '选择 agent',
'mention.noMatches': '没有匹配的 agent',
```

Add concise English equivalents for every key.

- [ ] **Step 2: Migrate creation dialogs and command menu**

Use `useI18n()` in `CreateProjectDialog`, `CreateTaskDialog`, `AddAgentDialog`, and `CommandMenu`. Replace static strings, toast success messages, placeholder text, button labels, and dialog titles/descriptions.

- [ ] **Step 3: Migrate settings dialogs**

Use `useI18n()` throughout `SettingsDialogs.tsx`. Replace option descriptions by storing only option values and reading labels/descriptions through keys:

```tsx
const ROUTING_OPTIONS: MessageRoutingMode[] = ['mentions_only', 'fallback_reply', 'fallback_route'];
const INTERACTION_OPTIONS: TaskInteractionMode[] = ['ask_user', 'auto_recommended'];
```

Render:

```tsx
title={routingModeLabel(option)}
description={t(`routing.description.${option}` as MessageKey)}
```

Add routing and interaction description keys to `i18n.tsx`.

- [ ] **Step 4: Migrate task and workflow components**

Use `useI18n()` in `TaskBoard`, `TaskDetailPanel`, `WorkflowTimeline`, and `AgentRunPanel`. Replace label maps with i18n label helpers and replace `relativeTime()` with `formatRelativeTime()`.

- [ ] **Step 5: Migrate chat composer and message helpers**

Use `useI18n()` in `RichMessageComposer`, `MessageContent`, and `AgentMentionMenu`. Replace attachment aria labels, send labels, Markdown mode labels, copy labels, and command validation text.

- [ ] **Step 6: Verify component strings**

Run:

```bash
rg -n "[\\p{Han}]" packages/frontend/src/components packages/frontend/src/lib --glob '*.tsx' --glob '*.ts'
```

Expected: hardcoded Chinese UI strings are gone from migrated files. Chinese may remain inside `i18n.tsx` dictionaries.

- [ ] **Step 7: Build and commit component migration**

Run:

```bash
npm run build
```

Expected: build succeeds.

Commit:

```bash
git add packages/frontend/src/lib/i18n.tsx packages/frontend/src/components packages/frontend/src/lib/types.ts packages/frontend/src/lib/utils.ts
git commit -m "refactor(frontend): 国际化组件文案"
```

---

### Task 6: Final Verification and Cleanup

**Files:**
- Verify: all modified frontend files
- Modify only if verification finds a concrete issue.

- [ ] **Step 1: Check remaining Chinese outside dictionaries**

Run:

```bash
rg -n "[\\p{Han}]" packages/frontend/src --glob '*.tsx' --glob '*.ts'
```

Expected: Chinese appears only in `packages/frontend/src/lib/i18n.tsx` dictionaries, comments, or intentionally untranslated user/provider names.

- [ ] **Step 2: Check locale type is constrained**

Run:

```bash
rg -n "Locale|zh|en|openclaw-room-locale" packages/frontend/src/lib/i18n.tsx packages/frontend/src/main.tsx packages/frontend/src/components/AppShell.tsx
```

Expected:

- `Locale` is exactly `'zh' | 'en'`.
- `DEFAULT_LOCALE` is `zh`.
- `isLocale()` accepts only `zh` and `en`.
- storage key is `openclaw-room-locale`.

- [ ] **Step 3: Run production build**

Run:

```bash
npm run build
```

Expected: backend TypeScript build and frontend Vite build complete successfully.

- [ ] **Step 4: Start dev server for manual smoke**

Run:

```bash
npm run dev
```

Expected:

- Backend starts on `PORT` or default `7330`.
- Vite frontend starts on `http://localhost:5173` or the next available Vite port.

- [ ] **Step 5: Browser smoke checks**

Open the frontend URL and verify:

- First load defaults to Chinese when `localStorage.removeItem('openclaw-room-locale')` is run.
- Switching to `EN` updates sidebar, dashboard, dialogs, task board, composer, and settings text without reload.
- Refresh keeps English.
- Switching back to `中文` updates text immediately.
- `document.documentElement.lang` is `zh` or `en` according to the active language.
- No obvious overflow in sidebar controls, task board buttons, or modal footer buttons.

- [ ] **Step 6: Final git status review**

Run:

```bash
git status --short
```

Expected: only this task's intended frontend files are modified, plus any pre-existing unrelated untracked files still remain untouched.

- [ ] **Step 7: Commit final cleanup if needed**

If Step 1-5 required fixes, commit them:

```bash
git add packages/frontend/src
git commit -m "fix(frontend): 完善多语言切换细节"
```

If no fixes were needed, do not create an empty commit.

## Self-Review

- Spec coverage: tasks cover `zh` / `en`, default `zh`, localStorage persistence, `document.documentElement.lang`, UI switcher, static UI text, toast, placeholder, aria-label, enum labels, relative time, and build/manual verification.
- Scope: implementation stays in `packages/frontend` and does not change backend API, schema, database, or user-generated content.
- Type consistency: all translation access goes through `MessageKey`; locale type is exactly `zh | en`; enum labels use typed helper functions on the i18n context.
- Verification: each migration stage runs `npm run build`; final stage includes hardcoded Chinese scan and browser smoke checks.
