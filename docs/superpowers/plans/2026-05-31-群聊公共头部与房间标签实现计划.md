# 群聊公共头部与房间标签 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让群聊页面使用公共 header，并在 header 下方提供当前项目内所有群聊的持久化排序 tab 栏，支持新增、切换、重命名、置顶和删除。

**Architecture:** 后端给 `rooms` 增加 `last_opened_at` 与 `pinned_at`，通过 `PATCH /rooms/:id` 统一更新房间名称、使用顺序和置顶状态。前端将公共导航放到 `AppShell` header，新增 `RoomTabsBar` 作为群聊页局部导航，`RoomPage` 只负责把房间列表、切换和 mutations 串起来。

**Tech Stack:** Node.js, Express, better-sqlite3, Zod, React 18, TypeScript, TanStack Query, React Router, Radix DropdownMenu, lucide-react, Tailwind/CSS variables.

---

## File Structure

- Modify: `packages/backend/src/db.ts`
  - 在 `rooms` 建表语句中加入 `last_opened_at`、`pinned_at`。
  - 为已有数据库添加安全迁移。
  - 增加适合 tab 排序的索引。
- Modify: `packages/backend/src/types.ts`
  - 扩展 `Room` 类型。
- Modify: `packages/backend/src/repos/rooms.ts`
  - 调整 `listByProject` 排序。
  - 创建房间时写入 `last_opened_at`。
  - 新增 `update` 方法，支持重命名、最近打开、置顶。
- Modify: `packages/backend/src/routes.ts`
  - 新增 `PATCH /rooms/:id`。
  - 调整创建房间返回值，确保新字段完整。
- Create: `packages/backend/src/repos/rooms.test.ts`
  - 覆盖排序、创建、更新、删除的核心行为。
- Modify: `packages/frontend/src/lib/types.ts`
  - 扩展 `Room` 类型。
- Modify: `packages/frontend/src/lib/api.ts`
  - 新增 `updateRoom`。
- Modify: `packages/frontend/src/components/AppShell.tsx`
  - 公共 header 对所有路由展示。
  - 项目菜单移入 header，hover 展示全部项目，点击兼容触屏。
- Create: `packages/frontend/src/components/RoomTabsBar.tsx`
  - 房间 tab、右键菜单、更多按钮辅助入口、新增按钮、重命名弹窗、删除确认。
- Modify: `packages/frontend/src/pages/RoomPage.tsx`
  - 引入 `RoomTabsBar`。
  - 移除旧 `RoomSwitcher` 和重复的房间导航职责。
  - 连接 `createRoom`、`updateRoom`、`deleteRoom` mutation。
- Modify: `packages/frontend/src/index.css`
  - 公共 header、项目 hover 菜单、房间 tab 栏、极简主题和响应式样式。

## Task 1: Backend Room Metadata

**Files:**
- Modify: `packages/backend/src/db.ts`
- Modify: `packages/backend/src/types.ts`
- Modify: `packages/backend/src/repos/rooms.ts`
- Create: `packages/backend/src/repos/rooms.test.ts`

- [ ] **Step 1: Extend the Room type**

Update `packages/backend/src/types.ts`:

```ts
export interface Room {
  id: string;
  project_id: string;
  name: string;
  description: string | null;
  created_at: number;
  last_opened_at: number | null;
  pinned_at: number | null;
}
```

- [ ] **Step 2: Extend the rooms schema and migration**

In `packages/backend/src/db.ts`, change the `rooms` table definition to include:

```sql
last_opened_at INTEGER,
pinned_at INTEGER,
```

Then add a migration block near the existing `PRAGMA table_info(...)` migrations:

```ts
const roomColumns = db.prepare('PRAGMA table_info(rooms)').all() as { name: string }[];
const roomColumnNames = new Set(roomColumns.map((column) => column.name));

if (!roomColumnNames.has('last_opened_at')) {
  db.exec('ALTER TABLE rooms ADD COLUMN last_opened_at INTEGER');
}

if (!roomColumnNames.has('pinned_at')) {
  db.exec('ALTER TABLE rooms ADD COLUMN pinned_at INTEGER');
}

db.exec('CREATE INDEX IF NOT EXISTS idx_rooms_project_usage ON rooms(project_id, pinned_at DESC, last_opened_at DESC, created_at DESC)');
```

- [ ] **Step 3: Update room repo ordering and creation**

In `packages/backend/src/repos/rooms.ts`, change `listByProject`:

```ts
listByProject(projectId: string): Room[] {
  return db
    .prepare(`
      SELECT * FROM rooms
      WHERE project_id = ?
      ORDER BY
        pinned_at IS NULL ASC,
        pinned_at DESC,
        COALESCE(last_opened_at, created_at) DESC,
        created_at DESC
    `)
    .all(projectId) as Room[];
},
```

Change `create` so new rooms enter the recent order:

```ts
create(input: { project_id: string; name: string; description?: string; ensureDefaultPlanner?: boolean }): Room {
  const id = nanoid(12);
  const timestamp = now();
  db.prepare(
    `INSERT INTO rooms (id, project_id, name, description, created_at, last_opened_at, pinned_at)
     VALUES (?, ?, ?, ?, ?, ?, NULL)`,
  ).run(id, input.project_id, input.name.trim(), input.description ?? null, timestamp, timestamp);
  if (input.ensureDefaultPlanner !== false) {
    roomAgentRepo.ensureDefaultPlanner(id);
  }
  return this.get(id)!;
},
```

- [ ] **Step 4: Add `roomRepo.update`**

Add this method inside `roomRepo`:

```ts
update(
  id: string,
  patch: {
    name?: string;
    last_opened_at?: number | null;
    pinned_at?: number | null;
  },
): Room | undefined {
  const existing = this.get(id);
  if (!existing) return undefined;

  const nextName = patch.name !== undefined ? patch.name.trim() : existing.name;
  if (!nextName) throw new Error('room name is required');

  db.prepare(`
    UPDATE rooms
    SET name = ?, last_opened_at = ?, pinned_at = ?
    WHERE id = ?
  `).run(
    nextName,
    patch.last_opened_at !== undefined ? patch.last_opened_at : existing.last_opened_at,
    patch.pinned_at !== undefined ? patch.pinned_at : existing.pinned_at,
    id,
  );

  return this.get(id);
},
```

- [ ] **Step 5: Add backend repo tests**

Create `packages/backend/src/repos/rooms.test.ts`:

```ts
import test from 'node:test';
import assert from 'node:assert/strict';
import { roomRepo } from './rooms.js';
import { projectRepo } from './projects.js';

test('roomRepo orders pinned rooms first then recent rooms', () => {
  const project = projectRepo.create({
    name: `排序项目 ${Date.now()}`,
    path: `/tmp/opendeepsea-room-order-${Date.now()}`,
  });
  const older = roomRepo.create({ project_id: project.id, name: '旧房间', ensureDefaultPlanner: false });
  const newer = roomRepo.create({ project_id: project.id, name: '新房间', ensureDefaultPlanner: false });
  const pinned = roomRepo.create({ project_id: project.id, name: '置顶房间', ensureDefaultPlanner: false });

  roomRepo.update(older.id, { last_opened_at: 100 });
  roomRepo.update(newer.id, { last_opened_at: 300 });
  roomRepo.update(pinned.id, { pinned_at: 200, last_opened_at: 50 });

  assert.deepEqual(roomRepo.listByProject(project.id).map((room) => room.id), [
    pinned.id,
    newer.id,
    older.id,
  ]);
});

test('roomRepo trims renamed room names and rejects blank names', () => {
  const project = projectRepo.create({
    name: `重命名项目 ${Date.now()}`,
    path: `/tmp/opendeepsea-room-rename-${Date.now()}`,
  });
  const room = roomRepo.create({ project_id: project.id, name: '原名称', ensureDefaultPlanner: false });

  const renamed = roomRepo.update(room.id, { name: '  新名称  ' });
  assert.equal(renamed?.name, '新名称');
  assert.throws(() => roomRepo.update(room.id, { name: '   ' }), /room name is required/);
});
```

- [ ] **Step 6: Run targeted backend tests**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/repos/rooms.test.ts
```

Expected: tests pass. If the package script does not forward file arguments, run:

```bash
cd packages/backend && node --import tsx --test src/repos/rooms.test.ts
```

- [ ] **Step 7: Commit backend metadata**

```bash
git add packages/backend/src/db.ts packages/backend/src/types.ts packages/backend/src/repos/rooms.ts packages/backend/src/repos/rooms.test.ts
git commit -m "feat(backend): 持久化群聊排序状态"
```

## Task 2: Room API Route and Frontend Client Types

**Files:**
- Modify: `packages/backend/src/routes.ts`
- Modify: `packages/frontend/src/lib/types.ts`
- Modify: `packages/frontend/src/lib/api.ts`

- [ ] **Step 1: Add PATCH route validation**

In `packages/backend/src/routes.ts`, add this route between `router.get('/rooms/:id'...)` and `router.delete('/rooms/:id'...)`:

```ts
router.patch('/rooms/:id', (req, res) => {
  const schema = z.object({
    name: z.string().optional(),
    last_opened_at: z.number().int().nullable().optional(),
    pinned_at: z.number().int().nullable().optional(),
  }).strict();
  const parsed = schema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  try {
    const room = roomRepo.update(req.params.id, parsed.data);
    if (!room) return res.status(404).json({ error: 'not found' });
    res.json(room);
  } catch (err) {
    if ((err as Error).message === 'room name is required') {
      return res.status(400).json({ error: 'room name is required' });
    }
    throw err;
  }
});
```

- [ ] **Step 2: Extend frontend Room type**

In `packages/frontend/src/lib/types.ts`, update `Room` to match the backend:

```ts
export interface Room {
  id: string;
  project_id: string;
  name: string;
  description: string | null;
  created_at: number;
  last_opened_at: number | null;
  pinned_at: number | null;
}
```

- [ ] **Step 3: Add `api.updateRoom`**

In `packages/frontend/src/lib/api.ts`, place this after `getRoom`:

```ts
updateRoom: (
  id: string,
  input: { name?: string; last_opened_at?: number | null; pinned_at?: number | null },
) =>
  request<Room>(`/rooms/${id}`, {
    method: 'PATCH',
    body: JSON.stringify(input),
  }),
```

- [ ] **Step 4: Run targeted backend route coverage**

Run existing backend tests first to catch route regressions:

```bash
npm run test -w @openclaw-room/backend
```

Expected: backend test suite passes.

- [ ] **Step 5: Commit API changes**

```bash
git add packages/backend/src/routes.ts packages/frontend/src/lib/types.ts packages/frontend/src/lib/api.ts
git commit -m "feat(api): 支持更新群聊元数据"
```

## Task 3: Public Header Project Menu

**Files:**
- Modify: `packages/frontend/src/components/AppShell.tsx`
- Modify: `packages/frontend/src/index.css`

- [ ] **Step 1: Render public header on room routes**

In `AppShell`, remove the `!isRoomRoute && (...)` guard around the public header so the header always renders.

The JSX should become:

```tsx
<header className="shell-public-header" aria-label={t('shell.sidebar.aria')}>
  <nav className="shell-public-nav" aria-label={t('shell.sidebar.aria')}>
    <SidebarLink to="/" active={location.pathname === '/'} icon={Home} label={t('shell.nav.development')} exact className="shell-public-link" />
    <SidebarLink to="/chat" icon={MessageCircle} label={t('shell.nav.chat')} className="shell-public-link" />
    <SidebarLink to="/agents" icon={Bot} label={t('shell.nav.agents')} className="shell-public-link" />
    <SidebarLink to="/skills" icon={ShieldCheck} label={t('shell.nav.skills')} className="shell-public-link" />
    <SidebarLink to="/files" icon={FolderKanban} label={t('shell.nav.files')} className="shell-public-link" />
    <SidebarLink to="/test" icon={TestTube2} label={t('shell.nav.test')} className="shell-public-link" />
  </nav>
  <HeaderProjectMenu projects={projects} currentProject={currentProject} />
</header>
```

- [ ] **Step 2: Add `HeaderProjectMenu`**

In `AppShell.tsx`, add imports:

```ts
import { ChevronDown, FolderOpen } from 'lucide-react';
```

Add component:

```tsx
function HeaderProjectMenu({
  projects,
  currentProject,
}: {
  projects: Awaited<ReturnType<typeof api.listProjects>>;
  currentProject?: Awaited<ReturnType<typeof api.listProjects>>[number];
}) {
  const { t } = useI18n();
  const [open, setOpen] = useState(false);

  return (
    <div
      className={cn('shell-project-menu', open && 'is-open')}
      onMouseEnter={() => setOpen(true)}
      onMouseLeave={() => setOpen(false)}
    >
      <button
        type="button"
        className="shell-project-trigger"
        aria-expanded={open}
        onClick={() => setOpen((value) => !value)}
      >
        <FolderOpen className="h-3.5 w-3.5" strokeWidth={1.8} />
        <span>{currentProject ? truncate(currentProject.name, 24) : t('shell.recentProjects')}</span>
        <ChevronDown className="h-3.5 w-3.5" strokeWidth={1.8} />
      </button>
      {open && (
        <div className="shell-project-dropdown" role="menu">
          {projects.map((project) => (
            <NavLink
              key={project.id}
              to={`/projects/${project.id}`}
              className={({ isActive }) => cn('shell-project-option', (isActive || currentProject?.id === project.id) && 'is-active')}
              role="menuitem"
            >
              <span className="shell-project-dot" />
              <span className="truncate">{project.name}</span>
            </NavLink>
          ))}
          {projects.length === 0 && (
            <div className="shell-project-empty">{t('shell.noProjects')}</div>
          )}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: Add header menu CSS**

In `packages/frontend/src/index.css`, near `.shell-public-header`, add styles:

```css
.shell-project-menu {
  position: relative;
  margin-left: auto;
}

.shell-project-trigger {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  height: 32px;
  max-width: 240px;
  border: 1px solid var(--color-border);
  border-radius: 7px;
  padding: 0 10px;
  background: var(--color-surface);
  color: var(--color-fg);
  font-size: 12px;
}

.shell-project-dropdown {
  position: absolute;
  top: calc(100% + 8px);
  right: 0;
  z-index: 60;
  width: min(320px, calc(100vw - 24px));
  max-height: 360px;
  overflow: auto;
  border: 1px solid var(--color-border);
  border-radius: 8px;
  background: var(--color-surface);
  box-shadow: var(--shadow-lg);
  padding: 6px;
}

.shell-project-option {
  display: flex;
  align-items: center;
  gap: 8px;
  min-height: 34px;
  border-radius: 6px;
  padding: 0 9px;
  color: var(--color-fg);
  font-size: 12px;
}

.shell-project-option:hover,
.shell-project-option.is-active {
  background: var(--color-surface-muted);
}

.shell-project-dot {
  width: 6px;
  height: 6px;
  flex: 0 0 auto;
  border-radius: 999px;
  background: #16c8e6;
}

.shell-project-empty {
  padding: 14px 10px;
  color: var(--color-fg-muted);
  font-size: 12px;
}
```

- [ ] **Step 4: Commit header changes**

```bash
git add packages/frontend/src/components/AppShell.tsx packages/frontend/src/index.css
git commit -m "feat(frontend): 统一公共头部项目菜单"
```

## Task 4: RoomTabsBar Component

**Files:**
- Create: `packages/frontend/src/components/RoomTabsBar.tsx`
- Modify: `packages/frontend/src/index.css`

- [ ] **Step 1: Create RoomTabsBar props and helpers**

Create `packages/frontend/src/components/RoomTabsBar.tsx` with this structure:

```tsx
import * as DropdownMenu from '@radix-ui/react-dropdown-menu';
import { MoreHorizontal, Pencil, Pin, PinOff, Plus, Trash2 } from 'lucide-react';
import { useEffect, useMemo, useState } from 'react';
import { Link } from 'react-router-dom';
import { Dialog, DialogContent } from './ui/Dialog';
import { Button } from './ui/Button';
import { Input } from './ui/Input';
import type { Room } from '../lib/types';
import { cn, truncate } from '../lib/utils';

export function sortRoomsForTabs(rooms: Room[]): Room[] {
  return [...rooms].sort((a, b) => {
    const aPinned = a.pinned_at ?? 0;
    const bPinned = b.pinned_at ?? 0;
    if (aPinned || bPinned) return bPinned - aPinned;
    return (b.last_opened_at ?? b.created_at) - (a.last_opened_at ?? a.created_at);
  });
}

export function RoomTabsBar({
  projectId,
  roomId,
  rooms,
  busyRoomId,
  creating,
  onCreateRoom,
  onRenameRoom,
  onTogglePin,
  onDeleteRoom,
}: {
  projectId: string;
  roomId: string;
  rooms: Room[];
  busyRoomId?: string | null;
  creating?: boolean;
  onCreateRoom: () => void;
  onRenameRoom: (room: Room, name: string) => Promise<void>;
  onTogglePin: (room: Room) => void;
  onDeleteRoom: (room: Room) => void;
}) {
  const sortedRooms = useMemo(() => sortRoomsForTabs(rooms), [rooms]);
  const [renamingRoom, setRenamingRoom] = useState<Room | null>(null);
  const [contextRoom, setContextRoom] = useState<{ room: Room; x: number; y: number } | null>(null);
  const [name, setName] = useState('');
  const [renameError, setRenameError] = useState<string | null>(null);

  useEffect(() => {
    setName(renamingRoom?.name ?? '');
    setRenameError(null);
  }, [renamingRoom]);

  useEffect(() => {
    if (!contextRoom) return;
    const close = () => setContextRoom(null);
    const onKeyDown = (event: KeyboardEvent) => {
      if (event.key === 'Escape') close();
    };
    window.addEventListener('click', close);
    window.addEventListener('keydown', onKeyDown);
    return () => {
      window.removeEventListener('click', close);
      window.removeEventListener('keydown', onKeyDown);
    };
  }, [contextRoom]);

  const submitRename = async () => {
    if (!renamingRoom) return;
    const trimmed = name.trim();
    if (!trimmed) {
      setRenameError('群聊名称不能为空');
      return;
    }
    try {
      await onRenameRoom(renamingRoom, trimmed);
      setRenamingRoom(null);
    } catch (err) {
      setRenameError((err as Error).message);
    }
  };

  return (
    <div className="room-tabs-bar" aria-label="群聊切换">
      <div className="room-tabs-scroll">
        {sortedRooms.map((room) => (
          <div
            key={room.id}
            className="room-tab-wrapper"
            onContextMenu={(event) => {
              event.preventDefault();
              setContextRoom({ room, x: event.clientX, y: event.clientY });
            }}
          >
            <Link
              to={`/projects/${projectId}/rooms/${room.id}`}
              className={cn('room-tab-link', room.id === roomId && 'is-active')}
              aria-current={room.id === roomId ? 'page' : undefined}
              title={room.name}
            >
              {room.pinned_at ? <Pin className="h-3.5 w-3.5" strokeWidth={1.8} /> : null}
              <span>{truncate(room.name, 26)}</span>
            </Link>
            <RoomTabMenu
              room={room}
              busy={busyRoomId === room.id}
              onRename={() => setRenamingRoom(room)}
              onTogglePin={() => onTogglePin(room)}
              onDelete={() => onDeleteRoom(room)}
            />
          </div>
        ))}
      </div>
      <button type="button" className="room-tab-add" onClick={onCreateRoom} disabled={creating} aria-label="新增群聊">
        <Plus className="h-4 w-4" strokeWidth={1.9} />
      </button>
      {contextRoom && (
        <RoomTabContextMenu
          room={contextRoom.room}
          x={contextRoom.x}
          y={contextRoom.y}
          onClose={() => setContextRoom(null)}
          onRename={() => setRenamingRoom(contextRoom.room)}
          onTogglePin={() => onTogglePin(contextRoom.room)}
          onDelete={() => onDeleteRoom(contextRoom.room)}
        />
      )}
      <Dialog open={Boolean(renamingRoom)} onOpenChange={(open) => !open && setRenamingRoom(null)}>
        <DialogContent title="重命名群聊" description={renamingRoom?.name ?? ''}>
          <div className="space-y-3">
            <Input value={name} onChange={(event) => setName(event.target.value)} autoFocus />
            {renameError && <p className="text-[12px] text-red-500">{renameError}</p>}
            <div className="flex justify-end gap-2">
              <Button type="button" variant="secondary" onClick={() => setRenamingRoom(null)}>取消</Button>
              <Button type="button" onClick={submitRename}>确认</Button>
            </div>
          </div>
        </DialogContent>
      </Dialog>
    </div>
  );
}
```

- [ ] **Step 2: Add menu components**

In the same file, add:

```tsx
function RoomTabContextMenu({
  room,
  x,
  y,
  onClose,
  onRename,
  onTogglePin,
  onDelete,
}: {
  room: Room;
  x: number;
  y: number;
  onClose: () => void;
  onRename: () => void;
  onTogglePin: () => void;
  onDelete: () => void;
}) {
  const run = (action: () => void) => {
    action();
    onClose();
  };

  return (
    <div
      className="room-tab-menu room-tab-menu-floating"
      style={{ left: x, top: y }}
      role="menu"
      aria-label={`${room.name} 操作`}
      onClick={(event) => event.stopPropagation()}
    >
      <button type="button" className="room-tab-menu-item" role="menuitem" onClick={() => run(onRename)}>
        <Pencil className="h-3.5 w-3.5" strokeWidth={1.8} />
        <span>重命名</span>
      </button>
      <button type="button" className="room-tab-menu-item" role="menuitem" onClick={() => run(onTogglePin)}>
        {room.pinned_at ? <PinOff className="h-3.5 w-3.5" strokeWidth={1.8} /> : <Pin className="h-3.5 w-3.5" strokeWidth={1.8} />}
        <span>{room.pinned_at ? '取消置顶' : '置顶'}</span>
      </button>
      <button type="button" className="room-tab-menu-item is-danger" role="menuitem" onClick={() => run(onDelete)}>
        <Trash2 className="h-3.5 w-3.5" strokeWidth={1.8} />
        <span>删除</span>
      </button>
    </div>
  );
}

function RoomTabMenu({
  room,
  busy,
  onRename,
  onTogglePin,
  onDelete,
}: {
  room: Room;
  busy: boolean;
  onRename: () => void;
  onTogglePin: () => void;
  onDelete: () => void;
}) {
  return (
    <DropdownMenu.Root>
      <DropdownMenu.Trigger asChild>
        <button type="button" className="room-tab-menu-trigger" disabled={busy} aria-label={`${room.name} 操作`}>
          <MoreHorizontal className="h-3.5 w-3.5" strokeWidth={1.8} />
        </button>
      </DropdownMenu.Trigger>
      <DropdownMenu.Portal>
        <DropdownMenu.Content className="room-tab-menu" align="start" sideOffset={8}>
          <DropdownMenu.Item className="room-tab-menu-item" onSelect={onRename}>
            <Pencil className="h-3.5 w-3.5" strokeWidth={1.8} />
            <span>重命名</span>
          </DropdownMenu.Item>
          <DropdownMenu.Item className="room-tab-menu-item" onSelect={onTogglePin}>
            {room.pinned_at ? <PinOff className="h-3.5 w-3.5" strokeWidth={1.8} /> : <Pin className="h-3.5 w-3.5" strokeWidth={1.8} />}
            <span>{room.pinned_at ? '取消置顶' : '置顶'}</span>
          </DropdownMenu.Item>
          <DropdownMenu.Item className="room-tab-menu-item is-danger" onSelect={onDelete}>
            <Trash2 className="h-3.5 w-3.5" strokeWidth={1.8} />
            <span>删除</span>
          </DropdownMenu.Item>
        </DropdownMenu.Content>
      </DropdownMenu.Portal>
    </DropdownMenu.Root>
  );
}
```

`RoomTabContextMenu` 是满足右键交互的主路径；`RoomTabMenu` 是对触屏、键盘和不习惯右键用户的辅助入口。

- [ ] **Step 3: Add tab CSS**

In `packages/frontend/src/index.css`, add:

```css
.room-tabs-bar {
  display: flex;
  align-items: center;
  gap: 8px;
  min-height: 44px;
  border-bottom: 1px solid var(--color-border);
  background: var(--color-bg);
  padding: 6px 10px;
}

.room-tabs-scroll {
  display: flex;
  min-width: 0;
  flex: 1 1 auto;
  gap: 6px;
  overflow-x: auto;
  scrollbar-width: thin;
}

.room-tab-wrapper {
  display: inline-flex;
  align-items: center;
  flex: 0 0 auto;
  border: 1px solid var(--color-border);
  border-radius: 7px;
  background: var(--color-surface);
}

.room-tab-link {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  max-width: 220px;
  min-height: 30px;
  padding: 0 9px;
  color: var(--color-fg-muted);
  font-size: 12px;
}

.room-tab-link:hover,
.room-tab-link.is-active {
  color: var(--color-fg);
}

.room-tab-wrapper:has(.room-tab-link.is-active) {
  border-color: var(--color-accent);
  background: var(--color-surface-muted);
}

.room-tab-menu-trigger,
.room-tab-add {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 30px;
  height: 30px;
  border-radius: 6px;
  color: var(--color-fg-muted);
}

.room-tab-add {
  flex: 0 0 auto;
  border: 1px solid var(--color-border);
  background: var(--color-surface);
}

.room-tab-menu {
  z-index: 80;
  min-width: 150px;
  border: 1px solid var(--color-border);
  border-radius: 8px;
  background: var(--color-surface);
  box-shadow: var(--shadow-lg);
  padding: 5px;
}

.room-tab-menu-floating {
  position: fixed;
}

.room-tab-menu-item {
  display: flex;
  align-items: center;
  gap: 8px;
  min-height: 32px;
  border-radius: 6px;
  padding: 0 8px;
  color: var(--color-fg);
  font-size: 12px;
}

.room-tab-menu-item:hover,
.room-tab-menu-item[data-highlighted] {
  background: var(--color-surface-muted);
}

.room-tab-menu-item.is-danger {
  color: #dc2626;
}
```

- [ ] **Step 4: Commit component**

```bash
git add packages/frontend/src/components/RoomTabsBar.tsx packages/frontend/src/index.css
git commit -m "feat(frontend): 添加群聊标签栏"
```

## Task 5: Integrate RoomTabsBar in RoomPage

**Files:**
- Modify: `packages/frontend/src/pages/RoomPage.tsx`
- Modify: `packages/frontend/src/index.css`

- [ ] **Step 1: Import RoomTabsBar and router helpers**

In `RoomPage.tsx`, add imports:

```ts
import { useNavigate } from 'react-router-dom';
import { RoomTabsBar, sortRoomsForTabs } from '../components/RoomTabsBar';
```

If `useNavigate` is already imported from `react-router-dom`, merge it into the existing import.

- [ ] **Step 2: Add room metadata mutations**

Inside `RoomPage`, after `rooms` query and before render, add:

```ts
const navigate = useNavigate();

const updateRoom = useMutation({
  mutationFn: ({ id, patch }: { id: string; patch: { name?: string; last_opened_at?: number | null; pinned_at?: number | null } }) =>
    api.updateRoom(id, patch),
  onSuccess: (updatedRoom) => {
    queryClient.setQueryData<Room | undefined>(['room', updatedRoom.id], updatedRoom);
    queryClient.setQueryData<Room[] | undefined>(['rooms', updatedRoom.project_id], (prev) =>
      prev?.map((room) => (room.id === updatedRoom.id ? updatedRoom : room)),
    );
  },
  onError: (err) => toast.error((err as Error).message),
});

const createRoom = useMutation({
  mutationFn: () => api.createRoom(projectId, { name: '新群聊' }),
  onSuccess: (createdRoom) => {
    queryClient.invalidateQueries({ queryKey: ['rooms', projectId] });
    navigate(`/projects/${projectId}/rooms/${createdRoom.id}`);
  },
  onError: (err) => toast.error((err as Error).message),
});

const deleteRoom = useMutation({
  mutationFn: (targetRoom: Room) => api.deleteRoom(targetRoom.id).then(() => targetRoom),
  onSuccess: (deletedRoom) => {
    const remainingRooms = sortRoomsForTabs(rooms.filter((item) => item.id !== deletedRoom.id));
    queryClient.invalidateQueries({ queryKey: ['rooms', projectId] });
    if (deletedRoom.id === roomId) {
      const nextRoom = remainingRooms[0];
      navigate(nextRoom ? `/projects/${projectId}/rooms/${nextRoom.id}` : `/projects/${projectId}`);
    }
  },
  onError: (err) => toast.error((err as Error).message),
});
```

- [ ] **Step 3: Persist recent use on active room**

Replace the current `recordRecentRoomVisit` effect with one that also updates backend usage:

```ts
useEffect(() => {
  if (!project || !room || room.project_id !== project.id) return;
  recordRecentRoomVisit({ project, room });
  updateRoom.mutate({
    id: room.id,
    patch: { last_opened_at: Date.now() },
  });
}, [project?.id, room?.id]);
```

Do not include `updateRoom` object in the dependency array if doing so causes repeated mutation loops; use only stable ids and suppress no lint unless the repository already follows that pattern. The repository currently has no lint gate, but the implementation should avoid unnecessary repeated updates.

- [ ] **Step 4: Render RoomTabsBar under the public header**

At the top of the `workspace-root`, insert `RoomTabsBar` before the main grid:

```tsx
<RoomTabsBar
  projectId={projectId}
  roomId={roomId}
  rooms={rooms}
  busyRoomId={updateRoom.variables?.id ?? deleteRoom.variables?.id ?? null}
  creating={createRoom.isPending}
  onCreateRoom={() => createRoom.mutate()}
  onRenameRoom={(targetRoom, name) =>
    new Promise<void>((resolve, reject) => {
      updateRoom.mutate(
        { id: targetRoom.id, patch: { name } },
        { onSuccess: () => resolve(), onError: reject },
      );
    })
  }
  onTogglePin={(targetRoom) => {
    updateRoom.mutate({
      id: targetRoom.id,
      patch: { pinned_at: targetRoom.pinned_at ? null : Date.now() },
    });
  }}
  onDeleteRoom={(targetRoom) => {
    const ok = window.confirm(`删除群聊「${targetRoom.name}」？此操作不可撤销。`);
    if (ok) deleteRoom.mutate(targetRoom);
  }}
/>
```

- [ ] **Step 5: Remove old room switcher responsibilities**

Remove `RoomTopNavigation` from `RoomPage` render if it only duplicates public header and room switching. Preserve memory panel toggle and agent selection by moving those controls into existing chat/task headers only if they would otherwise disappear.

Delete the unused `RoomSwitcher` function in `RoomPage.tsx` after confirming no references remain:

```bash
rg -n "RoomSwitcher|room-switcher|room-more-menu" packages/frontend/src/pages/RoomPage.tsx packages/frontend/src/index.css
```

- [ ] **Step 6: Commit integration**

```bash
git add packages/frontend/src/pages/RoomPage.tsx packages/frontend/src/index.css
git commit -m "feat(frontend): 接入群聊标签切换"
```

## Task 6: Theme, Responsive, and Visual Cleanup

**Files:**
- Modify: `packages/frontend/src/index.css`
- Modify: `packages/frontend/src/components/AppShell.tsx`
- Modify: `packages/frontend/src/components/RoomTabsBar.tsx`

- [ ] **Step 1: Keep minimal themes flat**

Add minimal-theme overrides for tab and header surfaces:

```css
[data-theme="minimal-light"] .room-tabs-bar,
[data-theme="minimal-dark"] .room-tabs-bar {
  background: var(--color-bg);
  backdrop-filter: none;
  box-shadow: none;
}

[data-theme="minimal-light"] .room-tab-wrapper,
[data-theme="minimal-dark"] .room-tab-wrapper,
[data-theme="minimal-light"] .shell-project-trigger,
[data-theme="minimal-dark"] .shell-project-trigger,
[data-theme="minimal-light"] .shell-project-dropdown,
[data-theme="minimal-dark"] .shell-project-dropdown {
  backdrop-filter: none;
  box-shadow: none;
}
```

- [ ] **Step 2: Mobile behavior**

Add responsive rules:

```css
@media (max-width: 860px) {
  .shell-public-header {
    gap: 8px;
    padding-inline: 10px;
  }

  .shell-public-nav {
    overflow-x: auto;
  }

  .shell-project-trigger span {
    max-width: 120px;
  }

  .room-tabs-bar {
    padding-inline: 8px;
  }

  .room-tab-link {
    max-width: 160px;
  }
}
```

- [ ] **Step 3: Accessibility pass**

Verify these attributes exist in code:

```tsx
aria-expanded={open}
aria-current={room.id === roomId ? 'page' : undefined}
aria-label="新增群聊"
aria-label={`${room.name} 操作`}
```

- [ ] **Step 4: Commit visual cleanup**

```bash
git add packages/frontend/src/index.css packages/frontend/src/components/AppShell.tsx packages/frontend/src/components/RoomTabsBar.tsx
git commit -m "fix(frontend): 收敛群聊标签主题样式"
```

## Task 7: Final Verification and Push

**Files:**
- Verify all modified files.

- [ ] **Step 1: Run targeted backend tests**

```bash
npm run test -w @openclaw-room/backend -- src/repos/rooms.test.ts
```

Expected: room repo tests pass.

- [ ] **Step 2: Run final build once**

Run only after all implementation tasks are complete:

```bash
npm run build
```

Expected: backend TypeScript build and frontend Vite build pass.

- [ ] **Step 3: Run UI smoke verification**

Start the dev server only after the final build passes:

```bash
npm run dev
```

Open `http://localhost:5173` and verify:

- 公共 header 在群聊页面出现。
- 项目菜单 hover 展示全部项目，点击也能展开。
- 群聊 tab 只显示当前项目内房间。
- 切换 tab 后当前房间更新，刷新后最近使用排序保留。
- 置顶、取消置顶、重命名、删除可用。
- 重命名使用轻量弹窗。
- 新增按钮固定在 tab 栏最右侧。
- 极简主题没有玻璃液化效果。
- 主区域仍保持左 60%、右 40%。

- [ ] **Step 4: Review diff**

```bash
git diff --stat HEAD
git diff HEAD -- packages/backend/src/db.ts packages/backend/src/repos/rooms.ts packages/backend/src/routes.ts packages/frontend/src/components/AppShell.tsx packages/frontend/src/components/RoomTabsBar.tsx packages/frontend/src/pages/RoomPage.tsx packages/frontend/src/index.css
```

Expected: diff only covers the planned files and behavior.

- [ ] **Step 5: Final commit and push**

If earlier task-level commits were skipped during inline execution, create one final commit:

```bash
git add packages/backend/src/db.ts packages/backend/src/types.ts packages/backend/src/repos/rooms.ts packages/backend/src/repos/rooms.test.ts packages/backend/src/routes.ts packages/frontend/src/lib/types.ts packages/frontend/src/lib/api.ts packages/frontend/src/components/AppShell.tsx packages/frontend/src/components/RoomTabsBar.tsx packages/frontend/src/pages/RoomPage.tsx packages/frontend/src/index.css
git commit -m "feat(frontend): 实现群聊公共头部标签栏"
```

Push the current branch:

```bash
git push origin feat/ai-task-os-architecture
```

## Self-Review Notes

- Spec coverage: 公共 header、项目 hover 菜单、当前项目房间 tab、持久化排序、置顶、重命名弹窗、删除确认、新增按钮、极简主题边界和最终验证均有对应任务。
- Scope: 本计划只修改 room 元数据、room API、公共 header 和群聊页面局部导航，不碰气泡目录或消息渲染体系。
- Build policy: 遵守“最终完成任务时才构建”，计划中只在最终验证阶段运行一次 `npm run build`。
