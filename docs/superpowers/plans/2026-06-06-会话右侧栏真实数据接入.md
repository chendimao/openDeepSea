# 会话右侧栏真实数据接入 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 Session Workspace 右侧栏的会话计划、代理运行、工具调用、本次会话变更全部接入当前 session 的 ACP 调用记录。

**Architecture:** 后端在 `session-workspace-view-model.ts` 统一从 `session_runs`、`session_agent_events`、`session_evidence_events` 派生 inspector 视图，snapshot 与 WebSocket 增量复用同一派生口径。前端只消费后端 payload，不解析 ACP 原始事件，并将“待提交变更”改为“本次会话变更”。

**Tech Stack:** Node.js、Express、SQLite、TypeScript、React 18、Vite、WebSocket、node:test。

---

## File Structure

- Modify: `packages/backend/src/session-workspace-view-model.ts`
  - 负责派生 `toolRows`、`diffRows`，新增 inspector 快照 builder，补齐 ACP payload 解析 helper。
- Modify: `packages/backend/src/session.routes.ts`
  - snapshot 调用新的 inspector 派生函数，停止用 `git status` 构建右侧栏变更列表。
- Modify: `packages/backend/src/session-runtime.ts`
  - 在记录 plan/tool/diff 相关 evidence 后广播 `session_inspector:snapshot`。
- Modify: `packages/backend/src/types.ts`
  - 扩展 `WsServerEvent`，新增 `session_inspector:snapshot` 类型。
- Test: `packages/backend/src/session-workspace-view-model.test.ts`
  - 覆盖 ACP plan/tool/diff 派生规则和 git dirty 排除。
- Test: `packages/backend/src/session-runtime.test.ts`
  - 覆盖新增 evidence 后会广播 inspector 快照。
- Modify: `packages/frontend/src/lib/ws.ts`
  - 扩展前端 `WsServerEvent` 类型。
- Modify: `packages/frontend/src/session-ui/session-workspace-events.ts`
  - 处理 `session_inspector:snapshot`，替换 `planItems`、`toolRows`、`diffRows`。
- Test: `packages/frontend/src/session-ui/session-workspace-events.test.ts`
  - 覆盖 inspector 快照 reducer 行为。
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
  - 移除 RunModule 假兜底值，更新“本次会话变更”文案。
- Test: `packages/frontend/src/session-ui/SessionShell.test.tsx`
  - 覆盖右侧栏新文案、无 run 空状态、不显示假值。

---

### Task 1: 后端 Inspector 派生测试

**Files:**
- Test: `packages/backend/src/session-workspace-view-model.test.ts`
- Read: `packages/backend/src/session-workspace-view-model.ts`
- Read: `packages/backend/src/session-types.ts`

- [x] **Step 1: 写 ACP 派生失败测试**

在 `packages/backend/src/session-workspace-view-model.test.ts` 新增测试文件，内容覆盖 plan/tool/diff 三类派生：

```ts
import test from 'node:test';
import assert from 'node:assert/strict';
import type { SessionEvidenceEvent } from './session-types.js';
import {
  buildSessionDiffRowsFromAcp,
  buildSessionInspectorSnapshot,
  buildSessionPlanItemsFromAcp,
  buildSessionToolRows,
} from './session-workspace-view-model.js';

const now = Date.now();

test('buildSessionPlanItemsFromAcp derives plan items from ACP plan_update evidence', () => {
  const evidence = [createEvidence({
    id: 'ev-plan',
    event_type: 'status',
    title: '计划更新',
    payload: {
      event: {
        type: 'plan_update',
        payload: {
          entries: [
            { id: 'read-context', title: '读取上下文', status: 'completed' },
            { id: 'wire-data', title: '接入真实数据', status: 'in_progress' },
          ],
        },
      },
    },
  })];

  const items = buildSessionPlanItemsFromAcp('session-1', evidence, []);

  assert.deepEqual(items.map((item) => ({
    title: item.title,
    status: item.status,
    source: item.source,
    evidence_event_id: item.evidence_event_id,
    priority: item.priority,
  })), [
    {
      title: '读取上下文',
      status: 'completed',
      source: 'acp_plan_update',
      evidence_event_id: 'ev-plan',
      priority: 0,
    },
    {
      title: '接入真实数据',
      status: 'in_progress',
      source: 'acp_plan_update',
      evidence_event_id: 'ev-plan',
      priority: 1,
    },
  ]);
});

test('buildSessionToolRows prefers ACP tool names and targets', () => {
  const rows = buildSessionToolRows([
    createEvidence({
      id: 'ev-tool',
      event_type: 'tool_call',
      title: '调用工具 apply_patch',
      payload: {
        event: {
          type: 'tool_call',
          status: 'started',
          payload: {
            name: 'apply_patch',
            input: '*** Update File: packages/frontend/src/session-ui/SessionShellView.tsx',
          },
        },
      },
    }),
    createEvidence({
      id: 'ev-command',
      event_type: 'tool_call',
      title: 'Command: npm run build',
      payload: {
        trace: {
          kind: 'command',
          command: 'npm run build',
        },
      },
    }),
  ]);

  assert.equal(rows[0]?.action, 'edit');
  assert.equal(rows[0]?.label, 'apply_patch');
  assert.equal(rows[0]?.target, 'packages/frontend/src/session-ui/SessionShellView.tsx');
  assert.equal(rows[0]?.status, 'running');
  assert.equal(rows[1]?.action, 'exec');
  assert.equal(rows[1]?.target, 'npm run build');
});

test('buildSessionDiffRowsFromAcp includes only ACP file changes and aggregates duplicate files', () => {
  const evidence = [
    createEvidence({
      id: 'ev-diff-1',
      event_type: 'file_diff',
      title: '修改文件 packages/frontend/src/session-ui/SessionShellView.tsx',
      payload: {
        event: {
          type: 'file_diff',
          payload: {
            path: 'packages/frontend/src/session-ui/SessionShellView.tsx',
            additions: 12,
            deletions: 3,
          },
        },
      },
    }),
    createEvidence({
      id: 'ev-diff-2',
      event_type: 'tool_call',
      title: '调用工具 apply_patch',
      payload: {
        event: {
          type: 'tool_call',
          payload: {
            name: 'apply_patch',
            input: '*** Update File: packages/frontend/src/session-ui/SessionShellView.tsx',
          },
        },
      },
    }),
  ];

  const rows = buildSessionDiffRowsFromAcp(evidence, []);

  assert.deepEqual(rows, [{
    path: 'packages/frontend/src/session-ui/SessionShellView.tsx',
    status: 'modified',
    additions: 12,
    deletions: 3,
    summary: 'apply_patch',
  }]);
});

test('buildSessionInspectorSnapshot combines plan, tool and session change rows', () => {
  const evidence = [createEvidence({
    id: 'ev-plan',
    event_type: 'status',
    title: '计划更新',
    payload: {
      event: {
        type: 'plan_update',
        payload: { entries: [{ title: '实现派生层', status: 'pending' }] },
      },
    },
  })];

  const snapshot = buildSessionInspectorSnapshot('session-1', evidence, []);

  assert.equal(snapshot.planItems[0]?.title, '实现派生层');
  assert.deepEqual(snapshot.toolRows, []);
  assert.deepEqual(snapshot.diffRows, []);
});

function createEvidence(input: Partial<SessionEvidenceEvent> & Pick<SessionEvidenceEvent, 'id' | 'event_type' | 'title'>): SessionEvidenceEvent {
  return {
    id: input.id,
    session_id: input.session_id ?? 'session-1',
    seq: input.seq ?? 1,
    event_type: input.event_type,
    severity: input.severity ?? 'info',
    source_run_id: input.source_run_id ?? 'run-1',
    source_message_id: input.source_message_id ?? null,
    title: input.title,
    summary: input.summary ?? null,
    payload: input.payload ?? {},
    created_at: input.created_at ?? now,
  };
}

```

- [x] **Step 2: 运行后端派生测试确认失败**

Run:

```bash
rtk npm --workspace packages/backend exec node -- --import tsx --test src/session-workspace-view-model.test.ts
```

Expected: FAIL，错误包含 `does not provide an export named 'buildSessionDiffRowsFromAcp'` 或同类缺失导出。

- [x] **Step 3: 提交失败测试**

```bash
rtk git add packages/backend/src/session-workspace-view-model.test.ts
rtk git commit -m "test(backend): 覆盖会话右侧栏ACP派生"
```

---

### Task 2: 后端 Inspector 派生实现

**Files:**
- Modify: `packages/backend/src/session-workspace-view-model.ts`
- Modify: `packages/backend/src/session.routes.ts`
- Test: `packages/backend/src/session-workspace-view-model.test.ts`

- [x] **Step 1: 实现派生 helper 和 inspector snapshot**

在 `packages/backend/src/session-workspace-view-model.ts` 中：

- 保留 `buildSessionToolRows(evidence)` 的公开函数名。
- 新增 `buildSessionPlanItemsFromAcp(sessionId, evidence, agentEvents)`。
- 新增 `buildSessionDiffRowsFromAcp(evidence, agentEvents)`。
- 新增 `buildSessionInspectorSnapshot(sessionId, evidence, agentEvents)`。
- 保留原 `buildSessionDiffRows(workspacePath)` 仅供历史调用或删除未引用代码。

核心实现形状：

```ts
export interface SessionInspectorSnapshot {
  planItems: SessionPlanItem[];
  toolRows: SessionToolRow[];
  diffRows: SessionDiffRow[];
}

export function buildSessionInspectorSnapshot(
  sessionId: string,
  evidence: SessionEvidenceEvent[],
  agentEvents: SessionAgentEvent[],
): SessionInspectorSnapshot {
  return {
    planItems: buildSessionPlanItemsFromAcp(sessionId, evidence, agentEvents),
    toolRows: buildSessionToolRows(evidence),
    diffRows: buildSessionDiffRowsFromAcp(evidence, agentEvents),
  };
}
```

解析 helper 必须满足：

- 使用 `Record<string, unknown>` 安全读取嵌套 payload。
- 对缺失字段返回空数组或 fallback 文案，不 throw。
- `apply_patch` input 中用 `/^\*\*\* (?:Update|Add|Delete) File: (.+)$/m` 提取 path。
- 状态映射只输出 `SessionPlanItemStatus` 和 `SessionToolRow['status']` 允许值。

- [x] **Step 2: 将 workspace snapshot 改为 ACP 派生口径**

在 `packages/backend/src/session.routes.ts` 的 `buildWorkspacePayload` 中，将：

```ts
toolRows: buildSessionToolRows(evidence),
diffRows: buildSessionDiffRows(resolveSessionWorkspacePath(activeSession, project)),
```

改为先构建 inspector：

```ts
const inspector = buildSessionInspectorSnapshot(activeSession.id, detail.evidence, detail.agentEvents);
```

并返回：

```ts
activeSession: {
  ...detail,
  planItems: inspector.planItems,
},
toolRows: inspector.toolRows,
diffRows: inspector.diffRows,
```

同时移除 `buildSessionToolRows`、`buildSessionDiffRows`、`resolveSessionWorkspacePath` 的旧 import，只保留仍被其他函数使用的 import。

- [x] **Step 3: 运行后端派生测试确认通过**

Run:

```bash
rtk npm --workspace packages/backend exec node -- --import tsx --test src/session-workspace-view-model.test.ts
```

Expected: PASS。

- [x] **Step 4: 提交后端派生实现**

```bash
rtk git add packages/backend/src/session-workspace-view-model.ts packages/backend/src/session.routes.ts packages/backend/src/session-workspace-view-model.test.ts
rtk git commit -m "feat(backend): 派生会话右侧栏真实数据"
```

---

### Task 3: WS Inspector 快照事件

**Files:**
- Modify: `packages/backend/src/types.ts`
- Modify: `packages/backend/src/session-runtime.ts`
- Test: `packages/backend/src/session-runtime.test.ts`
- Modify: `packages/frontend/src/lib/ws.ts`
- Modify: `packages/frontend/src/session-ui/session-workspace-events.ts`
- Test: `packages/frontend/src/session-ui/session-workspace-events.test.ts`

- [x] **Step 1: 后端和前端 WS 类型新增事件**

在 `packages/backend/src/types.ts` 和 `packages/frontend/src/lib/ws.ts` 的 `WsServerEvent` union 中新增：

```ts
| {
    type: 'session_inspector:snapshot';
    sessionId: string;
    planItems: import('./session-types.js').SessionPlanItem[];
    toolRows: import('./session-types.js').SessionToolRow[];
    diffRows: import('./session-types.js').SessionDiffRow[];
  }
```

前端文件使用已有 `SessionPlanItem`、`SessionToolRow`、`SessionDiffRow` 类型 import，事件写成：

```ts
| {
    type: 'session_inspector:snapshot';
    sessionId: string;
    planItems: SessionPlanItem[];
    toolRows: SessionToolRow[];
    diffRows: SessionDiffRow[];
  }
```

- [x] **Step 2: 写后端运行时 inspector 快照测试**

在 `packages/backend/src/session-runtime.test.ts` 新增测试：

```ts
test('runSessionAgent broadcasts inspector snapshot after ACP tool evidence', async () => {
  const sent: string[] = [];
  const socket = {
    OPEN: 1,
    readyState: 1,
    send: (payload: string) => sent.push(payload),
  } as unknown as import('ws').WebSocket;
  const project = projectRepo.create({
    name: 'runtime inspector project',
    path: mkdtempSync(join(tmpdir(), 'session-runtime-inspector-')),
  });
  const session = sessionRepo.create({
    project_id: project.id,
    title: 'Runtime Inspector',
    mode: 'code',
    provider: 'codex',
    workspace_path: project.path,
  });
  wsHub.subscribeSession(session.id, socket);

  setSessionRuntimeAdapterForTest({
    backend: 'codex',
    listSessions: async () => [],
    invoke: async ({ onChunk }) => {
      onChunk({
        stream: 'stdout',
        channel: 'tool',
        text: 'patch SessionShellView\n',
        rawType: 'tool_call',
        event: {
          id: 'tool-apply-patch',
          message_id: 'message-1',
          run_id: 'run-1',
          agent_id: 'planner',
          seq: 1,
          type: 'tool_call',
          status: 'started',
          title: '调用工具 apply_patch',
          payload: {
            name: 'apply_patch',
            input: '*** Update File: packages/frontend/src/session-ui/SessionShellView.tsx',
          },
          created_at: Date.now(),
        },
      });
      return { exitCode: 0, sessionId: 'acp-inspector', stderr: '' };
    },
  });

  await runSessionAgent({ sessionId: session.id, prompt: '继续', provider: 'codex' });

  const events = sent.map((payload) => JSON.parse(payload) as { type: string; toolRows?: Array<{ label: string; target: string }> });
  const inspector = events.find((event) => event.type === 'session_inspector:snapshot');
  assert.equal(inspector?.toolRows?.[0]?.label, 'apply_patch');
  assert.equal(inspector?.toolRows?.[0]?.target, 'packages/frontend/src/session-ui/SessionShellView.tsx');
  wsHub.removeSocket(socket);
});
```

- [x] **Step 3: 运行后端 runtime 测试确认失败**

Run:

```bash
rtk npm --workspace packages/backend exec node -- --import tsx --test src/session-runtime.test.ts
```

Expected: FAIL，错误显示没有收到 `session_inspector:snapshot`。

- [x] **Step 4: 后端运行时广播 inspector snapshot**

在 `packages/backend/src/session-runtime.ts`：

- import `sessionAgentEventRepo` 已存在。
- import `buildSessionInspectorSnapshot`。
- 新增 helper：

```ts
function broadcastSessionInspectorSnapshot(sessionId: string): void {
  const runs = sessionRunRepo.listBySession(sessionId);
  const evidence = sessionEvidenceRepo.listBySession(sessionId);
  const agentEvents = runs.flatMap((run) => sessionAgentEventRepo.listByRun(run.id));
  const snapshot = buildSessionInspectorSnapshot(sessionId, evidence, agentEvents);
  wsHub.broadcastSession(sessionId, {
    type: 'session_inspector:snapshot',
    sessionId,
    ...snapshot,
  });
}
```

- 新增 helper：

```ts
function shouldBroadcastInspectorSnapshot(eventType: SessionEvidenceType): boolean {
  return eventType === 'status' ||
    eventType === 'tool_call' ||
    eventType === 'tool_result' ||
    eventType === 'file_read' ||
    eventType === 'file_diff' ||
    eventType === 'test' ||
    eventType === 'build' ||
    eventType === 'browser_check';
}
```

在 `recordSessionChunk` 创建并广播 `session_evidence:new` 后：

```ts
if (shouldBroadcastInspectorSnapshot(evidenceType)) {
  broadcastSessionInspectorSnapshot(input.sessionId);
}
```

- [x] **Step 5: 前端 reducer 处理快照**

在 `packages/frontend/src/session-ui/session-workspace-events.ts` 的 `applySessionWorkspaceEvent` 中新增分支：

```ts
if (event.type === 'session_inspector:snapshot') {
  return {
    ...payload,
    toolRows: event.toolRows,
    diffRows: event.diffRows,
    activeSession: {
      ...payload.activeSession,
      planItems: event.planItems,
    },
  };
}
```

- [x] **Step 6: 写前端 reducer 测试**

在 `packages/frontend/src/session-ui/session-workspace-events.test.ts` 新增测试：

```ts
test('applySessionWorkspaceEvent replaces inspector rows from snapshot', () => {
  const payload = createPayload();
  const next = applySessionWorkspaceEvent(payload, {
    type: 'session_inspector:snapshot',
    sessionId: payload.activeSession.session.id,
    planItems: [{
      id: 'plan-real',
      session_id: payload.activeSession.session.id,
      parent_id: null,
      title: '真实计划项',
      description: null,
      status: 'in_progress',
      priority: 0,
      source: 'acp_plan_update',
      evidence_event_id: 'ev-plan',
      created_at: Date.now(),
      updated_at: Date.now(),
      completed_at: null,
    }],
    toolRows: [{
      id: 'tool-real',
      action: 'exec',
      label: 'exec_command',
      target: 'npm run build',
      status: 'completed',
      durationMs: null,
      severity: 'info',
      eventId: 'ev-tool',
      created_at: Date.now(),
    }],
    diffRows: [{
      path: 'packages/frontend/src/session-ui/SessionShellView.tsx',
      status: 'modified',
      additions: 4,
      deletions: 1,
      summary: 'apply_patch',
    }],
  });

  assert.equal(next.activeSession.planItems[0]?.title, '真实计划项');
  assert.equal(next.toolRows[0]?.target, 'npm run build');
  assert.equal(next.diffRows[0]?.summary, 'apply_patch');
});
```

- [x] **Step 7: 运行 WS/reducer 测试**

Run:

```bash
rtk npm --workspace packages/backend exec node -- --import tsx --test src/session-runtime.test.ts
rtk npm --workspace packages/frontend exec node -- --import tsx --test src/session-ui/session-workspace-events.test.ts
```

Expected: PASS。

- [x] **Step 8: 提交 WS 快照实现**

```bash
rtk git add packages/backend/src/types.ts packages/backend/src/session-runtime.ts packages/backend/src/session-runtime.test.ts packages/frontend/src/lib/ws.ts packages/frontend/src/session-ui/session-workspace-events.ts packages/frontend/src/session-ui/session-workspace-events.test.ts
rtk git commit -m "feat: 广播会话右侧栏派生快照"
```

---

### Task 4: 右侧栏文案与空状态

**Files:**
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
- Test: `packages/frontend/src/session-ui/SessionShell.test.tsx`

- [x] **Step 1: 写前端展示测试**

在 `packages/frontend/src/session-ui/SessionShell.test.tsx` 调整既有右侧栏测试：

- 将断言 `待提交变更` 改为 `本次会话变更`。
- 将断言 `Uncommitted` 改为 `Session Changes`。
- 保留 `1 文件已修改` 类断言时改为 `本会话 1 个文件变更`。
- 删除固定提示 `存在未应用的 Compact 预览` 的断言。

新增无 run 测试：

```ts
test('SessionShell renders empty run state without fake provider values', () => {
  const payload = createPayload();
  payload.activeSession.runs = [];

  const html = renderSessionShell(payload);

  assert.match(html, /暂无代理运行/);
  assert.doesNotMatch(html, /gpt-test/);
  assert.doesNotMatch(html, /02:14:05/);
});
```

- [x] **Step 2: 运行前端展示测试确认失败**

Run:

```bash
rtk npm --workspace packages/frontend exec node -- --import tsx --test src/session-ui/SessionShell.test.tsx
```

Expected: FAIL，错误包含找不到“本次会话变更”或仍存在旧文案。

- [x] **Step 3: 更新 `RunModule` 空状态**

在 `packages/frontend/src/session-ui/SessionShellView.tsx` 的 `RunModule` 顶部添加：

```tsx
if (!run) {
  return (
    <section className="deepsea-inspector-section deepsea-run-section">
      <h3>代理运行 (Active Run)</h3>
      <div className="deepsea-empty">暂无代理运行</div>
    </section>
  );
}
```

然后将：

```ts
const provider = run?.provider ?? status.provider.backend ?? 'codex';
const model = run?.model ?? status.provider.model ?? 'gpt-test';
const runLabel = run?.status ?? status.status;
```

改为：

```ts
const provider = run.provider;
const model = run.model;
const runLabel = run.status;
```

并将耗时从 `run ? ... : '02:14:05'` 改为：

```tsx
<strong className="deepsea-mono">{formatDuration(run.started_at, run.completed_at ?? Date.now())}</strong>
```

- [x] **Step 4: 更新变更模块文案**

在 `SessionShellView.tsx` 中将 `DiffModule` 的可见文案改为：

```tsx
const changedLabel = rows.length === 0 ? '本会话暂无文件变更' : `本会话 ${rows.length} 个文件变更`;
```

标题改为：

```tsx
<h3>本次会话变更 <span>(Session Changes)</span></h3>
```

空状态文件名改为：

```tsx
<em>no session changes</em>
```

删除固定 footer 中的：

```tsx
<p>存在未应用的 Compact 预览，Fork 可能丢失上下文</p>
```

保留现有 `/compact`、`/fork` 按钮。

- [x] **Step 5: 运行前端展示测试确认通过**

Run:

```bash
rtk npm --workspace packages/frontend exec node -- --import tsx --test src/session-ui/SessionShell.test.tsx
```

Expected: PASS。

- [x] **Step 6: 提交前端展示调整**

```bash
rtk git add packages/frontend/src/session-ui/SessionShellView.tsx packages/frontend/src/session-ui/SessionShell.test.tsx
rtk git commit -m "fix(frontend): 接入会话变更真实文案"
```

---

### Task 5: 集成验证与收尾

**Files:**
- Read: `package.json`
- Read: `packages/backend/package.json`
- Read: `packages/frontend/package.json`
- Optional Modify: only files failing due to type/test drift from Tasks 1-4

- [x] **Step 1: 运行定向测试**

Run:

```bash
rtk npm --workspace packages/backend exec node -- --import tsx --test src/session-workspace-view-model.test.ts src/session-runtime.test.ts
rtk npm --workspace packages/frontend exec node -- --import tsx --test src/session-ui/session-workspace-events.test.ts src/session-ui/SessionShell.test.tsx
```

Expected: PASS。

- [x] **Step 2: 运行构建门禁**

Run:

```bash
rtk npm run build
```

Expected: PASS，后端 TypeScript 编译和前端 Vite build 均成功。

- [x] **Step 3: 检查右侧栏旧文案残留**

Run:

```bash
rtk rg -n "待提交变更|Uncommitted|gpt-test|02:14:05|存在未应用的 Compact 预览" packages/frontend/src/session-ui packages/frontend/src/pages
```

Expected: 没有命中；若测试 fixture 需要旧文案，必须改为新文案。

- [x] **Step 4: 检查 git 状态**

Run:

```bash
rtk git status --short
```

Expected: 只包含本任务相关文件，且没有未预期生成物。

- [x] **Step 5: 最终提交**

如果 Step 1-4 产生补丁：

```bash
rtk git add packages/backend/src/session-workspace-view-model.ts packages/backend/src/session.routes.ts packages/backend/src/session-runtime.ts packages/backend/src/types.ts packages/frontend/src/lib/ws.ts packages/frontend/src/session-ui/session-workspace-events.ts packages/frontend/src/session-ui/SessionShellView.tsx packages/backend/src/session-workspace-view-model.test.ts packages/backend/src/session-runtime.test.ts packages/frontend/src/session-ui/session-workspace-events.test.ts packages/frontend/src/session-ui/SessionShell.test.tsx
rtk git commit -m "fix: 收口会话右侧栏真实数据接入"
```

如果没有补丁，不创建空提交。

---

## Spec Coverage Self-Review

- 会话计划来自 ACP `plan_update`：Task 1、Task 2、Task 3 覆盖。
- 代理运行真实展示且无假值：Task 4 覆盖。
- 工具调用来自 ACP evidence：Task 1、Task 2 覆盖。
- 本次会话变更只来自当前 session ACP 文件变更：Task 1、Task 2、Task 4 覆盖。
- snapshot 与 WebSocket 增量一致：Task 3 覆盖。
- 测试和构建门禁：Task 5 覆盖。
