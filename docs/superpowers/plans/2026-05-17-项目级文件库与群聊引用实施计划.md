# 项目级文件库与群聊引用 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 建立项目级文件库，并允许群聊消息引用同项目文件。

**Architecture:** 后端新增 `files` 与 `message_file_refs` 数据模型，统一管理项目级文件上传、列表、删除和消息引用。前端新增项目文件页与文件选择弹窗，聊天编辑器同时支持本地待上传文件和项目文件引用。消息 metadata 保留附件快照，保证历史渲染和 agent dispatch 的附件上下文继续工作。

**Tech Stack:** Node.js, Express, better-sqlite3, multer, React 18, TypeScript, Vite, TanStack Query, Tailwind utilities, Radix Dialog, lucide-react.

---

## 文件结构

- Modify: `packages/backend/src/db.ts`
  - 创建 `files` 和 `message_file_refs` 表及索引。
- Create: `packages/backend/src/repos/files.ts`
  - 封装项目文件 CRUD、引用记录、列表聚合查询。
- Modify: `packages/backend/src/uploads.ts`
  - 增加项目文件上传目录、路由前缀、metadata/file record 构建辅助函数。
- Modify: `packages/backend/src/server.ts`
  - 静态服务新增 `/uploads/files`。
- Modify: `packages/backend/src/types.ts`
  - 增加 `ProjectFile`、`ProjectFileWithRefs`，扩展 `MessageAttachmentMetadata.fileId/deleted`。
- Modify: `packages/backend/src/routes.ts`
  - 增加项目文件 API；扩展消息发送支持 `fileIds`。
- Modify: `packages/backend/src/dispatcher.ts`
  - 解析 `/uploads/files/<projectId>/<storedName>` 为本地路径。
- Create: `packages/backend/src/repos/files.test.ts`
  - 覆盖 repo 层文件列表、引用、软删除。
- Modify: `packages/backend/src/uploads.test.ts`
  - 覆盖项目文件路径和 metadata 构建。
- Modify: `packages/backend/src/dispatcher.test.ts`
  - 覆盖新文件 URL localPath 解析。
- Modify: `packages/frontend/src/lib/types.ts`
  - 增加项目文件类型，扩展消息附件类型。
- Modify: `packages/frontend/src/lib/api.ts`
  - 增加 list/upload/delete files API；扩展 sendMessage `fileIds`。
- Create: `packages/frontend/src/pages/FilesPage.tsx`
  - 项目文件管理页面。
- Create: `packages/frontend/src/components/FilePickerDialog.tsx`
  - 群聊编辑器内的项目文件选择弹窗。
- Modify: `packages/frontend/src/components/RichMessageComposer.tsx`
  - 支持本地附件和项目文件引用两类待发送附件。
- Modify: `packages/frontend/src/pages/RoomPage.tsx`
  - 向编辑器传入 `projectId`，发送 `fileIds`，渲染删除态附件。
- Modify: `packages/frontend/src/components/AppShell.tsx`
  - 文件菜单跳转当前项目文件页。
- Modify: `packages/frontend/src/main.tsx`
  - 注册 `/projects/:projectId/files` 路由。
- Modify: `packages/frontend/src/lib/i18n.tsx`
  - 增加中文和英文文案。
- Modify: `packages/frontend/src/index.css`
  - 增加文件页、文件选择器和附件删除态样式。

## 并行策略

不建议并行写入。此功能跨数据库 schema、消息发送 contract、共享类型、路由入口和编辑器组件，拆分后容易产生 shared contract 冲突。按单线程执行，完成后统一验证。

## Task 1: 后端文件数据模型

**Files:**
- Modify: `packages/backend/src/db.ts`
- Modify: `packages/backend/src/types.ts`
- Create: `packages/backend/src/repos/files.ts`
- Create: `packages/backend/src/repos/files.test.ts`

- [x] **Step 1: 编写 repo 测试**

在 `packages/backend/src/repos/files.test.ts` 新增测试，覆盖：

```ts
test('fileRepo creates and lists active project files with reference summary', () => {
  // 创建 project、room、message、file、ref
  // 断言 listByProject 返回 reference_count、last_referenced_room_name
});

test('fileRepo soft deletes project file and hides it from active list', () => {
  // 创建 file 后 softDelete
  // 断言 deleted_at 有值，listByProject 不返回
});

test('fileRepo rejects refs for duplicate message file pairs', () => {
  // 同一 message/file 插入两次，第二次应被忽略或不重复计数
});
```

- [x] **Step 2: 运行测试确认失败**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/repos/files.test.ts
```

Expected: FAIL，提示 `fileRepo` 或表不存在。

- [x] **Step 3: 增加数据库表**

在 `packages/backend/src/db.ts` 中增加：

```sql
CREATE TABLE IF NOT EXISTS files (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  original_name TEXT NOT NULL,
  stored_name TEXT NOT NULL,
  mime_type TEXT NOT NULL,
  size INTEGER NOT NULL,
  url TEXT NOT NULL,
  storage_path TEXT NOT NULL,
  uploaded_by_id TEXT,
  uploaded_by_name TEXT,
  created_at INTEGER NOT NULL,
  deleted_at INTEGER,
  FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
);
CREATE INDEX IF NOT EXISTS idx_files_project_created ON files(project_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_files_project_deleted ON files(project_id, deleted_at);

CREATE TABLE IF NOT EXISTS message_file_refs (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  room_id TEXT NOT NULL,
  message_id TEXT NOT NULL,
  file_id TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE,
  FOREIGN KEY (room_id) REFERENCES rooms(id) ON DELETE CASCADE,
  FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE CASCADE,
  FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE CASCADE,
  UNIQUE (message_id, file_id)
);
CREATE INDEX IF NOT EXISTS idx_message_file_refs_file ON message_file_refs(file_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_message_file_refs_room ON message_file_refs(room_id, created_at DESC);
```

- [x] **Step 4: 增加共享后端类型**

在 `packages/backend/src/types.ts` 增加：

```ts
export interface ProjectFile {
  id: string;
  project_id: string;
  original_name: string;
  stored_name: string;
  mime_type: string;
  size: number;
  url: string;
  storage_path: string;
  uploaded_by_id: string | null;
  uploaded_by_name: string | null;
  created_at: number;
  deleted_at: number | null;
}

export interface ProjectFileWithRefs extends ProjectFile {
  reference_count: number;
  last_referenced_at: number | null;
  last_referenced_room_id: string | null;
  last_referenced_room_name: string | null;
}
```

扩展 `MessageAttachmentMetadata`：

```ts
fileId?: string;
deleted?: boolean;
```

- [x] **Step 5: 实现 `fileRepo`**

在 `packages/backend/src/repos/files.ts` 实现：

```ts
export const fileRepo = {
  listByProject(projectId: string): ProjectFileWithRefs[] {},
  get(id: string): ProjectFile | undefined {},
  create(input: Omit<ProjectFile, 'id' | 'created_at' | 'deleted_at'>): ProjectFile {},
  softDelete(id: string): ProjectFile | undefined {},
  addMessageRefs(input: { project_id: string; room_id: string; message_id: string; file_ids: string[] }): void {},
  listActiveByIds(projectId: string, ids: string[]): ProjectFile[] {},
};
```

`addMessageRefs` 使用 `INSERT OR IGNORE` 避免重复引用。

- [x] **Step 6: 运行 repo 测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/repos/files.test.ts
```

Expected: PASS。

## Task 2: 后端上传存储与 API

**Files:**
- Modify: `packages/backend/src/uploads.ts`
- Modify: `packages/backend/src/uploads.test.ts`
- Modify: `packages/backend/src/server.ts`
- Modify: `packages/backend/src/routes.ts`

- [x] **Step 1: 编写上传辅助测试**

在 `packages/backend/src/uploads.test.ts` 增加：

```ts
test('project file upload path is scoped by project id', () => {
  // 断言 buildProjectFileUploadDir('p1') 位于 projectFileUploadRoot 下
});

test('buildProjectFileRecord maps multer file to project file input', () => {
  // 断言 original_name、stored_name、url、storage_path、mime_type、size
});
```

- [x] **Step 2: 运行测试确认失败**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/uploads.test.ts
```

Expected: FAIL，提示新 helper 不存在。

- [x] **Step 3: 实现项目文件上传 helper**

在 `packages/backend/src/uploads.ts` 增加：

```ts
export const projectFileUploadRoot = join(__dirname, '..', 'data', 'uploads', 'files');
export const projectFileUploadRoute = '/uploads/files';

export function buildProjectFileUploadDir(projectId: string): string {}
export async function ensureProjectFileUploadDir(projectId: string): Promise<string> {}
export function buildProjectFileUrl(projectId: string, storedName: string): string {}
export function buildProjectFileRecordInput(projectId: string, file: Express.Multer.File, uploader?: { uploaded_by_id?: string; uploaded_by_name?: string }) {}
export async function cleanupProjectUploadedFiles(files: Express.Multer.File[] | undefined): Promise<void> {}
```

项目目录必须通过 `resolve` 校验仍在 `projectFileUploadRoot` 下。

- [x] **Step 4: 暴露静态文件目录**

在 `packages/backend/src/server.ts` 增加：

```ts
await ensureProjectFileUploadRoot();
app.use(projectFileUploadRoute, express.static(projectFileUploadRoot, {
  fallthrough: false,
  immutable: true,
  maxAge: '7d',
  setHeaders: (res) => {
    res.setHeader('X-Content-Type-Options', 'nosniff');
    res.setHeader('Content-Disposition', 'attachment');
  },
}));
```

如需图片/PDF 内嵌预览，后续可调整 `Content-Disposition`，首版保持下载安全默认。

- [x] **Step 5: 增加文件 API**

在 `packages/backend/src/routes.ts` 增加：

```ts
router.get('/projects/:projectId/files', ...);
router.post('/projects/:projectId/files', ...);
router.delete('/files/:fileId', ...);
```

上传 API 使用 multer 写入 `buildProjectFileUploadDir(projectId)`，创建 `files` 记录后返回数组。

删除 API：

1. `fileRepo.get`
2. `fileRepo.softDelete`
3. 校验 `storage_path` 在 `projectFileUploadRoot` 内
4. `unlink`，忽略 `ENOENT`

- [x] **Step 6: 运行后端测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/uploads.test.ts src/repos/files.test.ts
```

Expected: PASS。

## Task 3: 消息发送支持文件库引用

**Files:**
- Modify: `packages/backend/src/routes.ts`
- Modify: `packages/backend/src/dispatcher.ts`
- Modify: `packages/backend/src/dispatcher.test.ts`
- Modify: `packages/backend/src/types.ts`

- [x] **Step 1: 编写消息引用测试**

在合适的 routes 测试文件中新增或扩展测试：

```ts
test('posting message with fileIds creates message refs and attachment snapshots', async () => {
  // 创建 project、room、file
  // POST /rooms/:roomId/messages { content, fileIds: [file.id] }
  // 断言 message.metadata.attachments[0].fileId === file.id
  // 断言 message_file_refs 有记录
});

test('posting message rejects fileIds from another project', async () => {
  // 引用其他项目 file，预期 400
});
```

- [x] **Step 2: 扩展 API 输入模型**

在 `packages/backend/src/routes.ts`：

- `jsonMessageSchema` 增加 `fileIds: z.array(z.string()).optional()`
- `multipartMessageSchema` 增加 `fileIds: z.string().optional()`
- 增加 `parseMultipartFileIds`

- [x] **Step 3: 实现文件引用解析**

在 `createAndDispatchUserMessage` 前：

1. 根据 `roomId` 读取 room，取得 `project_id`。
2. 对 multipart 本地上传文件创建 `files` 记录。
3. 对传入 `fileIds` 调用 `fileRepo.listActiveByIds(projectId, ids)`。
4. 若数量不匹配，返回 400。
5. 本地上传文件与引用文件合并为 attachment snapshots。
6. 创建 message 后调用 `fileRepo.addMessageRefs`。

- [x] **Step 4: 保持附件数量限制**

本地上传数量 + `fileIds` 去重后数量必须小于等于 `MAX_MESSAGE_FILES`。超过时返回 400。

- [x] **Step 5: 扩展 dispatch localPath 解析**

在 `packages/backend/src/dispatcher.ts` 支持：

```ts
if (attachment.url.startsWith(`${projectFileUploadRoute}/`)) {
  // 解析 projectId/storedName，限制在 projectFileUploadRoot 内
}
```

- [x] **Step 6: 增加 dispatcher 测试**

在 `packages/backend/src/dispatcher.test.ts` 增加新 URL 前缀 localPath 测试。

- [x] **Step 7: 运行定向后端测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/dispatcher.test.ts src/uploads.test.ts src/repos/files.test.ts
```

Expected: PASS。

## Task 4: 前端 API 与类型

**Files:**
- Modify: `packages/frontend/src/lib/types.ts`
- Modify: `packages/frontend/src/lib/api.ts`
- Modify: `packages/frontend/src/lib/i18n.tsx`

- [x] **Step 1: 增加前端类型**

在 `packages/frontend/src/lib/types.ts` 增加：

```ts
export interface ProjectFile {
  id: string;
  project_id: string;
  original_name: string;
  stored_name: string;
  mime_type: string;
  size: number;
  url: string;
  uploaded_by_id: string | null;
  uploaded_by_name: string | null;
  created_at: number;
  deleted_at: number | null;
  reference_count: number;
  last_referenced_at: number | null;
  last_referenced_room_id: string | null;
  last_referenced_room_name: string | null;
}
```

扩展 `MessageAttachmentMetadata`：

```ts
fileId?: string;
deleted?: boolean;
```

- [x] **Step 2: 增加 API 方法**

在 `packages/frontend/src/lib/api.ts` 增加：

```ts
listProjectFiles(projectId: string): Promise<ProjectFile[]>
uploadProjectFiles(projectId: string, files: File[]): Promise<ProjectFile[]>
deleteProjectFile(fileId: string): Promise<void>
```

扩展 `sendMessage` 入参：

```ts
input: { content: string; mentions?: string[]; files?: File[]; fileIds?: string[] }
```

multipart 时附加 `fileIds` JSON 字符串；无本地文件但有 `fileIds` 时可走 JSON。

- [x] **Step 3: 增加 i18n 文案**

在 `packages/frontend/src/lib/i18n.tsx` 添加文件页、上传、删除确认、选择文件、空状态、预览、下载等中英文文案。

- [x] **Step 4: TypeScript 验证**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected: 可能因后续页面未实现暂时失败；确认当前类型改动不引入明显语法错误。

## Task 5: 文件页面

**Files:**
- Create: `packages/frontend/src/pages/FilesPage.tsx`
- Modify: `packages/frontend/src/main.tsx`
- Modify: `packages/frontend/src/components/AppShell.tsx`
- Modify: `packages/frontend/src/index.css`

- [x] **Step 1: 创建 `FilesPage`**

页面实现：

1. 从 `useParams` 读取 `projectId`。
2. `useQuery(['project', projectId])` 获取项目。
3. `useQuery(['project-files', projectId])` 获取文件列表。
4. 上传按钮使用隐藏 `<input type="file" multiple>`。
5. 搜索框按 `original_name` 和 `mime_type` 本地过滤。
6. 文件行展示名称、大小、类型、上传时间、引用次数、最近引用群聊。
7. 操作按钮：预览、下载、删除。

- [x] **Step 2: 实现预览弹窗**

首版：

- 图片使用 `<img src={file.url}>`。
- PDF 使用新标签打开或 iframe；若 `Content-Disposition: attachment` 影响 iframe，则提供打开原文件。
- 其他类型显示文件信息和下载按钮。

- [x] **Step 3: 实现删除确认**

删除前用确认弹窗或 `window.confirm`。文案必须说明：删除会影响所有历史引用，历史消息只保留已删除状态。

- [x] **Step 4: 注册路由**

在 `packages/frontend/src/main.tsx`：

```tsx
<Route path="/projects/:projectId/files" element={<FilesPage />} />
```

- [x] **Step 5: 接入侧边栏文件菜单**

在 `packages/frontend/src/components/AppShell.tsx`：

```tsx
<SidebarLink
  to={currentProject ? `/projects/${currentProject.id}/files` : '/'}
  icon={FolderKanban}
  label={t('shell.nav.files')}
  inactive={!currentProject}
/>
```

避免项目页和文件页 active 状态互相污染。

- [x] **Step 6: 添加样式**

在 `packages/frontend/src/index.css` 增加文件页布局和文件表格/列表样式。保持现有 `surface-1`、CSS 变量、8px radius 风格。

- [x] **Step 7: 构建验证**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected: PASS。

## Task 6: 群聊文件选择器与编辑器引用

**Files:**
- Create: `packages/frontend/src/components/FilePickerDialog.tsx`
- Modify: `packages/frontend/src/components/RichMessageComposer.tsx`
- Modify: `packages/frontend/src/pages/RoomPage.tsx`
- Modify: `packages/frontend/src/index.css`

- [x] **Step 1: 创建 `FilePickerDialog`**

Props:

```ts
interface FilePickerDialogProps {
  projectId: string;
  disabled?: boolean;
  selectedFileIds: string[];
  onSelect: (files: ProjectFile[]) => void;
}
```

行为：

1. 查询 `api.listProjectFiles(projectId)`。
2. 支持搜索。
3. 支持多选。
4. 禁用已选文件。
5. 点击确认后返回选中文件。

- [x] **Step 2: 扩展 composer 附件模型**

在 `RichMessageComposer.tsx` 内部区分：

```ts
type ComposerAttachment =
  | { kind: 'local'; id: string; file: File; previewUrl: string | null }
  | { kind: 'project'; id: string; file: ProjectFile };
```

`onSend` 入参改为：

```ts
onSend: (input: { content: string; mentions?: string[]; files?: File[]; fileIds?: string[] }) => void;
```

- [x] **Step 3: 增加文件库按钮**

在现有上传按钮旁边增加按钮，使用 lucide 图标，例如 `FolderOpen` 或 `Files`。按钮打开 `FilePickerDialog`。

- [x] **Step 4: 附件栏统一展示**

本地附件和项目文件引用共用附件栏：

- 本地图片显示 object URL。
- 项目图片显示 `file.url`。
- 非图片显示文件图标。
- 移除按钮能移除对应 attachment。

- [x] **Step 5: 发送 fileIds**

`handleSubmit` 中：

```ts
files: localAttachments.map(...)
fileIds: projectAttachments.map(...)
```

发送后 reset 清理本地 object URL 与项目引用。

- [x] **Step 6: 更新 RoomPage**

在 `RoomPage.tsx`：

- 将 `projectId` 传给 `RichMessageComposer`。
- `handleSend` 接收 `fileIds`。
- `api.sendMessage(roomId, input)` 传递 `fileIds`。
- 空消息判断包含 `fileIds`。

- [x] **Step 7: 构建验证**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected: PASS。

## Task 7: 历史消息删除态与端到端验证

**Files:**
- Modify: `packages/frontend/src/pages/RoomPage.tsx`
- Modify: `packages/frontend/src/lib/messageMetadata.ts`
- Modify: `packages/backend/src/routes.ts` 可选

- [x] **Step 1: 渲染附件删除态**

在 `MessageAttachments` 中：

- 若 `attachment.deleted === true`，显示禁用卡片。
- 不提供下载和预览。
- 文案显示 `message.attachmentDeleted`。

- [x] **Step 2: 删除文件时标记历史 metadata（可选增强）**

如果实现成本可控，在后端 `DELETE /api/files/:fileId` 后更新包含该 `fileId` 的消息 metadata：

- 找到 `message_file_refs.file_id = fileId` 的消息。
- 将 metadata 中对应附件 `deleted` 设为 true。

如果成本偏高，本步骤可跳过，依赖 URL 失效和未来详情查询；但前端已支持删除态字段。

- [x] **Step 3: 后端完整测试**

Run:

```bash
npm run test -w @openclaw-room/backend
```

Expected: PASS。

- [x] **Step 4: 全量构建**

Run:

```bash
npm run build
```

Expected: PASS。

- [x] **Step 5: 手工 smoke 测试**

Run:

```bash
npm run dev
```

手工检查：

1. 打开项目，点击左侧文件菜单进入文件页。
2. 在文件页上传图片和文本文件。
3. 文件列表显示项目、时间、大小、引用次数。
4. 图片可预览，文件可下载。
5. 进入群聊，从文件库选择文件并发送。
6. 消息中展示引用文件。
7. 删除文件后，文件列表不再显示；新消息选择器不再显示。

## Task 8: 收尾与提交

**Files:**
- All changed files from previous tasks

- [x] **Step 1: 查看工作区差异**

Run:

```bash
git status --short
git diff --stat
```

确认只包含本功能相关文件。注意当前仓库可能已有其他未提交改动，不要回滚或混入无关改动。

- [x] **Step 2: 最终验证**

Run:

```bash
npm run test -w @openclaw-room/backend
npm run build
```

Expected: PASS。

- [x] **Step 3: 更新计划勾选状态**

将本计划已完成任务勾选，保留无法执行项的原因。

- [x] **Step 4: 提交实现**

仅 stage 本功能相关文件：

```bash
git add packages/backend/src/db.ts \
  packages/backend/src/types.ts \
  packages/backend/src/repos/files.ts \
  packages/backend/src/repos/files.test.ts \
  packages/backend/src/uploads.ts \
  packages/backend/src/uploads.test.ts \
  packages/backend/src/server.ts \
  packages/backend/src/routes.ts \
  packages/backend/src/dispatcher.ts \
  packages/backend/src/dispatcher.test.ts \
  packages/frontend/src/lib/types.ts \
  packages/frontend/src/lib/api.ts \
  packages/frontend/src/lib/i18n.tsx \
  packages/frontend/src/pages/FilesPage.tsx \
  packages/frontend/src/components/FilePickerDialog.tsx \
  packages/frontend/src/components/RichMessageComposer.tsx \
  packages/frontend/src/pages/RoomPage.tsx \
  packages/frontend/src/components/AppShell.tsx \
  packages/frontend/src/main.tsx \
  packages/frontend/src/index.css \
  docs/superpowers/plans/2026-05-17-项目级文件库与群聊引用实施计划.md

git commit -m "feat: 添加项目级文件库"
```

如果发现这些文件中包含本任务前已有改动，提交前必须先用 `git diff` 明确区分，避免覆盖或误提交无关内容。

## 审查点

本计划需要人工重点审查：

1. `files` 与 `message_file_refs` 的 schema 是否足够首版使用。
2. 删除文件时是否需要首版同步更新历史消息 metadata 的 `deleted` 字段。
3. 静态文件响应头是否允许内嵌预览；若继续 `Content-Disposition: attachment`，首版 PDF/文本应使用新标签或下载。
4. 聊天编辑器附件数量上限是否仍按“本地上传 + 文件库引用总数”计算为 5。
