# 知识库 Phase 3 知识沉淀 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户能把 Session 消息或手动内容保存为 `session_note` 知识笔记，并自动提取决策、约束、风险和经验 metadata。

**Architecture:** 新增 `knowledge-notes` 后端服务，直接写入现有 `knowledge_sources` / `knowledge_extractions` / `knowledge_chunks`，复用现有 summary/chunk/FTS 能力。新增 Session API 和前端消息操作按钮，保存后刷新知识库 query。

**Tech Stack:** TypeScript, Node.js, Express, SQLite/better-sqlite3, React 18, TanStack Query, node:test, Vite.

---

## File Structure

- Create: `packages/backend/src/knowledge-notes.ts` - Session 知识笔记创建、metadata 提取、去重。
- Create: `packages/backend/src/knowledge-notes.test.ts` - 覆盖保存、去重、跨 session 拒绝、metadata 提取和搜索。
- Modify: `packages/backend/src/routes.ts` - 增加 `POST /api/sessions/:sessionId/knowledge-notes`。
- Modify: `packages/backend/src/knowledge.routes.test.ts` or create route test in `knowledge-notes.test.ts` - 覆盖 API 行为。
- Modify: `packages/frontend/src/lib/types.ts` - 增加知识笔记响应类型。
- Modify: `packages/frontend/src/lib/api.ts` - 增加 `createSessionKnowledgeNote`。
- Modify: `packages/frontend/src/session-ui/SessionMessageBubble.tsx` - 增加可选保存按钮和 loading 状态。
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx` - 接线保存动作。
- Modify: `packages/frontend/src/pages/SessionWorkspacePage.tsx` - 调用 API、toast、invalidate knowledge query。
- Modify: frontend tests near `SessionWorkspacePage.test.tsx` / `SessionMessageBubble` tests - 覆盖按钮渲染和 API 调用。
- Create: `docs/superpowers/verification/2026-06-08-知识库Phase3-知识沉淀验收.md` - 验证记录。
- Modify: this plan - 勾选执行记录。

## Task 1: Backend Knowledge Notes Service

**Files:**
- Create: `packages/backend/src/knowledge-notes.test.ts`
- Create: `packages/backend/src/knowledge-notes.ts`
- Read: `packages/backend/src/knowledge-ingestion.ts`
- Read: `packages/backend/src/repos/knowledge.ts`
- Read: `packages/backend/src/repos/sessions.ts`

- [x] **Step 1: Write failing service tests**

Create tests:

1. `createSessionKnowledgeNote` saves a session message as `session_note`, writes extraction/chunks, and search can find it.
2. The same session and same content returns the existing source with `deduplicated: true`.
3. A message from another session is rejected.
4. Metadata extraction detects decisions, constraints, risks, and learnings.

- [x] **Step 2: Run RED**

```bash
cd packages/backend
/Users/chendimao/.local/share/mise/installs/node/22.18.0/bin/node --import tsx --test src/knowledge-notes.test.ts
```

Expected: fail because `knowledge-notes.ts` does not exist.

- [x] **Step 3: Implement service**

Implement:

```ts
export interface SessionKnowledgeNoteMetadata {
  decisions: string[];
  constraints: string[];
  risks: string[];
  learnings: string[];
}

export interface SessionKnowledgeNoteResult {
  source: KnowledgeSource;
  deduplicated: boolean;
  metadata: SessionKnowledgeNoteMetadata;
}

export function createSessionKnowledgeNote(input: {
  sessionId: string;
  messageId?: string | null;
  title?: string | null;
  content?: string | null;
}): SessionKnowledgeNoteResult
```

Use `sessionRepo.get`, `sessionMessageRepo.get`, `projectRepo.get`, `knowledgeRepo.ensureSource`, `knowledgeRepo.saveExtraction`, `knowledgeRepo.replaceChunks`, `knowledgeRepo.updateSourceStatus`, `hashText`, `splitKnowledgeChunks`, `summarizeKnowledgeText`.

- [x] **Step 4: Run GREEN**

Run the same command. Expected: pass.

## Task 2: Backend API Route

**Files:**
- Modify: `packages/backend/src/routes.ts`
- Modify: `packages/backend/src/knowledge-notes.test.ts`

- [x] **Step 1: Write failing API tests**

Add route tests using the existing Express router:

1. `POST /api/sessions/:sessionId/knowledge-notes` with `messageId` returns `201`, `source.source_type=session_note`, `deduplicated=false`.
2. Repeating the same request returns `200`, `deduplicated=true`.
3. Unknown session returns `404`; foreign message returns `400`.

- [x] **Step 2: Run RED**

```bash
cd packages/backend
/Users/chendimao/.local/share/mise/installs/node/22.18.0/bin/node --import tsx --test src/knowledge-notes.test.ts
```

Expected: route tests fail with 404.

- [x] **Step 3: Implement route**

Add request schema:

```ts
const sessionKnowledgeNoteSchema = z.object({
  messageId: z.string().trim().min(1).optional(),
  title: z.string().trim().min(1).optional(),
  content: z.string().optional(),
}).refine((value) => Boolean(value.messageId || value.content?.trim()), {
  message: 'messageId or content is required',
});
```

Add route:

```ts
router.post('/sessions/:sessionId/knowledge-notes', (req, res) => {
  const parsed = sessionKnowledgeNoteSchema.safeParse(req.body ?? {});
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  try {
    const result = createSessionKnowledgeNote({ sessionId: req.params.sessionId, ...parsed.data });
    res.status(result.deduplicated ? 200 : 201).json(result);
  } catch (error) {
    const message = (error as Error).message;
    if (message === 'session not found') return res.status(404).json({ error: message });
    res.status(400).json({ error: message });
  }
});
```

- [x] **Step 4: Run GREEN**

Run the same command. Expected: pass.

## Task 3: Frontend API and Message Action

**Files:**
- Modify: `packages/frontend/src/lib/types.ts`
- Modify: `packages/frontend/src/lib/api.ts`
- Modify: `packages/frontend/src/session-ui/SessionMessageBubble.tsx`
- Modify: `packages/frontend/src/session-ui/SessionShellView.tsx`
- Modify: `packages/frontend/src/pages/SessionWorkspacePage.tsx`
- Modify: frontend tests.

- [x] **Step 1: Write failing frontend tests**

Add tests that:

1. `SessionMessageBubble` renders `保存为知识` when `onSaveKnowledge` is passed and disables it while saving.
2. Session workspace action calls `api.createSessionKnowledgeNote` with `sessionId` and `messageId`.

- [x] **Step 2: Run RED**

```bash
npm run build -w @openclaw-room/frontend
```

Expected: fail if types/props/API are missing after tests are added, or targeted test command fails if frontend test runner is available.

- [x] **Step 3: Implement frontend**

Add API:

```ts
createSessionKnowledgeNote: (sessionId: string, input: { messageId?: string; title?: string; content?: string }) =>
  request<SessionKnowledgeNoteResponse>(`/sessions/${sessionId}/knowledge-notes`, {
    method: 'POST',
    body: JSON.stringify(input),
  })
```

Add optional props through `SessionShellView` to `SessionMessageBubble`:

```ts
onSaveKnowledge?: (message: SessionMessage) => void;
savingKnowledgeMessageId?: string | null;
```

Use a compact secondary button in message header:

```tsx
<button type="button" className="deepsea-message__action" disabled={saving} onClick={() => onSaveKnowledge(message)}>
  {saving ? '保存中' : '保存为知识'}
</button>
```

In `SessionWorkspacePage.tsx`, call API, show toast, invalidate `knowledge-*` queries.

- [x] **Step 4: Run GREEN**

Run the frontend build or available targeted test command. Expected: pass.

## Task 4: Final Verification, Review, Commit

**Files:**
- Create: `docs/superpowers/verification/2026-06-08-知识库Phase3-知识沉淀验收.md`
- Modify: `docs/superpowers/plans/2026-06-08-知识库Phase3-知识沉淀实施计划.md`

- [x] **Step 1: Run focused backend tests**

```bash
cd packages/backend
/Users/chendimao/.local/share/mise/installs/node/22.18.0/bin/node --import tsx --test src/knowledge-notes.test.ts src/knowledge-rag.test.ts src/knowledge.routes.test.ts src/session-file-reference-context.test.ts
```

- [x] **Step 2: Run build**

```bash
npm run build
```

- [x] **Step 3: Write verification note**

Record exact commands, pass/fail output, code review notes, and residual risks.

- [x] **Step 4: Code review**

Review against `docs/superpowers/specs/2026-06-08-知识库Phase3-知识沉淀设计.md`, focusing on session/project boundary, duplicate behavior, metadata extraction determinism, UI action clarity, and no unrelated UI churn.

- [x] **Step 5: Commit**

Commit only Phase 3 files:

```bash
git add docs/superpowers/specs/2026-06-08-知识库Phase3-知识沉淀设计.md docs/superpowers/plans/2026-06-08-知识库Phase3-知识沉淀实施计划.md docs/superpowers/verification/2026-06-08-知识库Phase3-知识沉淀验收.md packages/backend/src/knowledge-notes.ts packages/backend/src/knowledge-notes.test.ts packages/backend/src/routes.ts packages/frontend/src/lib/types.ts packages/frontend/src/lib/api.ts packages/frontend/src/session-ui/SessionMessageBubble.tsx packages/frontend/src/session-ui/SessionShellView.tsx packages/frontend/src/pages/SessionWorkspacePage.tsx
git commit -m "feat: 增加知识库会话沉淀"
```

## 实际执行记录

- 2026-06-08：完成后端 `knowledge-notes` 服务与 API，覆盖保存、去重、跨 session 拒绝、metadata 提取、搜索入库。
- 2026-06-08：完成前端 API、类型、Session 消息 `保存为知识` 入口、保存中状态、toast 和知识库 query invalidate。
- 2026-06-08：完成 RED/GREEN 验证，前端定向测试 108/108 通过，后端知识库定向测试 17/17 通过，`npm run build` 通过。
- 2026-06-08：完成浏览器 smoke，知识库页和会话页 console 0 errors；会话消息 header 可见 `保存为知识`。
- 2026-06-08：完成主会话代码审查；发现并修复去重查询依赖列表搜索的问题，新增 `knowledgeRepo.getSourceByExternalId()` 精确查询和仓储测试。
- 2026-06-08：精确暂存 Phase 3 文件，排除 package/desktop/release/screenshot 等无关工作区改动，准备提交。
