# 知识库 Phase 1 功能补齐 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 将当前 `/knowledge` 从带占位数据的知识库页面补齐为 Phase 1 可验收的真实资料管理、解析、检索、详情和显式引用入口。

**Architecture:** 后端在现有 `knowledgeRepo`、`ingestProjectFileIntoKnowledge` 和 `files/resource_assets` 基础上补齐 source detail、extraction、chunks、FTS search、reprocess、disable/restore/delete API，并用轻量 service 封装跨 repo 业务语义。前端扩展 API client 和 knowledge 类型，清除 `KnowledgePage.tsx` 中的 Stitch 假数据，接入真实统计、搜索、详情 tabs、资源操作和移动端单列体验。会话引用继续兼容 `libraryFileRefs`，但 runtime 上下文优先读取知识库 extraction/chunks。

**Tech Stack:** TypeScript, Express, Zod, better-sqlite3, node:test, React 18, TanStack Query, Vite, CSS, Playwright CLI.

---

## File Structure

- Modify: `packages/backend/src/repos/knowledge.ts` - 增加 source 删除、chunk enabled 更新、usage refs 写入和 list/search 支持项。
- Modify: `packages/backend/src/knowledge-types.ts` - 增加 route detail、extraction response、action input 和 usage ref 类型。
- Create: `packages/backend/src/knowledge-service.ts` - 封装 source 详情、原始资源映射、重处理、禁用/恢复、删除和 usage ref。
- Modify: `packages/backend/src/routes.ts` - 挂载 `/api/knowledge/sources/:sourceId`、extraction、chunks、search、reprocess、patch、delete。
- Create: `packages/backend/src/knowledge.routes.test.ts` - 覆盖 Phase 1 知识库 route 行为。
- Modify: `packages/backend/src/session-file-reference-context.ts` - library refs 优先查 knowledge source 和 chunks/extraction。
- Modify: `packages/backend/src/session-file-reference-context.test.ts` - 覆盖知识库上下文优先级、citation 和截断标记。
- Modify: `packages/frontend/src/lib/knowledgeDisplay.ts` - 增加 detail、extraction、chunk、search result、capability 类型和展示 helper。
- Modify: `packages/frontend/src/lib/knowledgeDisplay.test.ts` - 覆盖真实统计、metadata-only、search result 聚合和假数据防回归 helper。
- Modify: `packages/frontend/src/lib/api.ts` - 增加 knowledge detail、extraction、chunks、search、reprocess、patch、delete API。
- Modify: `packages/frontend/src/lib/api.test.ts` - 覆盖新增 API URL、method 和 payload。
- Modify: `packages/frontend/src/pages/KnowledgePage.tsx` - 清除 fallback 数据，接入真实 query、search、详情 tabs 和操作。
- Modify: `packages/frontend/src/index.css` - 调整知识库移动端单列、详情抽屉和按钮状态。
- Use Existing: `packages/frontend/src/components/ProjectFilePreviewDialog.tsx` - 作为预览能力入口，不重复实现预览器。

## Task 1: Backend Route Tests for Phase 1 Knowledge API

**Files:**
- Create: `packages/backend/src/knowledge.routes.test.ts`
- Read: `packages/backend/src/resource-assets.routes.test.ts`
- Read: `packages/backend/src/files.routes.test.ts`

- [x] **Step 1: Create route test fixture**

Create `packages/backend/src/knowledge.routes.test.ts` with an isolated DB and HTTP helper matching existing route tests:

```ts
import assert from 'node:assert/strict';
import { constants, mkdtempSync, writeFileSync } from 'node:fs';
import { access } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import test from 'node:test';

process.env.OPENCLAW_ROOM_DB = join(mkdtempSync(join(tmpdir(), 'opendeepsea-knowledge-routes-')), 'test.db');

const { app } = await import('./server.js');
const { projectRepo } = await import('./repos/projects.js');
const { roomRepo } = await import('./repos/rooms.js');
const { fileRepo } = await import('./repos/files.js');
const { knowledgeRepo } = await import('./repos/knowledge.js');

const server = app.listen(0);
test.after(() => server.close());

async function request(path: string, init: RequestInit = {}): Promise<Response> {
  const address = server.address();
  if (!address || typeof address === 'string') throw new Error('server not listening');
  return fetch(`http://127.0.0.1:${address.port}${path}`, init);
}

function createProject(name: string) {
  return projectRepo.create({
    name,
    path: mkdtempSync(join(tmpdir(), `opendeepsea-knowledge-route-${name}-`)),
  });
}

function createTextFile(projectId: string, content = '部署 A12 浮标需要验收。') {
  const root = mkdtempSync(join(tmpdir(), 'opendeepsea-knowledge-route-file-'));
  const storagePath = join(root, 'notes.md');
  writeFileSync(storagePath, content);
  return fileRepo.create({
    project_id: projectId,
    original_name: 'notes.md',
    stored_name: 'notes.md',
    mime_type: 'text/markdown',
    size: Buffer.byteLength(content),
    url: `/uploads/files/${projectId}/notes.md`,
    storage_path: storagePath,
    uploaded_by_id: 'user',
    uploaded_by_name: 'You',
  });
}
```

- [x] **Step 2: Add failing test for source detail and extraction**

Append this test:

```ts
test('knowledge source detail exposes latest extraction and original file capability', async () => {
  const project = createProject('detail');
  const file = createTextFile(project.id, '# 验收计划\n部署 A12 浮标。');
  const source = knowledgeRepo.ensureSource({
    project_id: project.id,
    source_type: 'uploaded_file',
    source_id: file.id,
    title: file.original_name,
    mime_type: file.mime_type,
    size: file.size,
    status: 'ready',
    summary: '部署 A12 浮标。',
    tags: ['验收'],
    metadata: { file_id: file.id },
  });
  const extraction = knowledgeRepo.saveExtraction({
    source_id: source.id,
    plain_text: '# 验收计划\n部署 A12 浮标。',
    markdown: '# 验收计划\n部署 A12 浮标。',
  });
  knowledgeRepo.replaceChunks(source.id, [{
    chunk_type: 'body',
    content: '部署 A12 浮标。',
    project_id: project.id,
  }]);

  const detailRes = await request(`/api/knowledge/sources/${source.id}`);
  assert.equal(detailRes.status, 200);
  const detail = await detailRes.json() as {
    id: string;
    latest_extraction_id: string | null;
    capabilities: { preview: boolean; download: boolean; reprocess: boolean };
    original_file: { id: string; url: string } | null;
  };
  assert.equal(detail.id, source.id);
  assert.equal(detail.latest_extraction_id, extraction.id);
  assert.equal(detail.capabilities.preview, true);
  assert.equal(detail.capabilities.download, true);
  assert.equal(detail.capabilities.reprocess, true);
  assert.equal(detail.original_file?.id, file.id);

  const extractionRes = await request(`/api/knowledge/sources/${source.id}/extraction`);
  assert.equal(extractionRes.status, 200);
  const extractionBody = await extractionRes.json() as { plain_text: string; markdown: string | null; truncated: boolean };
  assert.match(extractionBody.plain_text, /A12/);
  assert.equal(extractionBody.markdown, '# 验收计划\n部署 A12 浮标。');
  assert.equal(extractionBody.truncated, false);
});
```

- [x] **Step 3: Add failing test for chunks and search**

Append this test:

```ts
test('knowledge chunks and search routes expose citations', async () => {
  const project = createProject('search');
  const room = roomRepo.create({ project_id: project.id, name: 'Search Room' });
  const source = knowledgeRepo.ensureSource({
    project_id: project.id,
    room_id: room.id,
    source_type: 'uploaded_file',
    source_id: 'file-search-1',
    title: 'search-notes.md',
    status: 'ready',
  });
  const chunks = knowledgeRepo.replaceChunks(source.id, [
    { chunk_type: 'body', content: 'A12 验收需要截图。', project_id: project.id, room_id: room.id, enabled: 1 },
    { chunk_type: 'body', content: 'A12 隐藏内容。', project_id: project.id, room_id: room.id, enabled: 0 },
  ]);

  const chunksRes = await request(`/api/knowledge/sources/${source.id}/chunks?enabled=1`);
  assert.equal(chunksRes.status, 200);
  const chunkBody = await chunksRes.json() as Array<{ id: string; enabled: 0 | 1; content: string }>;
  assert.deepEqual(chunkBody.map((chunk) => [chunk.id, chunk.enabled]), [[chunks[0]!.id, 1]]);

  const searchRes = await request(`/api/knowledge/search?projectId=${project.id}&q=A12&roomId=${room.id}`);
  assert.equal(searchRes.status, 200);
  const results = await searchRes.json() as Array<{ chunk_id: string; citation: { source_id: string; chunk_id: string; room_id: string | null } }>;
  assert.equal(results.length, 1);
  assert.equal(results[0]?.chunk_id, chunks[0]?.id);
  assert.equal(results[0]?.citation.source_id, source.id);
  assert.equal(results[0]?.citation.room_id, room.id);
});
```

- [x] **Step 4: Add failing test for reprocess, disable, restore, and delete**

Append this test:

```ts
test('knowledge action routes reprocess, disable, restore, and delete source records', async () => {
  const project = createProject('actions');
  const file = createTextFile(project.id, '重处理前内容。');
  const source = knowledgeRepo.ensureSource({
    project_id: project.id,
    source_type: 'uploaded_file',
    source_id: file.id,
    title: file.original_name,
    status: 'failed',
    error: 'previous failure',
    metadata: { file_id: file.id },
  });

  const reprocessRes = await request(`/api/knowledge/sources/${source.id}/reprocess`, { method: 'POST' });
  assert.equal(reprocessRes.status, 200);
  const ready = await reprocessRes.json() as { id: string; status: string; error: string | null };
  assert.equal(ready.id, source.id);
  assert.equal(ready.status, 'ready');
  assert.equal(ready.error, null);

  const disableRes = await request(`/api/knowledge/sources/${source.id}`, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ status: 'disabled' }),
  });
  assert.equal(disableRes.status, 200);
  assert.equal((await disableRes.json() as { status: string }).status, 'disabled');
  assert.equal(knowledgeRepo.search({ projectId: project.id, query: '重处理前内容' }).length, 0);

  const restoreRes = await request(`/api/knowledge/sources/${source.id}`, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ status: 'ready' }),
  });
  assert.equal(restoreRes.status, 200);
  assert.equal((await restoreRes.json() as { status: string }).status, 'ready');
  assert.equal(knowledgeRepo.search({ projectId: project.id, query: '重处理前内容' }).length, 1);

  const deleteRes = await request(`/api/knowledge/sources/${source.id}`, { method: 'DELETE' });
  assert.equal(deleteRes.status, 204);
  assert.equal(knowledgeRepo.getSource(source.id), undefined);
  await access(file.storage_path, constants.F_OK);
});
```

- [x] **Step 5: Run tests and verify they fail**

Run:

```bash
npm test -w @openclaw-room/backend -- src/knowledge.routes.test.ts
```

Expected:

```text
FAIL because /api/knowledge/sources/:sourceId, /extraction, /chunks, /search, /reprocess, PATCH, and DELETE routes are not implemented.
```

- [x] **Step 6: Commit failing tests**

```bash
git add packages/backend/src/knowledge.routes.test.ts
git commit -m "test(backend): 添加知识库Phase1路由验收"
```

## Task 2: Backend Repository Helpers and Knowledge Service

**Files:**
- Modify: `packages/backend/src/repos/knowledge.ts`
- Modify: `packages/backend/src/knowledge-types.ts`
- Create: `packages/backend/src/knowledge-service.ts`
- Test: `packages/backend/src/knowledge.routes.test.ts`

- [x] **Step 1: Extend backend knowledge types**

Modify `packages/backend/src/knowledge-types.ts` and add:

```ts
export interface KnowledgeSourceDetail extends KnowledgeSourceListItem {
  project_name: string | null;
  room_name: string | null;
  latest_extraction_id: string | null;
  latest_extraction_at: number | null;
  reference_count: number;
  original_file: {
    id: string;
    name: string;
    url: string;
    storage_path: string;
    source_type: string;
  } | null;
  capabilities: {
    preview: boolean;
    download: boolean;
    reprocess: boolean;
    disable: boolean;
    delete: boolean;
  };
}

export interface KnowledgeExtractionResponse extends KnowledgeExtraction {
  truncated: boolean;
  returned_char_count: number;
  original_char_count: number;
}

export interface KnowledgeUsageRefInput {
  project_id: string;
  source_id: string;
  chunk_id?: string | null;
  ref_type: 'manual_reference' | 'session_message';
  ref_id: string;
  metadata?: Record<string, unknown>;
}
```

- [x] **Step 2: Add repository helpers**

Modify `packages/backend/src/repos/knowledge.ts` by adding methods inside `knowledgeRepo` before `search`:

```ts
  setChunksEnabled(sourceId: string, enabled: 0 | 1): KnowledgeChunk[] {
    const source = this.getSource(sourceId);
    if (!source) throw new Error('source_id is invalid');
    const ts = now();
    db.prepare('UPDATE knowledge_chunks SET enabled = ? WHERE source_id = ?').run(enabled, sourceId);
    db.prepare('UPDATE knowledge_sources SET updated_at = ? WHERE id = ?').run(ts, sourceId);
    return this.listChunks(sourceId);
  },

  deleteSource(sourceId: string): boolean {
    const source = this.getSource(sourceId);
    if (!source) return false;
    db.prepare('DELETE FROM knowledge_sources WHERE id = ?').run(sourceId);
    return true;
  },

  countUsageRefs(sourceId: string): number {
    const row = db.prepare('SELECT COUNT(*) AS count FROM knowledge_usage_refs WHERE source_id = ?')
      .get(sourceId) as { count: number } | undefined;
    return row?.count ?? 0;
  },

  recordUsageRef(input: KnowledgeUsageRefInput): void {
    db.prepare(
      `INSERT INTO knowledge_usage_refs (id, project_id, source_id, chunk_id, ref_type, ref_id, metadata_json, created_at)
       VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
    ).run(
      nanoid(16),
      input.project_id,
      input.source_id,
      input.chunk_id ?? null,
      input.ref_type,
      input.ref_id,
      stringifyJson(input.metadata ?? {}),
      now(),
    );
  },
```

Also import `KnowledgeUsageRefInput` from `../knowledge-types.js`.

- [x] **Step 3: Create knowledge service**

Create `packages/backend/src/knowledge-service.ts`:

```ts
import { fileRepo } from './repos/files.js';
import { knowledgeRepo } from './repos/knowledge.js';
import { projectRepo } from './repos/projects.js';
import { resourceAssetRepo } from './repos/resource-assets.js';
import { roomRepo } from './repos/rooms.js';
import { ingestProjectFileIntoKnowledge } from './knowledge-ingestion.js';
import type {
  KnowledgeExtractionResponse,
  KnowledgeSearchResult,
  KnowledgeSource,
  KnowledgeSourceDetail,
  KnowledgeSourceType,
  KnowledgeStatus,
} from './knowledge-types.js';

const MAX_EXTRACTION_RESPONSE_CHARS = 80_000;

export const knowledgeService = {
  getDetail(sourceId: string): KnowledgeSourceDetail | undefined {
    const source = knowledgeRepo.getSource(sourceId);
    if (!source) return undefined;
    const project = projectRepo.get(source.project_id);
    const room = source.room_id ? roomRepo.get(source.room_id) : undefined;
    const latestExtraction = knowledgeRepo.getLatestExtraction(source.id);
    const chunks = knowledgeRepo.listChunks(source.id);
    const originalFile = getOriginalFile(source);
    return {
      ...source,
      project_name: project?.name ?? null,
      room_name: room?.name ?? null,
      chunk_count: chunks.length,
      latest_extraction_id: latestExtraction?.id ?? null,
      latest_extraction_at: latestExtraction?.created_at ?? null,
      reference_count: knowledgeRepo.countUsageRefs(source.id),
      original_file: originalFile
        ? {
          id: originalFile.id,
          name: originalFile.original_name,
          url: originalFile.url,
          storage_path: originalFile.storage_path,
          source_type: originalFile.source_type,
        }
        : null,
      capabilities: {
        preview: Boolean(originalFile?.url || source.source_type === 'agent_document'),
        download: Boolean(originalFile?.url && source.source_type === 'uploaded_file'),
        reprocess: Boolean(originalFile),
        disable: source.status !== 'disabled',
        delete: true,
      },
    };
  },

  getExtraction(sourceId: string): KnowledgeExtractionResponse | undefined {
    const extraction = knowledgeRepo.getLatestExtraction(sourceId);
    if (!extraction) return undefined;
    const plainText = extraction.plain_text ?? '';
    const markdown = extraction.markdown ?? null;
    const returnedText = plainText.slice(0, MAX_EXTRACTION_RESPONSE_CHARS);
    return {
      ...extraction,
      plain_text: returnedText,
      markdown: markdown && markdown.length > MAX_EXTRACTION_RESPONSE_CHARS
        ? markdown.slice(0, MAX_EXTRACTION_RESPONSE_CHARS)
        : markdown,
      truncated: plainText.length > MAX_EXTRACTION_RESPONSE_CHARS || Boolean(extraction.metadata.truncated),
      returned_char_count: returnedText.length,
      original_char_count: numberMetadata(extraction.metadata.original_char_count) ?? plainText.length,
    };
  },

  search(input: {
    projectId: string;
    roomId?: string;
    query: string;
    status?: KnowledgeStatus;
    sourceType?: KnowledgeSourceType;
    limit?: number;
  }): KnowledgeSearchResult[] {
    return knowledgeRepo.search({
      projectId: input.projectId,
      roomId: input.roomId,
      query: input.query,
      statuses: input.status ? [input.status] : undefined,
      sourceTypes: input.sourceType ? [input.sourceType] : undefined,
      limit: input.limit,
    });
  },

  async reprocess(sourceId: string): Promise<KnowledgeSource | undefined> {
    const source = knowledgeRepo.getSource(sourceId);
    if (!source) return undefined;
    const originalFile = getOriginalFile(source);
    if (!originalFile) {
      return knowledgeRepo.updateSourceStatus(sourceId, {
        status: 'failed',
        error: 'original resource is missing',
        metadata: { ...source.metadata, error: 'original resource is missing' },
        last_processed_at: Date.now(),
      });
    }
    return ingestProjectFileIntoKnowledge(originalFile);
  },

  updateStatus(sourceId: string, input: { status: 'ready' | 'disabled' | 'stale'; tags?: string[]; summary?: string | null }): KnowledgeSource | undefined {
    const source = knowledgeRepo.getSource(sourceId);
    if (!source) return undefined;
    if (input.status === 'disabled') {
      knowledgeRepo.setChunksEnabled(sourceId, 0);
      return knowledgeRepo.updateSourceStatus(sourceId, {
        status: 'disabled',
        tags: input.tags,
        summary: input.summary,
        metadata: { ...source.metadata, previous_status: source.status },
      });
    }
    if (input.status === 'ready') {
      knowledgeRepo.setChunksEnabled(sourceId, 1);
      const previousStatus = typeof source.metadata.previous_status === 'string'
        ? source.metadata.previous_status
        : null;
      const chunkCount = knowledgeRepo.listChunks(sourceId).length;
      return knowledgeRepo.updateSourceStatus(sourceId, {
        status: previousStatus === 'ready' || previousStatus === 'stale' || previousStatus === 'pending'
          ? previousStatus
          : chunkCount > 0 ? 'ready' : 'stale',
        tags: input.tags,
        summary: input.summary,
        metadata: { ...source.metadata, previous_status: null },
      });
    }
    return knowledgeRepo.updateSourceStatus(sourceId, {
      status: 'stale',
      tags: input.tags,
      summary: input.summary,
    });
  },

  deleteSource(sourceId: string): boolean {
    return knowledgeRepo.deleteSource(sourceId);
  },
};

function getOriginalFile(source: KnowledgeSource) {
  if (source.source_type === 'uploaded_file') return fileRepo.get(source.source_id);
  if (source.source_type === 'agent_document') {
    const assetId = source.source_id.startsWith('asset:') ? source.source_id.slice('asset:'.length) : source.source_id;
    const asset = resourceAssetRepo.get(assetId);
    if (!asset) return null;
    return fileRepo.get(`asset:${asset.id}`) ?? null;
  }
  const fileId = typeof source.metadata.file_id === 'string' ? source.metadata.file_id : null;
  return fileId ? fileRepo.get(fileId) ?? null : null;
}

function numberMetadata(value: unknown): number | null {
  return typeof value === 'number' && Number.isFinite(value) ? value : null;
}
```

- [x] **Step 4: Run backend tests and verify Task 1 failures are reduced**

Run:

```bash
npm test -w @openclaw-room/backend -- src/knowledge.routes.test.ts
```

Expected:

```text
Tests still fail because routes are not mounted yet, but TypeScript import errors for knowledge-service are absent.
```

- [x] **Step 5: Commit repo and service changes**

```bash
git add packages/backend/src/knowledge-types.ts packages/backend/src/repos/knowledge.ts packages/backend/src/knowledge-service.ts
git commit -m "feat(backend): 添加知识库服务层"
```

## Task 3: Backend Knowledge Routes

**Files:**
- Modify: `packages/backend/src/routes.ts`
- Test: `packages/backend/src/knowledge.routes.test.ts`

- [x] **Step 1: Import service and schemas**

Modify `packages/backend/src/routes.ts` near existing knowledge imports:

```ts
import { knowledgeService } from './knowledge-service.js';
```

Add schemas after `knowledgeListQuerySchema`:

```ts
const knowledgeSourceIdParamsSchema = z.object({
  sourceId: z.string().min(1),
});
const knowledgeChunksQuerySchema = z.object({
  enabled: z.coerce.number().int().refine((value) => value === 0 || value === 1).optional(),
  limit: z.coerce.number().int().positive().max(500).optional(),
  offset: z.coerce.number().int().min(0).optional(),
});
const knowledgeSearchQuerySchema = z.object({
  projectId: z.string().min(1),
  roomId: z.string().optional(),
  q: z.string().trim().min(1),
  status: knowledgeStatusSchema.optional(),
  sourceType: knowledgeSourceTypeSchema.optional(),
  limit: z.coerce.number().int().positive().max(100).optional(),
});
const knowledgePatchSchema = z.object({
  status: z.enum(['ready', 'disabled', 'stale']),
  tags: z.array(z.string()).optional(),
  summary: z.string().nullable().optional(),
});
```

- [x] **Step 2: Add detail, extraction, chunks, and search routes**

Insert routes after existing `router.get('/knowledge', ...)`:

```ts
router.get('/knowledge/search', (req, res) => {
  const parsed = knowledgeSearchQuerySchema.safeParse(req.query);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  const { projectId, roomId, q, status, sourceType, limit } = parsed.data;
  if (!projectRepo.get(projectId)) return res.status(404).json({ error: 'project not found' });
  if (roomId) {
    const room = roomRepo.get(roomId);
    if (!room) return res.status(404).json({ error: 'room not found' });
    if (room.project_id !== projectId) return res.status(400).json({ error: 'room does not belong to project' });
  }
  res.json(knowledgeService.search({ projectId, roomId, query: q, status, sourceType, limit }));
});

router.get('/knowledge/sources/:sourceId', (req, res) => {
  const parsed = knowledgeSourceIdParamsSchema.safeParse(req.params);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  const detail = knowledgeService.getDetail(parsed.data.sourceId);
  if (!detail) return res.status(404).json({ error: 'knowledge source not found' });
  res.json(detail);
});

router.get('/knowledge/sources/:sourceId/extraction', (req, res) => {
  const parsed = knowledgeSourceIdParamsSchema.safeParse(req.params);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  if (!knowledgeRepo.getSource(parsed.data.sourceId)) return res.status(404).json({ error: 'knowledge source not found' });
  const extraction = knowledgeService.getExtraction(parsed.data.sourceId);
  if (!extraction) return res.status(404).json({ error: 'knowledge extraction not found' });
  res.json(extraction);
});

router.get('/knowledge/sources/:sourceId/chunks', (req, res) => {
  const parsedParams = knowledgeSourceIdParamsSchema.safeParse(req.params);
  const parsedQuery = knowledgeChunksQuerySchema.safeParse(req.query);
  if (!parsedParams.success) return res.status(400).json({ error: parsedParams.error.flatten() });
  if (!parsedQuery.success) return res.status(400).json({ error: parsedQuery.error.flatten() });
  if (!knowledgeRepo.getSource(parsedParams.data.sourceId)) return res.status(404).json({ error: 'knowledge source not found' });
  const { enabled, limit = 100, offset = 0 } = parsedQuery.data;
  const chunks = knowledgeRepo.listChunks(parsedParams.data.sourceId)
    .filter((chunk) => enabled === undefined || chunk.enabled === enabled)
    .slice(offset, offset + limit);
  res.json(chunks);
});
```

- [x] **Step 3: Add reprocess, patch, and delete routes**

Append:

```ts
router.post('/knowledge/sources/:sourceId/reprocess', async (req, res) => {
  const parsed = knowledgeSourceIdParamsSchema.safeParse(req.params);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  const source = await knowledgeService.reprocess(parsed.data.sourceId);
  if (!source) return res.status(404).json({ error: 'knowledge source not found' });
  res.json(source);
});

router.patch('/knowledge/sources/:sourceId', (req, res) => {
  const parsedParams = knowledgeSourceIdParamsSchema.safeParse(req.params);
  const parsedBody = knowledgePatchSchema.safeParse(req.body);
  if (!parsedParams.success) return res.status(400).json({ error: parsedParams.error.flatten() });
  if (!parsedBody.success) return res.status(400).json({ error: parsedBody.error.flatten() });
  const source = knowledgeService.updateStatus(parsedParams.data.sourceId, parsedBody.data);
  if (!source) return res.status(404).json({ error: 'knowledge source not found' });
  res.json(source);
});

router.delete('/knowledge/sources/:sourceId', (req, res) => {
  const parsed = knowledgeSourceIdParamsSchema.safeParse(req.params);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  const deleted = knowledgeService.deleteSource(parsed.data.sourceId);
  if (!deleted) return res.status(404).json({ error: 'knowledge source not found' });
  res.status(204).end();
});
```

- [x] **Step 4: Run backend knowledge route tests**

Run:

```bash
npm test -w @openclaw-room/backend -- src/knowledge.routes.test.ts
```

Expected:

```text
PASS for knowledge.routes.test.ts.
```

- [x] **Step 5: Run related backend regression tests**

Run:

```bash
npm test -w @openclaw-room/backend -- src/repos/knowledge.test.ts src/knowledge-ingestion.test.ts src/files.routes.test.ts src/resource-assets.routes.test.ts
```

Expected:

```text
PASS for listed tests.
```

- [x] **Step 6: Commit backend routes**

```bash
git add packages/backend/src/routes.ts packages/backend/src/knowledge.routes.test.ts
git commit -m "feat(backend): 补齐知识库Phase1接口"
```

## Task 4: Session Reference Context Uses Knowledge First

**Files:**
- Modify: `packages/backend/src/session-file-reference-context.ts`
- Modify: `packages/backend/src/session-file-reference-context.test.ts`

- [x] **Step 1: Add failing session context test**

Modify `packages/backend/src/session-file-reference-context.test.ts` and add:

```ts
test('buildSessionFileReferenceContext prefers knowledge chunks for library file refs', async () => {
  const project = projectRepo.create({
    name: 'Knowledge Context',
    path: mkdtempSync(join(tmpdir(), 'opendeepsea-knowledge-context-')),
  });
  const file = fileRepo.create({
    project_id: project.id,
    original_name: 'context.md',
    stored_name: 'context.md',
    mime_type: 'text/markdown',
    size: 32,
    url: '',
    storage_path: '',
    uploaded_by_id: 'user',
    uploaded_by_name: 'You',
  });
  const source = knowledgeRepo.ensureSource({
    project_id: project.id,
    source_type: 'uploaded_file',
    source_id: file.id,
    title: file.original_name,
    status: 'ready',
  });
  knowledgeRepo.saveExtraction({ source_id: source.id, plain_text: 'fallback extraction' });
  knowledgeRepo.replaceChunks(source.id, [{
    chunk_type: 'body',
    content: 'A12 chunk from knowledge source.',
    project_id: project.id,
  }]);

  const context = await buildSessionFileReferenceContext({
    project,
    workspacePath: project.path,
    workspaceFileRefs: [],
    libraryFileRefs: [file.id],
  });

  assert.match(context.promptAddition, /Knowledge: context\.md/);
  assert.match(context.promptAddition, /A12 chunk from knowledge source/);
  assert.match(context.promptAddition, /source_id:/);
  assert.match(context.promptAddition, /chunk_id:/);
});
```

- [x] **Step 2: Import knowledge repo in session context**

Modify `packages/backend/src/session-file-reference-context.ts`:

```ts
import { knowledgeRepo } from './repos/knowledge.js';
```

- [x] **Step 3: Add knowledge lookup before legacy file injection**

Inside the `for (const ref of normalizeRefList(input.libraryFileRefs))` loop, after file validation and before agent document/uploaded file legacy branches, insert:

```ts
    const knowledgeBlock = renderKnowledgeReferenceBlock(input.project.id, file, libraryBudget);
    if (knowledgeBlock) {
      libraryBudget -= knowledgeBlock.usedChars;
      blocks.push(knowledgeBlock.block);
      continue;
    }
```

Add helper functions near existing render helpers:

```ts
function renderKnowledgeReferenceBlock(
  projectId: string,
  file: ProjectFile,
  budget: number,
): { block: string; usedChars: number } | null {
  if (budget <= 0) return null;
  const source = knowledgeRepo.listSources({
    projectId,
    sourceTypes: [file.source_type === 'agent_document' ? 'agent_document' : 'uploaded_file'],
    query: file.id,
    limit: 20,
  }).find((item) => item.source_id === file.id);
  if (!source || source.status !== 'ready') return null;
  const chunks = knowledgeRepo.listChunks(source.id).filter((chunk) => chunk.enabled === 1);
  if (chunks.length > 0) {
    const renderedChunks: string[] = [];
    let usedChars = 0;
    for (const chunk of chunks.slice(0, 8)) {
      if (usedChars >= budget) break;
      const limit = Math.min(chunk.content.length, budget - usedChars);
      const content = chunk.content.slice(0, limit);
      usedChars += content.length;
      renderedChunks.push([
        `#### chunk_id: ${chunk.id}`,
        `chunk_index: ${chunk.chunk_index}`,
        `truncated: ${chunk.content.length > limit ? 'true' : 'false'}`,
        '```',
        content,
        '```',
      ].join('\n'));
    }
    return {
      usedChars,
      block: [
        `### Knowledge: ${source.title}`,
        `source_id: ${source.id}`,
        `source_type: ${source.source_type}`,
        `citation_key: knowledge:${source.id}`,
        ...renderedChunks,
      ].join('\n'),
    };
  }
  const extraction = knowledgeRepo.getLatestExtraction(source.id);
  if (!extraction?.plain_text) return null;
  const content = extraction.plain_text.slice(0, budget);
  return {
    usedChars: content.length,
    block: [
      `### Knowledge: ${source.title}`,
      `source_id: ${source.id}`,
      `extraction_id: ${extraction.id}`,
      `source_type: ${source.source_type}`,
      `truncated: ${extraction.plain_text.length > content.length ? 'true' : 'false'}`,
      `citation_key: knowledge:${source.id}`,
      '```',
      content,
      '```',
    ].join('\n'),
  };
}
```

- [x] **Step 4: Run session context tests**

Run:

```bash
npm test -w @openclaw-room/backend -- src/session-file-reference-context.test.ts
```

Expected:

```text
PASS for session-file-reference-context.test.ts.
```

- [x] **Step 5: Commit session context changes**

```bash
git add packages/backend/src/session-file-reference-context.ts packages/backend/src/session-file-reference-context.test.ts
git commit -m "feat(backend): 会话引用优先使用知识库内容"
```

## Task 5: Frontend Knowledge API and Types

**Files:**
- Modify: `packages/frontend/src/lib/knowledgeDisplay.ts`
- Modify: `packages/frontend/src/lib/knowledgeDisplay.test.ts`
- Modify: `packages/frontend/src/lib/api.ts`
- Modify: `packages/frontend/src/lib/api.test.ts`

- [x] **Step 1: Extend frontend knowledge types**

Modify `packages/frontend/src/lib/knowledgeDisplay.ts`:

```ts
export interface KnowledgeOriginalFile {
  id: string;
  name: string;
  url: string;
  storage_path: string;
  source_type: string;
}

export interface KnowledgeSourceCapabilities {
  preview: boolean;
  download: boolean;
  reprocess: boolean;
  disable: boolean;
  delete: boolean;
}

export interface KnowledgeSourceDetail extends KnowledgeSource {
  latest_extraction_id?: string | null;
  latest_extraction_at?: number | null;
  original_file?: KnowledgeOriginalFile | null;
  capabilities?: KnowledgeSourceCapabilities;
}

export interface KnowledgeExtraction {
  id: string;
  source_id: string;
  plain_text: string;
  markdown: string | null;
  metadata: KnowledgeMetadata;
  created_at: number;
  truncated: boolean;
  returned_char_count: number;
  original_char_count: number;
}

export interface KnowledgeChunk {
  id: string;
  source_id: string;
  extraction_id: string | null;
  chunk_index: number;
  chunk_type: 'plain_text' | 'markdown' | 'code' | 'table' | 'summary' | 'body';
  heading: string | null;
  content: string;
  token_estimate: number | null;
  enabled: 0 | 1;
  metadata: KnowledgeMetadata;
  created_at: number;
}

export interface KnowledgeSearchResult {
  chunk_id: string;
  source_id: string;
  project_id: string;
  source_type: KnowledgeSourceType;
  title: string;
  tags: string[];
  chunk_index: number;
  chunk_type: KnowledgeChunk['chunk_type'];
  heading: string | null;
  content: string;
  snippet: string;
  score: number;
  metadata: KnowledgeMetadata;
  citation: {
    source_id: string;
    source_type: KnowledgeSourceType;
    source_title: string;
    external_source_id: string;
    chunk_id: string;
    chunk_index: number;
    heading: string | null;
    room_id: string | null;
  };
}
```

- [x] **Step 2: Add API client methods**

Modify imports in `packages/frontend/src/lib/api.ts` to include new types, then add methods after `listKnowledgeSources`:

```ts
  getKnowledgeSource: (sourceId: string) =>
    request<KnowledgeSourceDetail>(`/knowledge/sources/${encodeURIComponent(sourceId)}`),
  getKnowledgeExtraction: (sourceId: string) =>
    request<KnowledgeExtraction>(`/knowledge/sources/${encodeURIComponent(sourceId)}/extraction`),
  listKnowledgeChunks: (sourceId: string, filters: { enabled?: 0 | 1; limit?: number; offset?: number } = {}) => {
    const params = new URLSearchParams();
    if (filters.enabled !== undefined) params.set('enabled', String(filters.enabled));
    if (filters.limit) params.set('limit', String(filters.limit));
    if (filters.offset) params.set('offset', String(filters.offset));
    const query = params.toString();
    return request<KnowledgeChunk[]>(`/knowledge/sources/${encodeURIComponent(sourceId)}/chunks${query ? `?${query}` : ''}`);
  },
  searchKnowledge: (filters: {
    projectId: string;
    roomId?: string;
    query: string;
    status?: KnowledgeSourceStatus | '';
    sourceType?: KnowledgeSourceType | '';
    limit?: number;
  }) => {
    const params = new URLSearchParams({ projectId: filters.projectId, q: filters.query });
    if (filters.roomId) params.set('roomId', filters.roomId);
    if (filters.status) params.set('status', filters.status);
    if (filters.sourceType) params.set('sourceType', filters.sourceType);
    if (filters.limit) params.set('limit', String(filters.limit));
    return request<KnowledgeSearchResult[]>(`/knowledge/search?${params.toString()}`);
  },
  reprocessKnowledgeSource: (sourceId: string) =>
    request<KnowledgeSource>(`/knowledge/sources/${encodeURIComponent(sourceId)}/reprocess`, { method: 'POST' }),
  updateKnowledgeSource: (
    sourceId: string,
    input: { status: 'ready' | 'disabled' | 'stale'; tags?: string[]; summary?: string | null },
  ) =>
    request<KnowledgeSource>(`/knowledge/sources/${encodeURIComponent(sourceId)}`, {
      method: 'PATCH',
      body: JSON.stringify(input),
    }),
  deleteKnowledgeSource: (sourceId: string, options: { deleteOriginal?: boolean } = {}) => {
    const query = options.deleteOriginal ? '?deleteOriginal=1' : '';
    return request<void>(`/knowledge/sources/${encodeURIComponent(sourceId)}${query}`, { method: 'DELETE' });
  },
```

- [x] **Step 3: Add frontend API tests**

Modify `packages/frontend/src/lib/api.test.ts` and add:

```ts
test('knowledge detail API requests encoded source id', async () => {
  const requestedUrl = await captureApiRequest(
    () => api.getKnowledgeSource('source/id 1'),
    { id: 'source/id 1', project_id: 'p', source_type: 'uploaded_file', title: 'x', status: 'ready' },
  );
  assert.equal(requestedUrl, '/api/knowledge/sources/source%2Fid%201');
});

test('knowledge search API builds FTS query URL', async () => {
  const requestedUrl = await captureApiRequest(
    () => api.searchKnowledge({
      projectId: 'project-1',
      roomId: 'room-1',
      query: 'A12 验收',
      status: 'ready',
      sourceType: 'uploaded_file',
      limit: 10,
    }),
    [],
  );
  assert.equal(
    requestedUrl,
    '/api/knowledge/search?projectId=project-1&q=A12+%E9%AA%8C%E6%94%B6&roomId=room-1&status=ready&sourceType=uploaded_file&limit=10',
  );
});

test('knowledge action APIs send method and payload', async () => {
  const originalFetch = globalThis.fetch;
  const requests: Array<{ url: string; method: string; body: string | null }> = [];
  globalThis.fetch = (async (input: RequestInfo | URL, init?: RequestInit) => {
    requests.push({
      url: String(input),
      method: init?.method ?? 'GET',
      body: typeof init?.body === 'string' ? init.body : null,
    });
    return new Response(JSON.stringify({ id: 'source-1', status: 'ready' }), {
      status: init?.method === 'DELETE' ? 204 : 200,
      headers: { 'Content-Type': 'application/json' },
    });
  }) as typeof fetch;

  try {
    await api.reprocessKnowledgeSource('source-1');
    await api.updateKnowledgeSource('source-1', { status: 'disabled' });
    await api.deleteKnowledgeSource('source-1');
  } finally {
    globalThis.fetch = originalFetch;
  }

  assert.deepEqual(requests.map((request) => [request.url, request.method]), [
    ['/api/knowledge/sources/source-1/reprocess', 'POST'],
    ['/api/knowledge/sources/source-1', 'PATCH'],
    ['/api/knowledge/sources/source-1', 'DELETE'],
  ]);
  assert.equal(requests[1]?.body, JSON.stringify({ status: 'disabled' }));
});
```

- [x] **Step 4: Run frontend build for type coverage**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected:

```text
tsc -b and vite build complete without TypeScript errors.
```

- [x] **Step 5: Commit frontend API and types**

```bash
git add packages/frontend/src/lib/api.ts packages/frontend/src/lib/api.test.ts packages/frontend/src/lib/knowledgeDisplay.ts packages/frontend/src/lib/knowledgeDisplay.test.ts
git commit -m "feat(frontend): 添加知识库Phase1客户端类型"
```

## Task 6: Knowledge Page Real Data and Detail Tabs

**Files:**
- Modify: `packages/frontend/src/pages/KnowledgePage.tsx`
- Modify: `packages/frontend/src/index.css`

- [x] **Step 1: Remove hard-coded fallback constants**

In `packages/frontend/src/pages/KnowledgePage.tsx`, delete `FALLBACK_STATS` and `FALLBACK_ROWS`. Replace dashboard stats computation with:

```ts
  const dashboardStats = useMemo<KnowledgeDashboardStats>(() => {
    const pending = Math.max(0, liveStats.total - liveStats.ready - liveStats.processing - liveStats.failed);
    return {
      total: liveStats.total,
      ready: liveStats.ready,
      processing: liveStats.processing,
      pending,
      failed: liveStats.failed,
      chunks: liveStats.chunks,
      totalSize: liveStats.totalSize,
    };
  }, [liveStats]);
```

Replace row computation with:

```ts
  const rows = useMemo(() => {
    return visibleSources.map(createKnowledgeRow);
  }, [visibleSources]);

  const selectedRow = rows.find((row) => row.id === selectedSourceId) ?? null;
  const pathLabel = selectedProject
    ? `${selectedProject.name} · ${selectedProject.path}`
    : '所有项目';
```

- [x] **Step 2: Add source detail queries**

Add state:

```ts
  const [activeDetailTab, setActiveDetailTab] = useState<'overview' | 'preview' | 'extraction' | 'summary' | 'chunks' | 'refs'>('overview');
```

Add queries:

```ts
  const selectedSourceIdForQuery = selectedRow?.source?.id ?? null;
  const { data: selectedDetail } = useQuery({
    queryKey: ['knowledge-source', selectedSourceIdForQuery],
    queryFn: () => api.getKnowledgeSource(selectedSourceIdForQuery!),
    enabled: !!selectedSourceIdForQuery,
  });
  const { data: selectedExtraction } = useQuery({
    queryKey: ['knowledge-extraction', selectedSourceIdForQuery],
    queryFn: () => api.getKnowledgeExtraction(selectedSourceIdForQuery!),
    enabled: !!selectedSourceIdForQuery && activeDetailTab === 'extraction',
  });
  const { data: selectedChunks = [] } = useQuery({
    queryKey: ['knowledge-chunks', selectedSourceIdForQuery],
    queryFn: () => api.listKnowledgeChunks(selectedSourceIdForQuery!, { limit: 200 }),
    enabled: !!selectedSourceIdForQuery && activeDetailTab === 'chunks',
  });
```

- [x] **Step 3: Add FTS search query when a project is selected**

Add:

```ts
  const keyword = (filters.keyword ?? '').trim();
  const shouldSearchKnowledge = Boolean(selectedProjectId && keyword);
  const { data: searchResults = [], isLoading: searchLoading } = useQuery({
    queryKey: ['knowledge-search', selectedProjectId, activeRoomId, filters.status ?? '', filters.sourceType ?? '', keyword],
    queryFn: () => api.searchKnowledge({
      projectId: selectedProjectId,
      roomId: activeRoomId || undefined,
      status: filters.status || undefined,
      sourceType: filters.sourceType || undefined,
      query: keyword,
      limit: 50,
    }),
    enabled: shouldSearchKnowledge,
  });
```

Display a compact search result count above the table:

```tsx
{shouldSearchKnowledge ? (
  <div className="mb-2 rounded border border-slate-200 bg-slate-50 px-3 py-2 text-[11px] text-slate-500">
    {searchLoading ? '全文检索中...' : `全文检索命中 ${searchResults.length} 个 chunk`}
  </div>
) : null}
```

- [x] **Step 4: Replace fake sidebar counts**

In `KnowledgeSidebar`, remove hard-coded `我的收藏 56`、`最近使用 128`、`回收站 8` and resource type fixed counts. Render only real available filters:

```tsx
<SidebarPrimaryItem icon={FolderOpen} label="全部资源" count={formatCount(stats.total)} active onClick={() => onFiltersChange({ projectId: filters.projectId ?? '' })} />
```

For resource type buttons, derive counts from the loaded `sources` collection. If the page has no derived count for a type, render the button label without a count badge.

- [x] **Step 5: Replace details panel with real source data**

Change `KnowledgeDetailsPanel` props:

```ts
function KnowledgeDetailsPanel({
  row,
  detail,
  extraction,
  chunks,
  activeTab,
  onTabChange,
}: {
  row: KnowledgeResourceRow | null;
  detail?: KnowledgeSourceDetail;
  extraction?: KnowledgeExtraction;
  chunks: KnowledgeChunk[];
  activeTab: 'overview' | 'preview' | 'extraction' | 'summary' | 'chunks' | 'refs';
  onTabChange: (tab: 'overview' | 'preview' | 'extraction' | 'summary' | 'chunks' | 'refs') => void;
}): JSX.Element
```

If `row` is null, render overview:

```tsx
if (!row) {
  return (
    <aside className="knowledge-details">
      <div className="flex h-11 shrink-0 items-center border-b border-slate-200 p-3">
        <h2 className="text-[14px] font-bold text-slate-800">知识库概览</h2>
      </div>
      <div className="knowledge-details__body knowledge-scrollbar">
        <p className="text-[12px] leading-5 text-slate-500">选择资源后查看摘要、解析内容和 chunks。</p>
      </div>
    </aside>
  );
}
```

Use real values:

```tsx
const source = detail ?? row.source;
const chunkCount = detail?.chunk_count ?? row.source?.chunk_count ?? 0;
const referenceCount = detail?.reference_count ?? row.source?.reference_count ?? 0;
const pagesLabel = chunkCount > 0 ? `${chunkCount} chunks` : '无 chunks';
```

- [x] **Step 6: Implement tab bodies**

Inside details body, render:

```tsx
{activeTab === 'overview' ? <KnowledgeOverview row={row} detail={detail} /> : null}
{activeTab === 'preview' ? <KnowledgePreviewHint detail={detail} /> : null}
{activeTab === 'extraction' ? <KnowledgeExtractionPanel extraction={extraction} /> : null}
{activeTab === 'summary' ? <KnowledgeSummaryPanel source={detail ?? row.source} /> : null}
{activeTab === 'chunks' ? <KnowledgeChunksPanel chunks={chunks} /> : null}
{activeTab === 'refs' ? <KnowledgeRefsPanel detail={detail} /> : null}
```

Add small components in the same file initially:

```tsx
function KnowledgeExtractionPanel({ extraction }: { extraction?: KnowledgeExtraction }): JSX.Element {
  if (!extraction) return <p className="text-[12px] text-slate-400">暂无提取内容。</p>;
  return (
    <pre className="max-h-[420px] overflow-auto rounded border border-slate-200 bg-slate-50 p-3 text-[11px] leading-5 text-slate-700">
      {extraction.markdown || extraction.plain_text}
    </pre>
  );
}

function KnowledgeChunksPanel({ chunks }: { chunks: KnowledgeChunk[] }): JSX.Element {
  if (chunks.length === 0) return <p className="text-[12px] text-slate-400">暂无 chunks。</p>;
  return (
    <div className="space-y-2">
      {chunks.map((chunk) => (
        <div key={chunk.id} className="rounded border border-slate-200 bg-white p-2">
          <div className="mb-1 font-mono text-[10px] text-slate-400">#{chunk.chunk_index} · {chunk.token_estimate ?? 0} tokens · {chunk.enabled ? 'enabled' : 'disabled'}</div>
          <p className="line-clamp-4 text-[11px] leading-5 text-slate-600">{chunk.content}</p>
        </div>
      ))}
    </div>
  );
}
```

- [x] **Step 7: Run frontend build**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected:

```text
Frontend TypeScript and Vite build pass.
```

- [x] **Step 8: Commit real data page update**

```bash
git add packages/frontend/src/pages/KnowledgePage.tsx packages/frontend/src/index.css
git commit -m "feat(frontend): 知识库页面接入真实数据"
```

## Task 7: Knowledge Page Actions and Preview Integration

**Files:**
- Modify: `packages/frontend/src/pages/KnowledgePage.tsx`
- Use Existing: `packages/frontend/src/components/ProjectFilePreviewDialog.tsx`

- [x] **Step 1: Add preview state and convert knowledge source to project file**

Import `ProjectFilePreviewDialog` and `ProjectFile`:

```ts
import { ProjectFilePreviewDialog } from '../components/ProjectFilePreviewDialog';
import type { ProjectFile } from '../lib/types';
```

Add state:

```ts
  const [previewFile, setPreviewFile] = useState<ProjectFile | null>(null);
```

Add helper:

```ts
function knowledgeDetailToPreviewFile(detail: KnowledgeSourceDetail): ProjectFile | null {
  if (!detail.original_file) return null;
  return {
    id: detail.original_file.id,
    project_id: detail.project_id,
    source_type: detail.source_type === 'agent_document' ? 'agent_document' : 'uploaded_file',
    original_name: detail.original_file.name,
    stored_name: detail.original_file.name,
    mime_type: detail.mime_type ?? 'application/octet-stream',
    size: detail.size ?? 0,
    url: detail.original_file.url,
    storage_path: detail.original_file.storage_path,
    uploaded_by_id: null,
    uploaded_by_name: null,
    source_message_id: null,
    source_room_id: detail.room_id ?? null,
    source_agent_id: typeof detail.metadata?.source_agent_id === 'string' ? detail.metadata.source_agent_id : null,
    source_task_id: typeof detail.metadata?.source_task_id === 'string' ? detail.metadata.source_task_id : null,
    source_display_name: null,
    source_label: typeof detail.metadata?.source_label === 'string' ? detail.metadata.source_label : null,
    source_context_id: null,
    source_context_name: null,
    source_context_type: null,
    content: null,
    created_at: detail.created_at ?? Date.now(),
    deleted_at: null,
    reference_count: detail.reference_count ?? 0,
    last_referenced_message_id: null,
    last_referenced_room_id: null,
    last_referenced_room_name: null,
  };
}
```

- [x] **Step 2: Add mutations**

Add:

```ts
  const reprocess = useMutation({
    mutationFn: (sourceId: string) => api.reprocessKnowledgeSource(sourceId),
    onSuccess: async () => {
      await queryClient.invalidateQueries({ queryKey: ['knowledge-sources'] });
      await queryClient.invalidateQueries({ queryKey: ['knowledge-source'] });
      toast.success('已重新处理资源');
    },
    onError: (err) => toast.error((err as Error).message),
  });

  const updateSource = useMutation({
    mutationFn: ({ sourceId, status }: { sourceId: string; status: 'ready' | 'disabled' | 'stale' }) =>
      api.updateKnowledgeSource(sourceId, { status }),
    onSuccess: async () => {
      await queryClient.invalidateQueries({ queryKey: ['knowledge-sources'] });
      await queryClient.invalidateQueries({ queryKey: ['knowledge-source'] });
      toast.success('已更新资源状态');
    },
    onError: (err) => toast.error((err as Error).message),
  });

  const deleteSource = useMutation({
    mutationFn: (sourceId: string) => api.deleteKnowledgeSource(sourceId),
    onSuccess: async () => {
      setSelectedSourceId(null);
      await queryClient.invalidateQueries({ queryKey: ['knowledge-sources'] });
      toast.success('已删除知识记录');
    },
    onError: (err) => toast.error((err as Error).message),
  });
```

- [x] **Step 3: Wire details action buttons**

Pass action handlers to `KnowledgeDetailsPanel`:

```tsx
<KnowledgeDetailsPanel
  row={selectedRow}
  detail={selectedDetail}
  extraction={selectedExtraction}
  chunks={selectedChunks}
  activeTab={activeDetailTab}
  onTabChange={setActiveDetailTab}
  onPreview={(detail) => {
    const file = knowledgeDetailToPreviewFile(detail);
    if (file) setPreviewFile(file);
  }}
  onReprocess={(sourceId) => reprocess.mutate(sourceId)}
  onToggleDisabled={(source) => updateSource.mutate({ sourceId: source.id, status: source.status === 'disabled' ? 'ready' : 'disabled' })}
  onDelete={(sourceId) => {
    if (window.confirm('删除知识记录？原始文件默认保留。')) deleteSource.mutate(sourceId);
  }}
/>
```

Render dialog:

```tsx
<ProjectFilePreviewDialog
  file={previewFile}
  projectId={previewFile?.project_id}
  onOpenChange={(open) => !open && setPreviewFile(null)}
/>
```

- [x] **Step 4: Replace fake footer buttons**

In `KnowledgeDetailsPanel`, replace static buttons with disabled-aware handlers:

```tsx
<button
  type="button"
  className="knowledge-detail-primary"
  disabled={!detail?.capabilities?.preview}
  onClick={() => detail && onPreview?.(detail)}
>
  <Eye className="h-2.5 w-2.5" strokeWidth={2.2} />
  预览
</button>
<a
  className="knowledge-detail-secondary"
  href={detail?.original_file?.url || undefined}
  download={detail?.original_file?.name}
  aria-disabled={!detail?.capabilities?.download}
>
  <Download className="h-2.5 w-2.5" strokeWidth={2.2} />
  下载
</a>
<button type="button" className="knowledge-detail-secondary" onClick={() => source && onToggleDisabled?.(source)}>
  <Link2 className="h-2.5 w-2.5" strokeWidth={2.2} />
  {source?.status === 'disabled' ? '恢复检索' : '禁用检索'}
</button>
<button type="button" className="knowledge-detail-secondary" onClick={() => source && onReprocess?.(source.id)}>
  <RefreshCcw className="h-2.5 w-2.5" strokeWidth={2.2} />
  重新处理
</button>
<button type="button" className="knowledge-detail-more is-danger" onClick={() => source && onDelete?.(source.id)}>
  <Trash2 className="h-2.5 w-2.5" strokeWidth={2.2} />
</button>
```

- [x] **Step 5: Add copy reference behavior**

For `引用到会话`, implement a Phase 1 fallback that copies a stable reference key:

```ts
async function copyKnowledgeReference(source: KnowledgeSource): Promise<void> {
  await navigator.clipboard.writeText(`knowledge:${source.id}`);
  toast.success('已复制知识引用 ID', { description: `knowledge:${source.id}` });
}
```

Phase 1 的 `引用到会话` 按钮固定调用该函数并复制 `knowledge:<sourceId>`；会话选择器不在本轮范围内。

- [x] **Step 6: Run frontend build**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected:

```text
Frontend build passes, no JSX or type errors.
```

- [x] **Step 7: Commit action integration**

```bash
git add packages/frontend/src/pages/KnowledgePage.tsx packages/frontend/src/index.css
git commit -m "feat(frontend): 接入知识库资源操作"
```

## Task 8: Responsive Knowledge UI and No-Fake-Data Regression

**Files:**
- Modify: `packages/frontend/src/pages/KnowledgePage.tsx`
- Modify: `packages/frontend/src/index.css`

- [x] **Step 1: Add mobile list mode guard**

In `KnowledgeResourceTable`, render a card list for small screens:

```tsx
<div className="knowledge-mobile-list">
  {rows.map((row) => (
    <button key={row.id} type="button" className="knowledge-mobile-card" onClick={() => onSelect(row)}>
      <span className="knowledge-mobile-card__title">{row.title}</span>
      <span className="knowledge-mobile-card__meta">{row.typeLabel} · {row.statusLabel} · {row.sizeLabel}</span>
      <span className="knowledge-mobile-card__meta">{row.tags.slice(0, 3).join(' · ') || '暂无标签'}</span>
    </button>
  ))}
</div>
```

Keep the table wrapper for desktop.

- [x] **Step 2: Add CSS for mobile cards and hidden desktop table**

Modify `packages/frontend/src/index.css`:

```css
.knowledge-mobile-list {
  display: none;
}

.knowledge-mobile-card {
  display: flex;
  min-width: 0;
  width: 100%;
  flex-direction: column;
  gap: 4px;
  border-bottom: 1px solid #f1f5f9;
  background: #ffffff;
  padding: 12px;
  text-align: left;
}

.knowledge-mobile-card__title {
  overflow-wrap: anywhere;
  color: #0f172a;
  font-size: 13px;
  font-weight: 700;
  line-height: 1.35;
}

.knowledge-mobile-card__meta {
  overflow-wrap: anywhere;
  color: #64748b;
  font-size: 11px;
  line-height: 1.4;
}

@media (max-width: 768px) {
  .knowledge-table-wrap table {
    display: none;
  }

  .knowledge-mobile-list {
    display: block;
  }

  .knowledge-action-group {
    flex-wrap: wrap;
  }

  .knowledge-filter-button,
  .knowledge-sort-button {
    max-width: 100%;
  }
}
```

- [x] **Step 3: Remove unimplemented pagination pages**

Change `KnowledgePagination` to show only total and page-size text. Phase 1 uses the existing limit-based list contract and does not render numbered pages:

```tsx
function KnowledgePagination({ total }: { total: number }): JSX.Element {
  return (
    <div className="knowledge-pagination">
      <div className="text-[11px] font-medium text-slate-400">共 {formatCount(total)} 项</div>
      <div className="text-[11px] text-slate-400">当前最多显示 500 项</div>
    </div>
  );
}
```

- [x] **Step 4: Search for fake-data literals**

Run:

```bash
rg -n "1248|1028|128\\.6|500 GB|45 页|我的收藏|回收站|最近使用|Ocean Platform|PRD|\\+3|125" packages/frontend/src/pages/KnowledgePage.tsx packages/frontend/src/index.css
```

Expected:

```text
No matches in KnowledgePage.tsx or knowledge CSS blocks.
```

- [x] **Step 5: Commit responsive cleanup**

```bash
git add packages/frontend/src/pages/KnowledgePage.tsx packages/frontend/src/index.css
git commit -m "fix(frontend): 清理知识库占位数据与移动端表格"
```

## Task 9: Final Verification, Screenshots, Review, and Commit

**Files:**
- Read/Verify: all changed files
- Create: `docs/superpowers/verification/2026-06-08-知识库Phase1功能补齐验收.md`
- Screenshot output: `output/playwright/knowledge-phase1-desktop-1280x1024.png`
- Screenshot output: `output/playwright/knowledge-phase1-mobile-390x844.png`

- [x] **Step 1: Run backend targeted tests**

Run:

```bash
npm test -w @openclaw-room/backend -- src/knowledge.routes.test.ts src/session-file-reference-context.test.ts src/repos/knowledge.test.ts src/knowledge-ingestion.test.ts src/files.routes.test.ts src/resource-assets.routes.test.ts
```

Expected:

```text
All listed backend tests pass.
```

- [x] **Step 2: Run frontend build**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected:

```text
Frontend build passes.
```

- [x] **Step 3: Run full build**

Run:

```bash
npm run build
```

Expected:

```text
Backend tsc and frontend build both pass.
```

- [x] **Step 4: Start or reuse dev server**

If `http://localhost:5173/knowledge` is already available, reuse it. Otherwise run:

```bash
npm run dev
```

Expected:

```text
Frontend available at http://localhost:5173 and backend proxy serves /api/knowledge.
```

- [x] **Step 5: Capture desktop screenshot**

Run:

```bash
playwright-cli open http://localhost:5173/knowledge
playwright-cli resize 1280 1024
playwright-cli screenshot --filename=output/playwright/knowledge-phase1-desktop-1280x1024.png
```

Expected:

```text
Screenshot shows real counts, no fixed fake pagination, and usable details panel.
```

- [x] **Step 6: Capture mobile screenshot**

Run:

```bash
playwright-cli resize 390 844
playwright-cli screenshot --filename=output/playwright/knowledge-phase1-mobile-390x844.png
```

Expected:

```text
Screenshot shows single-column list cards, no horizontal wide table as primary mobile view.
```

- [x] **Step 7: Write verification note**

Create `docs/superpowers/verification/2026-06-08-知识库Phase1功能补齐验收.md`:

```md
# 知识库 Phase 1 功能补齐验收

- 日期：2026-06-08
- 范围：`/knowledge` Phase 1 MVP

## 命令验证

- `npm test -w @openclaw-room/backend -- src/knowledge.routes.test.ts src/session-file-reference-context.test.ts src/repos/knowledge.test.ts src/knowledge-ingestion.test.ts src/files.routes.test.ts src/resource-assets.routes.test.ts`
  - 结果：通过
- `npm run build -w @openclaw-room/frontend`
  - 结果：通过
- `npm run build`
  - 结果：通过

## 浏览器验证

- 桌面截图：`output/playwright/knowledge-phase1-desktop-1280x1024.png`
- 移动截图：`output/playwright/knowledge-phase1-mobile-390x844.png`

## 验收结论

1. 文本/Markdown 上传后进入知识库并可查看 extraction、summary 和 chunks。
2. 图片资源不伪造 chunk 和页数。
3. 全文搜索返回 chunk citation。
4. 重处理、禁用、恢复和删除可用。
5. 会话引用优先使用知识库内容。
6. 页面不再显示 Stitch 示例固定计数。
```

- [ ] **Step 8: Run code review**

Use `superpowers:requesting-code-review` with:

```text
Review the Knowledge Phase 1 implementation against docs/superpowers/specs/2026-06-08-知识库Phase1功能补齐设计.md and docs/superpowers/plans/2026-06-08-知识库Phase1功能补齐实施计划.md. Focus on API semantics, deletion/disable behavior, source/chunk citation correctness, no fake UI data, mobile layout, and tests.
```

Expected:

```text
Reviewer findings are either addressed or explicitly documented as follow-up if outside Phase 1.
```

- [ ] **Step 9: Commit verification note and final fixes**

```bash
git add docs/superpowers/verification/2026-06-08-知识库Phase1功能补齐验收.md output/playwright/knowledge-phase1-desktop-1280x1024.png output/playwright/knowledge-phase1-mobile-390x844.png
git commit -m "docs: 添加知识库Phase1验收记录"
```

- [ ] **Step 10: Final status**

Run:

```bash
git status --short
```

Expected:

```text
Only unrelated pre-existing changes remain, or the tree is clean for files touched by this plan.
```

## 实际执行记录

- 本次执行没有按计划拆分为多个中间 commit，改为在 Phase 1 完成后统一提交，避免和当前工作区已有 staged 变更混淆。
- Task 9 Step 3 已执行 `npm run build`，但根目录全量构建未通过；失败来自既有 `src/online-skills/cache.test.ts` 和 `src/online-skills/client.test.ts` 引用缺失模块及隐式 `any`，不是本次知识库 Phase 1 改动触发。详细证据见 `docs/superpowers/verification/2026-06-08-知识库Phase1功能补齐验收.md`。
- 本次通过的 Phase 1 验证包括后端知识库相关定向测试、前端 API 单测、前端构建、假数据扫描、桌面/移动端截图和 DOM 响应式检查。
