# 主题兼容修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让当前会话页与系统设置页兼容 Apple 亮色、Apple 暗色、Anthropic 亮色、Anthropic 暗色四种主题。

**Architecture:** 保持现有 `data-theme` 与 CSS token 架构，不重写页面结构。先补齐全局语义变量，再让会话页的 `deepsea-*` token 响应 Apple 暗色，最后用设置页局部 token 覆盖硬编码浅色。

**Tech Stack:** React 18、TypeScript、Vite、Tailwind、CSS custom properties。

---

### Task 1: 补齐全局主题变量

**Files:**
- Modify: `packages/frontend/src/index.css`

- [x] **Step 1: 在四套主题中补齐缺失变量**

增加 `--color-bg-soft`、`--color-panel`、`--color-fg-subtle`、`--color-surface-muted`、`--color-surface-1`、`--color-surface-2`、`--background`、`--foreground`、`--secondary`。这些变量供图片生成、设置表单、PromptArea 等组件使用。

- [x] **Step 2: 运行构建验证**

Run: `npm run build`

Expected: TypeScript 与 Vite 构建退出码为 0。

### Task 2: 修复当前会话页 Apple 暗色

**Files:**
- Modify: `packages/frontend/src/index.css`
- Modify: `packages/frontend/src/session-ui/session-os.css`

- [x] **Step 1: 给 `apple-dark` 补齐 `--deepsea-*` token**

让 `deepsea-shell`、状态栏、消息卡片、上下文菜单等会话页组件在 Apple 暗色中使用暗色 surface、border、text。

- [x] **Step 2: 增加 Apple 暗色选择器覆盖**

为 `.deepsea-shell`、`.session-shell--empty`、`.deepsea-topbar`、`.deepsea-history`、`.deepsea-inspector`、`.deepsea-composer`、`.deepsea-bottom-status` 等加 `[data-theme="apple-dark"]` 覆盖，消除浅色渐变和白色玻璃残留。

- [x] **Step 3: 运行构建验证**

Run: `npm run build`

Expected: 构建退出码为 0。

### Task 3: 修复系统设置页主题兼容

**Files:**
- Modify: `packages/frontend/src/pages/SystemSettingsPage.css`

- [x] **Step 1: 在设置页定义局部语义 token**

把设置页 shell、sidebar、card、input、footer、active state 映射到全局 `--color-*` 变量。

- [x] **Step 2: 在 CSS 文件末尾追加覆盖规则**

覆盖原有固定浅色样式，不改 `SystemSettingsPage.tsx` 中已有业务逻辑和未提交改动。

- [x] **Step 3: 运行构建验证**

Run: `npm run build`

Expected: 构建退出码为 0。

### Task 4: 视觉烟测与提交

**Files:**
- Verify only: no planned source changes.

- [x] **Step 1: 启动前端或全量 dev 服务**

Run: `npm run dev` 或仓库已有可用 dev 服务。

- [x] **Step 2: 用浏览器切换四个主题检查**

检查 `/settings` 与当前会话页：背景、文字、卡片、输入框、导航、底部状态栏没有明显亮色泄漏。

- [x] **Step 3: 最终构建验证**

Run: `npm run build`

Expected: 构建退出码为 0。

- [x] **Step 4: 提交**

Run: `git commit -m "fix(frontend): 修复主题兼容"`

Expected: 生成包含本次主题兼容修复的提交。
