# ACP 事件时间线与 UI 集成 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在纯 ACP 聊天中完整展示 thinking、tool call、command output、file diff、plan/next_steps 和 raw fallback，并为 Claude Code、OpenCode、Codex 的 ACP server 接入保留统一事件边界。

**Architecture:** 先在现有 CLI JSON adapter 上建立 `AgentTimelineEvent` 标准化事件模型，后端把 provider 输出同时写入消息 metadata 与 WebSocket `event` 流。前端新增 `AgentTimeline` 组件渲染折叠时间线，现有 `thinking/tool/command` trace 保留兼容，后续再把 provider 层切到 `@agentclientprotocol/sdk`。

**Tech Stack:** TypeScript、Node.js、Express、WebSocket、SQLite metadata JSON、React 18、Vite、现有 `node --test` 测试。

---

## 文件结构

- Modify: `packages/backend/src/types.ts`：新增 timeline event 类型，扩展 `MessageTrace` 和 `WsServerEvent`。
- Modify: `packages/backend/src/acp/types.ts`：扩展 `AcpStreamChannel` / `AcpStreamTrace`，允许 adapter 传出 timeline event。
- Create: `packages/backend/src/acp/timeline.ts`：集中生成、归一化、合并 ACP timeline event。
- Create: `packages/backend/src/acp/timeline.test.ts`：覆盖 thinking、tool、command、diff、plan、raw fallback。
- Modify: `packages/backend/src/acp/claudecode.ts`：从 Claude/Codex/OpenCode JSON 中提取更多事件并保留 raw。
- Modify: `packages/backend/src/repos/messages.ts`：合并 `trace.events`，兼容旧 `thinking/tool_calls/commands`。
- Modify: `packages/backend/src/dispatcher.ts`：广播 `channel: 'event'` 的结构化 timeline event。
- Modify: `packages/backend/src/dispatcher.test.ts`：补充 message stream event 断言。
- Modify: `packages/frontend/src/lib/types.ts`：同步 timeline event 类型。
- Modify: `packages/frontend/src/lib/ws.ts`：扩展 `message:stream` 类型。
- Modify: `packages/frontend/src/lib/messageMetadata.ts`：解析并校验 `trace.events`。
- Modify: `packages/frontend/src/lib/messageMetadata.test.ts`：覆盖合法/非法 timeline events。
- Create: `packages/frontend/src/components/AgentTimeline.tsx`：新增房间消息时间线 UI。
- Create: `packages/frontend/src/components/AgentTimeline.test.tsx`：覆盖默认折叠、diff、plan、command、raw。
- Modify: `packages/frontend/src/components/MessageContent.tsx`：用 `AgentTimeline` 替换现有 trace panel 或在内部迁移。
- Modify: `packages/frontend/src/components/MessageContent.test.tsx`：更新 trace 渲染断言。
- Modify: `packages/frontend/src/pages/RoomPage.tsx`：合并 `event` 流到对应消息 metadata。
- Modify: `packages/frontend/src/index.css`：新增 timeline、diff、command、plan、raw 样式。
- Modify: `docs/superpowers/plans/2026-05-26-ACP事件时间线与UI集成实施计划.md`：执行时勾选步骤。

## Task 1: 后端 Timeline 类型与 Normalizer

**Files:**
- Modify: `packages/backend/src/types.ts`
- Modify: `packages/backend/src/acp/types.ts`
- Create: `packages/backend/src/acp/timeline.ts`
- Create: `packages/backend/src/acp/timeline.test.ts`

- [ ] **Step 1: 写失败测试**

在 `packages/backend/src/acp/timeline.test.ts` 新增测试文件，覆盖基础 normalizer：

```ts
import test from 'node:test';
import assert from 'node:assert/strict';
import {
  createTimelineEvent,
  normalizeTimelineEventFromTrace,
  normalizeRawTimelineEvent,
} from './timeline.js';

test('normalizeTimelineEventFromTrace maps thinking trace to timeline event', () => {
  const event = normalizeTimelineEventFromTrace({
    messageId: 'msg-1',
    runId: 'run-1',
    agentId: 'planner',
    seq: 1,
    channel: 'thinking',
    text: '先读取 dispatcher.ts',
    trace: { kind: 'thinking', text: '先读取 dispatcher.ts' },
  });

  assert.equal(event.type, 'thinking');
  assert.equal(event.status, 'delta');
  assert.equal(event.title, '思考过程');
  assert.equal(event.payload.text, '先读取 dispatcher.ts');
  assert.equal(event.message_id, 'msg-1');
  assert.equal(event.run_id, 'run-1');
  assert.equal(event.agent_id, 'planner');
});

test('normalizeTimelineEventFromTrace maps tool trace to tool_call event', () => {
  const event = normalizeTimelineEventFromTrace({
    messageId: 'msg-1',
    runId: 'run-1',
    agentId: 'executor',
    seq: 2,
    channel: 'tool',
    text: 'Read {"file":"src/app.ts"}',
    trace: { kind: 'tool', name: 'Read', input: '{"file":"src/app.ts"}' },
  });

  assert.equal(event.type, 'tool_call');
  assert.equal(event.status, 'completed');
  assert.equal(event.title, '调用工具 Read');
  assert.equal(event.payload.name, 'Read');
  assert.equal(event.payload.input, '{"file":"src/app.ts"}');
});

test('normalizeRawTimelineEvent keeps unknown provider events as raw', () => {
  const event = normalizeRawTimelineEvent({
    messageId: 'msg-1',
    runId: 'run-1',
    agentId: 'executor',
    seq: 3,
    provider: 'codex',
    rawType: 'unknown.event',
    raw: { type: 'unknown.event', value: 1 },
  });

  assert.equal(event.type, 'raw');
  assert.equal(event.status, 'completed');
  assert.equal(event.title, '原始事件 unknown.event');
  assert.deepEqual(event.raw, { type: 'unknown.event', value: 1 });
});

test('createTimelineEvent creates stable payload and raw fields', () => {
  const event = createTimelineEvent({
    messageId: 'msg-1',
    runId: 'run-1',
    agentId: 'executor',
    seq: 4,
    type: 'file_diff',
    status: 'completed',
    title: '修改文件 src/app.ts',
    payload: {
      path: 'src/app.ts',
      patch: '-old\n+new',
      additions: 1,
      deletions: 1,
    },
    raw: { type: 'patch' },
  });

  assert.equal(event.id, 'run-1:4');
  assert.equal(event.type, 'file_diff');
  assert.equal(event.payload.path, 'src/app.ts');
  assert.equal(event.raw?.type, 'patch');
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/acp/timeline.test.ts
```

Expected: FAIL，提示 `Cannot find module './timeline.js'` 或缺少导出。

- [ ] **Step 3: 新增后端类型**

在 `packages/backend/src/types.ts` 的 `MessageTraceCommand` 后加入：

```ts
export type AgentTimelineEventType =
  | 'thinking'
  | 'assistant_message'
  | 'tool_call'
  | 'tool_result'
  | 'command'
  | 'command_output'
  | 'file_diff'
  | 'plan_update'
  | 'web_search'
  | 'permission_request'
  | 'error'
  | 'raw';

export type AgentTimelineEventStatus = 'started' | 'delta' | 'completed' | 'failed';

export type AgentTimelinePayload = Record<string, unknown>;

export interface AgentTimelineEvent {
  id: string;
  message_id: string;
  run_id: string;
  agent_id: string;
  seq: number;
  type: AgentTimelineEventType;
  status: AgentTimelineEventStatus;
  title: string;
  payload: AgentTimelinePayload;
  raw?: Record<string, unknown>;
  created_at: number;
}
```

把 `MessageTrace` 改为：

```ts
export interface MessageTrace {
  thinking?: MessageTraceThinking[];
  tool_calls?: MessageTraceToolCall[];
  commands?: MessageTraceCommand[];
  events?: AgentTimelineEvent[];
}
```

把 `WsServerEvent` 中 `message:stream` 的 `channel` union 扩展为：

```ts
channel?: 'answer' | 'thinking' | 'tool' | 'command' | 'event';
event?: AgentTimelineEvent;
```

- [ ] **Step 4: 扩展 ACP stream 类型**

在 `packages/backend/src/acp/types.ts` 中导入 `AgentTimelineEvent`：

```ts
import type { AcpBackend, AcpPermissionMode, AgentTimelineEvent, CliSessionSummary } from '../types.js';
```

把 channel 扩展为：

```ts
export type AcpStreamChannel = 'answer' | 'activity' | 'thinking' | 'tool' | 'command' | 'event';
```

在 `AcpStreamChunk` 中加入：

```ts
  event?: AgentTimelineEvent;
  rawEvent?: Record<string, unknown>;
```

- [ ] **Step 5: 实现 `timeline.ts`**

创建 `packages/backend/src/acp/timeline.ts`：

```ts
import type {
  AgentTimelineEvent,
  AgentTimelineEventStatus,
  AgentTimelineEventType,
} from '../types.js';
import type { AcpStreamTrace } from './types.js';

export function createTimelineEvent(args: {
  messageId: string;
  runId: string;
  agentId: string;
  seq: number;
  type: AgentTimelineEventType;
  status: AgentTimelineEventStatus;
  title: string;
  payload?: Record<string, unknown>;
  raw?: Record<string, unknown>;
}): AgentTimelineEvent {
  return {
    id: `${args.runId}:${args.seq}`,
    message_id: args.messageId,
    run_id: args.runId,
    agent_id: args.agentId,
    seq: args.seq,
    type: args.type,
    status: args.status,
    title: args.title,
    payload: args.payload ?? {},
    ...(args.raw ? { raw: args.raw } : {}),
    created_at: Date.now(),
  };
}

export function normalizeTimelineEventFromTrace(args: {
  messageId: string;
  runId: string;
  agentId: string;
  seq: number;
  channel: 'thinking' | 'tool' | 'command';
  text: string;
  trace?: AcpStreamTrace;
}): AgentTimelineEvent {
  if (args.trace?.kind === 'thinking') {
    return createTimelineEvent({
      messageId: args.messageId,
      runId: args.runId,
      agentId: args.agentId,
      seq: args.seq,
      type: 'thinking',
      status: 'delta',
      title: '思考过程',
      payload: { text: args.trace.text, encrypted: args.trace.encrypted === true },
    });
  }

  if (args.trace?.kind === 'tool') {
    const isResult = Boolean(args.trace.output) || args.trace.name === 'tool_result';
    return createTimelineEvent({
      messageId: args.messageId,
      runId: args.runId,
      agentId: args.agentId,
      seq: args.seq,
      type: isResult ? 'tool_result' : 'tool_call',
      status: 'completed',
      title: isResult ? `工具结果 ${args.trace.name}` : `调用工具 ${args.trace.name}`,
      payload: {
        name: args.trace.name,
        input: args.trace.input,
        output: args.trace.output,
      },
    });
  }

  if (args.trace?.kind === 'command') {
    return createTimelineEvent({
      messageId: args.messageId,
      runId: args.runId,
      agentId: args.agentId,
      seq: args.seq,
      type: 'command',
      status: 'completed',
      title: `执行命令 ${args.trace.command}`,
      payload: {
        command: args.trace.command,
        output: args.trace.output,
      },
    });
  }

  return createTimelineEvent({
    messageId: args.messageId,
    runId: args.runId,
    agentId: args.agentId,
    seq: args.seq,
    type: args.channel === 'thinking' ? 'thinking' : args.channel === 'tool' ? 'tool_call' : 'command',
    status: args.channel === 'thinking' ? 'delta' : 'completed',
    title: args.channel === 'thinking' ? '思考过程' : args.channel === 'tool' ? '工具调用' : '命令执行',
    payload: { text: args.text },
  });
}

export function normalizeRawTimelineEvent(args: {
  messageId: string;
  runId: string;
  agentId: string;
  seq: number;
  provider: string;
  rawType?: string;
  raw: Record<string, unknown>;
}): AgentTimelineEvent {
  return createTimelineEvent({
    messageId: args.messageId,
    runId: args.runId,
    agentId: args.agentId,
    seq: args.seq,
    type: 'raw',
    status: 'completed',
    title: `原始事件 ${args.rawType ?? args.provider}`,
    payload: {
      provider: args.provider,
      raw_type: args.rawType,
    },
    raw: args.raw,
  });
}
```

- [ ] **Step 6: 运行后端测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/acp/timeline.test.ts
```

Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add packages/backend/src/types.ts packages/backend/src/acp/types.ts packages/backend/src/acp/timeline.ts packages/backend/src/acp/timeline.test.ts
git commit -m "feat(acp): 增加时间线事件模型"
```

## Task 2: 后端消息持久化与 WebSocket 事件流

**Files:**
- Modify: `packages/backend/src/repos/messages.ts`
- Modify: `packages/backend/src/dispatcher.ts`
- Modify: `packages/backend/src/dispatcher.test.ts`

- [ ] **Step 1: 写失败测试**

在 `packages/backend/src/dispatcher.test.ts` 中新增或扩展 ACP streaming 测试，断言收到 `channel: 'event'`：

```ts
test('respondAsAgent broadcasts timeline event stream for trace chunks', async () => {
  const captured = captureRoomEvents('room-acp-events');
  mockAdapterInvoke(async ({ onChunk }) => {
    onChunk({
      stream: 'stdout',
      channel: 'thinking',
      text: '先分析代码',
      trace: { kind: 'thinking', text: '先分析代码' },
    });
    onChunk({
      stream: 'stdout',
      channel: 'tool',
      text: 'Read {"file":"src/app.ts"}',
      trace: { kind: 'tool', name: 'Read', input: '{"file":"src/app.ts"}' },
    });
    onChunk({ stream: 'stdout', channel: 'answer', text: '完成' });
    return { exitCode: 0, sessionId: 'session-1', stderr: '' };
  });

  await respondAsAgent({
    roomId: 'room-acp-events',
    agent: createRoomAgent({ agent_id: 'executor' }),
    prompt: '检查代码',
  });

  const eventStreams = captured.events.filter(
    (event) => event.type === 'message:stream' && event.channel === 'event',
  );
  assert.equal(eventStreams.length, 2);
  assert.equal(eventStreams[0]?.event?.type, 'thinking');
  assert.equal(eventStreams[1]?.event?.type, 'tool_call');
  assert.equal(eventStreams[1]?.event?.payload.name, 'Read');

  captured.cleanup();
});
```

如果当前测试 helper 名称不同，复用文件内已有 `captureRoomEvents`、adapter mock、agent factory，不新增全局测试基础设施。

- [ ] **Step 2: 运行目标测试确认失败**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/dispatcher.test.ts
```

Expected: FAIL，原因是没有 `channel: 'event'` 或 `event` 字段。

- [ ] **Step 3: 合并 `trace.events`**

在 `packages/backend/src/repos/messages.ts` 的 `mergeMessageTrace` 返回对象中加入：

```ts
    ...(patch.events ? {
      events: mergeTraceEvents(current.events, patch.events),
    } : {}),
```

在文件内新增 helper：

```ts
function mergeTraceEvents(
  current: MessageTrace['events'] | undefined,
  patch: NonNullable<MessageTrace['events']>,
): NonNullable<MessageTrace['events']> {
  const byId = new Map<string, NonNullable<MessageTrace['events']>[number]>();
  for (const event of current ?? []) byId.set(event.id, event);
  for (const event of patch) {
    const previous = byId.get(event.id);
    if (!previous) {
      byId.set(event.id, event);
      continue;
    }
    byId.set(event.id, {
      ...previous,
      ...event,
      payload: mergeTimelinePayload(previous.payload, event.payload),
      raw: event.raw ?? previous.raw,
    });
  }
  return [...byId.values()].sort((a, b) => a.seq - b.seq);
}

function mergeTimelinePayload(
  previous: Record<string, unknown>,
  next: Record<string, unknown>,
): Record<string, unknown> {
  const merged = { ...previous, ...next };
  if (typeof previous.text === 'string' && typeof next.text === 'string') {
    merged.text = previous.text + next.text;
  }
  if (typeof previous.output === 'string' && typeof next.output === 'string') {
    merged.output = previous.output + next.output;
  }
  if (typeof previous.stdout === 'string' && typeof next.stdout === 'string') {
    merged.stdout = previous.stdout + next.stdout;
  }
  if (typeof previous.stderr === 'string' && typeof next.stderr === 'string') {
    merged.stderr = previous.stderr + next.stderr;
  }
  return merged;
}
```

- [ ] **Step 4: 广播结构化 event**

在 `packages/backend/src/dispatcher.ts` 导入：

```ts
import { normalizeTimelineEventFromTrace } from './acp/timeline.js';
```

把 `onTrace` 改成同时写旧 trace 和新 event：

```ts
  const onTrace = (channel: 'thinking' | 'tool' | 'command', chunk: string, trace?: AcpStreamTrace): void => {
    const text = chunk.trim();
    if (!text) return;
    const event = normalizeTimelineEventFromTrace({
      messageId: placeholder.id,
      runId: run.id,
      agentId: agent.agent_id,
      seq: streamSeq + 1,
      channel,
      text,
      trace,
    });
    const updatedMessage = messageRepo.mergeTrace(placeholder.id, {
      ...toTracePatch(channel, text, trace),
      events: [event],
    });
    if (!args.internalMessage) {
      wsHub.broadcast(roomId, {
        type: 'message:stream',
        roomId,
        messageId: placeholder.id,
        runId: run.id,
        channel: 'event',
        chunk: '',
        event,
        done: false,
        seq: ++streamSeq,
        status: 'streaming',
        message: updatedMessage,
      });
    }
  };
```

- [ ] **Step 5: 保持 answer 流不变**

确认 `onStdout` 仍然只广播 `channel: 'answer'` 或不带 channel 的正文流，不要把最终 assistant 文本放进 timeline，避免正文重复显示。

- [ ] **Step 6: 运行后端测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/acp/timeline.test.ts src/dispatcher.test.ts
```

Expected: `timeline.test.ts` PASS；`dispatcher.test.ts` 若存在历史失败，记录与本任务无关的失败名称，并确认新增测试 PASS。

- [ ] **Step 7: Commit**

```bash
git add packages/backend/src/repos/messages.ts packages/backend/src/dispatcher.ts packages/backend/src/dispatcher.test.ts
git commit -m "feat(chat): 推送ACP时间线事件流"
```

## Task 3: Adapter 解析 diff、plan、raw fallback

**Files:**
- Modify: `packages/backend/src/acp/timeline.ts`
- Modify: `packages/backend/src/acp/timeline.test.ts`
- Modify: `packages/backend/src/acp/claudecode.ts`
- Modify: `packages/backend/src/acp/claudecode.test.ts`

- [ ] **Step 1: 写事件识别测试**

在 `packages/backend/src/acp/timeline.test.ts` 增加：

```ts
import { normalizeKnownProviderEvent } from './timeline.js';

test('normalizeKnownProviderEvent maps patch payload to file_diff', () => {
  const event = normalizeKnownProviderEvent({
    messageId: 'msg-1',
    runId: 'run-1',
    agentId: 'executor',
    seq: 5,
    provider: 'codex',
    raw: {
      type: 'patch',
      path: 'src/app.ts',
      patch: '-old\n+new',
      additions: 1,
      deletions: 1,
    },
  });

  assert.equal(event.type, 'file_diff');
  assert.equal(event.title, '修改文件 src/app.ts');
  assert.equal(event.payload.patch, '-old\n+new');
});

test('normalizeKnownProviderEvent maps plan_update payload to plan_update', () => {
  const event = normalizeKnownProviderEvent({
    messageId: 'msg-1',
    runId: 'run-1',
    agentId: 'planner',
    seq: 6,
    provider: 'codex',
    raw: {
      type: 'plan_update',
      entries: [
        { title: '开发 UI', status: 'in_progress' },
        { title: '运行测试', status: 'pending' },
      ],
    },
  });

  assert.equal(event.type, 'plan_update');
  assert.equal(event.title, '计划更新');
  assert.deepEqual(event.payload.entries, [
    { title: '开发 UI', status: 'in_progress' },
    { title: '运行测试', status: 'pending' },
  ]);
});
```

- [ ] **Step 2: 实现 `normalizeKnownProviderEvent`**

在 `packages/backend/src/acp/timeline.ts` 增加：

```ts
export function normalizeKnownProviderEvent(args: {
  messageId: string;
  runId: string;
  agentId: string;
  seq: number;
  provider: string;
  raw: Record<string, unknown>;
}): AgentTimelineEvent {
  const rawType = typeof args.raw.type === 'string' ? args.raw.type : undefined;
  const payload = getProviderPayload(args.raw);

  const diff = extractDiffPayload(args.raw, payload);
  if (diff) {
    return createTimelineEvent({
      messageId: args.messageId,
      runId: args.runId,
      agentId: args.agentId,
      seq: args.seq,
      type: 'file_diff',
      status: 'completed',
      title: `修改文件 ${diff.path}`,
      payload: diff,
      raw: args.raw,
    });
  }

  const planEntries = extractPlanEntries(args.raw, payload);
  if (planEntries) {
    return createTimelineEvent({
      messageId: args.messageId,
      runId: args.runId,
      agentId: args.agentId,
      seq: args.seq,
      type: 'plan_update',
      status: 'completed',
      title: '计划更新',
      payload: { entries: planEntries },
      raw: args.raw,
    });
  }

  return normalizeRawTimelineEvent({
    messageId: args.messageId,
    runId: args.runId,
    agentId: args.agentId,
    seq: args.seq,
    provider: args.provider,
    rawType,
    raw: args.raw,
  });
}

function getProviderPayload(raw: Record<string, unknown>): Record<string, unknown> {
  const payload = raw.payload;
  return payload && typeof payload === 'object' && !Array.isArray(payload)
    ? payload as Record<string, unknown>
    : raw;
}

function extractDiffPayload(
  raw: Record<string, unknown>,
  payload: Record<string, unknown>,
): Record<string, unknown> | null {
  const type = typeof raw.type === 'string' ? raw.type : '';
  const path = firstString(payload.path, payload.file, payload.file_path, payload.filePath);
  const patch = firstString(payload.patch, payload.diff, payload.unified_diff, payload.unifiedDiff);
  if (!path || !patch) return null;
  if (!/patch|diff|edit|apply_patch|file/i.test(type) && !patch.includes('\n+') && !patch.includes('\n-')) return null;
  return {
    path,
    patch,
    additions: typeof payload.additions === 'number' ? payload.additions : countPatchLines(patch, '+'),
    deletions: typeof payload.deletions === 'number' ? payload.deletions : countPatchLines(patch, '-'),
  };
}

function extractPlanEntries(
  raw: Record<string, unknown>,
  payload: Record<string, unknown>,
): unknown[] | null {
  const type = typeof raw.type === 'string' ? raw.type : '';
  const entries = Array.isArray(payload.entries)
    ? payload.entries
    : Array.isArray(payload.plan)
      ? payload.plan
      : Array.isArray(payload.next_steps)
        ? payload.next_steps
        : null;
  if (!entries) return null;
  return /plan|next_steps/i.test(type) ? entries : null;
}

function firstString(...values: unknown[]): string | null {
  for (const value of values) {
    if (typeof value === 'string' && value.trim()) return value.trim();
  }
  return null;
}

function countPatchLines(patch: string, prefix: '+' | '-'): number {
  return patch
    .split('\n')
    .filter((line) => line.startsWith(prefix) && !line.startsWith(`${prefix}${prefix}${prefix}`))
    .length;
}
```

- [ ] **Step 3: 让 parser 传出 raw event**

在 `packages/backend/src/acp/claudecode.ts` 中扩展 `NormalizedStdoutChunk`，允许 `rawEvent?: Record<string, unknown>`。在 `normalizeStdoutChunkWithSnapshots` JSON parse 成功后，对当前未产生 `answer/activity/trace` 的 JSON 行，返回：

```ts
return [{
  channel: 'event' as const,
  text: '',
  rawType: typeof obj['type'] === 'string' ? obj['type'] : undefined,
  rawEvent: obj,
}];
```

- [ ] **Step 4: 在 dispatcher 中归一化 raw event**

在 `dispatcher.ts` 的 `onChunk` 中增加：

```ts
        else if (chunk.stream === 'stdout' && chunk.channel === 'event' && chunk.rawEvent) {
          onTimelineRawEvent(chunk.rawEvent, chunk.rawType);
        }
```

新增 `onTimelineRawEvent`，用 `normalizeKnownProviderEvent` 生成 event，写入 `trace.events` 并广播 `channel: 'event'`。如果 raw event 已经被 trace 解析为 thinking/tool/command，不要重复发送 raw；判断方式是 parser 不应对已解析事件返回 raw fallback。

- [ ] **Step 5: 扩展 claudecode parser 测试**

在 `packages/backend/src/acp/claudecode.test.ts` 增加测试：

```ts
test('normalizeStdoutChunk preserves unknown JSON event as raw event channel', () => {
  const chunks = normalizeStdoutChunk('{"type":"vendor.custom","value":1}\n');
  assert.equal(chunks.length, 1);
  assert.equal(chunks[0]?.channel, 'event');
  assert.deepEqual(chunks[0]?.rawEvent, { type: 'vendor.custom', value: 1 });
});
```

并增加 patch/plan JSON 事件能进入 `channel: 'event'` 的测试。

- [ ] **Step 6: 运行 adapter 测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/acp/timeline.test.ts src/acp/claudecode.test.ts
```

Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add packages/backend/src/acp/timeline.ts packages/backend/src/acp/timeline.test.ts packages/backend/src/acp/claudecode.ts packages/backend/src/acp/claudecode.test.ts packages/backend/src/dispatcher.ts packages/backend/src/acp/types.ts
git commit -m "feat(acp): 保留diff和plan原始事件"
```

## Task 4: 前端类型、metadata 解析与流式合并

**Files:**
- Modify: `packages/frontend/src/lib/types.ts`
- Modify: `packages/frontend/src/lib/ws.ts`
- Modify: `packages/frontend/src/lib/messageMetadata.ts`
- Modify: `packages/frontend/src/lib/messageMetadata.test.ts`
- Modify: `packages/frontend/src/pages/RoomPage.tsx`
- Modify: `packages/frontend/src/pages/RoomPage.test.ts`

- [ ] **Step 1: 写 metadata 解析失败测试**

在 `packages/frontend/src/lib/messageMetadata.test.ts` 增加：

```ts
test('parseMessageMetadata accepts timeline events in trace metadata', () => {
  const parsed = parseMessageMetadata(JSON.stringify({
    trace: {
      events: [
        {
          id: 'run-1:1',
          message_id: 'msg-1',
          run_id: 'run-1',
          agent_id: 'executor',
          seq: 1,
          type: 'file_diff',
          status: 'completed',
          title: '修改文件 src/app.ts',
          payload: { path: 'src/app.ts', patch: '-old\n+new' },
          raw: { type: 'patch' },
          created_at: 1,
        },
      ],
    },
  }));

  assert.equal(parsed.trace?.events?.length, 1);
  assert.equal(parsed.trace?.events?.[0]?.type, 'file_diff');
  assert.equal(parsed.trace?.events?.[0]?.payload.path, 'src/app.ts');
});

test('parseMessageMetadata ignores invalid timeline events', () => {
  const parsed = parseMessageMetadata(JSON.stringify({
    trace: {
      events: [
        { id: '', type: 'file_diff' },
        { id: 'x', type: 'unknown', status: 'completed', payload: {} },
      ],
    },
  }));

  assert.equal(parsed.trace, undefined);
});
```

- [ ] **Step 2: 同步前端类型**

在 `packages/frontend/src/lib/types.ts` 中添加与后端一致的：

```ts
export type AgentTimelineEventType = ...;
export type AgentTimelineEventStatus = 'started' | 'delta' | 'completed' | 'failed';
export type AgentTimelinePayload = Record<string, unknown>;
export interface AgentTimelineEvent { ... }
```

并扩展：

```ts
export interface MessageTrace {
  thinking?: MessageTraceThinking[];
  tool_calls?: MessageTraceToolCall[];
  commands?: MessageTraceCommand[];
  events?: AgentTimelineEvent[];
}
```

- [ ] **Step 3: 扩展 WebSocket 类型**

在 `packages/frontend/src/lib/ws.ts` 导入 `AgentTimelineEvent`，并把 `message:stream` 改为：

```ts
      channel?: 'answer' | 'thinking' | 'tool' | 'command' | 'event';
      event?: AgentTimelineEvent;
```

- [ ] **Step 4: 解析 timeline events**

在 `packages/frontend/src/lib/messageMetadata.ts` 添加 timeline event 校验。最小实现：

```ts
const timelineEventTypes = new Set<AgentTimelineEventType>([
  'thinking',
  'assistant_message',
  'tool_call',
  'tool_result',
  'command',
  'command_output',
  'file_diff',
  'plan_update',
  'web_search',
  'permission_request',
  'error',
  'raw',
]);

const timelineEventStatuses = new Set<AgentTimelineEventStatus>([
  'started',
  'delta',
  'completed',
  'failed',
]);

function readTimelineEvents(value: unknown): AgentTimelineEvent[] | undefined {
  if (!Array.isArray(value)) return undefined;
  const events = value.filter(isTimelineEvent);
  return events.length > 0 ? events : undefined;
}

function isTimelineEvent(value: unknown): value is AgentTimelineEvent {
  if (!value || typeof value !== 'object' || Array.isArray(value)) return false;
  const event = value as Record<string, unknown>;
  return typeof event.id === 'string' && event.id.length > 0
    && typeof event.message_id === 'string' && event.message_id.length > 0
    && typeof event.run_id === 'string' && event.run_id.length > 0
    && typeof event.agent_id === 'string' && event.agent_id.length > 0
    && typeof event.seq === 'number'
    && typeof event.type === 'string' && timelineEventTypes.has(event.type as AgentTimelineEventType)
    && typeof event.status === 'string' && timelineEventStatuses.has(event.status as AgentTimelineEventStatus)
    && typeof event.title === 'string' && event.title.length > 0
    && !!event.payload && typeof event.payload === 'object' && !Array.isArray(event.payload)
    && typeof event.created_at === 'number';
}
```

在现有 trace parser 里把 `events` 合进去。

- [ ] **Step 5: 合并 WebSocket event 流**

在 `packages/frontend/src/pages/RoomPage.tsx` 中扩展 `StreamTraceChannel`：

```ts
type StreamTraceChannel = 'thinking' | 'tool' | 'command' | 'event';
```

改造 `mergeMessageStreamTrace`：如果 `channel === 'event'` 且 server event 有 `event`，按 `event.id` 合并到 `metadata.trace.events`。新增 helper：

```ts
function appendTimelineEvent(trace: MessageTrace | undefined, event: AgentTimelineEvent): MessageTrace {
  const events = [...(trace?.events ?? [])];
  const index = events.findIndex((item) => item.id === event.id);
  if (index === -1) return { ...trace, events: [...events, event].sort((a, b) => a.seq - b.seq) };
  const previous = events[index];
  events[index] = {
    ...previous,
    ...event,
    payload: mergeTimelinePayload(previous.payload, event.payload),
    raw: event.raw ?? previous.raw,
  };
  return { ...trace, events: events.sort((a, b) => a.seq - b.seq) };
}
```

如果现有 listener 只传 `channel/chunk`，把调用签名扩展为 `mergeMessageStreamTrace(message, event)`，从完整 WebSocket event 中读取 `event.event`。

- [ ] **Step 6: 运行前端解析测试**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected: TypeScript PASS。若 frontend 没有 test script，只用 build 验证类型。

- [ ] **Step 7: Commit**

```bash
git add packages/frontend/src/lib/types.ts packages/frontend/src/lib/ws.ts packages/frontend/src/lib/messageMetadata.ts packages/frontend/src/lib/messageMetadata.test.ts packages/frontend/src/pages/RoomPage.tsx packages/frontend/src/pages/RoomPage.test.ts
git commit -m "feat(frontend): 接收ACP时间线事件"
```

## Task 5: 房间 Timeline UI 与样式

**Files:**
- Create: `packages/frontend/src/components/AgentTimeline.tsx`
- Create: `packages/frontend/src/components/AgentTimeline.test.tsx`
- Modify: `packages/frontend/src/components/MessageContent.tsx`
- Modify: `packages/frontend/src/components/MessageContent.test.tsx`
- Modify: `packages/frontend/src/index.css`

- [ ] **Step 1: 写 UI 渲染测试**

创建 `packages/frontend/src/components/AgentTimeline.test.tsx`：

```tsx
import test from 'node:test';
import assert from 'node:assert/strict';
import { renderToStaticMarkup } from 'react-dom/server';
import { AgentTimeline } from './AgentTimeline';
import type { MessageTrace } from '../lib/types';

test('AgentTimeline renders thinking collapsed by default and shows full text in details', () => {
  const trace: MessageTrace = {
    events: [
      {
        id: 'run-1:1',
        message_id: 'msg-1',
        run_id: 'run-1',
        agent_id: 'planner',
        seq: 1,
        type: 'thinking',
        status: 'delta',
        title: '思考过程',
        payload: { text: '完整 thinking 原文' },
        created_at: 1,
      },
    ],
  };

  const html = renderToStaticMarkup(<AgentTimeline trace={trace} />);
  assert.match(html, /<details class="agent-timeline-card is-thinking"/);
  assert.doesNotMatch(html, /<details class="agent-timeline-card is-thinking" open=""/);
  assert.match(html, /完整 thinking 原文/);
});

test('AgentTimeline renders file diff lines', () => {
  const trace: MessageTrace = {
    events: [
      {
        id: 'run-1:2',
        message_id: 'msg-1',
        run_id: 'run-1',
        agent_id: 'executor',
        seq: 2,
        type: 'file_diff',
        status: 'completed',
        title: '修改文件 src/app.ts',
        payload: { path: 'src/app.ts', patch: '-old\n+new', additions: 1, deletions: 1 },
        created_at: 1,
      },
    ],
  };

  const html = renderToStaticMarkup(<AgentTimeline trace={trace} />);
  assert.match(html, /src\/app\.ts/);
  assert.match(html, /diff-line is-removed/);
  assert.match(html, /diff-line is-added/);
});

test('AgentTimeline renders plan update with Chinese labels', () => {
  const trace: MessageTrace = {
    events: [
      {
        id: 'run-1:3',
        message_id: 'msg-1',
        run_id: 'run-1',
        agent_id: 'planner',
        seq: 3,
        type: 'plan_update',
        status: 'completed',
        title: '计划更新',
        payload: { entries: [{ title: '开发 UI', status: 'in_progress' }] },
        created_at: 1,
      },
    ],
  };

  const html = renderToStaticMarkup(<AgentTimeline trace={trace} />);
  assert.match(html, /计划更新/);
  assert.match(html, /开发 UI/);
  assert.match(html, /进行中/);
});
```

- [ ] **Step 2: 实现 `AgentTimeline.tsx`**

创建组件，最小实现必须包括：

```tsx
import type { AgentTimelineEvent, MessageTrace } from '../lib/types';

export function AgentTimeline({ trace }: { trace?: MessageTrace }): JSX.Element | null {
  const events = trace?.events?.length ? trace.events : legacyTraceToEvents(trace);
  if (!events.length) return null;
  return (
    <section className="agent-timeline" aria-label="ACP 执行过程">
      {events.map((event) => (
        <TimelineCard key={event.id} event={event} />
      ))}
    </section>
  );
}

function TimelineCard({ event }: { event: AgentTimelineEvent }): JSX.Element {
  const defaultOpen = event.type !== 'thinking' && event.type !== 'raw';
  return (
    <details className={`agent-timeline-card is-${event.type.replace('_', '-')}`} open={defaultOpen}>
      <summary className="agent-timeline-summary">
        <span className="agent-timeline-kind">{getEventLabel(event)}</span>
        <strong>{event.title}</strong>
        <span className="agent-timeline-status">{formatStatus(event.status)}</span>
      </summary>
      <div className="agent-timeline-body">
        <EventBody event={event} />
      </div>
    </details>
  );
}
```

`EventBody` 至少覆盖：

- `thinking`：显示 `payload.text`。
- `tool_call/tool_result`：显示工具名、输入、输出。
- `command/command_output`：显示 command、stdout/stderr/output。
- `file_diff`：调用 `DiffBlock` 渲染 patch 行。
- `plan_update`：显示 entries，字段状态中文化。
- `raw`：显示 JSON。

- [ ] **Step 3: 接入 MessageContent**

在 `packages/frontend/src/components/MessageContent.tsx` 中导入：

```ts
import { AgentTimeline } from './AgentTimeline';
```

把底部：

```tsx
<MessageTracePanels trace={trace} />
```

替换为：

```tsx
<AgentTimeline trace={trace} />
```

保留旧 `MessageTracePanels` 代码到本任务结束时可删除；如果删除会牵连大量测试，先保留但不再调用。

- [ ] **Step 4: 添加样式**

在 `packages/frontend/src/index.css` 追加样式：

```css
.agent-timeline {
  margin-top: 0.75rem;
  display: grid;
  gap: 0.5rem;
}

.agent-timeline-card {
  border: 1px solid hsl(var(--border) / 0.7);
  border-radius: 0.85rem;
  background: linear-gradient(180deg, hsl(var(--card) / 0.92), hsl(var(--muted) / 0.35));
  overflow: hidden;
}

.agent-timeline-summary {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.65rem 0.8rem;
  cursor: pointer;
  list-style: none;
}

.agent-timeline-summary::-webkit-details-marker {
  display: none;
}

.agent-timeline-kind {
  border-radius: 999px;
  padding: 0.15rem 0.45rem;
  font-size: 0.72rem;
  color: hsl(var(--muted-foreground));
  background: hsl(var(--muted) / 0.8);
}

.agent-timeline-summary strong {
  min-width: 0;
  flex: 1;
  font-size: 0.86rem;
}

.agent-timeline-status {
  font-size: 0.72rem;
  color: hsl(var(--muted-foreground));
}

.agent-timeline-body {
  border-top: 1px solid hsl(var(--border) / 0.55);
  padding: 0.75rem 0.8rem;
}

.agent-timeline-pre {
  max-height: 18rem;
  overflow: auto;
  white-space: pre-wrap;
  word-break: break-word;
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 0.78rem;
}

.agent-timeline-diff {
  overflow: auto;
  border-radius: 0.65rem;
  background: hsl(var(--background) / 0.82);
}

.diff-line {
  display: block;
  padding: 0 0.65rem;
  white-space: pre;
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 0.78rem;
}

.diff-line.is-added {
  color: #166534;
  background: rgba(34, 197, 94, 0.12);
}

.diff-line.is-removed {
  color: #991b1b;
  background: rgba(239, 68, 68, 0.12);
}

.agent-timeline-plan {
  display: grid;
  gap: 0.45rem;
}

.agent-timeline-plan li {
  border-radius: 0.6rem;
  padding: 0.55rem 0.65rem;
  background: hsl(var(--muted) / 0.45);
}
```

- [ ] **Step 5: 运行前端构建**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add packages/frontend/src/components/AgentTimeline.tsx packages/frontend/src/components/AgentTimeline.test.tsx packages/frontend/src/components/MessageContent.tsx packages/frontend/src/components/MessageContent.test.tsx packages/frontend/src/index.css
git commit -m "feat(ui): 展示ACP执行时间线"
```

## Task 6: ACP Agent Registry 配置入口

**Files:**
- Create: `packages/backend/src/acp/registry.ts`
- Create: `packages/backend/src/acp/registry.test.ts`
- Modify: `packages/backend/src/acp/index.ts` 或现有 adapter 入口文件

- [ ] **Step 1: 写 registry 测试**

创建 `packages/backend/src/acp/registry.test.ts`：

```ts
import test from 'node:test';
import assert from 'node:assert/strict';
import { getDefaultAcpAgentServers } from './registry.js';

test('getDefaultAcpAgentServers includes Claude Code OpenCode and Codex', () => {
  const servers = getDefaultAcpAgentServers();
  assert.deepEqual(servers.map((server) => server.provider).sort(), ['claudecode', 'codex', 'opencode']);
  assert.ok(servers.find((server) => server.provider === 'claudecode')?.command.includes('npx'));
  assert.ok(servers.find((server) => server.provider === 'codex')?.args.includes('@zed-industries/codex-acp'));
  assert.ok(servers.find((server) => server.provider === 'opencode')?.command.includes('opencode'));
});
```

- [ ] **Step 2: 实现 registry**

创建 `packages/backend/src/acp/registry.ts`：

```ts
import type { AcpBackend } from '../types.js';

export interface AcpAgentServerConfig {
  provider: Extract<AcpBackend, 'claudecode' | 'codex' | 'opencode'>;
  command: string;
  args: string[];
  transport: 'stdio';
  enabled: boolean;
}

export function getDefaultAcpAgentServers(): AcpAgentServerConfig[] {
  return [
    {
      provider: 'claudecode',
      command: 'npx',
      args: ['@agentclientprotocol/claude-agent-acp'],
      transport: 'stdio',
      enabled: true,
    },
    {
      provider: 'codex',
      command: 'npx',
      args: ['@zed-industries/codex-acp'],
      transport: 'stdio',
      enabled: true,
    },
    {
      provider: 'opencode',
      command: 'opencode',
      args: ['acp'],
      transport: 'stdio',
      enabled: true,
    },
  ];
}
```

- [ ] **Step 3: 暂不替换现有 adapter**

本任务只提供 registry 配置入口和测试，不在同一任务中引入 `@agentclientprotocol/sdk` 或改 provider runtime，避免把 timeline UI 修复和 transport 重构耦合。

- [ ] **Step 4: 运行 registry 测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/acp/registry.test.ts
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add packages/backend/src/acp/registry.ts packages/backend/src/acp/registry.test.ts
git commit -m "feat(acp): 增加多ACP服务配置"
```

## Task 7: 集成验证与真实浏览器验收

**Files:**
- Modify: `docs/superpowers/plans/2026-05-26-ACP事件时间线与UI集成实施计划.md`

- [ ] **Step 1: 运行后端目标测试**

Run:

```bash
npm run test -w @openclaw-room/backend -- src/acp/timeline.test.ts src/acp/claudecode.test.ts src/acp/registry.test.ts
```

Expected: PASS。

- [ ] **Step 2: 运行前端构建**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected: PASS。

- [ ] **Step 3: 运行全量构建**

Run:

```bash
npm run build
```

Expected: PASS。

- [ ] **Step 4: 真实浏览器验证**

启动服务：

```bash
npm run dev
```

在浏览器打开目标房间，例如：

```text
http://localhost:5173/projects/TLvki8NldJar/rooms/oQgccc7zv8be
```

发送一条能触发读文件、改文件、运行命令的消息，例如：

```text
@前端开发工程师 请检查消息时间线组件，读取相关文件后做一个小的安全样式调整，并运行构建验证。
```

Expected:

- 消息正文仍然流式显示。
- 消息下方出现 ACP 执行过程时间线。
- Thinking 卡片默认折叠，展开后有可读 thinking 或 encrypted 提示。
- Tool Call 卡片显示工具名和参数。
- Command 卡片显示命令输出。
- File Diff 卡片显示红绿 diff。
- Planner 的 next_steps 以中文结构化卡片展示。

- [ ] **Step 5: 记录已知失败**

如果 `packages/backend/src/dispatcher.test.ts` 仍有历史 `task_readiness/任务生成结构化输出规则` 失败，不在本任务内修复；在最终回复里明确列出失败名称和原因。

- [ ] **Step 6: 最终 commit**

如果只勾选计划或补充验收记录：

```bash
git add docs/superpowers/plans/2026-05-26-ACP事件时间线与UI集成实施计划.md
git commit -m "docs(acp): 记录事件时间线验收"
```

如果没有文档变化，不创建空 commit。

## 收尾要求

- 每个任务完成后运行对应测试并提交 commit。
- 任何任务不得恢复自动任务创建或 workflow 创建。
- UI 变更必须覆盖 `AgentTimeline`，不能只把事件写进 metadata。
- 如果 provider 没有返回 thinking 原文，只显示真实返回内容或 encrypted 提示，不伪造思考。
- 最终回复必须列出已运行验证命令、通过情况、未解决的历史失败。
