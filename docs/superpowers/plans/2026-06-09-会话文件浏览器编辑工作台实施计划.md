# 会话文件浏览器编辑工作台 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 将会话中间区文件浏览器从只读预览升级为轻量本地代码编辑工作台，支持文件树选中、新建、重命名、删除、右键菜单、Monaco 可编辑、显式保存和未保存确认。

**Architecture:** 后端先补 workspace 写操作服务函数和 REST API，并沿用现有路径安全边界。前端由 `FileBrowserWorkspace` 统一协调 tabs、active path、selected entry、dirty map 和 pending action，文件树负责目录操作交互，编辑器负责 draft/save 状态。复杂状态计算抽成纯函数，先用 node:test 覆盖，再接 React 组件和浏览器 smoke。

**Tech Stack:** Node.js、Express、Zod、better-sqlite3 项目仓储、React 18、TypeScript、TanStack Query、Monaco Editor、FlexLayout、Radix 菜单/对话框、lucide-react、Playwright CLI。

---

## 关联设计

- Spec: `docs/superpowers/specs/2026-06-09-会话文件浏览器编辑工作台设计.md`
- 当前相关前端入口：
  - `packages/frontend/src/session-ui/FileBrowserWorkspace.tsx`
  - `packages/frontend/src/session-ui/WorkspaceFileTree.tsx`
  - `packages/frontend/src/session-ui/FileViewers.tsx`
  - `packages/frontend/src/session-ui/WorkspaceFileTabs.tsx`
  - `packages/frontend/src/session-ui/workspace-file-model.ts`
  - `packages/frontend/src/session-ui/session-os.css`
- 当前相关后端入口：
  - `packages/backend/src/workspace-files.ts`
  - `packages/backend/src/workspace-files.test.ts`
  - `packages/backend/src/routes.ts`
  - `packages/backend/src/types.ts`
  - `packages/frontend/src/lib/api.ts`
  - `packages/frontend/src/lib/types.ts`

## 工作区注意事项

当前工作区可能存在与知识库或截图有关的未提交改动。执行本计划时每个任务只暂存本任务明确列出的文件。不要暂存或回滚未列出的文件。

---

## 文件结构

### 后端

- Modify: `packages/backend/src/workspace-files.ts`
  - 新增 entry 名称校验、创建文件、创建文件夹、保存文本文件、重命名 entry、删除 entry、entry 元数据构造。
  - `readWorkspaceFilePreview` 返回 `mtimeMs`。
- Modify: `packages/backend/src/workspace-files.test.ts`
  - 增加 workspace 写操作单元测试。
- Modify: `packages/backend/src/types.ts`
  - `WorkspaceFilePreview` 增加 `mtimeMs`。
- Modify: `packages/backend/src/routes.ts`
  - 增加 POST/PUT/PATCH/DELETE workspace 路由。

### 前端 API 和模型

- Modify: `packages/frontend/src/lib/types.ts`
  - `WorkspaceFilePreview` 增加 `mtimeMs`。
  - 增加 workspace 写操作响应类型。
- Modify: `packages/frontend/src/lib/api.ts`
  - 增加 create/save/rename/delete workspace entry API。
- Modify: `packages/frontend/src/lib/api.test.ts`
  - 增加 API URL、method、body 编码测试。
- Create: `packages/frontend/src/session-ui/workspace-file-operations.ts`
  - 纯函数：名称校验、父目录解析、tab path 迁移、删除影响计算、dirty 覆盖判断。
- Create: `packages/frontend/src/session-ui/workspace-file-operations.test.ts`
  - 覆盖纯函数。
- Modify: `packages/frontend/src/session-ui/workspace-file-model.ts`
  - 扩展 tab 模型以支持 dirty 标记所需的 path/name 更新工具。
- Modify: `packages/frontend/src/session-ui/workspace-file-model.test.ts`
  - 覆盖 tab rename/delete 辅助行为。

### 前端组件

- Modify: `packages/frontend/src/session-ui/FileBrowserWorkspace.tsx`
  - 协调 tabs、selected entry、dirty state、pending action。
- Modify: `packages/frontend/src/session-ui/WorkspaceFileTree.tsx`
  - 支持 selected entry、右键菜单、inline create/rename、真实新建按钮、刷新目录。
- Modify: `packages/frontend/src/session-ui/FileViewers.tsx`
  - Monaco 改为可编辑，暴露 onChange/onSave/onReload。
- Modify: `packages/frontend/src/session-ui/WorkspaceFileTabs.tsx`
  - tab dirty 标记，关闭 dirty tab 前走上层确认。
- Modify: `packages/frontend/src/session-ui/session-os.css`
  - 新增 selected/dirty/inline input/context menu/save toolbar/confirm dialog 样式。

---

## Task 1: 后端 workspace 写操作 API

**Files:**
- Modify: `packages/backend/src/workspace-files.ts`
- Modify: `packages/backend/src/workspace-files.test.ts`
- Modify: `packages/backend/src/types.ts`
- Modify: `packages/backend/src/routes.ts`

- [x] **Step 1: 写失败测试，覆盖创建文件和文件夹**

在 `packages/backend/src/workspace-files.test.ts` import 增加：

```ts
import { readFileSync, existsSync } from 'node:fs';
import {
  createWorkspaceDirectory,
  createWorkspaceFile,
} from './workspace-files.js';
```

添加测试：

```ts
test('workspace write operations create files and directories without overwriting existing entries', async () => {
  const projectRoot = createWorkspaceRoot('openclaw-workspace-write-create-');

  try {
    mkdirSync(join(projectRoot, 'src'), { recursive: true });

    const file = await createWorkspaceFile(projectRoot, {
      parentPath: 'src',
      name: 'new-file.ts',
      content: 'export const value = 1;\n',
    });
    assert.equal(file.path, 'src/new-file.ts');
    assert.equal(file.type, 'file');
    assert.equal(readFileSync(join(projectRoot, 'src', 'new-file.ts'), 'utf-8'), 'export const value = 1;\n');

    const directory = await createWorkspaceDirectory(projectRoot, {
      parentPath: 'src',
      name: 'components',
    });
    assert.equal(directory.path, 'src/components');
    assert.equal(directory.type, 'directory');
    assert.equal(existsSync(join(projectRoot, 'src', 'components')), true);

    await assert.rejects(
      () => createWorkspaceFile(projectRoot, { parentPath: 'src', name: 'new-file.ts', content: '' }),
      /WORKSPACE_PATH_EXISTS/,
    );
    await assert.rejects(
      () => createWorkspaceDirectory(projectRoot, { parentPath: 'src', name: 'components' }),
      /WORKSPACE_PATH_EXISTS/,
    );
  } finally {
    cleanupWorkspaceRoot(projectRoot);
  }
});
```

- [x] **Step 2: 写失败测试，覆盖非法名称和 ignored path**

在同一测试文件添加：

```ts
test('workspace write operations reject invalid names and ignored targets', async () => {
  const projectRoot = createWorkspaceRoot('openclaw-workspace-write-invalid-');

  try {
    await assert.rejects(
      () => createWorkspaceFile(projectRoot, { parentPath: '', name: '', content: '' }),
      /WORKSPACE_ENTRY_NAME_INVALID/,
    );
    await assert.rejects(
      () => createWorkspaceFile(projectRoot, { parentPath: '', name: '..', content: '' }),
      /WORKSPACE_ENTRY_NAME_INVALID/,
    );
    await assert.rejects(
      () => createWorkspaceFile(projectRoot, { parentPath: '', name: 'nested/file.ts', content: '' }),
      /WORKSPACE_ENTRY_NAME_INVALID/,
    );
    await assert.rejects(
      () => createWorkspaceFile(projectRoot, { parentPath: '', name: '.env', content: 'SECRET=1\n' }),
      /WORKSPACE_PATH_IGNORED/,
    );
    await assert.rejects(
      () => createWorkspaceDirectory(projectRoot, { parentPath: '', name: 'node_modules' }),
      /WORKSPACE_PATH_IGNORED/,
    );
  } finally {
    cleanupWorkspaceRoot(projectRoot);
  }
});
```

- [x] **Step 3: 写失败测试，覆盖保存、mtime 冲突和强制覆盖**

在 import 增加 `statSync`：

```ts
import { readFileSync, existsSync, statSync } from 'node:fs';
```

添加测试：

```ts
test('workspace save writes text files and detects external modification conflicts', async () => {
  const projectRoot = createWorkspaceRoot('openclaw-workspace-write-save-');

  try {
    writeFileSync(join(projectRoot, 'app.ts'), 'export const value = 1;\n');
    const preview = await readWorkspaceFilePreview(projectRoot, 'app.ts');
    assert.equal(typeof preview.mtimeMs, 'number');

    const saved = await saveWorkspaceTextFile(projectRoot, {
      path: 'app.ts',
      content: 'export const value = 2;\n',
      expectedMtimeMs: preview.mtimeMs,
    });
    assert.equal(saved.content, 'export const value = 2;\n');
    assert.equal(readFileSync(join(projectRoot, 'app.ts'), 'utf-8'), 'export const value = 2;\n');

    const staleMtime = saved.mtimeMs;
    writeFileSync(join(projectRoot, 'app.ts'), 'external change\n');
    assert.notEqual(statSync(join(projectRoot, 'app.ts')).mtimeMs, staleMtime);

    await assert.rejects(
      () => saveWorkspaceTextFile(projectRoot, {
        path: 'app.ts',
        content: 'local draft\n',
        expectedMtimeMs: staleMtime,
      }),
      /WORKSPACE_FILE_CONFLICT/,
    );

    const forced = await saveWorkspaceTextFile(projectRoot, {
      path: 'app.ts',
      content: 'local draft\n',
      expectedMtimeMs: staleMtime,
      force: true,
    });
    assert.equal(forced.content, 'local draft\n');
  } finally {
    cleanupWorkspaceRoot(projectRoot);
  }
});
```

同时在测试 import 中加入待实现函数：

```ts
saveWorkspaceTextFile,
```

- [x] **Step 4: 写失败测试，覆盖重命名和删除**

在 import 增加：

```ts
renameWorkspaceEntry,
deleteWorkspaceEntry,
```

添加测试：

```ts
test('workspace write operations rename and delete files and directories', async () => {
  const projectRoot = createWorkspaceRoot('openclaw-workspace-write-rename-delete-');

  try {
    mkdirSync(join(projectRoot, 'src', 'nested'), { recursive: true });
    writeFileSync(join(projectRoot, 'src', 'old.ts'), 'old\n');
    writeFileSync(join(projectRoot, 'src', 'nested', 'child.ts'), 'child\n');

    const renamedFile = await renameWorkspaceEntry(projectRoot, {
      path: 'src/old.ts',
      name: 'new.ts',
    });
    assert.equal(renamedFile.oldPath, 'src/old.ts');
    assert.equal(renamedFile.newPath, 'src/new.ts');
    assert.equal(existsSync(join(projectRoot, 'src', 'old.ts')), false);
    assert.equal(existsSync(join(projectRoot, 'src', 'new.ts')), true);

    const renamedDir = await renameWorkspaceEntry(projectRoot, {
      path: 'src/nested',
      name: 'renamed',
    });
    assert.equal(renamedDir.newPath, 'src/renamed');
    assert.equal(existsSync(join(projectRoot, 'src', 'renamed', 'child.ts')), true);

    await assert.rejects(
      () => renameWorkspaceEntry(projectRoot, { path: 'src/new.ts', name: 'renamed' }),
      /WORKSPACE_PATH_EXISTS/,
    );

    await deleteWorkspaceEntry(projectRoot, { path: 'src/renamed' });
    assert.equal(existsSync(join(projectRoot, 'src', 'renamed')), false);

    await deleteWorkspaceEntry(projectRoot, { path: 'src/new.ts' });
    assert.equal(existsSync(join(projectRoot, 'src', 'new.ts')), false);
  } finally {
    cleanupWorkspaceRoot(projectRoot);
  }
});
```

- [x] **Step 5: 运行后端 workspace 测试，确认失败原因是函数未定义**

Run:

```bash
npm exec -w @openclaw-room/backend -- tsx src/workspace-files.test.ts
```

Expected: FAIL，错误包含 `createWorkspaceFile`、`saveWorkspaceTextFile`、`renameWorkspaceEntry` 或 `deleteWorkspaceEntry` 未导出。

- [x] **Step 6: 扩展后端类型**

在 `packages/backend/src/types.ts` 的 `WorkspaceFilePreview` 中增加：

```ts
mtimeMs: number;
```

如果当前接口定义在文件中不止一处，保持名称和字段顺序一致：

```ts
export interface WorkspaceFilePreview {
  path: string;
  content: string;
  truncated: boolean;
  language: string | null;
  mimeType: string | null;
  size: number;
  mtimeMs: number;
}
```

- [x] **Step 7: 在 workspace-files.ts 增加错误码和请求类型**

在 `WorkspaceFileErrorCode` union 中加入：

```ts
  | 'WORKSPACE_PATH_EXISTS'
  | 'WORKSPACE_ENTRY_NAME_INVALID'
  | 'WORKSPACE_FILE_CONFLICT'
```

在文件顶部 fs imports 中加入：

```ts
import { constants as fsConstants, promises as fs } from 'node:fs';
```

如果已有 `constants as fsConstants` import，则改成：

```ts
import { constants as fsConstants, promises as fs } from 'node:fs';
```

新增导出类型：

```ts
export interface CreateWorkspaceFileInput {
  parentPath?: string;
  name: string;
  content?: string;
}

export interface CreateWorkspaceDirectoryInput {
  parentPath?: string;
  name: string;
}

export interface SaveWorkspaceTextFileInput {
  path: string;
  content: string;
  expectedMtimeMs?: number | null;
  force?: boolean;
}

export interface RenameWorkspaceEntryInput {
  path: string;
  name: string;
}

export interface DeleteWorkspaceEntryInput {
  path: string;
}

export interface RenameWorkspaceEntryResult {
  oldPath: string;
  newPath: string;
  entry: WorkspaceDirectoryEntry;
}
```

- [x] **Step 8: 增加 entry 名称校验和 entry 构造函数**

在 `workspace-files.ts` 中 `isIgnoredWorkspacePath` 后增加：

```ts
export function validateWorkspaceEntryName(name: string): string {
  if (typeof name !== 'string') throw workspaceFileError('WORKSPACE_ENTRY_NAME_INVALID');
  const trimmed = name.trim();
  if (!trimmed || trimmed === '.' || trimmed === '..') {
    throw workspaceFileError('WORKSPACE_ENTRY_NAME_INVALID');
  }
  if (trimmed.includes('/') || trimmed.includes('\\') || trimmed.includes('\0')) {
    throw workspaceFileError('WORKSPACE_ENTRY_NAME_INVALID');
  }
  return trimmed;
}

function ensureCreatableWorkspacePath(relativePath: string, extraIgnoredDirs: string[] = []): void {
  ensureWorkspacePathAllowed(relativePath, null);
  if (isIgnoredWorkspacePath(relativePath, extraIgnoredDirs)) {
    throw workspaceFileError('WORKSPACE_PATH_IGNORED');
  }
}

async function assertWorkspacePathDoesNotExist(absolutePath: string): Promise<void> {
  try {
    await lstat(absolutePath);
    throw workspaceFileError('WORKSPACE_PATH_EXISTS');
  } catch (error) {
    if ((error as NodeJS.ErrnoException).code === 'ENOENT') return;
    throw error;
  }
}

async function buildWorkspaceDirectoryEntry(
  absolutePath: string,
  relativePath: string,
): Promise<WorkspaceDirectoryEntry> {
  const entryStats = await lstat(absolutePath);
  const name = relativePath.split('/').pop() ?? relativePath;
  const isDirectory = entryStats.isDirectory();
  return {
    name,
    path: relativePath,
    type: isDirectory ? 'directory' : 'file',
    size: isDirectory ? null : entryStats.size,
    mimeType: isDirectory ? null : inferMimeType(name),
    language: isDirectory ? null : inferLanguage(name),
  };
}
```

- [x] **Step 9: 修改 preview 返回 mtimeMs**

在 `readWorkspaceFilePreview` 中把 return 改为带 mtime：

```ts
return {
  ...buildPreviewFromBuffer(resolved.relativePath, fileSize, contentBuffer, WORKSPACE_PREVIEW_TEXT_LIMIT),
  mtimeMs: fileStats.mtimeMs,
};
```

如果 TypeScript 提示 `buildPreviewFromBuffer` 返回类型不匹配，保持 `WorkspaceFilePreview` 类型包含 `mtimeMs`。

- [x] **Step 10: 实现 create/save/rename/delete 服务函数**

在 `readWorkspaceFilePreview` 后、`readWorkspaceFileReference` 前加入：

```ts
export async function createWorkspaceFile(
  projectPath: string,
  input: CreateWorkspaceFileInput,
  extraIgnoredDirs: string[] = [],
): Promise<WorkspaceDirectoryEntry> {
  const parent = await resolveWorkspacePath(projectPath, input.parentPath ?? '');
  ensureWorkspacePathAllowed(parent.relativePath, parent.symlinkTargetRelativePath);
  const parentStats = await lstat(parent.absolutePath);
  if (!parentStats.isDirectory()) throw workspaceFileError('WORKSPACE_PATH_NOT_DIRECTORY');

  const name = validateWorkspaceEntryName(input.name);
  const relativePath = joinRelativePath(parent.relativePath, name);
  ensureCreatableWorkspacePath(relativePath, extraIgnoredDirs);
  const absolutePath = join(parent.absolutePath, name);
  await assertWorkspacePathDoesNotExist(absolutePath);
  await fs.writeFile(absolutePath, input.content ?? '', 'utf-8');
  return buildWorkspaceDirectoryEntry(absolutePath, relativePath);
}

export async function createWorkspaceDirectory(
  projectPath: string,
  input: CreateWorkspaceDirectoryInput,
  extraIgnoredDirs: string[] = [],
): Promise<WorkspaceDirectoryEntry> {
  const parent = await resolveWorkspacePath(projectPath, input.parentPath ?? '');
  ensureWorkspacePathAllowed(parent.relativePath, parent.symlinkTargetRelativePath);
  const parentStats = await lstat(parent.absolutePath);
  if (!parentStats.isDirectory()) throw workspaceFileError('WORKSPACE_PATH_NOT_DIRECTORY');

  const name = validateWorkspaceEntryName(input.name);
  const relativePath = joinRelativePath(parent.relativePath, name);
  ensureCreatableWorkspacePath(relativePath, extraIgnoredDirs);
  const absolutePath = join(parent.absolutePath, name);
  await assertWorkspacePathDoesNotExist(absolutePath);
  await fs.mkdir(absolutePath);
  return buildWorkspaceDirectoryEntry(absolutePath, relativePath);
}

export async function saveWorkspaceTextFile(
  projectPath: string,
  input: SaveWorkspaceTextFileInput,
): Promise<WorkspaceFilePreview> {
  const resolved = await resolveWorkspacePath(projectPath, input.path);
  ensureWorkspacePathAllowed(resolved.relativePath, resolved.symlinkTargetRelativePath);
  const fileStats = await lstat(resolved.absolutePath);
  if (!fileStats.isFile()) throw workspaceFileError('WORKSPACE_PATH_NOT_FILE');

  if (!input.force && input.expectedMtimeMs !== undefined && input.expectedMtimeMs !== null) {
    if (Math.abs(fileStats.mtimeMs - input.expectedMtimeMs) > 1) {
      throw workspaceFileError('WORKSPACE_FILE_CONFLICT');
    }
  }

  const existing = await fs.readFile(resolved.absolutePath);
  if (!isTextBuffer(existing)) throw workspaceFileError('WORKSPACE_FILE_BINARY');

  await fs.writeFile(resolved.absolutePath, input.content, 'utf-8');
  return readWorkspaceFilePreview(projectPath, resolved.relativePath);
}

export async function renameWorkspaceEntry(
  projectPath: string,
  input: RenameWorkspaceEntryInput,
  extraIgnoredDirs: string[] = [],
): Promise<RenameWorkspaceEntryResult> {
  const source = await resolveWorkspacePath(projectPath, input.path);
  ensureWorkspacePathAllowed(source.relativePath, source.symlinkTargetRelativePath);
  const sourceStats = await lstat(source.absolutePath);
  const name = validateWorkspaceEntryName(input.name);
  const parentPath = parentWorkspacePath(source.relativePath);
  const targetRelativePath = joinRelativePath(parentPath, name);
  ensureCreatableWorkspacePath(targetRelativePath, extraIgnoredDirs);
  const targetAbsolutePath = join(source.projectRealPath, ...targetRelativePath.split('/'));
  await assertWorkspacePathDoesNotExist(targetAbsolutePath);
  await fs.rename(source.absolutePath, targetAbsolutePath);
  const entry = await buildWorkspaceDirectoryEntry(targetAbsolutePath, targetRelativePath);
  if (sourceStats.isDirectory() && entry.type !== 'directory') {
    throw workspaceFileError('WORKSPACE_PATH_NOT_DIRECTORY');
  }
  return { oldPath: source.relativePath, newPath: targetRelativePath, entry };
}

export async function deleteWorkspaceEntry(
  projectPath: string,
  input: DeleteWorkspaceEntryInput,
): Promise<void> {
  const resolved = await resolveWorkspacePath(projectPath, input.path);
  ensureWorkspacePathAllowed(resolved.relativePath, resolved.symlinkTargetRelativePath);
  const entryStats = await lstat(resolved.absolutePath);
  if (entryStats.isDirectory()) {
    await fs.rm(resolved.absolutePath, { recursive: true, force: false });
    return;
  }
  if (entryStats.isFile()) {
    await fs.unlink(resolved.absolutePath);
    return;
  }
  throw workspaceFileError('WORKSPACE_PATH_NOT_FILE');
}

function parentWorkspacePath(relativePath: string): string {
  const parts = normalizeWorkspacePath(relativePath).split('/').filter(Boolean);
  parts.pop();
  return parts.join('/');
}
```

- [x] **Step 11: 修正 TypeScript 细节**

检查 `join(source.projectRealPath, ...targetRelativePath.split('/'))` 对根级目标可用。若 `targetRelativePath` 为空不可能出现，因为 rename 必须有 source path 和 new name。

确保 `workspaceFileError`、`ensureWorkspacePathAllowed`、`joinRelativePath`、`inferMimeType`、`inferLanguage`、`isTextBuffer` 已在同文件作用域可用。

- [x] **Step 12: 运行后端 workspace 测试**

Run:

```bash
npm exec -w @openclaw-room/backend -- tsx src/workspace-files.test.ts
```

Expected: PASS，所有 workspace 文件测试通过。

- [x] **Step 13: 增加 routes 写接口测试或扩展现有路由 smoke**

如果项目已有 workspace route 相关测试，优先补到现有文件；否则在 `packages/backend/src/workspace-files.test.ts` 覆盖服务层后，`routes.ts` 路由用最小 zod 和错误映射代码实现，最终由前端 smoke 验证。

- [x] **Step 14: 修改 routes.ts import**

在 `packages/backend/src/routes.ts` 的 workspace-files import 中加入：

```ts
  createWorkspaceDirectory,
  createWorkspaceFile,
  deleteWorkspaceEntry,
  renameWorkspaceEntry,
  saveWorkspaceTextFile,
```

- [x] **Step 15: 增加 workspace 写路由**

在 `GET /projects/:projectId/workspace/file` 后加入：

```ts
router.post('/projects/:projectId/workspace/file', async (req, res, next) => {
  if (!requireLocalAccess(req, res)) return;
  const project = projectRepo.get(req.params.projectId);
  if (!project) return res.status(404).json({ error: 'project not found' });
  const parsed = z.object({
    parentPath: z.string().optional(),
    name: z.string(),
    content: z.string().optional(),
  }).safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  const extraIgnoredDirs = settingsRepo.resolveWorkspaceExcludedDirs(project.id);

  try {
    const entry = await createWorkspaceFile(project.path, parsed.data, extraIgnoredDirs);
    return res.status(201).json({ entry });
  } catch (error) {
    if (error instanceof WorkspaceFileError) {
      return res.status(workspaceFileErrorStatus(error)).json({ error: error.code });
    }
    next(error);
  }
});

router.post('/projects/:projectId/workspace/directory', async (req, res, next) => {
  if (!requireLocalAccess(req, res)) return;
  const project = projectRepo.get(req.params.projectId);
  if (!project) return res.status(404).json({ error: 'project not found' });
  const parsed = z.object({
    parentPath: z.string().optional(),
    name: z.string(),
  }).safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  const extraIgnoredDirs = settingsRepo.resolveWorkspaceExcludedDirs(project.id);

  try {
    const entry = await createWorkspaceDirectory(project.path, parsed.data, extraIgnoredDirs);
    return res.status(201).json({ entry });
  } catch (error) {
    if (error instanceof WorkspaceFileError) {
      return res.status(workspaceFileErrorStatus(error)).json({ error: error.code });
    }
    next(error);
  }
});

router.put('/projects/:projectId/workspace/file', async (req, res, next) => {
  if (!requireLocalAccess(req, res)) return;
  const project = projectRepo.get(req.params.projectId);
  if (!project) return res.status(404).json({ error: 'project not found' });
  const parsed = z.object({
    path: z.string().min(1),
    content: z.string(),
    expectedMtimeMs: z.number().optional().nullable(),
    force: z.boolean().optional(),
  }).safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });

  try {
    const preview = await saveWorkspaceTextFile(project.path, parsed.data);
    return res.json(preview);
  } catch (error) {
    if (error instanceof WorkspaceFileError) {
      return res.status(workspaceFileErrorStatus(error)).json({ error: error.code });
    }
    next(error);
  }
});

router.patch('/projects/:projectId/workspace/entry', async (req, res, next) => {
  if (!requireLocalAccess(req, res)) return;
  const project = projectRepo.get(req.params.projectId);
  if (!project) return res.status(404).json({ error: 'project not found' });
  const parsed = z.object({
    path: z.string().min(1),
    name: z.string(),
  }).safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });
  const extraIgnoredDirs = settingsRepo.resolveWorkspaceExcludedDirs(project.id);

  try {
    const result = await renameWorkspaceEntry(project.path, parsed.data, extraIgnoredDirs);
    return res.json(result);
  } catch (error) {
    if (error instanceof WorkspaceFileError) {
      return res.status(workspaceFileErrorStatus(error)).json({ error: error.code });
    }
    next(error);
  }
});

router.delete('/projects/:projectId/workspace/entry', async (req, res, next) => {
  if (!requireLocalAccess(req, res)) return;
  const project = projectRepo.get(req.params.projectId);
  if (!project) return res.status(404).json({ error: 'project not found' });
  const parsed = z.object({ path: z.string().min(1) }).safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });

  try {
    await deleteWorkspaceEntry(project.path, parsed.data);
    return res.status(204).end();
  } catch (error) {
    if (error instanceof WorkspaceFileError) {
      return res.status(workspaceFileErrorStatus(error)).json({ error: error.code });
    }
    next(error);
  }
});
```

- [x] **Step 16: 更新 workspaceFileErrorStatus**

在 `routes.ts` 的 `workspaceFileErrorStatus` 中补充：

```ts
if (error.code === 'WORKSPACE_PATH_EXISTS' || error.code === 'WORKSPACE_FILE_CONFLICT') return 409;
if (error.code === 'WORKSPACE_ENTRY_NAME_INVALID') return 400;
```

- [x] **Step 17: 运行后端 build**

Run:

```bash
npm run build -w @openclaw-room/backend
```

Expected: PASS。

- [x] **Step 18: 提交 Task 1**

```bash
git add packages/backend/src/workspace-files.ts packages/backend/src/workspace-files.test.ts packages/backend/src/types.ts packages/backend/src/routes.ts
git commit -m "feat(backend): 增加工作区文件写操作"
```

---

## Task 2: 前端 API 类型和纯操作模型

**Files:**
- Modify: `packages/frontend/src/lib/types.ts`
- Modify: `packages/frontend/src/lib/api.ts`
- Modify: `packages/frontend/src/lib/api.test.ts`
- Create: `packages/frontend/src/session-ui/workspace-file-operations.ts`
- Create: `packages/frontend/src/session-ui/workspace-file-operations.test.ts`
- Modify: `packages/frontend/src/session-ui/workspace-file-model.ts`
- Modify: `packages/frontend/src/session-ui/workspace-file-model.test.ts`

- [x] **Step 1: 写 API 测试**

在 `packages/frontend/src/lib/api.test.ts` 添加：

```ts
test('workspace write APIs encode methods and request bodies', async () => {
  const createdFile = await captureApiRequest(() => api.createWorkspaceFile('project-1', {
    parentPath: 'src',
    name: 'new.ts',
    content: 'hello\n',
  }));
  assert.equal(createdFile.url, '/api/projects/project-1/workspace/file');
  assert.equal(createdFile.init?.method, 'POST');
  assert.deepEqual(JSON.parse(String(createdFile.init?.body)), {
    parentPath: 'src',
    name: 'new.ts',
    content: 'hello\n',
  });

  const createdDirectory = await captureApiRequest(() => api.createWorkspaceDirectory('project-1', {
    parentPath: '',
    name: 'docs',
  }));
  assert.equal(createdDirectory.url, '/api/projects/project-1/workspace/directory');
  assert.equal(createdDirectory.init?.method, 'POST');

  const saved = await captureApiRequest(() => api.saveWorkspaceFile('project-1', {
    path: 'src/new.ts',
    content: 'updated\n',
    expectedMtimeMs: 123,
  }));
  assert.equal(saved.url, '/api/projects/project-1/workspace/file');
  assert.equal(saved.init?.method, 'PUT');

  const renamed = await captureApiRequest(() => api.renameWorkspaceEntry('project-1', {
    path: 'src/new.ts',
    name: 'renamed.ts',
  }));
  assert.equal(renamed.url, '/api/projects/project-1/workspace/entry');
  assert.equal(renamed.init?.method, 'PATCH');

  const deleted = await captureApiRequest(() => api.deleteWorkspaceEntry('project-1', {
    path: 'src/renamed.ts',
  }));
  assert.equal(deleted.url, '/api/projects/project-1/workspace/entry');
  assert.equal(deleted.init?.method, 'DELETE');
});
```

如果当前测试工具函数名称不是 `captureApiRequest`，使用文件中已有的请求捕获 helper，并保持断言字段一致。

- [x] **Step 2: 写操作模型测试**

创建 `packages/frontend/src/session-ui/workspace-file-operations.test.ts`：

```ts
import assert from 'node:assert/strict';
import test from 'node:test';
import {
  closeTabsForDeletedEntry,
  dirtyFilesUnderEntry,
  parentPathForCreate,
  renameDirtyPaths,
  renameWorkspaceTabPaths,
  validateWorkspaceEntryNameInput,
} from './workspace-file-operations';
import { createWorkspaceFileTab, type WorkspaceFileTab } from './workspace-file-model';
import type { WorkspaceDirectoryEntry } from '../lib/types';

test('parentPathForCreate resolves root, selected folders, and selected file parents', () => {
  assert.equal(parentPathForCreate(null), '');
  assert.equal(parentPathForCreate(entry({ path: 'src', name: 'src', type: 'directory' })), 'src');
  assert.equal(parentPathForCreate(entry({ path: 'src/App.tsx', name: 'App.tsx', type: 'file' })), 'src');
  assert.equal(parentPathForCreate(entry({ path: 'README.md', name: 'README.md', type: 'file' })), '');
});

test('validateWorkspaceEntryNameInput rejects invalid file manager names', () => {
  assert.equal(validateWorkspaceEntryNameInput('App.tsx'), null);
  assert.match(validateWorkspaceEntryNameInput('') ?? '', /名称不能为空/);
  assert.match(validateWorkspaceEntryNameInput('..') ?? '', /不能是/);
  assert.match(validateWorkspaceEntryNameInput('a/b') ?? '', /不能包含/);
});

test('renameWorkspaceTabPaths updates files and nested directory tabs', () => {
  const tabs = [
    tab('src/App.tsx'),
    tab('src/components/Button.tsx'),
    tab('README.md'),
  ];
  assert.deepEqual(renameWorkspaceTabPaths(tabs, 'src/App.tsx', 'src/Main.tsx', 'file').map((item) => item.path), [
    'src/Main.tsx',
    'src/components/Button.tsx',
    'README.md',
  ]);
  assert.deepEqual(renameWorkspaceTabPaths(tabs, 'src', 'packages/frontend/src', 'directory').map((item) => item.path), [
    'packages/frontend/src/App.tsx',
    'packages/frontend/src/components/Button.tsx',
    'README.md',
  ]);
});

test('closeTabsForDeletedEntry closes files and nested directory tabs', () => {
  const tabs = [tab('src/App.tsx'), tab('src/components/Button.tsx'), tab('README.md')];
  assert.deepEqual(closeTabsForDeletedEntry(tabs, 'src/App.tsx', 'file').map((item) => item.path), [
    'src/components/Button.tsx',
    'README.md',
  ]);
  assert.deepEqual(closeTabsForDeletedEntry(tabs, 'src', 'directory').map((item) => item.path), ['README.md']);
});

test('dirty helpers find and rename affected dirty paths', () => {
  const dirty = {
    'src/App.tsx': { path: 'src/App.tsx' },
    'src/components/Button.tsx': { path: 'src/components/Button.tsx' },
    'README.md': { path: 'README.md' },
  };
  assert.deepEqual(dirtyFilesUnderEntry(dirty, 'src', 'directory'), ['src/App.tsx', 'src/components/Button.tsx']);
  assert.deepEqual(Object.keys(renameDirtyPaths(dirty, 'src', 'packages/frontend/src', 'directory')), [
    'packages/frontend/src/App.tsx',
    'packages/frontend/src/components/Button.tsx',
    'README.md',
  ]);
});

function entry(overrides: Partial<WorkspaceDirectoryEntry>): WorkspaceDirectoryEntry {
  return {
    path: 'src/App.tsx',
    name: 'App.tsx',
    type: 'file',
    size: null,
    mimeType: 'text/plain',
    language: 'typescript',
    ...overrides,
  };
}

function tab(path: string): WorkspaceFileTab {
  return createWorkspaceFileTab(entry({
    path,
    name: path.split('/').pop() ?? path,
  }));
}
```

- [x] **Step 3: 运行前端新测试，确认失败**

Run:

```bash
npm exec -w @openclaw-room/frontend -- tsx src/session-ui/workspace-file-operations.test.ts
```

Expected: FAIL，模块不存在。

- [x] **Step 4: 扩展前端类型**

在 `packages/frontend/src/lib/types.ts` 中更新：

```ts
export interface WorkspaceFilePreview {
  path: string;
  content: string;
  truncated: boolean;
  language: string | null;
  mimeType: string | null;
  size: number;
  mtimeMs: number;
}

export interface WorkspaceEntryMutationResponse {
  entry: WorkspaceDirectoryEntry;
}

export interface WorkspaceRenameEntryResponse {
  oldPath: string;
  newPath: string;
  entry: WorkspaceDirectoryEntry;
}
```

如果已有 `WorkspaceFilePreview`，只添加 `mtimeMs`。

- [x] **Step 5: 增加前端 API 方法**

在 `packages/frontend/src/lib/api.ts` workspace 方法附近添加：

```ts
  createWorkspaceFile: (
    projectId: string,
    payload: { parentPath?: string; name: string; content?: string },
  ) => workspaceRequest<WorkspaceEntryMutationResponse>(
    `/projects/${projectId}/workspace/file`,
    { method: 'POST', body: JSON.stringify(payload) },
  ),
  createWorkspaceDirectory: (
    projectId: string,
    payload: { parentPath?: string; name: string },
  ) => workspaceRequest<WorkspaceEntryMutationResponse>(
    `/projects/${projectId}/workspace/directory`,
    { method: 'POST', body: JSON.stringify(payload) },
  ),
  saveWorkspaceFile: (
    projectId: string,
    payload: { path: string; content: string; expectedMtimeMs?: number | null; force?: boolean },
  ) => workspaceRequest<WorkspaceFilePreview>(
    `/projects/${projectId}/workspace/file`,
    { method: 'PUT', body: JSON.stringify(payload) },
  ),
  renameWorkspaceEntry: (
    projectId: string,
    payload: { path: string; name: string },
  ) => workspaceRequest<WorkspaceRenameEntryResponse>(
    `/projects/${projectId}/workspace/entry`,
    { method: 'PATCH', body: JSON.stringify(payload) },
  ),
  deleteWorkspaceEntry: (
    projectId: string,
    payload: { path: string },
  ) => workspaceRequest<void>(
    `/projects/${projectId}/workspace/entry`,
    { method: 'DELETE', body: JSON.stringify(payload) },
  ),
```

确保 `WorkspaceEntryMutationResponse`、`WorkspaceRenameEntryResponse`、`WorkspaceFilePreview` 已从 types import。

- [x] **Step 6: 创建 workspace-file-operations.ts**

创建 `packages/frontend/src/session-ui/workspace-file-operations.ts`：

```ts
import type { WorkspaceDirectoryEntry } from '../lib/types';
import type { WorkspaceFileTab } from './workspace-file-model';
import { workspaceFileTabId } from './workspace-file-model';

export type WorkspaceEntryKind = 'file' | 'directory';

export function parentPathForCreate(entry: WorkspaceDirectoryEntry | null): string {
  if (!entry) return '';
  if (entry.type === 'directory') return entry.path;
  return parentPath(entry.path);
}

export function validateWorkspaceEntryNameInput(name: string): string | null {
  const trimmed = name.trim();
  if (!trimmed) return '名称不能为空';
  if (trimmed === '.' || trimmed === '..') return '名称不能是 . 或 ..';
  if (/[\\/\0]/u.test(trimmed)) return '名称不能包含路径分隔符';
  return null;
}

export function renameWorkspaceTabPaths(
  tabs: WorkspaceFileTab[],
  oldPath: string,
  newPath: string,
  entryType: WorkspaceEntryKind,
): WorkspaceFileTab[] {
  return tabs.map((tab) => {
    const nextPath = renamedPath(tab.path, oldPath, newPath, entryType);
    if (!nextPath || nextPath === tab.path) return tab;
    return {
      ...tab,
      id: workspaceFileTabId(nextPath),
      path: nextPath,
      name: basename(nextPath),
    };
  });
}

export function closeTabsForDeletedEntry(
  tabs: WorkspaceFileTab[],
  deletedPath: string,
  entryType: WorkspaceEntryKind,
): WorkspaceFileTab[] {
  return tabs.filter((tab) => !isPathAffected(tab.path, deletedPath, entryType));
}

export function dirtyFilesUnderEntry<T extends { path: string }>(
  dirtyByPath: Record<string, T>,
  entryPath: string,
  entryType: WorkspaceEntryKind,
): string[] {
  return Object.keys(dirtyByPath).filter((path) => isPathAffected(path, entryPath, entryType));
}

export function renameDirtyPaths<T extends { path: string }>(
  dirtyByPath: Record<string, T>,
  oldPath: string,
  newPath: string,
  entryType: WorkspaceEntryKind,
): Record<string, T> {
  const next: Record<string, T> = {};
  Object.entries(dirtyByPath).forEach(([path, state]) => {
    const renamed = renamedPath(path, oldPath, newPath, entryType);
    if (!renamed) {
      next[path] = state;
      return;
    }
    next[renamed] = { ...state, path: renamed };
  });
  return next;
}

function renamedPath(path: string, oldPath: string, newPath: string, entryType: WorkspaceEntryKind): string | null {
  if (entryType === 'file') return path === oldPath ? newPath : null;
  if (path === oldPath) return newPath;
  const prefix = `${oldPath}/`;
  return path.startsWith(prefix) ? `${newPath}/${path.slice(prefix.length)}` : null;
}

function isPathAffected(path: string, targetPath: string, entryType: WorkspaceEntryKind): boolean {
  if (entryType === 'file') return path === targetPath;
  return path === targetPath || path.startsWith(`${targetPath}/`);
}

function parentPath(path: string): string {
  const parts = path.split('/').filter(Boolean);
  parts.pop();
  return parts.join('/');
}

function basename(path: string): string {
  return path.split('/').filter(Boolean).pop() ?? path;
}
```

- [x] **Step 7: 运行前端操作模型测试**

Run:

```bash
npm exec -w @openclaw-room/frontend -- tsx src/session-ui/workspace-file-operations.test.ts
```

Expected: PASS。

- [x] **Step 8: 运行 API 测试**

Run:

```bash
npm exec -w @openclaw-room/frontend -- tsx src/lib/api.test.ts
```

Expected: PASS。

- [x] **Step 9: 提交 Task 2**

```bash
git add packages/frontend/src/lib/types.ts packages/frontend/src/lib/api.ts packages/frontend/src/lib/api.test.ts packages/frontend/src/session-ui/workspace-file-operations.ts packages/frontend/src/session-ui/workspace-file-operations.test.ts packages/frontend/src/session-ui/workspace-file-model.ts packages/frontend/src/session-ui/workspace-file-model.test.ts
git commit -m "feat(frontend): 增加工作区文件操作模型"
```

---

## Task 3: 文件树选中态、新建、右键菜单、重命名、删除

**Files:**
- Modify: `packages/frontend/src/session-ui/FileBrowserWorkspace.tsx`
- Modify: `packages/frontend/src/session-ui/WorkspaceFileTree.tsx`
- Modify: `packages/frontend/src/session-ui/session-os.css`

- [x] **Step 1: 在 FileBrowserWorkspace 增加 selected entry 状态和目录刷新版本**

在 `FileBrowserWorkspace.tsx` 中增加状态：

```ts
const [selectedEntry, setSelectedEntry] = useState<WorkspaceDirectoryEntry | null>(null);
const [treeRefreshVersion, setTreeRefreshVersion] = useState(0);
```

项目切换时重置：

```ts
useEffect(() => {
  setTabs([]);
  setActivePath(null);
  setSelectedEntry(null);
  setTreeRefreshVersion((version) => version + 1);
}, [projectId]);
```

- [x] **Step 2: 扩展 WorkspaceFileTree props**

在 `WorkspaceFileTree.tsx` props 中加入：

```ts
selectedPath?: string | null;
refreshVersion?: number;
onSelectEntry: (entry: WorkspaceDirectoryEntry | null) => void;
onCreateFile: (parentPath: string, name: string) => Promise<void>;
onCreateDirectory: (parentPath: string, name: string) => Promise<void>;
onRenameEntry: (entry: WorkspaceDirectoryEntry, name: string) => Promise<void>;
onDeleteEntry: (entry: WorkspaceDirectoryEntry) => Promise<void>;
```

在 `FileBrowserWorkspace` 传入：

```tsx
selectedPath={selectedEntry?.path ?? null}
refreshVersion={treeRefreshVersion}
onSelectEntry={setSelectedEntry}
```

- [x] **Step 3: 实现顶部新建 handler**

在 `FileBrowserWorkspace.tsx` 中 import：

```ts
import { useQueryClient } from '@tanstack/react-query';
import { api } from '../lib/api';
import { parentPathForCreate } from './workspace-file-operations';
```

增加：

```ts
const queryClient = useQueryClient();

const refreshDirectory = useCallback((path: string) => {
  void queryClient.invalidateQueries({ queryKey: ['workspace-tree', projectId, path] });
  setTreeRefreshVersion((version) => version + 1);
}, [projectId, queryClient]);

const createFile = useCallback(async (parentPath: string, name: string) => {
  const result = await api.createWorkspaceFile(projectId, { parentPath, name });
  refreshDirectory(parentPath);
  setSelectedEntry(result.entry);
  openFile(result.entry);
}, [openFile, projectId, refreshDirectory]);

const createDirectory = useCallback(async (parentPath: string, name: string) => {
  const result = await api.createWorkspaceDirectory(projectId, { parentPath, name });
  refreshDirectory(parentPath);
  setSelectedEntry(result.entry);
}, [projectId, refreshDirectory]);
```

传入树组件：

```tsx
onCreateFile={createFile}
onCreateDirectory={createDirectory}
```

- [x] **Step 4: 在 WorkspaceFileTree 添加 inline input 状态**

增加类型：

```ts
type InlineEntryAction =
  | { type: 'create-file'; parentPath: string; error?: string }
  | { type: 'create-directory'; parentPath: string; error?: string }
  | { type: 'rename'; entry: WorkspaceDirectoryEntry; error?: string };
```

增加状态：

```ts
const [inlineAction, setInlineAction] = useState<InlineEntryAction | null>(null);
const [inlineName, setInlineName] = useState('');
```

- [x] **Step 5: 把右上角图标改为真实按钮**

把 header 里的 `aria-hidden` div 替换为：

```tsx
<div>
  <button
    type="button"
    aria-label="新建文件夹"
    onClick={() => {
      const parentPath = parentPathForCreate(selectedEntryFromTree);
      setInlineAction({ type: 'create-directory', parentPath });
      setInlineName('');
    }}
  >
    <FolderPlus aria-hidden="true" />
  </button>
  <button
    type="button"
    aria-label="新建文件"
    onClick={() => {
      const parentPath = parentPathForCreate(selectedEntryFromTree);
      setInlineAction({ type: 'create-file', parentPath });
      setInlineName('');
    }}
  >
    <FilePlus aria-hidden="true" />
  </button>
</div>
```

`selectedEntryFromTree` 使用 `visibleTreeItems[selectedPath]` 找到 data；无选择传 `null`。

- [x] **Step 6: 点击文件和文件夹时同步 selected entry**

在 `onPrimaryAction` 中：

```ts
onPrimaryAction={(item) => {
  onSelectEntry(item.data);
  if (item.data.type === 'file') onOpenFile(item.data);
}}
```

在 `onExpandItem` 和 `onCollapseItem` 中也先 `onSelectEntry(item.data)`，让点击文件夹变成选中。

- [x] **Step 7: 给 tree title 增加 selected 和 dirty 数据属性**

`WorkspaceTreeTitle` props 增加：

```ts
isSelected: boolean;
isDirty?: boolean;
```

span 增加：

```tsx
data-selected={isSelected ? 'true' : undefined}
data-dirty={isDirty ? 'true' : undefined}
```

本任务先将 `isDirty` 传 false，Task 4 接入真实 dirty。

- [x] **Step 8: 实现 inline input 渲染**

在 tree header/search 后、tree 环境前渲染：

```tsx
{inlineAction ? (
  <form
    className="deepsea-workspace-tree__inline-entry"
    onSubmit={(event) => {
      event.preventDefault();
      void submitInlineAction();
    }}
  >
    <input
      autoFocus
      value={inlineName}
      onChange={(event) => setInlineName(event.currentTarget.value)}
      onKeyDown={(event) => {
        if (event.key === 'Escape') setInlineAction(null);
      }}
      placeholder={inlineAction.type === 'create-directory' ? '文件夹名称' : '文件名称'}
    />
    {inlineAction.error ? <span>{inlineAction.error}</span> : null}
  </form>
) : null}
```

实现 `submitInlineAction`：

```ts
const submitInlineAction = async () => {
  if (!inlineAction) return;
  const error = validateWorkspaceEntryNameInput(inlineName);
  if (error) {
    setInlineAction({ ...inlineAction, error });
    return;
  }
  try {
    if (inlineAction.type === 'create-file') {
      await onCreateFile(inlineAction.parentPath, inlineName.trim());
    } else if (inlineAction.type === 'create-directory') {
      await onCreateDirectory(inlineAction.parentPath, inlineName.trim());
    } else {
      await onRenameEntry(inlineAction.entry, inlineName.trim());
    }
    setInlineAction(null);
    setInlineName('');
  } catch (error) {
    setInlineAction({
      ...inlineAction,
      error: error instanceof Error ? error.message : '操作失败',
    });
  }
};
```

- [x] **Step 9: 实现最小右键菜单**

如果项目已有 Radix DropdownMenu，可先用 fixed div 菜单，避免新增依赖。增加状态：

```ts
const [contextMenu, setContextMenu] = useState<{
  x: number;
  y: number;
  entry: WorkspaceDirectoryEntry | null;
} | null>(null);
```

给 title span 包一层或添加 `onContextMenu` 到 button title：

```tsx
onContextMenu={(event) => {
  event.preventDefault();
  onSelectEntry(item.data);
  setContextMenu({ x: event.clientX, y: event.clientY, entry: item.data });
}}
```

菜单渲染：

```tsx
{contextMenu ? (
  <div
    className="deepsea-workspace-context-menu"
    style={{ left: contextMenu.x, top: contextMenu.y }}
    role="menu"
  >
    {contextMenu.entry?.type === 'file' ? (
      <button type="button" role="menuitem" onClick={() => contextMenu.entry && onOpenFile(contextMenu.entry)}>
        打开
      </button>
    ) : null}
    <button type="button" role="menuitem" onClick={() => startCreateFromContext('file')}>新建文件</button>
    <button type="button" role="menuitem" onClick={() => startCreateFromContext('directory')}>新建文件夹</button>
    {contextMenu.entry ? (
      <button type="button" role="menuitem" onClick={() => startRename(contextMenu.entry)}>重命名</button>
    ) : null}
    {contextMenu.entry ? (
      <button type="button" role="menuitem" data-danger="true" onClick={() => void onDeleteEntry(contextMenu.entry!)}>
        删除
      </button>
    ) : null}
    {contextMenu.entry ? (
      <button type="button" role="menuitem" onClick={() => void navigator.clipboard?.writeText(contextMenu.entry!.path)}>
        复制相对路径
      </button>
    ) : null}
  </div>
) : null}
```

添加 document click/Escape 关闭菜单。

- [x] **Step 10: 实现 rename/delete handlers**

在 `FileBrowserWorkspace.tsx` import：

```ts
import { closeTabsForDeletedEntry, renameDirtyPaths, renameWorkspaceTabPaths } from './workspace-file-operations';
```

增加：

```ts
const renameEntry = useCallback(async (entry: WorkspaceDirectoryEntry, name: string) => {
  const result = await api.renameWorkspaceEntry(projectId, { path: entry.path, name });
  const parent = entry.path.split('/').slice(0, -1).join('/');
  refreshDirectory(parent);
  setTabs((current) => renameWorkspaceTabPaths(current, result.oldPath, result.newPath, entry.type));
  setActivePath((current) => {
    if (!current) return current;
    const renamed = renameWorkspaceTabPaths([{ id: '', path: current, name: '', mimeType: null, language: null, size: null, viewerKind: 'text' }], result.oldPath, result.newPath, entry.type)[0];
    return renamed?.path ?? current;
  });
  setSelectedEntry(result.entry);
}, [projectId, refreshDirectory]);

const deleteEntry = useCallback(async (entry: WorkspaceDirectoryEntry) => {
  const confirmed = window.confirm(entry.type === 'directory'
    ? `将永久删除 ${entry.path} 及其内容，是否继续？`
    : `将永久删除 ${entry.path}，是否继续？`);
  if (!confirmed) return;
  await api.deleteWorkspaceEntry(projectId, { path: entry.path });
  const parent = entry.path.split('/').slice(0, -1).join('/');
  refreshDirectory(parent);
  setTabs((current) => closeTabsForDeletedEntry(current, entry.path, entry.type));
  setSelectedEntry(null);
}, [projectId, refreshDirectory]);
```

本任务使用 `window.confirm` 作为可工作的二次确认；Task 5 替换成项目风格 Dialog。

- [x] **Step 11: 增加 CSS**

在 `session-os.css` 文件树区域添加：

```css
.deepsea-workspace-tree__header button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 22px;
  height: 22px;
  border: 0;
  border-radius: 4px;
  background: transparent;
  color: inherit;
}
.deepsea-workspace-tree__header button:hover,
.deepsea-workspace-tree__header button:focus-visible {
  outline: 0;
  background: rgba(0, 74, 198, 0.08);
  color: #004ac6;
}
.deepsea-workspace-tree__title[data-selected="true"] {
  color: #003594;
}
.deepsea-workspace-tree .rct-tree-item-button:has(.deepsea-workspace-tree__title[data-selected="true"]) {
  background: rgba(0, 74, 198, 0.07);
}
.deepsea-workspace-tree__inline-entry {
  display: grid;
  gap: 4px;
  padding: 6px 10px;
}
.deepsea-workspace-tree__inline-entry input {
  height: 24px;
  border: 1px solid rgba(0, 74, 198, 0.28);
  border-radius: 4px;
  background: rgba(255, 255, 255, 0.82);
  padding: 0 8px;
  color: #191c1e;
  font-size: 11px;
}
.deepsea-workspace-tree__inline-entry span {
  color: #b42318;
  font-size: 10px;
}
.deepsea-workspace-context-menu {
  position: fixed;
  z-index: 50;
  min-width: 156px;
  border: 1px solid rgba(255, 255, 255, 0.52);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.94);
  padding: 4px;
  box-shadow: 0 8px 18px rgba(15, 23, 42, 0.12);
}
.deepsea-workspace-context-menu button {
  display: flex;
  width: 100%;
  align-items: center;
  height: 26px;
  border: 0;
  border-radius: 5px;
  background: transparent;
  padding: 0 8px;
  color: #191c1e;
  font-size: 11px;
  text-align: left;
}
.deepsea-workspace-context-menu button:hover,
.deepsea-workspace-context-menu button:focus-visible {
  outline: 0;
  background: rgba(0, 74, 198, 0.08);
}
.deepsea-workspace-context-menu button[data-danger="true"] {
  color: #b42318;
}
```

- [x] **Step 12: 运行前端 build**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected: PASS。

- [x] **Step 13: 浏览器 smoke 文件树 CRUD**

Run dev server if needed:

```bash
npm run dev
```

Use Playwright:

```bash
playwright-cli open http://localhost:5173/projects/fsMwh7D-unbi/sessions/y8UIvt0KTB8BTPhR
```

验证：

- 点击文件夹后出现 selected 样式。
- 顶部新建文件在选中文件夹下创建。
- 右键文件可重命名。
- 右键删除弹确认，确认后树中消失。

- [x] **Step 14: 提交 Task 3**

```bash
git add packages/frontend/src/session-ui/FileBrowserWorkspace.tsx packages/frontend/src/session-ui/WorkspaceFileTree.tsx packages/frontend/src/session-ui/session-os.css
git commit -m "feat(frontend): 增加文件树管理操作"
```

---

## Task 4: Monaco 可编辑、dirty 状态和显式保存

**Files:**
- Modify: `packages/frontend/src/session-ui/FileBrowserWorkspace.tsx`
- Modify: `packages/frontend/src/session-ui/FileViewers.tsx`
- Modify: `packages/frontend/src/session-ui/WorkspaceFileTabs.tsx`
- Modify: `packages/frontend/src/session-ui/session-os.css`

- [x] **Step 1: 在 FileBrowserWorkspace 增加 DirtyFileState**

添加类型：

```ts
interface DirtyFileState {
  path: string;
  savedContent: string;
  draftContent: string;
  mtimeMs: number | null;
  saving: boolean;
  saveError: string | null;
}
```

状态：

```ts
const [dirtyByPath, setDirtyByPath] = useState<Record<string, DirtyFileState>>({});
```

- [x] **Step 2: 扩展 WorkspaceFileTabs props**

在 `WorkspaceFileTabs.tsx` props 增加：

```ts
dirtyPaths: Set<string>;
onDraftChange: (path: string, savedContent: string, draftContent: string, mtimeMs: number | null) => void;
onSave: (path: string, options?: { force?: boolean }) => Promise<void>;
```

传给 `FileViewer`：

```tsx
<FileViewer
  projectId={projectId}
  tab={tab}
  dirtyState={dirtyByPath[tab.path] ?? null}
  onDraftChange={onDraftChange}
  onSave={onSaveFile}
/>
```

- [x] **Step 3: tab 标题显示 dirty**

在 `onRenderTab` 中：

```tsx
data-dirty={tab && dirtyPaths.has(tab.path) ? 'true' : undefined}
```

标题内容增加：

```tsx
{tab && dirtyPaths.has(tab.path) ? <span className="deepsea-file-tab-title__dirty" aria-hidden="true" /> : null}
```

- [x] **Step 4: FileViewers 改为可编辑**

修改 `FileViewer` props：

```ts
export function FileViewer({
  projectId,
  tab,
  dirtyState,
  onDraftChange,
  onSave,
}: {
  projectId: string;
  tab: WorkspaceFileTab;
  dirtyState: DirtyFileState | null;
  onDraftChange: (path: string, savedContent: string, draftContent: string, mtimeMs: number | null) => void;
  onSave: (path: string, options?: { force?: boolean }) => Promise<void>;
}): JSX.Element
```

把 `MonacoTextViewer` options 改为：

```ts
readOnly: false,
```

Editor value 使用：

```tsx
value={dirtyState?.draftContent ?? data.content}
onChange={(value) => {
  onDraftChange(tab.path, data.content, value ?? '', data.mtimeMs);
}}
```

- [x] **Step 5: 加编辑器工具行和保存按钮**

在 Editor 前添加：

```tsx
<div className="deepsea-file-editor-toolbar">
  <span title={tab.path}>{tab.path}</span>
  {dirtyState ? <strong>未保存</strong> : <em>已保存</em>}
  {dirtyState?.saveError ? <small>{dirtyState.saveError}</small> : null}
  <button type="button" onClick={() => void onSave(tab.path)} disabled={!dirtyState || dirtyState.saving}>
    <Save aria-hidden="true" />
    保存
  </button>
</div>
```

从 lucide import `Save`。

- [x] **Step 6: 实现 draft change 和保存**

在 `FileBrowserWorkspace.tsx`：

```ts
const onDraftChange = useCallback((path: string, savedContent: string, draftContent: string, mtimeMs: number | null) => {
  setDirtyByPath((current) => {
    if (draftContent === savedContent) {
      const next = { ...current };
      delete next[path];
      return next;
    }
    return {
      ...current,
      [path]: {
        path,
        savedContent,
        draftContent,
        mtimeMs,
        saving: current[path]?.saving ?? false,
        saveError: null,
      },
    };
  });
}, []);

const saveFile = useCallback(async (path: string, options: { force?: boolean } = {}) => {
  const dirty = dirtyByPath[path];
  if (!dirty) return;
  setDirtyByPath((current) => ({
    ...current,
    [path]: { ...dirty, saving: true, saveError: null },
  }));
  try {
    const preview = await api.saveWorkspaceFile(projectId, {
      path,
      content: dirty.draftContent,
      expectedMtimeMs: dirty.mtimeMs,
      force: options.force,
    });
    setDirtyByPath((current) => {
      const next = { ...current };
      delete next[path];
      return next;
    });
    void queryClient.invalidateQueries({ queryKey: ['workspace-file-preview', projectId, path] });
    void queryClient.setQueryData(['workspace-file-preview', projectId, path], preview);
  } catch (error) {
    setDirtyByPath((current) => ({
      ...current,
      [path]: {
        ...dirty,
        saving: false,
        saveError: error instanceof Error ? error.message : '保存失败',
      },
    }));
  }
}, [dirtyByPath, projectId, queryClient]);
```

- [x] **Step 7: 加 Cmd/Ctrl+S**

在 `FileBrowserWorkspace.tsx` 增加 effect：

```ts
useEffect(() => {
  const onKeyDown = (event: KeyboardEvent) => {
    if (!(event.metaKey || event.ctrlKey) || event.key.toLowerCase() !== 's') return;
    event.preventDefault();
    if (activePath) void saveFile(activePath);
  };
  window.addEventListener('keydown', onKeyDown);
  return () => window.removeEventListener('keydown', onKeyDown);
}, [activePath, saveFile]);
```

- [x] **Step 8: CSS dirty 和 toolbar**

添加：

```css
.deepsea-file-tab-title[data-dirty="true"] {
  font-weight: 700;
}
.deepsea-file-tab-title__dirty {
  width: 6px;
  height: 6px;
  border-radius: 999px;
  background: #004ac6;
}
.deepsea-file-editor-toolbar {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 32px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.28);
  background: rgba(255, 255, 255, 0.36);
  padding: 0 10px;
  color: rgba(67, 70, 84, 0.72);
  font-size: 11px;
}
.deepsea-file-editor-toolbar > span {
  min-width: 0;
  flex: 1 1 auto;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.deepsea-file-editor-toolbar strong {
  color: #004ac6;
  font-size: 10px;
}
.deepsea-file-editor-toolbar em {
  color: rgba(67, 70, 84, 0.52);
  font-style: normal;
  font-size: 10px;
}
.deepsea-file-editor-toolbar small {
  color: #b42318;
  font-size: 10px;
}
.deepsea-file-editor-toolbar button {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  height: 24px;
  border: 1px solid rgba(0, 74, 198, 0.16);
  border-radius: 5px;
  background: rgba(0, 74, 198, 0.08);
  padding: 0 8px;
  color: #004ac6;
  font-size: 11px;
}
```

- [x] **Step 9: 构建验证**

Run:

```bash
npm run build -w @openclaw-room/frontend
```

Expected: PASS。

- [x] **Step 10: 浏览器 smoke 保存**

验证：

- 打开文本文件，编辑内容。
- tab 出现 dirty 标记。
- `Cmd/Ctrl+S` 后 dirty 清除。
- 刷新文件后内容保留。

- [x] **Step 11: 提交 Task 4**

```bash
git add packages/frontend/src/session-ui/FileBrowserWorkspace.tsx packages/frontend/src/session-ui/FileViewers.tsx packages/frontend/src/session-ui/WorkspaceFileTabs.tsx packages/frontend/src/session-ui/session-os.css
git commit -m "feat(frontend): 支持工作区文件编辑保存"
```

---

## Task 5: 未保存确认流、冲突处理和最终 smoke

**Files:**
- Modify: `packages/frontend/src/session-ui/FileBrowserWorkspace.tsx`
- Modify: `packages/frontend/src/session-ui/FileViewers.tsx`
- Modify: `packages/frontend/src/session-ui/WorkspaceFileTree.tsx`
- Modify: `packages/frontend/src/session-ui/session-os.css`

- [x] **Step 1: 增加 pending action 类型**

在 `FileBrowserWorkspace.tsx` 添加：

```ts
type PendingWorkspaceAction =
  | { type: 'close-tab'; path: string }
  | { type: 'delete-entry'; entry: WorkspaceDirectoryEntry }
  | { type: 'rename-entry'; entry: WorkspaceDirectoryEntry; name: string }
  | { type: 'switch-project' }
  | { type: 'save-conflict'; path: string };

type DirtyConfirmState = {
  dirtyPaths: string[];
  action: PendingWorkspaceAction;
};
```

- [x] **Step 2: 加 dirty confirm 状态和 helper**

```ts
const [dirtyConfirm, setDirtyConfirm] = useState<DirtyConfirmState | null>(null);

const runOrConfirmDirty = useCallback((dirtyPaths: string[], action: PendingWorkspaceAction) => {
  if (dirtyPaths.length === 0) {
    void runPendingAction(action);
    return;
  }
  setDirtyConfirm({ dirtyPaths, action });
}, [runPendingAction]);
```

`runPendingAction` 用 `useCallback` 实现 close/delete/rename。

- [x] **Step 3: close tab 接入 dirty confirm**

把 `closeFile` 改为：

```ts
const closeFile = useCallback((path: string) => {
  runOrConfirmDirty(dirtyByPath[path] ? [path] : [], { type: 'close-tab', path });
}, [dirtyByPath, runOrConfirmDirty]);
```

真正关闭逻辑放到：

```ts
const closeFileWithoutConfirm = useCallback((path: string) => {
  setTabs((current) => {
    const result = closeWorkspaceFileTab(current, path, activePath);
    setActivePath(result.activePath);
    return result.tabs;
  });
  setDirtyByPath((current) => {
    const next = { ...current };
    delete next[path];
    return next;
  });
}, [activePath]);
```

- [x] **Step 4: delete/rename 接入 dirty confirm**

删除前：

```ts
const dirtyPaths = dirtyFilesUnderEntry(dirtyByPath, entry.path, entry.type);
runOrConfirmDirty(dirtyPaths, { type: 'delete-entry', entry });
```

重命名前：

```ts
const dirtyPaths = dirtyFilesUnderEntry(dirtyByPath, entry.path, entry.type);
runOrConfirmDirty(dirtyPaths, { type: 'rename-entry', entry, name });
```

- [x] **Step 5: 实现确认 Dialog**

先用项目已有 Dialog primitives；若没有局部封装，用 `@radix-ui/react-dialog` 直接实现。

Dialog 内容：

```tsx
{dirtyConfirm ? (
  <Dialog.Root open onOpenChange={(open) => !open && setDirtyConfirm(null)}>
    <Dialog.Portal>
      <Dialog.Overlay className="deepsea-workspace-dialog-overlay" />
      <Dialog.Content className="deepsea-workspace-dialog">
        <Dialog.Title>存在未保存修改</Dialog.Title>
        <p>以下文件有未保存修改：</p>
        <ul>
          {dirtyConfirm.dirtyPaths.slice(0, 5).map((path) => <li key={path}>{path}</li>)}
        </ul>
        {dirtyConfirm.dirtyPaths.length > 5 ? <p>还有 {dirtyConfirm.dirtyPaths.length - 5} 个文件</p> : null}
        <div>
          <button type="button" onClick={() => void saveDirtyAndContinue(dirtyConfirm)}>保存并继续</button>
          <button type="button" onClick={() => discardDirtyAndContinue(dirtyConfirm)}>放弃并继续</button>
          <button type="button" onClick={() => setDirtyConfirm(null)}>取消</button>
        </div>
      </Dialog.Content>
    </Dialog.Portal>
  </Dialog.Root>
) : null}
```

- [x] **Step 6: 实现保存全部/放弃全部继续**

```ts
const saveDirtyAndContinue = async (state: DirtyConfirmState) => {
  for (const path of state.dirtyPaths) {
    await saveFile(path);
  }
  setDirtyConfirm(null);
  await runPendingAction(state.action);
};

const discardDirtyAndContinue = (state: DirtyConfirmState) => {
  setDirtyByPath((current) => {
    const next = { ...current };
    state.dirtyPaths.forEach((path) => delete next[path]);
    return next;
  });
  setDirtyConfirm(null);
  void runPendingAction(state.action);
};
```

- [x] **Step 7: 加 beforeunload**

```ts
useEffect(() => {
  const onBeforeUnload = (event: BeforeUnloadEvent) => {
    if (Object.keys(dirtyByPath).length === 0) return;
    event.preventDefault();
    event.returnValue = '';
  };
  window.addEventListener('beforeunload', onBeforeUnload);
  return () => window.removeEventListener('beforeunload', onBeforeUnload);
}, [dirtyByPath]);
```

- [x] **Step 8: 保存冲突 Dialog**

在 `saveFile` catch 中判断：

```ts
const message = error instanceof Error ? error.message : '保存失败';
if (message.includes('WORKSPACE_FILE_CONFLICT')) {
  setDirtyConfirm({ dirtyPaths: [path], action: { type: 'save-conflict', path } });
  return;
}
```

冲突 Dialog 可以复用 dirty confirm 的结构，但按钮改为：

- `重新加载磁盘版本`
- `强制覆盖`
- `取消`

最小实现可以在 `dirtyConfirm.action.type === 'save-conflict'` 时渲染专门按钮。

- [x] **Step 9: Dialog CSS**

```css
.deepsea-workspace-dialog-overlay {
  position: fixed;
  inset: 0;
  z-index: 80;
  background: rgba(15, 23, 42, 0.22);
}
.deepsea-workspace-dialog {
  position: fixed;
  top: 50%;
  left: 50%;
  z-index: 81;
  width: min(420px, calc(100vw - 32px));
  transform: translate(-50%, -50%);
  border: 1px solid rgba(255, 255, 255, 0.52);
  border-radius: 8px;
  background: #f7f9fb;
  padding: 16px;
  color: #191c1e;
  box-shadow: 0 18px 32px rgba(15, 23, 42, 0.18);
}
.deepsea-workspace-dialog h2 {
  margin: 0 0 8px;
  font-size: 14px;
}
.deepsea-workspace-dialog p,
.deepsea-workspace-dialog li {
  color: rgba(67, 70, 84, 0.78);
  font-size: 12px;
}
.deepsea-workspace-dialog div:last-child {
  display: flex;
  justify-content: flex-end;
  gap: 8px;
  margin-top: 14px;
}
```

- [x] **Step 10: 最终自动化验证**

Run:

```bash
npm exec -w @openclaw-room/backend -- tsx src/workspace-files.test.ts
npm exec -w @openclaw-room/frontend -- tsx src/session-ui/workspace-file-operations.test.ts
npm exec -w @openclaw-room/frontend -- tsx src/session-ui/workspace-file-model.test.ts
npm exec -w @openclaw-room/frontend -- tsx src/lib/api.test.ts
npm run build -w @openclaw-room/backend
npm run build -w @openclaw-room/frontend
```

Expected: all commands PASS；frontend build 允许既有 chunk size warning。

- [x] **Step 11: 浏览器最终 smoke**

在本地 route 验证：

```text
http://localhost:5173/projects/fsMwh7D-unbi/sessions/y8UIvt0KTB8BTPhR
```

Checklist：

- 文件树点击文件和文件夹都有选中态。
- 顶部新建文件和新建文件夹真实可用。
- 选中文件夹时在该文件夹下新建。
- 选中文件时在其父目录下新建。
- 无选择时在根目录新建。
- 右键菜单可新建、重命名、删除、复制相对路径。
- 重名时提示错误且不覆盖。
- 删除前有二次确认。
- Monaco 可编辑。
- `Cmd/Ctrl+S` 保存后刷新仍保留内容。
- dirty close 出现保存 / 放弃 / 取消。

- [x] **Step 12: 提交 Task 5**

```bash
git add packages/frontend/src/session-ui/FileBrowserWorkspace.tsx packages/frontend/src/session-ui/FileViewers.tsx packages/frontend/src/session-ui/WorkspaceFileTree.tsx packages/frontend/src/session-ui/session-os.css
git commit -m "feat(frontend): 完成文件编辑未保存确认流"
```

---

## Final Verification

- [x] **Step 1: 检查 git 状态，只剩用户无关脏文件**

```bash
git status --short
```

Expected: 本计划相关文件均已提交；未提交项只包含计划开始前已存在的无关后端/截图/tmp 文件。

- [x] **Step 2: 运行最终验证命令**

```bash
npm exec -w @openclaw-room/backend -- tsx src/workspace-files.test.ts
npm exec -w @openclaw-room/frontend -- tsx src/session-ui/workspace-file-operations.test.ts
npm exec -w @openclaw-room/frontend -- tsx src/session-ui/workspace-file-model.test.ts
npm exec -w @openclaw-room/frontend -- tsx src/lib/api.test.ts
npm run build -w @openclaw-room/backend
npm run build -w @openclaw-room/frontend
```

Expected: all PASS；frontend chunk warning 可记录为既有风险。

- [x] **Step 3: 浏览器 smoke 截图和 DOM 验证**

使用 Playwright CLI 验证主要用户流。截图只作为本地证据，不提交：

```bash
playwright-cli open http://localhost:5173/projects/fsMwh7D-unbi/sessions/y8UIvt0KTB8BTPhR
```

Expected:

- 文件树 selected 和 active 状态可区分。
- 新建/重命名/删除真实改变磁盘和树。
- Monaco dirty/save 状态正确。
- 未保存确认流可阻断破坏性操作。

- [x] **Step 4: 最终提交总结**

确认所有实现任务提交完成后，不要自动 merge 或 push。给用户报告：

- 完成的提交列表。
- 验证命令和结果。
- 浏览器 smoke 结果。
- 遗留风险或明确无遗留风险。
