# Electron 无边框窗口与内嵌窗口控制 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Electron 版去掉系统原生标题栏，并把最小化、最大化/还原、关闭控件内嵌到现有 AppShell 顶部栏中。

**Architecture:** Electron main 进程负责无边框窗口配置、窗口状态读取、窗口生命周期 IPC 和状态事件广播。Preload 在现有 `window.openDeepSeaDesktop` contract 上暴露平台与窗口控制 API。React `AppShell` 只在 Electron 环境渲染窗口按钮，并通过 CSS 把顶部栏划分为窗口拖拽区与交互点击区。

**Tech Stack:** Electron `BrowserWindow` / `ipcMain` / `contextBridge`、React 18、TypeScript、lucide-react、node:test、tsx、Vite workspace build。

---

## 文件结构

- Modify: `packages/desktop/src/main.ts`
  - `BrowserWindow` 启用 `frame: false`。
  - 新增窗口控制 IPC channel 常量、窗口状态序列化、状态广播。
  - 注册最小化、最大化/还原、关闭、读取状态 handler。
- Modify: `packages/desktop/src/preload.ts`
  - 在 `window.openDeepSeaDesktop` 中补充 `platform` 与窗口控制 API。
  - 为窗口状态订阅提供 unsubscribe 函数。
- Modify: `packages/frontend/src/vite-env.d.ts`
  - 扩展 `Window.openDeepSeaDesktop` 类型，包含 `platform`、窗口状态和窗口控制方法。
- Modify: `packages/frontend/src/components/AppShell.tsx`
  - 新增 `DesktopWindowControls`、`useDesktopWindowState`、Electron 环境判断。
  - macOS 控件放在左侧品牌前，Windows/Linux 控件放在右侧 actions 后。
  - 使用 `Minus`、`Square`、`Copy`、`X` 图标。
- Modify: `packages/frontend/src/components/AppShell.test.tsx`
  - 增加 Web 环境隐藏窗口控件测试。
  - 增加 macOS 左侧窗口控件测试。
  - 增加 Windows/Linux 右侧窗口控件测试。
  - 增加最大化状态 label 测试。
- Modify: `packages/frontend/src/session-ui/session-os.css`
  - 顶部栏 Electron 环境启用 `-webkit-app-region: drag`。
  - 所有顶部栏交互元素启用 `-webkit-app-region: no-drag`。
  - 增加窗口控制按钮布局、hover/focus、关闭按钮危险色反馈。
- Modify: `docs/superpowers/verification/2026-06-09-Electron可安装MVP验收.md`
  - 记录本轮无边框窗口验证结果。

## Task 1: Electron 窗口配置与窗口控制 IPC

**Files:**
- Modify: `packages/desktop/src/main.ts`

- [ ] **Step 1: 写入窗口 IPC contract 和状态 helper**

在 `LOCAL_TOKEN_CHANNEL` 下方加入以下常量和类型：

```ts
const DESKTOP_WINDOW_CHANNELS = {
  getWindowState: 'opendeepsea:get-window-state',
  minimizeWindow: 'opendeepsea:minimize-window',
  toggleMaximizeWindow: 'opendeepsea:toggle-maximize-window',
  closeWindow: 'opendeepsea:close-window',
  windowStateChanged: 'opendeepsea:window-state-changed',
} as const;

type DesktopWindowState = {
  isMaximized: boolean;
  isFullScreen: boolean;
};
```

在 `ipcMain.handle(DESKTOP_DATA_CHANNELS.restartApp, ...)` 后方加入窗口 handler：

```ts
ipcMain.handle(DESKTOP_WINDOW_CHANNELS.getWindowState, (event): DesktopWindowState => {
  return getWindowState(BrowserWindow.fromWebContents(event.sender));
});
ipcMain.handle(DESKTOP_WINDOW_CHANNELS.minimizeWindow, (event) => {
  BrowserWindow.fromWebContents(event.sender)?.minimize();
  return getWindowState(BrowserWindow.fromWebContents(event.sender));
});
ipcMain.handle(DESKTOP_WINDOW_CHANNELS.toggleMaximizeWindow, (event) => {
  const win = BrowserWindow.fromWebContents(event.sender);
  if (!win) return getWindowState(null);
  if (win.isMaximized()) {
    win.unmaximize();
  } else {
    win.maximize();
  }
  return getWindowState(win);
});
ipcMain.handle(DESKTOP_WINDOW_CHANNELS.closeWindow, (event) => {
  BrowserWindow.fromWebContents(event.sender)?.close();
  return { ok: true };
});
```

在 `createMainWindow()` 前加入 helper：

```ts
function getWindowState(win: BrowserWindow | null): DesktopWindowState {
  return {
    isMaximized: win?.isMaximized() ?? false,
    isFullScreen: win?.isFullScreen() ?? false,
  };
}

function emitWindowState(win: BrowserWindow): void {
  if (win.webContents.isDestroyed()) return;
  win.webContents.send(DESKTOP_WINDOW_CHANNELS.windowStateChanged, getWindowState(win));
}
```

- [ ] **Step 2: 启用无边框 BrowserWindow 并广播状态变化**

在 `createMainWindow()` 的 `new BrowserWindow({ ... })` options 中加入：

```ts
frame: false,
```

在 `win.once('ready-to-show', ...)` 后加入：

```ts
  win.on('maximize', () => emitWindowState(win));
  win.on('unmaximize', () => emitWindowState(win));
  win.on('enter-full-screen', () => emitWindowState(win));
  win.on('leave-full-screen', () => emitWindowState(win));
  win.on('ready-to-show', () => emitWindowState(win));
```

- [ ] **Step 3: 运行桌面构建验证**

Run:

```bash
npm run build -w @openclaw-room/desktop
```

Expected: PASS。若失败是 TypeScript 类型错误，修正本任务新增的 main 进程代码。

- [ ] **Step 4: 提交 Electron main 改动**

```bash
git add packages/desktop/src/main.ts
git commit -m "feat(desktop): 增加无边框窗口控制IPC"
```

## Task 2: Preload 与前端类型 contract

**Files:**
- Modify: `packages/desktop/src/preload.ts`
- Modify: `packages/frontend/src/vite-env.d.ts`

- [ ] **Step 1: 扩展前端类型声明**

在 `packages/frontend/src/vite-env.d.ts` 的数据目录类型之后加入：

```ts
type OpenDeepSeaDesktopPlatform = 'darwin' | 'win32' | 'linux' | string;

type OpenDeepSeaDesktopWindowState = {
  isMaximized: boolean;
  isFullScreen: boolean;
};
```

在 `Window.openDeepSeaDesktop` 内加入：

```ts
    platform: OpenDeepSeaDesktopPlatform;
    getWindowState: () => Promise<OpenDeepSeaDesktopWindowState>;
    minimizeWindow: () => Promise<OpenDeepSeaDesktopWindowState>;
    toggleMaximizeWindow: () => Promise<OpenDeepSeaDesktopWindowState>;
    closeWindow: () => Promise<{ ok: true }>;
    onWindowStateChanged: (listener: (state: OpenDeepSeaDesktopWindowState) => void) => () => void;
```

- [ ] **Step 2: 扩展 preload API**

在 `packages/desktop/src/preload.ts` 的 `TOKEN_CHANNEL` 下方加入：

```ts
const DESKTOP_WINDOW_CHANNELS = {
  getWindowState: 'opendeepsea:get-window-state',
  minimizeWindow: 'opendeepsea:minimize-window',
  toggleMaximizeWindow: 'opendeepsea:toggle-maximize-window',
  closeWindow: 'opendeepsea:close-window',
  windowStateChanged: 'opendeepsea:window-state-changed',
} as const;

type DesktopWindowState = {
  isMaximized: boolean;
  isFullScreen: boolean;
};
```

在 `contextBridge.exposeInMainWorld('openDeepSeaDesktop', { ... })` 对象开头加入：

```ts
  platform: process.platform,
  getWindowState: () =>
    ipcRenderer.invoke(DESKTOP_WINDOW_CHANNELS.getWindowState) as Promise<DesktopWindowState>,
  minimizeWindow: () =>
    ipcRenderer.invoke(DESKTOP_WINDOW_CHANNELS.minimizeWindow) as Promise<DesktopWindowState>,
  toggleMaximizeWindow: () =>
    ipcRenderer.invoke(DESKTOP_WINDOW_CHANNELS.toggleMaximizeWindow) as Promise<DesktopWindowState>,
  closeWindow: () =>
    ipcRenderer.invoke(DESKTOP_WINDOW_CHANNELS.closeWindow) as Promise<{ ok: true }>,
  onWindowStateChanged: (listener: (state: DesktopWindowState) => void) => {
    const handler = (_event: Electron.IpcRendererEvent, state: DesktopWindowState) => listener(state);
    ipcRenderer.on(DESKTOP_WINDOW_CHANNELS.windowStateChanged, handler);
    return () => ipcRenderer.removeListener(DESKTOP_WINDOW_CHANNELS.windowStateChanged, handler);
  },
```

- [ ] **Step 3: 运行桌面与前端类型构建**

Run:

```bash
npm run build -w @openclaw-room/desktop
npm run build -w @openclaw-room/frontend
```

Expected: 两条命令均 PASS。

- [ ] **Step 4: 提交 preload 和类型 contract**

```bash
git add packages/desktop/src/preload.ts packages/frontend/src/vite-env.d.ts
git commit -m "feat(desktop): 暴露窗口控制预加载API"
```

## Task 3: AppShell 内嵌窗口控制

**Files:**
- Modify: `packages/frontend/src/components/AppShell.tsx`
- Modify: `packages/frontend/src/components/AppShell.test.tsx`

- [ ] **Step 1: 写失败测试**

在 `AppShell.test.tsx` 的现有 AppShell header 测试后加入：

```tsx
test('AppShell hides desktop window controls in web runtime', () => {
  deleteDesktopApi();

  const html = renderAppShell('/projects/project-1');

  assert.doesNotMatch(html, /desktop-window-controls/);
  assert.doesNotMatch(html, /最小化窗口/);
});

test('AppShell renders macOS window controls before brand identity', () => {
  installDesktopApi({ platform: 'darwin', isMaximized: false });

  const html = renderAppShell('/projects/project-1');

  assert.match(html, /desktop-window-controls desktop-window-controls--mac/);
  assert.match(html, /aria-label="最小化窗口"/);
  assert.match(html, /aria-label="最大化窗口"/);
  assert.ok(html.indexOf('desktop-window-controls--mac') < html.indexOf('deepsea-brand'));
});

test('AppShell renders Windows window controls after action icons', () => {
  installDesktopApi({ platform: 'win32', isMaximized: false });

  const html = renderAppShell('/projects/project-1');

  assert.match(html, /desktop-window-controls desktop-window-controls--system/);
  assert.match(html, /aria-label="最小化窗口"/);
  assert.match(html, /aria-label="最大化窗口"/);
  assert.match(html, /aria-label="关闭窗口"/);
  assert.ok(html.indexOf('deepsea-action-icons') < html.indexOf('desktop-window-controls--system'));
});

test('AppShell labels maximized state as restore action', () => {
  const source = readFileSync(new URL('./AppShell.tsx', import.meta.url), 'utf8');

  assert.match(source, /windowState\.isMaximized \? '还原窗口' : '最大化窗口'/);
  assert.match(source, /windowState\.isMaximized \? Copy : Square/);
});
```

在文件底部 `renderAppShell()` 前加入 helper：

```ts
function installDesktopApi({
  platform,
  isMaximized,
}: {
  platform: string;
  isMaximized: boolean;
}): void {
  Object.defineProperty(globalThis, 'window', {
    configurable: true,
    value: {
      openDeepSeaDesktop: {
        platform,
        getWindowState: async () => ({ isMaximized, isFullScreen: false }),
        minimizeWindow: async () => ({ isMaximized, isFullScreen: false }),
        toggleMaximizeWindow: async () => ({ isMaximized: !isMaximized, isFullScreen: false }),
        closeWindow: async () => ({ ok: true }),
        onWindowStateChanged: () => () => undefined,
      },
    },
  });
}

function deleteDesktopApi(): void {
  Object.defineProperty(globalThis, 'window', {
    configurable: true,
    value: {},
  });
}
```

- [ ] **Step 2: 运行失败测试**

Run:

```bash
cd packages/frontend && node --import tsx --test src/components/AppShell.test.tsx
```

Expected: FAIL，至少 macOS/Windows 控件相关断言失败，因为 `DesktopWindowControls` 尚未实现。

- [ ] **Step 3: 实现 AppShell 控件**

在 `AppShell.tsx` 的 lucide import 中加入：

```ts
  Copy,
  Minus,
  Square,
  X,
```

在 `AppShell` 组件内 `const themeStyle = getThemeStyle(theme);` 后加入：

```ts
  const desktopApi = getDesktopApi();
  const isMacDesktop = desktopApi?.platform === 'darwin';
```

在 `<div className="deepsea-topbar__identity">` 内、品牌 `NavLink` 前加入：

```tsx
          {isMacDesktop && <DesktopWindowControls placement="mac" />}
```

在 `<div className="deepsea-topbar__actions">` 内、`</div>` 关闭前加入：

```tsx
          {desktopApi && !isMacDesktop && <DesktopWindowControls placement="system" />}
```

在 `HeaderNavItem` interface 前加入：

```tsx
type DesktopApi = NonNullable<Window['openDeepSeaDesktop']>;

function getDesktopApi(): DesktopApi | null {
  if (typeof window === 'undefined') return null;
  const api = window.openDeepSeaDesktop;
  if (!api?.minimizeWindow || !api.toggleMaximizeWindow || !api.closeWindow) return null;
  return api;
}

function DesktopWindowControls({ placement }: { placement: 'mac' | 'system' }): JSX.Element | null {
  const desktopApi = getDesktopApi();
  const [windowState, setWindowState] = useState(() => ({
    isMaximized: false,
    isFullScreen: false,
  }));

  useEffect(() => {
    if (!desktopApi) return undefined;
    let disposed = false;
    const unsubscribe = desktopApi.onWindowStateChanged?.((state) => setWindowState(state));
    void desktopApi.getWindowState?.().then((state) => {
      if (!disposed) setWindowState(state);
    });
    return () => {
      disposed = true;
      unsubscribe?.();
    };
  }, [desktopApi]);

  if (!desktopApi) return null;

  const maximizeLabel = windowState.isMaximized ? '还原窗口' : '最大化窗口';
  const MaximizeIcon = windowState.isMaximized ? Copy : Square;

  return (
    <div className={cn('desktop-window-controls', placement === 'mac' ? 'desktop-window-controls--mac' : 'desktop-window-controls--system')} aria-label="窗口控制">
      <button type="button" className="desktop-window-control" aria-label="最小化窗口" onClick={() => void desktopApi.minimizeWindow()}>
        <Minus aria-hidden="true" />
      </button>
      <button type="button" className="desktop-window-control" aria-label={maximizeLabel} onClick={() => void desktopApi.toggleMaximizeWindow()}>
        <MaximizeIcon aria-hidden="true" />
      </button>
      <button type="button" className="desktop-window-control desktop-window-control--close" aria-label="关闭窗口" onClick={() => void desktopApi.closeWindow()}>
        <X aria-hidden="true" />
      </button>
    </div>
  );
}
```

- [ ] **Step 4: 运行 AppShell 测试确认通过**

Run:

```bash
cd packages/frontend && node --import tsx --test src/components/AppShell.test.tsx
```

Expected: PASS。

- [ ] **Step 5: 提交 AppShell 控件**

```bash
git add packages/frontend/src/components/AppShell.tsx packages/frontend/src/components/AppShell.test.tsx
git commit -m "feat(frontend): 内嵌桌面窗口控制"
```

## Task 4: 顶部栏拖拽区与窗口控制样式

**Files:**
- Modify: `packages/frontend/src/components/AppShell.test.tsx`
- Modify: `packages/frontend/src/session-ui/session-os.css`

- [ ] **Step 1: 写 CSS 结构测试**

在 `AppShell.test.tsx` 现有 source-based 测试后加入：

```ts
test('desktop window controls CSS defines drag and no-drag regions', () => {
  const css = readFileSync(new URL('../session-ui/session-os.css', import.meta.url), 'utf8');

  assert.match(css, /\.deepsea-topbar[\s\S]*-webkit-app-region: drag/);
  assert.match(css, /\.deepsea-topbar :where\([\s\S]*-webkit-app-region: no-drag/);
  assert.match(css, /\.desktop-window-controls[\s\S]*-webkit-app-region: no-drag/);
  assert.match(css, /\.desktop-window-control--close:hover/);
});
```

- [ ] **Step 2: 运行失败测试**

Run:

```bash
cd packages/frontend && node --import tsx --test src/components/AppShell.test.tsx
```

Expected: FAIL，提示 CSS 中缺少 drag/no-drag 或 window controls 样式。

- [ ] **Step 3: 实现 CSS**

在 `packages/frontend/src/session-ui/session-os.css` 的 `.deepsea-topbar { ... }` 规则内加入：

```css
  -webkit-app-region: drag;
```

在 `.deepsea-topbar__identity { ... }` 规则后加入：

```css
.deepsea-topbar :where(a, button, input, textarea, select, [role="button"], [role="menu"], [role="menuitem"]) {
  -webkit-app-region: no-drag;
}
```

在 `.deepsea-action-icons { ... }` 后加入：

```css
.desktop-window-controls {
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  gap: 2px;
  -webkit-app-region: no-drag;
}

.desktop-window-controls--mac {
  margin-right: 6px;
}

.desktop-window-controls--system {
  margin-left: 6px;
}

.desktop-window-control {
  display: grid;
  place-items: center;
  width: 34px;
  height: 34px;
  border: 0;
  border-radius: 8px;
  background: transparent;
  color: var(--deepsea-muted);
  transition: background 160ms, color 160ms;
}

.desktop-window-control:hover,
.desktop-window-control:focus-visible {
  background: rgba(255, 255, 255, 0.62);
  color: var(--deepsea-primary);
}

.desktop-window-control--close:hover,
.desktop-window-control--close:focus-visible {
  background: color-mix(in srgb, var(--deepsea-danger) 14%, transparent);
  color: var(--deepsea-danger);
}

.desktop-window-control svg {
  width: 15px;
  height: 15px;
}
```

- [ ] **Step 4: 运行前端测试确认通过**

Run:

```bash
cd packages/frontend && node --import tsx --test src/components/AppShell.test.tsx
```

Expected: PASS。

- [ ] **Step 5: 提交 CSS 拖拽与样式改动**

```bash
git add packages/frontend/src/components/AppShell.test.tsx packages/frontend/src/session-ui/session-os.css
git commit -m "feat(frontend): 支持桌面顶部栏拖拽区"
```

## Task 5: 构建、烟测记录与收尾审查

**Files:**
- Modify: `docs/superpowers/verification/2026-06-09-Electron可安装MVP验收.md`

- [ ] **Step 1: 运行目标测试与构建**

Run:

```bash
cd packages/frontend && node --import tsx --test src/components/AppShell.test.tsx
npm run build -w @openclaw-room/frontend
npm run build -w @openclaw-room/desktop
```

Expected: 三条命令均 PASS。

- [ ] **Step 2: 桌面运行烟测**

Run:

```bash
npm run dev:desktop
```

Expected:

- Electron 窗口打开后没有系统原生标题栏。
- 当前 macOS 开发机窗口按钮在顶部栏左侧。
- 顶部栏空白区域可拖动窗口。
- 顶部栏导航、设置、通知、菜单可以点击。
- 最小化、最大化/还原、关闭按钮可以调用对应窗口行为。

烟测结束后停止本任务启动的 dev 进程。

- [ ] **Step 3: 更新验收记录**

在 `docs/superpowers/verification/2026-06-09-Electron可安装MVP验收.md` 末尾追加：

```md
## 2026-06-09 无边框窗口与内嵌窗口控制

- `cd packages/frontend && node --import tsx --test src/components/AppShell.test.tsx`：PASS。
- `npm run build -w @openclaw-room/frontend`：PASS。
- `npm run build -w @openclaw-room/desktop`：PASS。
- `npm run dev:desktop` smoke：Electron 窗口无系统标题栏，macOS 控件位于左侧，顶部栏拖拽、导航点击、设置点击、通知点击、菜单点击、最小化、最大化/还原、关闭均通过。
```

如果 smoke 因本机环境无法运行，记录实际失败原因和已完成的替代验证，不写 PASS。

- [ ] **Step 4: 代码审查**

审查重点：

- `frame: false` 是否只影响 Electron 窗口。
- IPC handler 是否只控制发起调用的窗口。
- preload 是否没有暴露 Electron 对象本身。
- Web 环境缺少 `window.openDeepSeaDesktop` 时是否不渲染、不报错。
- CSS `-webkit-app-region: drag` 是否只在顶部栏使用，交互元素是否覆盖 `no-drag`。
- AppShell 是否保持顶部栏高度和现有导航结构。

- [ ] **Step 5: 提交验证记录和最终修复**

```bash
git add docs/superpowers/verification/2026-06-09-Electron可安装MVP验收.md
git status --short
git commit -m "docs(desktop): 记录无边框窗口验收"
```

只暂存本任务相关文件。若 `git status --short` 显示其他无关改动，保持未暂存。

## Plan Self-Review

- 规格目标覆盖：Electron 无边框窗口由 Task 1 覆盖；preload API 与平台字段由 Task 2 覆盖；AppShell 内嵌控件与平台位置由 Task 3 覆盖；拖拽区和 no-drag 点击区由 Task 4 覆盖；构建、smoke 和验收记录由 Task 5 覆盖。
- 非目标约束：计划不改后端 HTTP API，不实现托盘、菜单栏、全屏沉浸、自动更新，也不在 Web 环境模拟窗口按钮。
- 类型一致性：计划统一使用 `DesktopWindowState` / `OpenDeepSeaDesktopWindowState`，字段为 `isMaximized` 和 `isFullScreen`；窗口 channel 名称在 main 与 preload 中保持一致。
- 工作区安全：计划每次提交都显式列出本任务相关文件，避免暂存当前工作区已有知识库和截图改动。
