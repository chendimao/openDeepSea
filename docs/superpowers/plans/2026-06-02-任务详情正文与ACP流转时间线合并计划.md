# 任务详情正文与 ACP 流转时间线合并 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 将任务详情中的任务正文并入 `timeline` 视图，与 ACP 流转记录按时间从上到下展示。

**Architecture:** 前端在 `TaskDetailPanel.tsx` 内新增本地 timeline item 模型，把 `task.description` 映射为正文条目，再与现有 `TaskEvent` 合并排序渲染。后端 API、数据库和事件采集逻辑保持不变。

**Tech Stack:** React 18, TypeScript, Vite, Node `node:test`, `react-dom/server` render tests。

---

## 文件结构

- Modify: `packages/frontend/src/components/TaskDetailPanel.tsx`
  - 新增任务正文 timeline item 类型和构建函数。
  - 新增合并排序函数。
  - 新增支持正文条目的 timeline 渲染组件，复用现有事件渲染逻辑。
  - 移除独立描述区块。
- Modify: `packages/frontend/src/components/TaskDetailPanel.test.ts`
  - 覆盖合并排序和 `chat` layer 隐藏行为。
- Modify: `packages/frontend/src/components/TaskDetailPanel.render.test.tsx`
  - 覆盖渲染结果：正文与事件在同一 timeline 容器内展示。

---

### Task 1: 添加纯函数测试

**Files:**
- Modify: `packages/frontend/src/components/TaskDetailPanel.test.ts`
- Modify: `packages/frontend/src/components/TaskDetailPanel.tsx`

- [x] **Step 1: 写失败测试**

在 `packages/frontend/src/components/TaskDetailPanel.test.ts` 的 import 中加入计划暴露的函数：

```ts
import {
  buildTaskTimelineItems,
  describeTaskEvent,
  selectTaskDetailEvents,
  type TaskLayerVisibility,
} from './TaskDetailPanel';
```

在现有测试后新增：

```ts
test('buildTaskTimelineItems merges task body with events by created_at ascending', () => {
  const task = createTask({
    description: '用户回复正文',
    created_at: 10,
  });
  const laterEvent = createEvent('runtime_event', 'runtime', { command: 'npm test' });
  laterEvent.id = 'event-later';
  laterEvent.created_at = 20;
  const earlierEvent = createEvent('task_created', 'activity');
  earlierEvent.id = 'event-earlier';
  earlierEvent.created_at = 5;

  const items = buildTaskTimelineItems(task, [laterEvent, earlierEvent], visible);

  assert.deepEqual(items.map((item) => item.id), ['event-earlier', 'task-body-task-1', 'event-later']);
  assert.equal(items[1]?.kind, 'body');
});

test('buildTaskTimelineItems hides task body when chat layer is disabled', () => {
  const task = createTask({
    description: '这段正文不应展示',
    created_at: 10,
  });

  const items = buildTaskTimelineItems(task, [], {
    ...visible,
    chat: false,
  });

  assert.deepEqual(items, []);
});
```

在测试文件底部新增 helper：

```ts
function createTask(overrides: Partial<Task> = {}): Task {
  return {
    id: 'task-1',
    room_id: 'room-1',
    title: '测试任务',
    description: null,
    status: 'todo',
    priority: 'normal',
    interaction_mode: 'ask_user',
    created_from: 'manual',
    source_message_id: null,
    assigned_agent_id: null,
    completed_at: null,
    created_at: 1,
    updated_at: 1,
    ...overrides,
  };
}
```

同时把测试文件类型 import 改为：

```ts
import type { MessageLayer, Task, TaskEvent, TaskEventType } from '../lib/types';
```

- [x] **Step 2: 运行测试确认失败**

Run:

```bash
node --import tsx packages/frontend/src/components/TaskDetailPanel.test.ts
```

Expected: FAIL，错误包含 `buildTaskTimelineItems` 未导出或未定义。

- [x] **Step 3: 添加最小类型和纯函数实现**

在 `packages/frontend/src/components/TaskDetailPanel.tsx` 中 `PLAN_EVENT_TYPES` 后添加：

```ts
export type TaskTimelineItem =
  | {
      kind: 'body';
      id: string;
      layer: Extract<MessageLayer, 'chat'>;
      created_at: number;
      content: string;
    }
  | {
      kind: 'event';
      id: string;
      layer: MessageLayer;
      created_at: number;
      event: TaskEvent;
    };

export function buildTaskTimelineItems(
  task: Task,
  events: TaskEvent[],
  layerVisibility: TaskLayerVisibility,
): TaskTimelineItem[] {
  const body = task.description?.trim();
  const items: TaskTimelineItem[] = events.map((event) => ({
    kind: 'event',
    id: event.id,
    layer: event.layer,
    created_at: event.created_at,
    event,
  }));

  if (body && layerVisibility.chat) {
    items.push({
      kind: 'body',
      id: `task-body-${task.id}`,
      layer: 'chat',
      created_at: task.created_at,
      content: body,
    });
  }

  return items.sort((left, right) => {
    if (left.created_at !== right.created_at) return left.created_at - right.created_at;
    if (left.kind === right.kind) return left.id.localeCompare(right.id);
    return left.kind === 'body' ? -1 : 1;
  });
}
```

- [x] **Step 4: 运行纯函数测试确认通过**

Run:

```bash
node --import tsx packages/frontend/src/components/TaskDetailPanel.test.ts
```

Expected: PASS，所有 `TaskDetailPanel.test.ts` 测试通过。

---

### Task 2: 合并渲染任务详情 Timeline

**Files:**
- Modify: `packages/frontend/src/components/TaskDetailPanel.tsx`
- Modify: `packages/frontend/src/components/TaskDetailPanel.render.test.tsx`

- [x] **Step 1: 写渲染失败测试**

在 `packages/frontend/src/components/TaskDetailPanel.render.test.tsx` import 中加入 `TaskTimeline`：

```ts
import { TaskEventTimeline, TaskExecutorSessions, TaskLayerToggles, TaskTimeline, type TaskLayerVisibility } from './TaskDetailPanel';
```

在文件中新增：

```ts
test('TaskTimeline renders task body and task events in one timeline container', () => {
  const html = renderToStaticMarkup(
    <TaskTimeline
      items={[
        {
          kind: 'body',
          id: 'task-body-task-1',
          layer: 'chat',
          created_at: 10,
          content: '用户回复正文应该进入时间线',
        },
        {
          kind: 'event',
          id: 'runtime-event',
          layer: 'runtime',
          created_at: 20,
          event: {
            id: 'runtime-event',
            task_id: 'task-1',
            room_id: 'room-1',
            seq: 2,
            type: 'runtime_event',
            layer: 'runtime',
            payload: {
              command: 'npm test',
            },
            source_run_id: null,
            created_at: 20,
          },
        },
      ]}
      formatRelativeTime={(timestamp) => `${timestamp}`}
      t={(key) => key}
    />,
  );

  assert.match(html, /用户回复正文应该进入时间线/);
  assert.match(html, /npm test/);
  assert.match(html, /task-event-timeline/);
  assert.doesNotMatch(html, /taskDetail.description/);
});
```

这个测试刻意渲染 `TaskTimeline` 而不是完整 `TaskDetailPanel`，避免 React Query provider 和默认 active view 影响断言；完整面板行为由 Task 2 Step 3 的删除独立描述 section 和 timeline 替换实现覆盖。

- [x] **Step 2: 运行渲染测试确认失败**

Run:

```bash
node --import tsx packages/frontend/src/components/TaskDetailPanel.render.test.tsx
```

Expected: FAIL，错误包含 `TaskTimeline` 未导出或元素类型无效。

- [x] **Step 3: 实现合并 timeline 渲染**

在 `TaskDetailPanel` 中计算合并条目：

```ts
const timelineItems = task
  ? buildTaskTimelineItems(task, timelineEvents, layerVisibility)
  : [];
```

删除独立描述 section：

```tsx
<section className="inspector-section">
  <Label>{t('taskDetail.description')}</Label>
  <div className="glass-info-card min-h-[76px] whitespace-pre-wrap px-3 py-2.5 text-[13px] leading-relaxed">
    {task.description || t('taskDetail.noDescription')}
  </div>
</section>
```

把 `timeline` 视图中的 `TaskEventTimeline` 替换为：

```tsx
<TaskTimeline
  items={timelineItems}
  emptyKey={visibleEvents.length === 0 ? 'taskDetail.noVisibleEvents' : 'taskDetail.noTimelineEvents'}
  formatRelativeTime={formatRelativeTime}
  t={t}
/>
```

在 `TaskEventTimeline` 后新增：

```tsx
export function TaskTimeline({
  items,
  emptyKey = 'taskDetail.noEvents',
  formatRelativeTime,
  t,
}: {
  items: TaskTimelineItem[];
  emptyKey?: MessageKey;
  formatRelativeTime: (timestamp: number) => string;
  t: (key: MessageKey, values?: Record<string, string | number>) => string;
}): JSX.Element {
  if (items.length === 0) {
    return (
      <div className="glass-info-card flex min-h-[96px] items-center gap-3 text-[12px] text-[var(--color-fg-muted)]">
        <Radio className="h-4 w-4 shrink-0 text-[var(--color-muted)]" strokeWidth={1.8} />
        <span>{t(emptyKey)}</span>
      </div>
    );
  }

  return (
    <div className="task-event-timeline glass-info-card">
      {items.map((item) => (
        item.kind === 'body' ? (
          <TaskBodyTimelineRow
            key={item.id}
            item={item}
            formatRelativeTime={formatRelativeTime}
            t={t}
          />
        ) : (
          <TaskEventTimelineRow
            key={item.id}
            event={item.event}
            formatRelativeTime={formatRelativeTime}
            t={t}
          />
        )
      ))}
    </div>
  );
}
```

把 `TaskEventTimeline` 的 `events.map` 内部 row 提取为：

```tsx
function TaskEventTimelineRow({
  event,
  formatRelativeTime,
  t,
}: {
  event: TaskEvent;
  formatRelativeTime: (timestamp: number) => string;
  t: (key: MessageKey, values?: Record<string, string | number>) => string;
}): JSX.Element {
  return (
    <div className="task-event-row">
      <span className="task-event-dot" data-layer={event.layer} />
      <div className="min-w-0 flex-1">
        <div className="flex items-center justify-between gap-2">
          <span className="truncate text-[12.5px] font-semibold text-[var(--color-fg)]">
            {eventTitle(event, t)}
          </span>
          <span className="shrink-0 font-mono text-[10.5px] text-[var(--color-muted)]">
            {formatRelativeTime(event.created_at)}
          </span>
        </div>
        <div className="mt-1 text-[11.5px] leading-relaxed text-[var(--color-fg-muted)]">
          {describeTaskEvent(event, t)}
        </div>
        <TaskEventPayloadDetails event={event} />
      </div>
    </div>
  );
}
```

把 `TaskEventTimeline` 的非空分支改成：

```tsx
return (
  <div className="task-event-timeline glass-info-card">
    {events.map((event) => (
      <TaskEventTimelineRow
        key={event.id}
        event={event}
        formatRelativeTime={formatRelativeTime}
        t={t}
      />
    ))}
  </div>
);
```

新增正文 row：

```tsx
function TaskBodyTimelineRow({
  item,
  formatRelativeTime,
  t,
}: {
  item: Extract<TaskTimelineItem, { kind: 'body' }>;
  formatRelativeTime: (timestamp: number) => string;
  t: (key: MessageKey, values?: Record<string, string | number>) => string;
}): JSX.Element {
  return (
    <div className="task-event-row">
      <span className="task-event-dot" data-layer="chat" />
      <div className="min-w-0 flex-1">
        <div className="flex items-center justify-between gap-2">
          <span className="truncate text-[12.5px] font-semibold text-[var(--color-fg)]">
            {t('taskDetail.body' as MessageKey)}
          </span>
          <span className="shrink-0 font-mono text-[10.5px] text-[var(--color-muted)]">
            {formatRelativeTime(item.created_at)}
          </span>
        </div>
        <div className="mt-1 whitespace-pre-wrap text-[12px] leading-relaxed text-[var(--color-fg-muted)]">
          {item.content}
        </div>
      </div>
    </div>
  );
}
```

如果 `taskDetail.body` 没有翻译键，则在 `packages/frontend/src/lib/i18n.tsx` 中加入中文和英文键：

```ts
'taskDetail.body': '正文',
```

```ts
'taskDetail.body': 'Body',
```

- [x] **Step 4: 运行渲染测试确认通过**

Run:

```bash
node --import tsx packages/frontend/src/components/TaskDetailPanel.render.test.tsx
```

Expected: PASS。

---

### Task 3: 最终验证与提交

**Files:**
- Modify: `docs/superpowers/plans/2026-06-02-任务详情正文与ACP流转时间线合并计划.md`
- Modify: implementation files from Task 1 and Task 2

- [x] **Step 1: 运行定向测试**

Run:

```bash
node --import tsx packages/frontend/src/components/TaskDetailPanel.test.ts
node --import tsx packages/frontend/src/components/TaskDetailPanel.render.test.tsx
```

Expected: 两个命令都 PASS。

- [x] **Step 2: 运行构建验证**

Run:

```bash
npm run build
```

Expected: backend TypeScript build 和 frontend Vite build 均成功。

- [x] **Step 3: 更新本计划勾选状态**

将已执行步骤从 `- [x]` 改为 `- [x]`，并在计划末尾追加：

```md
## 验证记录

- `node --import tsx packages/frontend/src/components/TaskDetailPanel.test.ts`：通过
- `node --import tsx packages/frontend/src/components/TaskDetailPanel.render.test.tsx`：通过
- `npm run build`：通过
```

- [x] **Step 4: 检查 git diff**

Run:

```bash
git diff -- packages/frontend/src/components/TaskDetailPanel.tsx packages/frontend/src/components/TaskDetailPanel.test.ts packages/frontend/src/components/TaskDetailPanel.render.test.tsx packages/frontend/src/lib/i18n.tsx docs/superpowers/plans/2026-06-02-任务详情正文与ACP流转时间线合并计划.md
```

Expected: diff 只包含本计划相关改动；没有无关格式化或大范围重排。

- [x] **Step 5: 提交实现**

Run:

```bash
git add packages/frontend/src/components/TaskDetailPanel.tsx packages/frontend/src/components/TaskDetailPanel.test.ts packages/frontend/src/components/TaskDetailPanel.render.test.tsx packages/frontend/src/lib/i18n.tsx docs/superpowers/plans/2026-06-02-任务详情正文与ACP流转时间线合并计划.md
git commit -m "fix(frontend): 合并任务正文与流转时间线"
```

Expected: commit 成功。

---

## Self-Review

- Spec coverage: 覆盖正文进入 `timeline`、按时间排序、移除独立描述区、保持后端不变、测试和构建验证。
- Placeholder scan: 无 `TBD`、`TODO`、`implement later` 或未说明的“适当处理”。
- Type consistency: `TaskTimelineItem`、`buildTaskTimelineItems`、`TaskTimeline`、`TaskEventTimelineRow` 命名一致；正文条目使用 `kind: 'body'`，事件条目使用 `kind: 'event'`。

## 验证记录

- `node --import tsx packages/frontend/src/components/TaskDetailPanel.test.ts`：通过，6 个测试全部通过。
- `node --import tsx packages/frontend/src/components/TaskDetailPanel.render.test.tsx`：通过，4 个测试全部通过。
- `npm run build`：通过；Vite 输出 chunk size warning，未阻塞构建。
